# 2026-08-19 階段L：計畫檔更新（phase-07／phase-08）TODO

## 目標

依 dev-prompt `phase0819-2.md` 任務 1：對照專案現況，把 `docs/plan/unfinish/phase-07-上傳驗收測試.md` 與 `phase-08-接上真實模型.md` 過時內容全部修正（並同步 `phase-00-總覽.md` 受影響列），**確保計畫檔全部更新完才進入開發**。

## 實作邏輯

- **先盤點再動筆**：已實查——master 36 passed（Phase 01〜06 完成）；smoke 檔現有 7 個測試（含階段I/J review 加的兩個守衛：text 全空白 422、U4 向量護欄）；`unit/test_indexing_service_unit.py` 已存在（P06）；conftest 已有 `wire_fake_ai`（autouse 三件全接＋teardown clear）與 `client`；pytest-bdd **8.1.0 已安裝**且與 pytest 9.1.1 相容性已用最小專案**實證**（Rule／中文步驟／datatable 全過）。
- **P07 計畫過時點**：
  1. 測試累計數 11 過時 → 重算 **40**（36 − 刪 smoke 7 ＋ feature 7 ＋ bilingual 1 ＋ design_rules 3）。
  2. 計畫要新建的 `test_indexing.py`（3 個）已被 P06 的 unit 檔覆蓋 → 不建、註記承接。
  3. smoke 檔刪除前要**遷移** 3 個 `.feature` 沒覆蓋的設計守衛（非圖片不呼叫看圖、text 全空白 422 逐字訊息、U4 向量護欄）到新檔 `tests/integration/test_upload_design_rules.py`——直接刪會丟掉 review 裁定加上的防線。
  4. 測試檔位置依 dev-prompt 分目錄：feature／bilingual／design_rules 都打 API＋DB → `tests/integration/`；`scenarios()` 相對路徑改 `../../docs/spec/features/…`。
  5. fixture 疊接：feature 檔需要「動態現在時間」→ 新 fixture **顯式依賴** conftest 的 `wire_fake_ai`，保證覆寫順序；bilingual 檔直接在測試本體覆寫 `get_vlm`（沿用 repo 慣例，不另設 fixture）。
  6. 「停 Ollama 跑測試」驗收改為 `OLLAMA_BASE_URL` 指死埠法（不動使用者服務）。
- **P08 計畫過時點**：
  1. 步驟 6 假設 CLAUDE.md 指令節是佔位——實際已是完整版 → 改為在既有指令碼區塊補一行煙霧測試指令。
  2. 驗收 6/7 累計數 11→40；Ollama 獨立性同上改死埠法。
  3. 補兩個備援：screencapture 黑屏 → qlmanage 渲染 HTML 假收據；沙箱擋 localhost → in-process TestClient 走真依賴。
  4. psql 指令一律補 `-p 5433`（非互動 shell 沒有 PGPORT）。
- 執行方式：計畫檔更新由主控親自執行（計畫即後續 subagent 的任務書）。

## 步驟

- [x] L1. 更新 `phase-07-上傳驗收測試.md`（TDD 化步驟、檔案路徑、守衛遷移表、fixture 疊接、驗收 40、常見問題）
- [x] L2. 更新 `phase-08-接上真實模型.md`（CLAUDE.md 步驟改寫、qlmanage／in-process 備援、psql 埠、驗收 40、死埠法）
- [x] L3. 更新 `phase-00-總覽.md`（P07/P08 兩列＋第三條 🔄 註記＋§4 tests/ 樹狀圖對齊現況）
- [x] L4. 自我核對三檔交叉一致；commit 96fb074＋寫階段L report

## 完成定義

phase-07／phase-08 每個步驟都能在當前專案狀態（36 passed、既有 conftest/fakes/unit 檔）下照做不誤；數字、路徑、fixture 名交叉一致；violations 為零。
