# 2026-09-01 階段一：八份計畫檔校準（Phase 73〜80）TODO

> 對應 dev-prompt：`docs/plan/dev-prompts/phase0901.md` 任務 1——
> 「先根據目前專案的狀態，逐個更新以下計畫檔案……每個計劃檔案都更新完再進行下一步」。
> 比照 2026-08-26「八份計畫檔校準 65〜72」與 2026-08-24 階段 SSS 的前例。

## 本次整體分幾個階段（總路線圖）

| 階段 | 內容 | TODO／REP 檔名 |
|---|---|---|
| **一** | 校準 phase-73〜80 八份計畫檔（本檔） | `2026-09-01-階段一-八份計畫檔校準73-80-*` |
| 二 | Phase 73 收尾確認 ＋ Phase 74／75 隱私閘門（契約假件 → 真模型、縮圖、計時） | `2026-09-01-階段二-隱私閘門74-75-*` |
| 三 | Phase 76 純重構五積木 ＋ Phase 77 雲端路契約與第五道安全網 | `2026-09-01-階段三-積木與雲端路契約76-77-*` |
| 四 | Phase 78 閘門接線與 fallback（Celery 改呼叫 `run_gated_ingest_job`） | `2026-09-01-階段四-閘門接線78-*` |
| 五 | Phase 79 雲端路本機端單圖 ＋ Phase 80 逾時與冪等 | `2026-09-01-階段五-雲端路79-80-*` |
| 六 | 總驗收：全量回歸、零依賴實證、親自 review、CLAUDE.md 現況同步 | `2026-09-01-階段六-總驗收與親自Review-*` |

全程**不 commit**（產品負責人指示）、不搬 finish/（歸檔隨 commit）。需要手機的測試：本輪 74〜80 **沒有**（無線鏡頭零改動）；若之後真有，停下來交給產品負責人。

## 為什麼要校準（實作邏輯）

八份計畫檔的成稿時間與 design6 的**改判**交錯：

```text
08-27 20:45  phase-73 定稿（當時 §6.6 尚未執行、分支還叫 master）
08-31 22:24  phase-76 定稿 ─┐
09-01 03:15  phase-77／79／80 定稿 ─┤ 都在改判「之前」
09-01 10:47  design6.md 改判：閘門只用 VLM 短問、不看檔名、跟頁首開關
09-01 10:50  phase-74／75 依改判重寫
09-01 10:51  總覽同步；phase-78 只補了檔頭警語（本文仍多處講「規則版」）
```

也就是說：74／75 是新的，78 半新半舊，76／77／79／80 引用 74／75 的地方全是舊契約。
另外 Phase 73 在 08-28 已由產品負責人自己完成 §6.6（建 GitHub repo、push、CI 綠），計畫檔的狀態表停在 08-27。

不校準就開工的話，實作者會照舊契約寫出 `RuleGate`／`PRIVACY_GATE_LOCAL_MODEL`，
而且會把閘門建在 worker 行程的 `config.AI_BACKEND` 上——那個值在 worker 永遠是 `local`，
頁首撥到雲端時閘門仍打本機，違反 D6 而且安靜（見下方裁決 R1）。

## 開工基準（2026-09-01 實測）

| 項目 | 值 |
|---|---|
| HEAD | `a53ab57`（chore: pytest 設定檔 merge 進 pyproject）、分支 `main` |
| remote | `https://github.com/1104030360/personalDocAI.git`（PUBLIC，2026-08-28 建；Actions `test` 綠 ×2） |
| 測試 | **543 passed ＋ 0 skipped**（33.5s；1 個環境層 StarletteDeprecationWarning，與專案碼無關） |
| 端點 | 22 |
| 容器 | db／redis healthy、app／worker Up（常駐映像，無 --reload）；Ollama 未開 |
| git | `app/`、`tests/`、`db/`、compose、Dockerfile 乾淨；未追蹤＝design6、dev-prompt、22 份 phase 檔 |

## 步驟

1. [x] 驗開工基準（Docker 原本沒開→`open -a Docker` 後四容器自回；`pytest -q` 543）。
2. [x] 讀 design6.md 全文、總覽 §2.1／§2.4〜2.7／§7／§9〜11、phase-73／74／75 全文、76〜80 章節與已知過期處；讀 `ingest_job.py`／`celery_app.py`／`dependencies.py`／`ai_timing.py`／`ingest_job_store.py`／`config.py`／`conftest.py` 現況。
3. [x] 寫共同事實簡報 `.superpowers/sdd/phase0901/brief-common.md`（專案事實、程式碼現況、裁決 R1〜R4、校準規則）。
4. [x] 由 controller 自己改總覽三處（唯一寫總覽的人）：§2.7 Phase 74 拿掉 `config.py`；§2.4.1 加 `build_privacy_gate_for_backend`；§10.2 新增追認項 **S**（＝裁決 R1）。
5. [x] 派 6 個平行 subagent（Opus）校準，一人一組、只准改自己名下的檔：
   - A：phase-73（狀態表更新、master→main、§6.6 已完成紀錄）＋ phase-74 ＋ phase-75（R1：加 `build_privacy_gate_for_backend`）
   - B：phase-76（整份重貼碼逐段對照現行 512 行；前置 564→568）
   - C：phase-77（拿掉「74 加了 PRIVACY_MODEL」的假設；config 插入點改 `SEARCH_MODE_LABELS` 之後）
   - D：phase-78（本文十幾處規則版殘留全清；R1 落地：celery_app 用 `build_privacy_gate_for_backend(job["ai_backend"])`、conftest 雙蓋、測試多斷言快照；煙霧段改寫）
   - E：phase-79（gated_ingest 重貼碼的「規則版不呼叫 load_bytes」註解；§10.2 R 順序）
   - F：phase-80（同 E 的註解；wait_result 五條規則逐字對照 §2.5）
6. [x] 收回六份回報，逐份看 diff；跨檔問題由 controller 裁決並回寫總覽／簡報；殘留關鍵字全庫 grep 歸零。
7. [x] 寫 REP（`2026-09-01-階段一-八份計畫檔校準73-80-REP.md`）。

## 裁決（controller，授權依據＝phase0901.md「所有決策不必徵求同意」）

| # | 裁決 | 理由 | 落在 |
|---|---|---|---|
| R0 | 不開 worktree、不 commit；每個 phase 的 review 用工作樹 tree 快照相減 | 產品負責人「先不 commit」＋要在本機 Docker（bind-mount ./app）手測 | 全部 |
| R1 | 閘門在 worker 必須用 `job["ai_backend"]` 快照建：新增 `dependencies.build_privacy_gate_for_backend(ai_backend)`；`get_privacy_gate()` ＝ 它吃 `config.AI_BACKEND`；celery_app 傳 `build_…(job["ai_backend"])`；conftest 兩支都 monkeypatch | D6「跟著開關走」＋ D14 快照；worker 的 `config.AI_BACKEND` 永遠 local | 75、78（總覽 §10.2 S） |
| R2 | Phase 73 §6.6 已由產品負責人完成；計畫檔只更新狀態、不搬 finish/ | 鐵律 12：歸檔隨 commit | 73 |
| R3 | 各 phase「步驟 N：commit」改成「不 commit——記快照」 | 同 R0 | 76〜80 |
| R4 | 79／80 整檔重貼碼內「規則版不呼叫 load_bytes／只看檔名」註解一律改 VlmGate 語意 | 改判 | 78、79、80 |
| R5 | Phase 73 §7 三格負面 CI 驗證（故意弄髒再 push）不列為結案條件，標 ⏸ 可選 | 73 自己寫「可之後再補」；需要 push＝外部副作用、無授權 | 73 |
| R6 | 校準 F 在 phase-80 改了一顆測試的斷言內容（讓斷言撐得起測試名的「之前」）——接受 | 可執行性要求；顆數不變 | 80 |
| R7 | 78／79／80 三份 gated_ingest.py 重貼碼的兩段註解措辭各異、語意一致——不統一 | 統一要連動「與前一 phase 差別」清單，風險大於收益 | 78〜80 |
| R8 | dev-prompt 寫的 phase-75 檔名與實檔不同——以實檔為準，不改 dev-prompt | 那是產品負責人的檔 | — |

## 安全鐵律（給 subagent 的約束）

- 只准改自己名下的計畫檔；`app/`、`tests/`、`docs/spec/`、總覽、他人計畫檔一律不碰。
- **禁跑 pytest**（避免撞測試庫）；核對測試名只准 `pytest --collect-only -q`。
- 不動設計決策（design6／總覽 §2.4〜2.7 是契約），只校事實引用與改判殘留。
- 顆數只增不減；R1 若讓 78 多一顆，在 78 §8 明寫「比總覽多 1 顆」。
