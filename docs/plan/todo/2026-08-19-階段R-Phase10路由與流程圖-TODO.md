# 2026-08-19 階段R：Phase 10 路由與流程圖 TODO

## 目標

照更新後的 `docs/plan/unfinish/phase-10-路由與流程圖.md`，以 TDD 做出詢問流程的前半段：LangGraph 流程圖骨架（START → route → 條件邊分岔 retrieve_metadata／retrieve_vector → END）＋ route 節點（LLM 一次結構化輸出同時judge查法與過濾條件、中英 few-shot、判斷失敗一律 fallback 語意查詢）；全量測試 50 → **55 passed**。

## 實作邏輯

- **執行方式**：同階段Q——主控派 opus 實作 subagent 照計畫逐字轉錄，獨立 opus reviewer 雙審，全程不 commit。
- **TDD 順序（計畫 🧪＋⚠️ 加註）**：先建 `tests/integration/test_workflow_route.py` 看紅（`ask_workflow`／`FakeRouter` 都不存在、該檔收集失敗，其餘 50 綠）→ 步驟 1 寫 `ask_workflow.py`（RouteDecision／ROUTE_PROMPT／OllamaRouter／AskState／AskDeps／build_workflow／run_ask）→ 步驟 2 才把 `FakeRouter`＋檔頂 import 加進 `tests/fakes.py`（**順序陷阱**：conftest 會 import fakes，ask_workflow 還是空檔時先加會讓整個套件收集爆掉）→ 轉綠。
- **關鍵設計約束**（不可違反）：路由由 LLM 判斷（關鍵字規則已被否決）；few-shot 錨點必含中英例句（含 "What drinks did I buy recently?"）；條件值照原文抽、不翻譯（§8.3）；fallback 統一寫在 route 節點（例外／格式不符 → mode="vector"、條件全空＝規則 Q3）；檢索直接復用 Phase 09 的 `photo_retriever`（request 五鍵介面已 byte-for-byte 落地）。
- **驗收**：計畫驗收 1〜4（驗收 4 需先 `uv pip install grandalf`，僅裝不進 requirements.txt——計畫 Q1 定位視覺化工具）；驗收 5（真模型路由煙霧，計畫標「可選」）由主控在階段S 親自跑，不由 implementer 碰真 Ollama。

## 步驟

- [x] R1. 派工 opus implementer：TDD 紅（新測試檔收集失敗、其餘 50 綠）→ 步驟 1→2 實作轉綠（單檔 5 passed／全量 55 passed）＋驗收 1〜4（全過；逐字轉寫零偏離；grandalf 僅裝 venv）
- [x] R2. 產生工作區 diff 審查包（347 行），派獨立 reviewer（opus）雙審 → ✅ Spec compliant＋Approved
- [x] R3. 0 Critical；1 Important 為 plan-mandated（fallback 無 log）→ 主控裁定遞延 Phase 11/13 並回報使用者；5 Minor 記 ledger 遞延；修正迴圈 0 輪
- [x] R4. 寫階段R report、更新 ledger

## 完成定義

`pytest tests/integration/test_workflow_route.py -v` = 5 passed；`pytest -q` = 55 passed；驗收 1〜4 全過（ASCII 圖見 route 分岔到兩個 retrieve 節點再會合 END）；review 無未處置 Critical/Important；pytest 全程未打真 Ollama。
