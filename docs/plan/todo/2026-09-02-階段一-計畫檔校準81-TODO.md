# 2026-09-02 階段一：Phase 81 計畫檔校準 TODO

> 對應 dev-prompt：`docs/plan/dev-prompts/phase0902.md` 任務 1。
> 計畫檔：`docs/plan/unfinish/phase-81-雲端路PDF.md`。上一場：`2026-09-01-階段六-總驗收與親自Review-TODO.md`。
> 工作區（gitignored）：`.superpowers/sdd/phase0902/`（ledger `progress.md`、`brief-common.md`、校準 brief 與回報）。

## 實作邏輯（新手白話）

Phase 81 的計畫檔是 2026-08-31 寫的，但程式碼在它成稿之後又動了三次，照著它做會做錯：

```text
計畫檔寫的                                   目前工作樹的實況（2026-09-02）
──────────────────────────────              ──────────────────────────────────────
函式／變數全是中文名（_用雲端結果落庫、信箱…）   → 今天上午產品負責人指示全部改英文（_store_cloud_result、mailbox…）
gated_ingest.py「整檔覆蓋」的碼是 79 版        → 80 落地版多了 R14 try、重讀行、M2 註解、_parse_understanding 新措辭
註解還在講「規則版只看檔名／模型備援版」        → 2026-09-01 改判：閘門＝VlmGate，一定讀檔、看圖不看檔名
開工基線 609、做完 616                        → 實測 613；81 核心 +7 ＝ 620
步驟 6 要 commit                              → 產品負責人指示全程不 commit
（沒寫）                                      → 上一場 review 兩度把「閘門對 PDF 全頁渲染只取第一頁」留給 81 評估
```

所以先校準、再實作（dev-prompt 任務 1 的順序不能倒）。校準由 Opus subagent 依 brief 執行，controller（我）裁決跨檔問題並親自複核。

## 步驟

- [x] 建工作區：`.superpowers/sdd/phase0902/`，抄上一場的 `snapshot-tree`／`review-package-tree`，記 BASE_TREE，寫 ledger（裁決 R0〜R7）與 pre-flight 表。
- [x] 寫 `brief-common.md`（事實、命名契約、裁決）、`implementer-rules.md`、`global-constraints.md`、`calib-81-brief.md`。
- [x] 派 Opus 校準 subagent 改計畫檔（A〜J 十項）；回報寫 `calib-81-report.md`。
- [x] controller 複核：殘留關鍵字 grep、NBSP、命名對照、步驟 4 編輯清單與實檔一致、顆數。
- [x] 總覽 §2.7 P81 補一行註記（R4 多動兩檔、+2 顆、累計 622）。
- [x] 寫階段一 REP。

## 裁決（詳見 ledger）

| # | 內容 |
|---|---|
| R1 | 識別字一律英文；`test_中文` 名維持 |
| R2 | `gated_ingest.py` 以實檔為基準只加 PDF 分支；計畫檔改成編輯清單 |
| R3 | 顆數 613 → 620（+7）→ **622**（+2） |
| R4 | 順手做 `render_pages(..., max_pages=None)`＋閘門 `max_pages=1`（+2 顆） |
| R5 | 不 commit |
| R6 | 改判前字眼清零 |
| R7 | 本 phase 不需要手機／真機／真模型測試 |
| R8 | `_store_pdf_result` 的區域變數採本機路同名 `already_done`（校準者提出、controller 裁決） |
| R9 | 校準者待裁決：comprehension 用 `page`、步驟 1b 放步驟 1 之後——皆接受 |
