# 2026-08-19 階段Z：Phase 14 極簡網頁介面 TODO

## 目標

照更新後的 `docs/plan/unfinish/phase-14-網頁介面.md`：兩個純 HTML 頁面（`app/static/upload.html`、`app/static/ask.html`）＋ `main.py` 一行 `app.mount("/ui", StaticFiles(...))`——**零框架、零打包、零新增端點、零新增自動化測試**；`pytest -q` 維持 **79 passed** 一個不變；頁面驗收以瀏覽器實際操作為準（主控在階段AA 以瀏覽器自動化工具親自執行，等同手動操作）。

## 實作邏輯

- **執行方式**：主控派 opus 實作 subagent 照計畫轉錄（HTML/JS 兩檔照抄＋main.py 掛載），獨立 opus reviewer 雙審（重點：零端點新增、同源 fetch、錯誤分支顯示、無外部相依），全程不 commit。
- **關鍵設計**：同源（頁面與 API 同 host:port）→ 不裝 CORSMiddleware；`fetch("/photos")`／`fetch("/ask")` 相對路徑；`STATIC_DIR = Path(__file__).resolve().parent / "static"` 絕對路徑掛載；`title` 維持 `personalDocAI`。
- **這不是第三個功能**：頁面只是既有兩個 API 的操作介面（design.md §3 明講），12 條 Rule 不受影響。
- **驗收分工**：implementer 做驗收 1（curl 200×2）、9（79 passed）、10（零新端點）、11（零前端相依）；驗收 2〜8（瀏覽器操作：上傳成功／415／中文問答／英文問答／查無／互連／Console 無錯）由主控在階段AA 以 Playwright 瀏覽器工具親自走一遍（真模型路徑）。

## 步驟

- [x] Z1. 派工 opus implementer：mkdir static → main.py 掛載 → 兩個 HTML 照計畫逐字建檔 → 驗收 1、9、10、11
- [x] Z2. 產生工作區 diff 審查包，派獨立 reviewer（opus）雙審
- [x] Z3. 修正迴圈（如有）；Minor 記 ledger 遞延
- [x] Z4. 寫階段Z report、更新 ledger

## 完成定義

`curl /ui/upload.html`＝200、`curl /ui/ask.html`＝200；`pytest -q` = 79 passed（零增減）；端點仍恰 3 個；`app/static/` 無任何外部函式庫引用、專案無 package.json/node_modules；review 無未處置 Critical/Important；未 commit。瀏覽器實操驗收（2〜8）在階段AA 完成後本 phase 才算全收。
