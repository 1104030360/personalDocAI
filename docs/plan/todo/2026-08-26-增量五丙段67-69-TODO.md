# 2026-08-26 增量五丙段前端（Phase 67〜70）TODO

> （執行中追記：Phase 70 依同一模式併入本階段，REP 合記於 67-70 一份。）

> 對應 dev-prompt：`docs/plan/dev-prompts/phase0826.md`。
> 計畫檔：`phase-67-全站進度面板.md`、`phase-68-上傳頁多檔選檔.md`、
> `phase-69-鏡頭連拍與桌面拿掉開鏈.md`（皆為 2026-08-26 校準版）。
> ★G2 素材已備（見 G2 驗收包）；依 phase0826.md 明示指名執行 65〜72 的授權接續丙段。

## 實作邏輯

乙段的新契約（202＋`GET /ingest-jobs`）已穩，前端追上：
- 67：`progress_panel.js`（全站唯一一份，2 秒輪詢、四種狀態、× dismiss、全空收起、
  頂欄 N 接手）掛桌面五頁、刪五頁各自的 25 行過渡 IIFE；+7 顆原始碼契約測試、
  `test_nav_header.py` 一顆原地換掉。
- 68：`upload.html` 加 `multiple`、每檔一個 POST、拿掉 201 開鏈與四個彈窗 script、文案改寫；+3 顆。
- 69：`camera-phone.html` 202 即可再拍＋本地窄條；`camera-desk.html` 刪 latest 開鏈、
  保留 `uploaded` 通知（計數＋解鎖）；+3 顆。

執行方式：每 phase 一個 Opus subagent 照計畫 TDD（先紅後綠），我逐 phase 親自 review diff；
瀏覽器實操驗收由我用 Chrome 做（切開發 overlay 讓容器看到新 static）。

## 我的執行者裁決（授權依據＝phase0826.md「所有決策不必徵求同意」）

1. **「分析中（第 0 次）」顯示窗**（校準時發現）：`progress_panel.js` 的狀態文字
   對 `analyzing` 用 `Math.max(attempt, 1)` 顯示保護——純顯示層、零後端改動；
   計畫檔與測試若釘到該行，一併同步並留註。
2. `camera-desk.html` 檔頭過期註解（仍寫已刪除的 `_ingest_image`）：Phase 69 順手修正
   （該頁本來就在大改；純註解、零行為）。

## 步驟

1. [x] Phase 67（subagent TDD）→ 我 review diff → 顆數 507→514
2. [x] Phase 68（subagent TDD）→ 我 review diff → 顆數 514→517
3. [x] Phase 69（subagent TDD）→ 我 review diff → 顆數 517→520
4. [~] 切開發 overlay 已完成；瀏覽器實操驗收（Chrome）**延後到最後一輪**——Chrome 擴充連著兩個瀏覽器、工具強制要使用者當面選一個，與「需要手機的鏡頭驗收」一起等產品負責人回座（其餘自動化驗收全過）：五頁面板、輪詢、失敗列 ×、
   換頁/F5 列還在、多檔選檔、console 乾淨——**手機連拍的真機部分留給產品負責人**
4b. [x] Phase 70（subagent TDD）→ 我 review diff → 顆數 520→523
5. [x] 寫 REP（67〜70 合一份）

## 鐵律

- 同一時間只有一份 pytest；顆數只增不減、0 skipped
- 面板不掛 camera-phone（總覽 §10 第 3 列裁決：手機用本地窄條）
- 禁 alert/confirm/prompt；零框架零 CDN；`docs/spec/` 唯讀；不 commit
