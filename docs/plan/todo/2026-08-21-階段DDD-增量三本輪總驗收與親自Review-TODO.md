# 2026-08-21 階段DDD：增量三本輪（Phase 28〜33）總驗收與親自 Review——TODO

## 這個階段要做什麼

Phase 28〜33 全部落地後的收官：①瀏覽器實操驗收（真伺服器 :8000＋真 gemma4＋正式庫——沿 P23/24/27 慣例）
②全量回歸與端點清點 ③controller 對 e29f5a1 之後的全部改動做最終親自 review ④文件收尾（總覽打勾、
CLAUDE.md 現況、REP）。全程由 controller 親自執行（產品負責人指示「最後你再親自 review 一遍」）。

## 步驟

- [x] 1. 全量 `pytest -q`＝218＝零 Ollama 同顆數；`/openapi.json`＝14（最終快照）
- [x] 2. 伺服器已在跑（實測端點 14 版），Playwright 實操（產品負責人中途指示「做完 33 就停」，範圍收斂）：
  - [x] a. 上傳鏈實走（#22 繳費單）：抽屜①採用「收據」定案 → 實體窗自動開 →④→ 收鏈；
        待辦窗 UI 另以真頁面驅動實測（預填→建立 201→onDone；模型該次未給待辦建議屬 D13 自由心證）
  - [x] b. 「再建議一個」實測（候選空→「沒有其他適合的實體了」＋整合測試蓋 exclude／登記路徑）；
        「稍後再說→續鏈」與 onAssigned 共用同一函式（瀏覽器實測 onAssigned 路徑＋存根覆蓋 onClosed）
  - [x] c. 空關不跳：真上傳 #22 實體關結束後待辦窗未出現（實測）
  - [x] d. 三分頁全項：待決定補完鏈（#21 歸「文件」→實體窗→③自創→②重複釘 409 紅字→④完成→離開待決定 7→6）、
        資料夾牆 DIV.photo-static 點無反應、待辦列表兩列到期排序（09-01 在前）＋點列 href 原圖 _blank、
        `?tab=tasks` 直達＋aria-current、計數正確；抽屜窗無×／Esc 無效／下拉排除收件箱回歸
  - [x] e. PDF：**裁掉真模型瀏覽器實測**（「做完 33 就停」；PDF 後端 7 顆整合測試＋BDD Rule 把關、
        UI 與單圖共用 `開始歸類()`；風景圖 422 順帶驗證上傳失敗顯示）
  - [x] f. curl 六項：重複釘 409／已定案 PATCH 409／標題空白 422（中文訊息）／合法建立 201／重複待辦 409／GET /tasks
  - [x] g. console 全程僅 favicon 404＋兩筆刻意錯誤測試（422 風景、409 重複釘），零 JS 錯誤
- [x] 3. 最終親自 review：每 phase 完成當下已逐檔 review（XX〜CCC）；靜態掃碼 alert 0、無 DELETE 端點、SQL 只在 repository
- [x] 4. 文件收尾：phase-31/33 清單補打勾、總覽 §2＋§6 打勾、CLAUDE.md 現況更新（31〜33 部分未 commit）
- [x] 5. 本階段 REP＋總結報告（含全部裁定清單）

## 備註

- 瀏覽器驗收會在正式庫留下少量真實測試資料（照片／實體／待辦）——沿既有慣例（P24 的實測軌跡亦留庫）。
- e29f5a1 之後的新工作依產品負責人指示**不 commit**。
