# 2026-08-19 階段U：Phase 11 回答與詢問端點 TODO

## 目標

照更新後的 `docs/plan/unfinish/phase-11-回答與詢問端點.md`，以 TDD 做出詢問流程的後半段：`generate` 節點（LLM 只依撈到的照片內容回答、**回答語言跟隨提問語言**、查無就說查無、不得編造；`ANSWER_PROMPT` 三條鐵律）＋ `api/routers/ask.py` 的 `POST /ask` 端點（`search_mode`／`retrieved_photo_ids` 直接取自流程 state、不經 AI）；全量測試 55 → **60 passed**。

## 實作邏輯

- **執行方式**：同階段Q／R——主控派 opus 實作 subagent 照計畫逐字轉錄，獨立 opus reviewer 雙審，全程不 commit。
- **TDD 順序（計畫 🧪 加註）**：先建 `tests/integration/test_ask_endpoint.py` 看紅（該檔 import 的 `get_answerer`／`FakeAnswerLLM` 不存在 → 僅該檔收集失敗、其餘 55 綠）→ 轉綠依序：步驟 7（`FakeAnswerLLM`，只依賴 langchain `Document`）→ 步驟 1（`ANSWER_PROMPT`／`AnswerClient`／`OllamaAnswerer`）→ **步驟 2＋8 一起做**（⚠️ `AskDeps` 加必填 `answerer` 當下 `test_workflow_route` 5 測即壞，馬上補）→ 步驟 3（`AskRequest` 加 `min_length=1`）→ 步驟 4（dependencies 增量加三組注入點）→ 步驟 5（`ask.py` router）→ 步驟 6（main.py 掛第二個 router）。
- **關鍵設計約束**（不可違反）：查無仍走 `generate` 交 LLM 回覆（不可寫死跳過）；`search_mode`／`retrieved_photo_ids` 不經 AI、由 `config.SEARCH_MODE_LABELS` 轉全名；回答語言跟隨提問靠 prompt 一句鐵律，**產品程式碼不做語言偵測**（`_looks_english` 只准出現在測試假件）；照片內容原文照抄不翻譯。
- **驗收**：計畫驗收 1〜4（單檔 5 passed／全量 60 passed／grep 鐵律字句／流程圖四節點）由 implementer 做；驗收 5〜8（uvicorn＋curl 真模型煙霧，需 Ollama）由主控在階段W 親自跑，不由 implementer 碰真 Ollama。

## 步驟

- [x] U1. 派工 opus implementer：TDD 紅（新測試檔收集失敗、其餘 55 綠）→ 依 🧪 順序轉綠（單檔 5 passed／全量 60 passed）＋驗收 1〜4（中途一次暫時性網路錯誤，SendMessage 續跑完成；DONE_WITH_CONCERNS×2 疑慮轉交 review 裁決）
- [x] U2. 產生工作區 diff 審查包（537 行，8 檔 +275/−15），派獨立 reviewer（opus）雙審 → ✅ Spec compliant＋Approved（逐字一致性 11 區塊全過）
- [x] U3. 0 Critical；1 Important 為 plan-mandated 前瞻風險（conftest 安全網）→ 主控裁定列 Phase 12 步驟 0；6 Minor：2 條根因在需求書（計畫已校準、程式修正併步驟 0）、其餘記 ledger 遞延；修正迴圈 0 輪
- [x] U4. 寫階段U report、更新 ledger

## 完成定義

`pytest tests/integration/test_ask_endpoint.py -v` = 5 passed；`pytest -q` = 60 passed；grep 鐵律字句命中；流程圖節點含 `route`／`retrieve_metadata`／`retrieve_vector`／`generate`；review 無未處置 Critical/Important；pytest 全程未打真 Ollama；未 commit。
