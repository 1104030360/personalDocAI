# 2026-08-26 八份計畫檔校準（Phase 65〜72）TODO

> 對應 dev-prompt：`docs/plan/dev-prompts/phase0826.md` 任務 1——
> 「先根據目前專案的狀態，逐個更新以下計畫檔案……每個計劃檔案都更新完再進行下一步」。
> 比照 2026-08-24 階段 SSS「七份計畫檔校準」的前例。

## 為什麼要校準（實作邏輯）

八份計畫檔（phase-65〜72）成稿於 2026-08-25 約 13:00，**早於**同日稍晚才完成的
Phase 59〜64 實作（commit `f1a7e71`，23:55）。也就是說：計畫檔引用 59〜64 的
函式簽章、測試名、顆數基線時，寫的是「定稿計畫預期的樣子」，不是「實際落地的樣子」。
59〜64 全程照定稿做，大多應該對得上，但**必須逐項核對**，對不上的地方要先改計畫檔，
才能開始實作——否則實作者照著過期的計畫寫，會在函式名／欄位名／顆數上白白撞牆。

## 開工基準（2026-08-26 實測）

| 項目 | 值 |
|---|---|
| HEAD | `f1a7e71`（增量五乙段 59〜64） |
| 測試 | **493 passed ＋ 0 skipped**（65.7s） |
| 端點 | 22（`test_端點數不變` 釘住） |
| 容器 | `app`（開發 overlay `--reload`）＋`db`（healthy）；**尚無** redis／worker |
| requirements | **尚無** celery／redis |
| git | 乾淨（僅 `?? docs/plan/dev-prompts/phase0826.md`） |

## 步驟

1. [x] 驗開工基準（docker ps／pytest -q／git status）——上表
2. [x] 派 4 個平行 subagent 校準（每個負責 2 份，只准改自己名下的計畫檔）：
   - A：phase-65（Celery 與 Redis 實作）＋ phase-66（Compose 加 redis 與 worker）
   - B：phase-67（全站進度面板）＋ phase-68（上傳頁多檔選檔）
   - C：phase-69（鏡頭連拍與桌面拿掉開鏈）＋ phase-70（待決定走完整三關）
   - D：phase-71（錯誤收尾與全量回歸）＋ phase-72（規格檔改版與驗收包）
3. [x] 校準重點（每份都要過一輪）：
   - 顆數基線（現在是 493，照編號做的推算值見總覽 §9）
   - 引用 57〜64 的函式簽章／欄位名／測試名是否與**實際程式碼**一致
     （`run_ingest_job` 簽章、`JobStore` 欄位、`staging_service` 五函式、
     conftest 新增的共用工具名、`vlm_timing_target`、202 回應形狀、
     `GET /ingest-jobs`／dismiss 的實際行為）
   - 引用的檔案路徑／行號／grep 關鍵字是否還找得到
   - 計畫內嵌的程式碼片段與現況介面是否相容
4. [x] 我親自抽查各 agent 的改動（git diff 逐份看過；65 全文、72 全文抽查，其餘依回報）
5. [x] 寫 REP（`2026-08-26-八份計畫檔校準65-72-REP.md`）

## 安全鐵律（給 subagent 的約束，也記在這裡）

- 校準階段**只准改 `docs/plan/unfinish/phase-6N-*.md` 自己名下那兩份**；
  `app/`、`tests/`、`docs/spec/`、總覽、他人名下計畫檔一律不碰
- **禁跑 pytest**（避免撞測試庫）；要核對測試名用 `pytest --collect-only -q`（不執行、不碰庫）
- 不准為了「把數字對齊」去改測試或刪測試的計畫——顆數只增不減是總覽 §9 鐵律
