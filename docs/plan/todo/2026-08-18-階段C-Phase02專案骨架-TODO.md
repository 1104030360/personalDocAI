# 2026-08-18 階段C：Phase 02 分層專案骨架 TODO

## 目標

照更新後的 `docs/plan/unfinish/phase-02-專案骨架.md` 建出 design.md §4.1 的分層骨架：21 個 `.py` 檔、`.env`、`.gitignore`、`core/config.py`、兩份 schemas、只掛 `/health` 的 `main.py`，FastAPI 可啟動，並通過該文件全部 6 條驗收。

## 實作邏輯

- 先建骨架（目錄＋空殼檔）→ 再填設定與資料格式 → 最後啟動驗證。順序照文件步驟 1〜7。
- 「測試先行」形式同階段B：文件 6 條驗收（含預期輸出逐字比對）就是本階段的測試；每條驗收在寫完對應檔案後立即執行。
- 明確**不建**：`app/models/`、`app/core/security.py`、`alembic/`、`app/static/`（P14 才建）——驗收 2 會逐項檢查。
- `.env` 的 `DATABASE_URL` 用 `:5433`（階段A 的決策）。

## 步驟

- [x] C1. 步驟 1：mkdir＋touch 建出全部目錄與 21 個 `.py` 空殼＋`db/schema.sql`
- [x] C2. 步驟 2：建 `.env`（DATABASE_URL 帶 5433）與 `.gitignore`
- [x] C3. 步驟 3：寫 `app/core/config.py`（照文件逐字，DATABASE_URL 預設值帶 5433）
- [x] C4. 步驟 4〜5：寫 `app/schemas/photo.py`、`app/schemas/ask.py`
- [x] C5. 步驟 6：寫 `app/main.py`（只掛 `/health`＋兩個 TODO 註解）
- [x] C6. 步驟 7＋驗收：背景啟動 uvicorn → 跑 6 條驗收（目錄清單 21 行、不存在清單 3 OK、config 印值、/health、/docs、兩份 schemas dump）→ 關閉服務
- [x] C7. 回填專案 `CLAUDE.md` 的「## 指令」節（CLAUDE.md 自己要求骨架建立後立即回填；保留「## 指令」標題不改名，P08/P13 之後要找它）
- [x] C8. 寫階段C report＋總驗收（逐一確認 phase-00/01/02 文件提出的想法全部實現）

## 完成定義

6 條驗收全部拿到預期輸出；`CLAUDE.md` 指令節已回填；phase-00/01/02 的內容逐項核對完成。
