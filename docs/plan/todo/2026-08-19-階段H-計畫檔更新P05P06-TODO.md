# 2026-08-19 階段H：計畫檔更新（phase-05／phase-06）TODO

## 目標

依 dev-prompt `phase0819-1.md` 任務 1：對照專案現況，把 `docs/plan/unfinish/phase-05-看圖模組.md` 與 `phase-06-向量與寫入.md` 過時內容全部修正（並同步 `phase-00-總覽.md` 受影響的兩列），**確保計畫檔全部更新完才進入開發**。

## 實作邏輯

- **先盤點再動筆**：已實查——19 測試全綠（unit 2＋integration 17）；`vlm_service.py`／`indexing_service.py`／`dependencies.py` 為空檔；`tests/conftest.py` 已存在（Phase 3 版，含測試庫導向＋每測清空，**無** `client` fixture）；`pytest.ini`／`tests/fakes.py` 不存在；測試已分 `tests/unit/`＋`tests/integration/`；Ollama 真的在跑（gemma4 9.6GB＋bge-m3）；langchain-core 1.5.6／langchain-ollama 1.1.0。
- **計畫檔過時點（更新對象）**：
  1. phase-05 要「建立」conftest.py／pytest.ini／根目錄 smoke 測試——實際 conftest 已存在（改為「擴充」）、測試檔須分目錄（整合→`tests/integration/`、單元→`tests/unit/`，依 dev-prompt 指示）。
  2. 兩份計畫的「測試累計數 4／6」與現況 19 矛盾——TDD 下重算為 **P05=29、P06=34**。
  3. **計畫沒發現的陷阱**：`test_photos_upload.py` 兩個「佔位 201」測試在接上 VLM 後若不覆寫 `get_vlm` 會**真的呼叫本機 gemma4**（P06 後還會呼叫真 OllamaEmbeddings）→ 更新版計畫加「改寫這兩個測試」步驟＋conftest `wire_fake_ai` autouse 安全網（get_vlm 預設看不懂假件；P06 起加 FakeEmbeddings／FixedClock）。
  4. TDD 順序：兩份計畫改為「步驟 0 先寫全部測試跑紅燈 → 實作 → 綠燈」（沿用階段E/F 慣例）。
  5. phase-06 的「合併順序固定」測試屬純函式 → 移到 `tests/unit/test_indexing_service_unit.py`；`wire_fakes` fixture 從 smoke 檔上移到 conftest。
- **API 已查證（LangChain 官方文件，連結見 report）**：image content block `{"type":"image","base64":…,"mime_type":…}`、`with_structured_output(Pydantic)`、`OllamaEmbeddings(model, base_url)` 均為 1.x 現行寫法，計畫碼不需改。
- 執行方式：計畫檔更新由主控親自執行（計畫即後續 subagent 的任務書）；後續開發（階段I/J）交 opus subagent 實作、主控 review。

## 步驟

- [x] H1. 更新 `phase-05-看圖模組.md`：前置條件（現況 19 測試＋Ollama 在跑警語）、步驟重排為 TDD（步驟 0 測試先行：pytest.ini＋fakes.py＋conftest 擴充＋unit 6 個＋smoke 4 個〔`tests/integration/`〕＋改寫佔位 2 個）、驗收標準（29 passed）、常見問題與完成狀態同步
- [x] H2. 更新 `phase-06-向量與寫入.md`：步驟重排為 TDD（步驟 0：fakes 擴充＋conftest wire_fake_ai 擴充＋unit 4 個＋smoke +1＋英文斷言改巢狀）、驗收標準（34 passed）、完成狀態同步
- [x] H3. 更新 `phase-00-總覽.md`：P05/P06 兩列（新增測試明細＋累計 29/34）、暫時檔路徑註記（`tests/integration/test_upload_smoke.py`）
- [x] H4. 自我核對：三份檔案交叉一致（檔案路徑、fixture 名稱、測試數、步驟編號），且未違反任何已定案決策
- [x] H5. commit（docs：計畫檔更新＋收錄 dev-prompts）＋寫階段H report → commit 230bb77

## 完成定義

phase-05／phase-06 每個步驟都能在**當前**專案狀態下照做不誤（檔案路徑存在性、測試數、fixture 名稱皆與現況一致）；違反處為零；phase-00 對照列同步；commit 完成。
