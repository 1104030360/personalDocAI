# 2026-08-19 階段G：總驗收與收尾 TODO

## 目標

依 phase0819 約束 5：**逐一確認** phase-03 與 phase-04 文件提出的想法全部實現；主控做最終親自 review；把已完成的 phase 計畫歸檔到 `docs/plan/finish/`；校正 `CLAUDE.md` 現況描述。

## 實作邏輯

- **總驗收＝把兩份計畫的驗收清單從頭再跑一次**（不信任「剛剛跑過」，重跑取證）：pytest 全量、SQL 唯一性 grep、兩庫 schema、正式庫 0 筆、端點行為（TestClient 取證 415/201/openapi）。
- **逐項核對**：對照 phase-03 七條＋phase-04 驗收清單與「完成後的專案狀態」敘述，逐條打勾。
- **最終親自 review**（使用者要求）：主控通讀全部新增/修改檔案（非抽查），檢查分層依賴方向（main→router→config；repository→session→config；無反向）、測試誠實性（斷言真實行為，零 mock）。
- **歸檔**：phase-01〜04 計畫檔搬 `docs/plan/finish/`；phase-00 §1 補歸檔規則說明。已確認無任何檔案以路徑引用這四份文件，搬移安全。
- **CLAUDE.md 校正**（結構真的改變才改）：現況（01〜04 完成、19 測試、tests 結構）、指令節（測試自 P03 起）、規格進度段與重要陷阱段的 plan 目錄敘述。
- **異常清點**：`find -newermt` 全專案盤點今日變動，逐一歸因；無法歸因於本次工作的一律記錄並回報使用者。

## 步驟

- [x] G1. 全量重跑：`python -m pytest tests -q` → **19 passed**；bare `pytest -q` 亦 19 passed（venv 內可用，CLAUDE.md 指令維持有效）；`\d photo` 兩庫欄位型別＋hnsw ✓；TestClient 取證 openapi=['/health','/photos']、非圖片 415＋規格訊息、PNG 201 size=70；兩庫 count=0；5432 資料庫清單完好
- [x] G2. 逐項核對 phase-03（步驟 0〜5＋驗收 1〜7）與 phase-04（步驟 0〜4＋驗收 0〜6）→ 全數 ✅（明細見 REP）
- [x] G3. 最終親自 review：10 個新增/修改檔全數通讀；依賴方向正確；測試斷言真實行為（HTTP 狀態碼/JSON/DB 計數），零 mock；計畫碼兩處缺陷已回修計畫檔（bytes literal、紅燈數敘述）
- [x] G4. 歸檔 phase-01〜04 → `docs/plan/finish/`；phase-00 §1 補「歸檔規則」說明
- [x] G5. 更新 CLAUDE.md 四處（現況／指令／規格進度／重要陷阱）
- [x] G6. 寫階段G report＋總結（含異常記錄：phase-05、phase-08 於 09:10 有 session 外部 mtime 變動，內容無本次痕跡；.pytest_cache 為 pytest 正常產物）

## 完成定義

兩份 phase 文件逐項核對全數 ✅；19 個測試全綠；歸檔與 CLAUDE.md 一致；report 落檔。→ **全部完成**
