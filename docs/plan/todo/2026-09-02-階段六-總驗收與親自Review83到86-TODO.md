# 2026-09-02 階段六：Phase 83〜86 總驗收與親自 Review TODO

> 對應 dev-prompt：`docs/plan/dev-prompts/phase0902-1.md` 約束 5（逐一確認四份計畫檔的想法全部實現）與產品負責人「最後你再親自 review 一遍」。上一階段：`2026-09-02-階段五-AWS寄物櫃與佇列83到86-TODO.md`。ledger：`.superpowers/sdd/phase0902-1/progress.md`。

## 實作邏輯（新手白話）

兩個 task 的 review 都 Approved，但各留幾條 Minor；controller 裁決（R10／R11）把七條可行的併成**一次** fix wave（產品碼只動一行 log，其餘是守門測試與文件措辭），再派一次 scoped re-review。最終整體 review 由 controller 親自做（不另派一席：分支只有兩個 task、Opus 已各審過全 diff）：逐行讀 `aws_mailbox.py`／`get_cloud_route`／兩個新測試檔／`aws_check.py`／LAUNCH.md 小節 → 自己跑全部證據 → 逐條核對四份計畫檔 §6 與總覽 → 收尾文件與 memory。

## 步驟

- [x] fix wave（resume Task 1 實作者）：83 Minor ①②④ ＋ 86 Minor M1〜M4 → 全量 **644**、三死埠 644、ruff、變異證據 ×2。
- [x] scoped re-review（Opus）：七條 ADDRESSED、零新破壞（1 Minor 文件數字，controller 已改）。
- [x] controller 親讀 fix diff。
- [x] 最終驗證（controller）：全量 644／0 skipped；三死埠 644；端點 22；`ruff format --check`＋`check`；boto3 只在 `aws_mailbox.py`（`dependencies.py` 檔頭無 aws_mailbox）；三個 app 檔零 SQL token；七個改到的 py 檔 tokenize 零中文識別字；追蹤檔零帳號 ID／bucket 後綴／佇列 URL；`docs/spec/`／compose／`db/`／前端零改動；`data/` 乾淨；AWS 最終狀態（兩佇列 0／0、S3 空、`.env` off／300、開關 local）。
- [x] 四份計畫檔 §6 全部 `- [x]`、§8 數字；總覽 §2.2／§2.7／§9 row 83〜86 加「實 641／644」註記。
- [x] 寫階段六 REP；更新 memory（increment6-plan-status）；ledger 列「Rulings I made」總表。
- [x] 提醒：下一步 ★G2 之前是 Phase 87〜90（工人在 Mac 上跑）；EC2 仍不開；`docs/plan/aws/` 四份新手步驟檔帶 `--profile personaldocai-admin` 與本機不符，由產品負責人決定要不要改。

## 驗收

| 項目 | 預期 |
|---|---|
| 全量／三死埠 | **644 passed、0 skipped**，兩者相同 |
| 端點 | 22 |
| 改到的檔 | 恰 8 個程式檔（`requirements.txt`、`aws_mailbox.py`、`dependencies.py`、`test_aws_mailbox_unit.py`、`test_dependencies_cloud_unit.py`、`test_cloud_ingest_unit.py`、`test_design5_error_paths.py`、`scripts/aws_check.py`）＋ `deploy/aws/s3-lifecycle.json`、`LAUNCH.md`、計畫檔／總覽／TODO／REP |
| 未 commit | HEAD 仍 `a159131`、零 staged |
