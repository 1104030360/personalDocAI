# 2026-08-18 階段B：Phase 01 環境準備 TODO

## 目標

照更新後的 `docs/plan/unfinish/phase-01-環境準備.md` 把三塊地基準備好：Python 虛擬環境＋套件、PostgreSQL@17（5433）＋pgvector＋兩個資料庫、Ollama 兩個模型，並通過該文件全部 9 條驗收。

## 實作邏輯

- 「測試先行」在本階段的形式：phase-01 的 **9 條驗收標準就是測試**——每一步做完立刻執行對應驗收指令、比對預期輸出，全部通過才算完成（正式的 pytest 測試依計畫從 Phase 05 開始，不提前建立，以免破壞已審查的測試數鏈）。
- 耗時的模型下載（好幾 GB）最先啟動、背景進行，與其他安裝並行。

## 步驟

- [x] B1. `uv venv --python 3.12` 建 `.venv`（uv 已裝，跳過安裝）
- [x] B2. 建 `requirements.txt`（照文件內容逐字）＋ `uv pip install -r requirements.txt`
- [x] B3. `brew install postgresql@17` → 改 `port = 5433` → `brew services start postgresql@17` → 確認 @14（5432）仍正常
- [x] B4. `~/.zshrc` 加 PATH＋PGPORT=5433；`brew install pgvector`
- [x] B5. `createdb visual_memory`、`createdb visual_memory_test`（連 5433）＋兩庫 `CREATE EXTENSION vector`
- [x] B6. 等待／確認 `ollama pull gemma4` 與 `ollama pull bge-m3` 完成（背景已啟動；gemma4 若名稱不存在，依文件 FAQ Q4 換模型並記錄）
- [x] B7. 逐條執行 9 條驗收（Python 版本、套件 import、兩庫 pgvector、Ollama 服務、兩模型在列、中文向量、英文向量、看圖測試）
- [x] B8. 寫階段B report

## 完成定義

9 條驗收全部拿到預期輸出；既有 @14 資料庫完全未受影響（`psql -p 5432 -l` 前後一致）。
