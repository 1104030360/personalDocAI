# 2026-08-21 階段KK：總驗收與親自 Review（P18〜P20 收尾）——TODO

## 實作邏輯

三個 phase 都由 Opus subagent 依計畫實作、每個 phase 完成當下我已各 review 一次 diff＋複跑測試。本階段做**整輪收尾**：

1. **親自全面 review**：把整個工作樹 diff（產品碼＋測試＋規格檔）從頭到尾再看一遍——不是抽查，是使用者指定的最終人工把關。重點：分層紀律（SQL 只在 repository）、不吞錯、規格檔紅線（自然語言詢問.feature 零改動）、design1.md §14 被否決方案沒有回魂、P18〜P20 驗收清單交叉再核。
2. **全量回歸**＝124 passed＋12 條 Rule（P20 改版後為 10＋5=15 例）全綠。
3. **真模型手動煙霧**（不進 CI；P18 步驟 8＋P19 步驟 7 延到這裡統一做）：印出注入清單後的真 prompt＋clamp 實測（讀正式庫，唯讀）；uvicorn 起真伺服器＋真 Ollama 上傳真照片 → 檢查 `data/` 落檔、縮圖/原圖端點 200、正式庫舊照片（路徑 NULL）404、新回應四欄位齊全。
4. **文件收尾**：CLAUDE.md 現況段更新為 P18〜20 完成的敘述（專案結構真的變了：上傳語意改版、端點 4→6、測試 103→124、規格檔 7→10 Rule）。

## 步驟

1. [x] Phase 20 完成後：親自複跑全量＋規格檔（124／17 passed），review P20 diff
2. [x] 整輪 diff 總 review（16 檔逐一過目；與計畫零實作偏差、分層與紅線全守住）
3. [x] P18〜P20 三份驗收清單逐項終核（最終新鮮批次全過）
4. [x] 真模型煙霧：P18 步驟 8（六行清單注入＋clamp 五例全對，正式庫唯讀）
5. [x] 真模型煙霧：真收據 JPEG＋真 gemma4 → 201、suggested=收據、落庫未分類、data/ 落檔 SHA1 一致、縮圖 410×512、讀圖 200、舊照片 404（正式庫 3 列）
6. [x] CLAUDE.md 現況段更新（P18〜20 成果段＋124 顆＋15 條 Rule 註解＋唯讀例外註記）
7. [x] 寫階段KK REP＋最終總結（commit 拆法與歸檔建議見 REP；發現使用者已備妥下一輪 phase0821-1.md，未擅自展開）
