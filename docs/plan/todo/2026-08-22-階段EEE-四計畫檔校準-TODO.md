# 階段EEE TODO：Phase 34〜37 四份計畫檔對現況校準（phase0822 任務 1）

> 日期：2026-08-22　狀態：✅ 完成（見同名 REP）
> 對應 dev-prompt：`docs/plan/dev-prompts/phase0822.md` 任務 1——
> 「先根據目前專案的狀態，逐個更新四份計畫檔案，全部更新完才進下一步」。

## 實作邏輯

四份計畫（phase-34〜37）寫成於 Phase 28〜33 實作**之前或之中**，其中 35／36 已於
2026-08-22 由產品負責人補「已釐清」段；但**對照現有程式碼**仍缺具體落點（介面簽名、
遷移腳本、端點數、測試檔位置）。本階段逐檔比對 `app/` 現況，把「實作時才會發現的
落差」先寫死進計畫，讓後續 TDD 實作照著走、不必邊做邊猜。

現況基線（2026-08-22 實測）：`pytest -q`＝**218 passed**；`/openapi.json` 端點＝**14**；
git HEAD＝`0cabb45`（Phase 31〜33 **已包含在此 commit**——CLAUDE.md 說「31〜33 未 commit」
是過時敘述，列入 Phase 37 的 CLAUDE.md 更新）。

## 逐檔核對結果與要補的內容

### phase-34-詢問三路.md

核對 OK：RouteDecision／ROUTE_PROMPT／SEARCH_MODE_LABELS／FakeRouter／FakeAnswerLLM
名稱與位置全對；端點數不變（14）正確；`find_entity_by_name` 已存在（P29）。

要補（計畫沒寫到、實作必經）：
1. 路由要注入「現有實體名單」→ `RouterClient.route` 介面必須多收實體名單參數，
   OllamaRouter／FakeRouter／ask.py（端點層讀 `list_entities()`）三處同步。
2. task 路的 Document：`metadata["id"]` 必須＝**來源照片 id**（`retrieved_photo_ids`
   契約不變、FakeAnswerLLM 免改）。
3. `list_photos_with_entity` 回傳欄位要能直接餵 `row_to_document`。
4. `search_tasks(due_before)` 語意明確化（None＝全部；有值＝排除無期限）。
5. 「這週」→ `due_within_days=7`；測試檔落點。

### phase-35-抽屜糾錯few-shot.md

核對 OK：`folder_correction` 表已建（P29）；已釐清 B／D 兩決策已入檔。

要補：
1. `photo.suggested_category` 是**新欄位＝要遷移**：schema.sql＋migrate_design3.sql
   加冪等 ALTER＋正式庫執行（計畫原文只說「新欄位」沒說遷移路徑）。
2. `PHOTO_COLUMNS` 加欄；`insert_photo` 多收參數（clamp 成「未分類」→ 存 NULL）。
3. `VLMClient.understand` 簽名加 corrections（protocol＋OllamaVLM＋FakeVLM＋router；
   PDF 多頁共用同一份）。
4. 待決定分頁畫①的資料路徑：GET /folders/{id} 照片摘要 4→5 鍵（+suggested_category），
   既有「恰四鍵」測試同步修訂。
5. 糾錯比對 casefold() 不分大小寫；記錄失敗不影響歸類本體（try/except log）。

### phase-36-無線鏡頭.md

核對 OK：路線 B 已釐清段完整；D5 未推翻項清楚。

要補：
1. **端點 14→17**（HTTP 三支；WS 不進 openapi.json——清點方式要寫明）。
2. QR 實作定案：segno（純 Python 零相依）→ requirements.txt；session 回應帶 qr_svg。
3. session 存放設計細節（secrets.token_urlsafe、time.monotonic 時基可注入、
   新建即汰舊、桌面 WS 斷線即失效）。
4. WS 純 relay 設計；亂／過期 token：HTTP 404、WS 拒連。
5. LAN IP 推導規則（request host 優先、localhost 才 socket 偵測）。
6. mkcert 步驟含 **iPhone 信任 CA** 的操作、寫進 CLAUDE.md 指令區。
7. 真機驗收（iPhone）＝產品負責人手動；自動化測試只到 TestClient WS／token 生命週期。

### phase-37-增量三錯誤收尾與全量回歸.md

要補：
1. 錯誤表加 camera 三列（亂 token 404／過期 404／WS 拒連）。
2. 端點數改 **17**；健檢含 suggested_category 欄與六表。
3. CLAUDE.md 更新要修正「31〜33 未 commit」過時敘述。
4. 「歸檔隨 commit」：本輪產品負責人指示不 commit → 歸檔留待 commit 時執行。

### 連帶（總覽）

phase-00-增量三總覽.md §3「端點 9→14」補 P36 +3=17 註記；§2 進度表隨各 phase 完成打勾。

## 步驟

- [x] 讀四份計畫＋design3.md＋總覽
- [x] 讀 app/ 現況（ask_workflow／retrieval／repository／vlm_service／photos router／
      config／dependencies／fakes／conftest／schema／migrate）＋前端結構
- [x] 跑 baseline pytest（218 passed）
- [x] 依上表逐檔更新 phase-34〜37（每檔加「2026-08-22 對現況校準」段）
- [x] 總覽 §3 補端點註記
- [x] 寫階段EEE REP
