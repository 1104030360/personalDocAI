# 2026-08-19 階段K：總驗收與收尾 TODO

## 目標

dev-prompt `phase0819-1.md` 的收官：主控**親自** review 整條分支（使用者指定）、逐一確認 phase-05／phase-06 兩份計畫檔每個想法皆已實現（約束 5）、全量回歸取證，然後歸檔已完成計畫、校正 CLAUDE.md、把 `feature/phase-05-06` 併回 `master`。

## 實作邏輯

- **不信任「剛剛跑過」**（沿用階段G 慣例）：總驗收一律由主控重新執行取證——pytest 全量、兩份計畫的驗收指令重點項、SQL 唯一性 grep、端點行為、兩庫收尾 0 筆、5432 完好。
- **主控親自 review**（使用者明示「最後你再親自review 一遍」）：通讀本分支全部 app/tests 變更檔原始碼（不只看 diff 摘要），檢查分層依賴方向、測試誠實性、與計畫逐字一致性、subagent review 是否有漏。
- **逐項核對**：兩份計畫檔的「逐步驟操作」與「驗收標準」逐條打勾；`.feature` Rule U1〜U7 行為落地狀態確認。
- 收尾動作依既有慣例：完成的 phase 計畫移 `docs/plan/finish/`、CLAUDE.md 現況段校正、merge master（--no-ff，保留 phase 區塊）、刪除 feature 分支。

## 步驟

- [ ] K1. 全量回歸取證：pytest 36 passed（重跑）、smoke/unit 分檔、資料庫兩庫 0 筆、SQL 唯一性、5432 清單完好
- [ ] K2. 主控親自通讀本分支全部 app/tests 變更（10 檔）＋抽驗端點行為（openapi、415/422/201）
- [ ] K3. 逐項核對 phase-05／phase-06 每個步驟與驗收條目（含被裁定跳過者的記錄）
- [ ] K4. 歸檔 phase-05／06 → `docs/plan/finish/`；CLAUDE.md 現況段校正
- [ ] K5. 寫階段K report；merge --no-ff 回 master、刪 feature 分支；最終回報（含全部 rulings 清單）

## 完成定義

36 passed 重現；兩份計畫逐條 ✅（跳過者有裁定記錄）；主控 review 無未解 Critical/Important；master 上拿到完整成果；文件（TODO×4、REP×4、歸檔、CLAUDE.md）齊備。
