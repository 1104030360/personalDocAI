# 2026-08-19 階段P：計畫檔更新（phase-09／phase-10）TODO

## 目標

依 dev-prompt `phase1819-3.md` 任務 1：對照專案現況，把 `docs/plan/unfinish/phase-09-檢索層.md` 與 `phase-10-路由與流程圖.md` 的過時內容全部修正（並循慣例同步 `docs/plan/finish/phase-00-總覽.md` 受影響列），**確保計畫檔全部更新完才進入開發**。

## 實作邏輯

- **先盤點再動筆**（已實查）：
  - 基線 master `pytest -q` = **40 passed**（Phase 01〜08 完成）；兩計畫寫的累計數 21／26 是 P07 重算前的舊制數字。
  - 測試目錄自 P07 起已分層 `tests/unit/`＋`tests/integration/`；兩計畫仍寫 `tests/test_retrieval.py`、`tests/test_workflow_route.py`（歸屬子目錄「屆時再定」——現在就是屆時）。兩個新測試檔都連真測試庫 → 歸 **`tests/integration/`**。
  - `app/services/retrieval_service.py`、`ask_workflow.py` 存在但為**空檔**（Phase02 骨架佔位）→ 計畫「寫檔」步驟可直接照做，無衝突。
  - `config.py` 已有 `RECENT_DAYS`／`TOP_K`；`photo_repository.py` 的既有 import 行、`PHOTO_COLUMNS`、`insert_photo` 簽名與計畫假設**完全吻合**，程式碼區塊不必改。
  - 套件實測：langgraph **1.2.11**、langchain-core **1.5.6**、langchain-ollama **1.1.0** 已裝；`grandalf`（P10 驗收 4 畫 ASCII 圖用）未裝，計畫 Q1 已寫安裝指令。
- **P09 過時點**：驗收 2 累計數 21（=11+10）→ **50**（=40+10）；步驟 3／驗收 1 的測試路徑改 `tests/integration/test_retrieval.py`；「完成後的專案狀態」21 → 50；補「執行順序採 TDD」加註（任務書約束 1：先寫測試看紅、再實作轉綠）。
- **P10 過時點**：前置條件「測試累計 21」→ 50；驗收 2 累計數 26（=21+5）→ **55**（=50+5）；步驟 3／驗收 1 路徑改 `tests/integration/test_workflow_route.py`；步驟 2 的 `RouteDecision` import 改放 fakes.py 檔頂（循檔內既有 import 慣例）；「完成後的專案狀態」26 → 55；同補 TDD 加註。
- **總覽同步**（循階段L 慣例＋總覽 🔄 註記「P09 起開工前再重算」）：P09/P10 兩列改新數字與 integration/ 路徑；§4 tests/ 樹把兩檔移進 integration/ 分支；追加第四條 🔄 註記（P11 起仍舊制）。
- 執行方式：計畫檔更新由主控親自執行（計畫即後續 subagent 的任務書）；LangGraph／LangChain API 用法以官方文件 MCP 查證，不憑記憶。

## 步驟

- [x] P1. 更新 `phase-09-檢索層.md`（TDD 執行順序加註、測試檔路徑 → integration/、驗收與完成狀態累計數 21→50）
- [x] P2. 更新 `phase-10-路由與流程圖.md`（TDD 加註、路徑 → integration/、前置 21→50、驗收與完成狀態 26→55、fakes import 位置＋順序陷阱警告）
- [x] P3. 同步 `phase-00-總覽.md`（P09/P10 兩列 50/55、tests/ 樹兩檔移入 integration/、第四條 🔄 註記）
- [x] P4. 三檔交叉核對（grep 驗證：50/55 與 integration/ 路徑全一致、無殘留舊路徑）＋寫階段P report

## 完成定義

phase-09／phase-10 每個步驟都能在當前專案狀態（40 passed、tests 分層、空佔位 service 檔、langgraph 1.2.11）下照做不誤；數字與路徑三檔交叉一致。
