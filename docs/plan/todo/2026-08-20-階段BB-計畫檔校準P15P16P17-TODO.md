# 2026-08-20 階段BB：計畫檔校準（phase-15／16／17）TODO

## 目標

執行 dev-prompt `phase0820.md` 任務 1：對照專案現況（master 79 passed、Phase 01〜14 完成、`GET /` 轉址端點已於 2026-08-20 追加、正式庫 2 列真實照片），逐一校準 `phase-15-資料庫一次改版與預設資料夾.md`、`phase-16-資料夾資料層.md`、`phase-17-檔案儲存服務.md` 三份計畫；有過時就改、沒過時就留下「已實查」的證據，三份都確認完才准進入開發。

## 本輪階段規劃（dev-prompt phase0820.md 全程）

| 階段 | 內容 | 產出 |
|---|---|---|
| BB | 計畫檔校準 P15〜P17（本檔） | 校準結論＋證據 |
| CC | Phase 15：資料庫一次改版與預設資料夾（TDD，79→83） | folder 表＋種子＋遷移 |
| DD | Phase 16：資料夾資料層（TDD，83→93） | 五個 repository 函式 |
| EE | Phase 17：檔案儲存服務（TDD，93→103） | storage_service＋DATA_DIR 隔離 |
| FF | 總驗收與收尾（驗收清單逐項核對＋整體 review＋歸檔） | 全綠證據＋歸檔 |

執行方式：CC〜EE 各派一個 implementer subagent（Opus）照計畫逐字實作（計畫已含完整程式碼，屬「轉寫＋測試」型任務），每階段結束由本人親自 review 完整 diff 並重跑驗收；FF 由本人親自做整體 review。**依使用者指示，本輪全程不 git commit**（計畫內的 commit 步驟一律跳過，留待使用者確認後自行處理）。

## 實作邏輯

- **先盤點再動筆**：實跑 `pytest -q` 對基線；正式庫實查列數與 category；三份計畫引用的每一段「改前程式碼」逐字對照現檔；每個插入點（檔案、段落、行號）逐一確認存在。
- **重點風險專查**：Phase 15 聲稱「完全不改對外行為」，但 `PHOTO_COLUMNS` 加四欄會讓 `insert_photo`／`fetch_photo`／兩條 search 的回傳列多出新鍵——必須實讀 `photos.py` router、`retrieval_service.row_to_document`、`ask_workflow` 確認全部是「逐鍵取值」而非整列透傳，多出的鍵才真的無害。
- **單元測試專查**：確認 `tests/unit/test_photo_repository_unit.py` 沒有 mock SQL 字串或斷言 `PHOTO_COLUMNS` 內容，避免加欄位讓它紅。
- **驗收 grep 預演**：Phase 17 的 `from app.core.config import` 禁令、三份計畫共用的「SQL 只在 repository」grep，開工前先跑一次確認現況就是綠的。

## 步驟

- [x] BB1. 實查基線：`pytest -q`＝79 passed；`psql -d PersonalDocAI` 確認 photo 2 列、category 皆「收據」
- [x] BB2. Phase 15 逐段核對：schema.sql 現況、`photo_repository.py` docstring／`PHOTO_COLUMNS`／`insert_photo` SQL 三段「改前」引文、conftest `clean_photo_table` 引文、`test_photo_repository.py` 現有 10 測與 `_insert_sample` 簽名、預期紅燈數（4 failed, 10 passed）
- [x] BB3. Phase 15 行為面核對：router／retrieval／ask_workflow 逐鍵取值證明「加欄不漏鍵」；`clear_photos` 保留理由（`test_count_and_clear_photos` 仍用）；遷移腳本可重跑性與預期輸出行數
- [x] BB4. Phase 16 核對：`_vec`／`insert_photo` 簽名、種子 id 1〜6 依賴（reset_tables 重播）、`id == 7` 斷言前提、五函式鍵名契約
- [x] BB5. Phase 17 核對：conftest 插入點（pytest 第 10 行、config 第 12 行、`wire_fake_ai` 之後 `client` 之前）、fakes.py import 區引文、requirements.txt 段落插入點、config.py import 區與「業務常數」段、.gitignore 現況＋data/、`from app.core.config import` grep＝0
- [x] BB6. 依核對結果決定：需要改就逐檔改完再往下；不需要改就把證據寫進階段BB REP

## 完成定義

三份計畫對現況零矛盾（引文逐字相符、插入點存在、基線與預期測試數對齊、行為面風險排除），校準結論與證據寫成 REP；`pytest -q` 仍 79 passed（純文件／驗證工作）。
