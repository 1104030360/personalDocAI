# 2026-09-01 階段五：Phase 79 雲端路本機端單圖 ＋ Phase 80 逾時與冪等 TODO

> 對應 dev-prompt：`docs/plan/dev-prompts/phase0901.md` 任務 2。
> 上一階段：`2026-09-01-階段四-閘門接線78-TODO.md`。
> 計畫檔：`docs/plan/unfinish/phase-79-雲端路本機端單圖.md`、`phase-80-雲端路逾時與冪等.md`。

## 實作邏輯（新手白話）

Phase 78 之後，岔路口五條出口有四條通往本機，只剩「非敏感＋遠端開著」是 `NotImplementedError("Phase 79")`。
本階段把它補完——**全程仍不碰 AWS**（★G1 前），靳靠 Phase 77 的 `FakeMailbox`（一顆假件同時扮演 S3＋兩條佇列）
把整圈走通，boto3 要到 Phase 83 才出現。

```text
Phase 79（順利的那一圈）
  CloudRoute(mailbox, probe, *, timeout_seconds)
    available()      probe.is_running()；丟例外→False
    submit()         順序鐵律：PutObject context.json → PutObject input.* → SendMessage jobs {job_id, s3_key}
    wait_result()    基本版：收到自己的 job_id → GetObject result.json → dict；deadline 到 → None
    fetch_result()   GetObject result.json（崩潰重送用）
    cleanup()        刪三個 S3 物件（context／input／result）
  gated_ingest 的雲端成功路：
    submit 失敗 → cleanup、route=local、log "fallback=local reason=submit_failed"、run_ingest_job
    wait_result None → cleanup、route=local、"reason=result_timeout"、run_ingest_job
    result.understood=False → fail_job（**不是** fallback：遠端活著只是看不懂，§10.1 g）
    result.understood=True  → 本機 embed_understanding（最多 3 次，只重算向量、不重看圖）
                              → insert_photo_with_files → **立刻 store.update(photo_ids=[id])**（§10.2 R）
                              → cleanup → finish_image_job → log "雲端結果已入庫"
  tests/fakes.py 的 fake_worker_process_one(mailbox, understanding)：把 jobs 第一則變成 result.json＋一則 results 訊息

Phase 80（不順利才是常態）
  wait_result 完整五條規則（總覽 §2.5）：
    ① 迴圈到 deadline，每次 receive_result(wait_seconds=min(20, 剩餘))
    ② 自己的 job_id：result.json 有→回 dict；沒有→刪訊息、回 None
    ③ 別人的 job_id：store 查無或 route==local → 殘訊息：刪訊息＋刪它的三個 S3 物件；否則 release_result_message＋sleep(1)
    ④ deadline 到 → None
    ⑤ body 只解析 job_id、不含位元組
  gated_ingest 補 route=="cloud" 崩潰重送：fetch_result 有 → **重新 store.get** 再落庫（D17 最後一道保險，校準 E／F 發現）；
    無 → 盡力清雲端、route=local、"reason=redelivered_without_result"、run_ingest_job
  _盡力清雲端()：cleanup 例外只 log（cloud 可能已是 CloudRouteOff）
```

## 步驟

- [x] Phase 79：實作 subagent（Opus）→ 601（+10）——4＋6 顆看紅 → `fake_worker_process_one` → `cloud_ingest.py` 加長（`CloudRoute` 本體）→ `gated_ingest.py` 整檔重貼（換掉 NotImplementedError）→ 把 78 的兩顆「遠端關閉／探測丟例外」升級成真 `CloudRoute(FakeMailbox(), FakeProbe(...))` → 全量 +10 → ruff → 三死埠。
- [x] Phase 79：審稿＋修正迴圈 → Approved（行為）；2 Important 測試守備 → R13 fix round 1（+1 顆＝602）；re-review 全數 ADDRESSED（重點：submit 順序 context→input→jobs 靠 `FakeMailbox.calls` 流水帳；jobs body 恰兩鍵無位元組；§10.2 R 的 photo_ids 先寫再 cleanup；understood=False 走 fail_job 不 fallback；status 全程 analyzing 不新增狀態；`kind=embed backend=local` 且無 `kind=vlm`）。
- [x] Phase 80：實作 subagent → 613（+11 含 R14）、6 顆真紅／5 顆預告綠、R14 變異證據——5＋5 顆看紅 → `wait_result` 五條規則＋`_處理別人的訊息`＋時鐘／sleep seam → `gated_ingest.py` 整檔重貼（`_繼續雲端路`＋`_盡力清雲端`）→ 全量 +10 → ruff → 三死埠。
- [x] Phase 80：審稿＋修正迴圈 → Approved，0 Critical／0 Important／4 Minor（計畫檔數字 controller 已修；CloudRouteOff 重送路零測試→總覽 P95 候選）（重點：五條規則逐字；每次等待 1〜20 秒；別人的訊息兩種處置；同一 job_id 送兩次仍一列；重讀 store 那一行）。
- [x] 我親自抽查 `cloud_ingest.py` 與 `gated_ingest.py` 全文。
- [x] 寫 REP（`2026-09-01-階段五-雲端路79-80-REP.md`）。

## 驗收

| 檢查 | 79 | 80 |
|---|---|---|
| 新增顆數 | `test_cloud_ingest_unit.py` +4、`test_gated_ingest.py` +6 | 同兩檔各 +5 |
| 全量 | 基線 591 → **601**、0 skipped | 601 → **611**、0 skipped |
| 端點 | 22 | 22 |
| 掃碼 | `cloud_ingest.py`／`gated_ingest.py` 無 boto3、無 SQL token；`FakeMailbox.jobs`／`results` 訊息 body 不含位元組 | 同左 |
| 零依賴 | 三死埠一起指顆數不變 | 同左 |
| 唯二暫時分支 | `NotImplementedError("Phase 79")` 消失；PDF 分支**不**加第三個 NotImplementedError | — |

## 安全鐵律

不 commit、不搬檔；一次只有一個 subagent 跑 pytest；★G1 前零 AWS（`CLOUD_ROUTE` 預設仍 off、正式路徑走不到 `CloudRoute`）；`docs/spec/`／compose／正式庫零改動。
