# 2026-08-19 階段J：Phase 06 向量與寫入（TDD）TODO

## 目標

照更新後的 `docs/plan/unfinish/phase-06-向量與寫入.md`，以 TDD 完成上傳流程最後三步：`indexing_service.py`（固定順序合併 Document＋轉向量）、`dependencies.py` 加 `get_embeddings`／`get_now`、router 完整五步（格式→看圖→合併轉向量→一條 INSERT→`UploadResponse` 201），通過驗收（`pytest -q` **35 passed** 為正式驗證）。

## 實作邏輯

- **先紅後綠（步驟 0 → 4）**：先擴充 `tests/fakes.py`（FakeEmbeddings 決定論向量＋FixedClock）、conftest `wire_fake_ai`（加 embeddings＋固定時鐘 2026-08-18 10:00）、新增 `tests/unit/test_indexing_service_unit.py`（4 個）、smoke +1（完整寫入並回 201）＋英文斷言改 metadata 巢狀，跑紅燈；再填 `indexing_service.py`、擴 `dependencies.py`、改寫 `photos.py` 轉綠。
- **BDD 對應**：Rule U4（向量由文字＋metadata 合併產生——unit 鎖固定順序「文字/類別/地點/物品/時間、空欄位省略、標籤固定中文值保原文」）、U5（上傳時間——FixedClock 驗 `uploaded_at`）、U6（201 回應 id＋文字＋四欄位 metadata——`UploadResponse`）、U2/U3（寫入 text 與四欄位——`fetch_photo` 驗 DB）。
- **同輸入同向量**：合併順序固定＋FakeEmbeddings 決定論（詞彙雜湊＋正規化），embed 用 `embed_query`（上傳與之後詢問同一種轉法）。
- **絕不打真模型**：conftest 安全網從本階段起三件全接（vlm／embeddings／clock）——Phase 5 改寫的兩個 201 測試如今走完整寫入流程，靠它保持決定論。
- 驗收第 7 條（可選 curl＋真 Ollama）本階段跳過——真模型路徑屬 Phase 8 手動煙霧職責（裁定見 ledger／階段J REP）。
- 執行方式：opus subagent 實作（計畫檔＝任務書，程式碼逐字轉寫），主控 dispatch reviewer 審查＋親自複跑驗收。

## 步驟

- [ ] J1. 步驟 0：測試先行（fakes 擴充、conftest wire_fake_ai 擴充、unit 4、smoke +1、英文斷言改巢狀）→ 紅燈留證（commit：test）
- [ ] J2. 步驟 1〜3：indexing_service.py／dependencies.py／photos.py（逐字轉寫計畫碼）→ 綠燈 35 passed（commit：feat）
- [ ] J3. 驗收 1〜6：smoke 6 passed、unit 4 passed、全量 35 passed、合併格式中/英、手動寫入腳本（向量長度 1024＋psql 查回）
- [ ] J4. 任務 review（reviewer subagent：規格符合＋程式品質）→ 發現須修則進 fix loop
- [ ] J5. 主控核可＋寫階段J report

## 完成定義

`pytest -q` 35 passed（unit 12＋integration 23）；驗收 1〜6 全過（7 跳過已裁定）；review 無未解 Critical/Important 發現；Phase 3〜5 既有行為未被更動；上傳流程五步完整跑通。
