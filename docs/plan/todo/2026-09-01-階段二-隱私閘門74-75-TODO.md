# 2026-09-01 階段二：Phase 73 收尾確認 ＋ Phase 74／75 隱私閘門 TODO

> 對應 dev-prompt：`docs/plan/dev-prompts/phase0901.md` 任務 2（TDD＋BDD 實作）。
> 上一階段：`2026-09-01-階段一-八份計畫檔校準73-80-TODO.md`（校準完成後才開工）。
> 計畫檔：`docs/plan/unfinish/phase-74-隱私閘門規則版.md`、`phase-75-隱私閘門本機模型備援.md`（檔名是歷史，內容已是 VLM 短問版）。

## 實作邏輯（新手白話）

增量六要把「明確不敏感」的照片卸到雲端看圖。卸之前必須先問「這張能不能出門」——這就是**隱私閘門**。
產品負責人 2026-09-01 拍板：閘門**只看圖**（同一顆看圖 VLM、另一份只問「敏不敏感、有沒有把握」的短 prompt），
**不看檔名**，失敗或沒把握一律當敏感（`UNCERTAIN`）留在本機。

```text
Phase 74（契約與假件）                    Phase 75（真模型、縮圖、計時）
  Verdict 三分類                             OllamaPrivacyModel（跟頁首開關：本機 Ollama／ollama.com）
  PrivacyJudgement{sensitive, confident}     shrink_for_model：長邊 ≤512、轉 PNG 再問
  judgement_to_verdict：                     PDF：先渲染第一頁再問；壞檔→UNCERTAIN
    sensitive→SENSITIVE（沒把握也算）        ai_timing 多一種 kind=privacy（前後兩行 log）
    不敏感且有把握→NON_SENSITIVE             build_privacy_gate_for_backend(ai_backend)（裁決 R1）
    其餘→UNCERTAIN                           get_privacy_gate() ＝ 上面那支吃 config.AI_BACKEND
  VlmGate(model)：讀檔→問模型→轉三分類；
    讀檔／問模型丟例外→UNCERTAIN；del filename
  get_privacy_gate() → VlmGate(永遠 UNCERTAIN 的占位模型)
  FakePrivacyGate／FakePrivacyModel 假件；wire_fake_ai 預掛 FakePrivacyGate(UNCERTAIN)
```

**兩個 phase 做完都還沒接線**（接線是 Phase 78）：對外行為零改變、端點仍 22。

## Phase 73 的收尾確認

Phase 73（pre-commit 與 CI）在階段一校準時已確認**全部完成**（commit `902360c`／`6df5fc6`／`4269985`／`a53ab57`；
產品負責人 08-28 建 repo、push、CI 綠 ×2）。本階段不再有程式工作；歸檔（`git mv` 到 finish/）隨產品負責人的 commit。

## 步驟

- [x] Phase 74：派實作 subagent（Opus）照計畫檔 TDD：先寫 11 顆測試看紅 → 建 `privacy_gate.py`／`get_privacy_gate`／假件／第七個 override → 11 綠 → 全量 554、0 skipped → ruff 全綠。
- [x] Phase 74：派審稿 subagent（Approved；1 Important plan-mandated→R9 移到 75；4 Minor 已裁決）（Opus）對「快照 diff」審 spec 合規＋品質；有 Critical／Important 就進修正迴圈（最多 5 輪）。
- [x] Phase 75：派實作 subagent（564 passed、死埠 564）：追加 9＋1 顆看紅 → `OllamaPrivacyModel`／`shrink_for_model`／PDF 第一頁／`_目標()` 加 `privacy`／`build_privacy_gate_for_backend`＋`get_privacy_gate` 改版 → 564、0 skipped → ruff。
- [x] Phase 75：審稿＋修正迴圈。→ Approved；1 Important（`judge()` 零覆蓋）裁決 R10 就地補 2 顆（564→566）；fix round 1 re-review 全數 ADDRESSED、零新破壞。
- [x] 我親自抽查：`privacy_gate.py` 全文、`dependencies.py` diff、conftest diff；`grep` 掃碼（無 `AI_BACKEND =`／`RuleGate`／`psycopg`）。
- [x] 寫 REP（`2026-09-01-階段二-隱私閘門74-75-REP.md`）。

## 驗收（每個 phase 都要）

| 檢查 | 74 | 75 |
|---|---|---|
| 該檔新增顆數 | `test_privacy_gate_unit.py` 11 | 同檔 +9、`test_ai_timing_unit.py` +1 |
| 全量 | 554 passed、0 skipped | 564 passed、0 skipped |
| 端點 | 22 | 22 |
| 掃碼 | `privacy_gate.py` 無 `RuleGate`／`SENSITIVE_KEYWORDS`／`AI_BACKEND =`；無 `psycopg`／`cursor(`／`.execute(` | 同左 ＋ 可讀 `config.AI_BACKEND` |
| 安全網 | `wire_fake_ai` 七個 override；`get_privacy_gate()` 直接呼叫仍回 `VlmGate` | pytest 仍走 `FakePrivacyGate`（否則會打真 Ollama） |
| 零依賴 | — | `OLLAMA_BASE_URL=http://127.0.0.1:9 pytest -q` 顆數相同 |

## 安全鐵律

- 不 commit、不搬檔；一次只有一個 subagent 跑 pytest；`docs/spec/` 零改動。
- Phase 75 接了真模型：**pytest 必須繼續走 FakePrivacyGate**，任何一顆漏掉安全網都會真的打 Ollama（Ollama 目前沒開，會是連線錯誤而非誤觸模型——但那顆就是紅的）。
