# 2026-09-02 階段八：雲端工人 Phase 87〜88（核心＋主迴圈＋Mac 端到端）— TODO

> dev-prompt：`docs/plan/dev-prompts/phase0902-2.md`。ledger：`.superpowers/sdd/phase0902-2/progress.md`。
> 計畫檔：`docs/plan/unfinish/phase-87-cloud_worker核心.md`、`phase-88-cloud_worker主迴圈與Mac端到端.md`（階段七已校準）。

## 實作邏輯

- **Phase 87**：新建 `app/workers/cloud_worker.py`，一個純函式 `process_job_message(mailbox, message, vlm)` 把一則 jobs 訊息從頭處理到尾——冪等檢查（`result.json` 已在→只補送 results；input 不在→只刪訊息；壞 s3_key→只刪訊息）→ 拿 `context.json`（缺→三份空清單）→ 由副檔名推 content_type → 用 Ollama Cloud 看圖最多 `VLM_MAX_ATTEMPTS` 次（PDF 逐頁各自算）→ **先** `put_object(result.json)`、**再** `send_result`、**最後** `delete_job_message`（design6 D9 順序鐵律）。工人**零資料庫、零 embedding、零 boto3 import**（只認 `CloudMailbox` Protocol，全部用 `FakeMailbox` 測）。
- **Phase 88**：在同一檔外面包一圈：`run_forever(mailbox, vlm, *, should_stop)` 長輪詢 20 秒、單則例外 log 後繼續、receive 失敗退避 5 秒；`_install_stop_signal()` 把 SIGTERM／SIGINT 變成旗標（做完手上那則才退）；`_configure_logging()` 比照 `app/main.py` 掛 handler；`main()` 組 `AwsMailbox`＋`OllamaCloudVLM`；`python -m app.workers.cloud_worker` 可跑。然後 controller 在 Mac 上對真 S3／真 SQS／真 Ollama Cloud 跑一次端到端（丁段驗收），並把操作寫進 `LAUNCH.md` §12（英文）與 `CLAUDE.md` 指令區（繁中）。
- **流程**：每個 phase 一位 Opus 實作者（TDD：先紅後綠）→ Opus 審稿者對工作樹快照 diff 做 spec＋品質 review → 有 Critical／Important 就 fix round＋scoped re-review → controller 記 ledger。端到端煙霧由 controller 親跑（腳本 `scratchpad/smoke88.sh`，沿用 Phase 86 的作法）。

## 步驟

- [x] Task 1：派 Phase 87 實作者（644 → 656 passed、0 skipped；三死埠 656；四檔與計畫檔碼區逐字相同）。
- [x] Task 1 review：Approved（0 Critical／0 Important／7 Minor，Minor 併入 Task 2 之後的一次 fix wave）。
- [x] Task 2：派 Phase 88 實作者（656 → 661 passed、0 skipped；LAUNCH.md §12、CLAUDE.md 小段皆與計畫檔逐字相同）。
- [x] Task 2 review：Approved（0 Critical／0 Important／5 Minor）；**controller 煙霧抓到 Important**（`python -m` 下 `logging.getLogger(__name__)` 變成 `__main__` logger、四種 INFO 被吞）→ fix round 1（字面 logger 名＋subprocess 測試＋退避前檢查停止旗標；661→663）→ scoped re-review 2/2 ADDRESSED。
- [x] controller 親跑 Phase 88 §4.6 端到端：收據 9 秒走完雲端路入庫 #67、證件 `route=local verdict=SENSITIVE` 零 S3、SIGTERM 20 秒內退出；fix 後再跑一輪（#69，8 秒）四行 INFO 全到齊；cleanup 完成（`.env` off／300、`CloudRouteOff`、開關 local、佇列 0／0、S3 空）。：終端機 A `python -m app.workers.cloud_worker`；開關撥 cloud；`.env` 切 `assume`／60（計畫寫 30，controller 給長輪詢 20 秒留餘裕）；上傳合成收據 → 工人看圖 → `result.json` → 本機入庫；合成證件 → `route=local`、零 S3；Ctrl+C／SIGTERM 優雅停；purge 殘訊息；`.env` 改回 off／300、開關撥回 local。
- [x] 寫階段八 REP（`docs/plan/report/2026-09-02-階段八-雲端工人87到88-REP.md`）。
