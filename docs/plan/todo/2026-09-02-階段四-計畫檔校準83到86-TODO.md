# 2026-09-02 階段四：Phase 83〜86 計畫檔校準 TODO

> 對應 dev-prompt：`docs/plan/dev-prompts/phase0902-1.md` 任務 1。上一階段：`2026-09-02-階段三-總驗收與親自Review-TODO.md`（Phase 81）。
> 計畫檔：`docs/plan/unfinish/phase-83-aws_mailbox模組.md`、`phase-84-建S3寄物櫃.md`、`phase-85-建SQS兩條佇列.md`、`phase-86-真AWS雲端路接線.md`。
> 工作區：`.superpowers/sdd/phase0902-1/`（ledger `progress.md`、`brief-common.md`、`calib-brief.md`、四份回報）。

## 實作邏輯（新手白話）

★G1 已過：產品負責人親自做完 Phase 82（開戶、$5 Budget、admin 與 mac 兩個 IAM 身分、CLI 打得通）、commit 並歸檔到 82，再用 dev-prompt 明示執行 83〜86——這就是總覽 §4 要的「一句明確的話」。

四份計畫檔成稿於 2026-08-31，之後的落差：

```text
計畫檔寫的                                  現況（2026-09-02）
──────────────────────────────            ────────────────────────────────────────
識別字中文（模式／信箱／stub 類別）          → 一律英文（產品負責人 2026-09-02 指示）
開工基線 616、83 收工 632、86 收工 634        → 實查 624 → 640 → 642
aws 指令帶 --profile personaldocai-admin      → ~/.aws 只有 [default]（＝admin），不帶 profile
get_cloud_route 現況碼用 `模式`               → 實檔已是 `mode`
Ollama 活著                                   → 目前沒開；86 煙霧前 open -a Ollama、開關撥 cloud 加速
「你執行 aws …」                              → AWS 資源建立／purge／.env／restart／煙霧由 controller 親自做（R3）
```

## 步驟

- [x] 建工作區、記 BASE_TREE、寫 ledger（R0〜R7）與 pre-flight 表、brief-common、rules、calib-brief。
- [x] 四個 Opus 校準者平行（A:83、B:84、C:85、D:86），只改自己名下檔。
- [x] controller 複核四份回報：命名表、顆數、`--profile`、commit 步驟、AWS 操作歸屬、NBSP、python 區塊 ruff。
- [x] 總覽 §2.7 P83／P86 與 §9 加「實查」註記（624／640／642）。
- [x] 寫階段四 REP。

## 裁決（詳見 ledger）

| # | 內容 |
|---|---|
| R1 | 識別字英文；跨檔名 `check_s3`／`check_sqs`／`main`／`CHECK_JOB_ID`、`mode`／`mailbox` |
| R2 | 顆數 624 → 640（83）→ 642（86） |
| R3 | AWS 資源建立／purge／.env／restart／煙霧由 controller 親做；subagent 零 aws |
| R4 | `scripts/aws_check.py` 不寫自動化測試 |
| R5 | 煙霧前開 Ollama、開關撥 cloud；做完改回 off／300 |
| R6 | 不需要手機 |
| R7 | 四校準者平行 |
| R8 | `aws_check.py` 兩個檢查函式一律 `-> None`（失敗 `SystemExit`） |
| R9 | `INPUT_EXTENSIONS`／`MAX_WAIT_SECONDS` 維持兩份（工人不拉 config；兩層各夾一次） |
