# 2026-08-21 階段SS：Phase 25 錯誤收尾與全量回歸——TODO

## 實作邏輯

依 `unfinish/phase-25-錯誤收尾與全量回歸.md`（階段RR 已校準）。「把新功能壞掉的樣子釘死」：

1. **新測試檔 `test_folder_error_paths.py`（9 顆）**：design1.md §12 每一列都有測試指著——已被 P15〜24 覆蓋的列只做對照不重寫；本檔補的是 ★ 標的九個缺口（失敗不建資料夾×2、清單外建議→未分類、未分類建議允許、重名大小寫 409、原圖被刪 404、PATCH embedding 失敗雙路徑零變動、無刪除端點掃碼）。**先紅的語意**：全綠＝前面 phase 都做對；紅＝回去修對應 phase 的產品碼，不改測試。
2. **全量回歸**：149 passed＋零 Ollama 依賴實證（`OLLAMA_BASE_URL` 指死埠同顆數）＋每檔顆數表。
3. **兩份規格**：ask 7 例全綠＋詢問規格自 init 後零改動（單筆 log 證明）；upload 10 例全綠。
4. **明確不做最終版腳本**＋**正式庫四查詢**（2 張舊照片在收據、路徑 NULL、收件箱恰 1、category 與 folder.name 零不一致）。
5. **真模型煙霧 14 項**：其中 1〜8（上傳三選項／關窗／瀏覽／占位）已由階段 OO／PP 的 Playwright 真模型實操完整覆蓋（同伺服器同模型同流程），採認該證據；本階段新走 4（自建「煙霧測試」資料夾）與 9〜14（ask 條件／語意／查無／英文＋terminal 無 traceback＋`ls data/`）。
6. **CLAUDE.md 增量式收尾**（校準版步驟 8；與 P26 一併於階段 TT 末寫入最終顆數與 style.css 敘述）。
7. 歸檔＋commit 延後（校準版步驟 9／10）。

## 步驟

1. [x] Opus subagent：`test_folder_error_paths.py` 落地 → **首跑 0 紅 9 綠**（P15〜24 全做對、零產品碼修改）→ 全量 **149**
2. [x] 我親自：終審測試檔（258 行 9 顆結構相符）＋複跑；規格證明（a4e44ef 零 diff＋單筆 log）；零 Ollama 同 149＋每檔顆數表（121＋28）
3. [x] 步驟 5 明確不做腳本逐項全符（端點 9、寫檔僅 storage、SQL 僅 repository…）；步驟 6 正式庫四查詢全過（2 舊照片安在）
4. [x] 真模型煙霧：1〜8 採認 OO/PP 32 項證據；9〜12 新走全過（metadata／vector／查無不虛構／英文）；13 零 500；14 data/ 8 對檔案
5. [x] CLAUDE.md 指令段＋陷阱段已寫；現況段收尾待 P26
6. [x] 寫階段SS REP＋總覽 P25 打勾

**先不 commit**。
