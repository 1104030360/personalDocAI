# 2026-08-21 階段RR：計畫檔校準（phase-25／26）——TODO

## 本輪（第三輪＝最終輪）全局規劃

依 `docs/plan/dev-prompts/phase0821-2.md`（接續 P18〜24 兩輪）。拆成四個階段：

| 階段 | 內容 | 產出 |
|---|---|---|
| **RR（本檔）** | 校準 phase-25／26 兩份計畫（任務 1） | 兩份計畫更新 |
| SS | Phase 25：錯誤收尾與全量回歸（TDD；§12 逐列把關＋全量＋煙霧＋CLAUDE.md） | +9 tests → 149 |
| TT | Phase 26：美化 UI/UX（frontend-design skill＋真實作品研究＋style.css＋前後截圖對比） | 五個 static 檔；仍 149 |
| UU | 總驗收＋親自 review＋REP＋最終總結 | 驗收報告 |

沿用工作法：P25 測試檔由 Opus subagent TDD 落地、其餘（檢查腳本／煙霧／CLAUDE.md／P26 設計與前端）由我親自執行（P26 是設計判斷重的 phase，計畫明令先載 design skill＋查真實作品）；**全程先不 commit**。

## 校準核對結果（實作邏輯）

兩份計畫寫於 2026-08-20（P15〜17 完成當天視角）；與現況的過時處：

1. **基線數字**：P25 開工＝**140**（剛實查）→ 完成 **149**（+9）；P26 前後皆 149。
2. **P25 步驟 0(b) 基線 commit 的 grep 撈不到**（歷史訊息是「Phase 11〜14」無「Phase 14」字樣）→ 釘死 `a4e44ef`；並改用更強證明：`自然語言詢問.feature` 自 init commit（`64c412f`）後**從未被任何 commit 改過**（剛實查 `git log -- 該檔` 僅一筆）。
3. **P25 步驟 5 的 SQL 泛用 grep 會誤中 `photos.py:225` 的中文註解**（「一條 UPDATE 同時寫…」，P21 計畫指定原文；階段QQ 已查證非 SQL）→ 改用精確 pattern `UPDATE photo`。
4. **P25 步驟 8（CLAUDE.md）整段依據過時**：原計畫假設 CLAUDE.md 還停在「79 tests／Phase 01〜14」，要求一次寫入整段增量敘述；實際上 P15〜24 的成果段已在前兩輪逐段寫入（現況已是「P18〜24 已完成」、140 顆）→ 改為增量式收尾（header 改全數完成、追加 P25〜26 短段、140→149、指令段補兩處、陷阱段補一條；`migrate_folders.sql` 指令已存在不重複）。
5. **P25 步驟 9（歸檔）數字與範圍**：15〜24 十份已於前兩輪 commit 時歸檔，`unfinish/` 只剩 3 份（phase-00＋25＋26）；且 `git mv` 會 stage、與「先不 commit」衝突 → 歸檔與 commit（步驟 10）一併延後，屆時只移剩餘 3 份。
6. **P26 步驟 8 的「零 Python／測試變動」git status 檢查**：本輪不 commit，P25 的合法產出（新測試檔、CLAUDE.md）會出現在 porcelain 輸出 → 校準為「對照 P25 完成時的快照，P26 自身只准動 `app/static/` 五檔」。
7. 其餘全準：P26 步驟 5a 預期的 class／id 名稱與現況完全一致（`#ask-form`／`#question-input`／`#result` 等，剛 grep 實查）；P25 新測試檔引用的函式（`count_photos`／`list_folders`／`find_folder_by_name`／`fetch_embedding`／`storage_service.absolute_path`）俱在。

## 步驟

1. [x] `phase-25`：基線釘 140／完成 149（含 4c 零 Ollama 預期）、步驟 0b 與 3b 改用 `a4e44ef`＋單一 commit 證明、步驟 5 SQL 改精確 pattern、步驟 8 改增量式 CLAUDE.md 指引、步驟 9/10 註記延後與剩餘 3 份、驗收清單同步
2. [x] `phase-26`：基線釘 149、步驟 8 ⑤ 的 git status 檢查校準（先不 commit 情境）
3. [x] 寫階段RR REP（`report/2026-08-21-階段RR-計畫檔校準P25P26-REP.md`）
