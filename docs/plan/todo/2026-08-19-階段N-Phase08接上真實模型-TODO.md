# 2026-08-19 階段N：Phase 08 接上真實模型 TODO

## 目標

照更新後的 `docs/plan/unfinish/phase-08-接上真實模型.md`：第一次真的打本機 Ollama——`bge-m3` 實測向量維度（驗證 `vector(1024)` 假設）與中英跨語言相似度、`gemma4` 真的看一張照片、走一次完整真實上傳（寫進正式庫）、把「真模型只做手動煙霧」的界線記進 CLAUDE.md 指令節。**本 phase 不改 `app/`、不新增自動化測試**（design.md §11：真模型不進驗收與 CI）。

## 實作邏輯

- **實測取代假設**：design.md §14 把「bge-m3＝1024 維」標成必須實作驗證的假設——`scripts/check_embedding_dim.py` 實測維度＋量「中英同義句 vs 無關句」相似度（多語能力的物理證據）。若維度不合，照腳本指示改 `EMBEDDING_DIM`＋`schema.sql` 重建兩庫。
- **正式路徑第一次通電**：接線在 Phase 5/6 的 `dependencies.py` 早已完成（`get_vlm`→OllamaVLM、`get_embeddings`→OllamaEmbeddings），本 phase 沒有開關要撥——直接呼叫 `OllamaVLM().understand()`（步驟 3）與真實 HTTP 上傳（步驟 4；環境擋 localhost 時用 in-process TestClient 備援，同一條路徑）。
- **真上傳寫正式庫並保留**（產品的正常產出）；測試照舊只碰測試庫。
- **界線入文件**：CLAUDE.md 既有指令碼區塊補兩行（煙霧測試指令＋「不進驗收與 CI」註記），其餘一字不動。
- 預期心態：gemma4 9.6GB 第一次載入 10〜60 秒屬正常；描述品質只求足以 demo。
- 執行方式：opus subagent 執行（腳本＋真模型取證＋文件行），主控 dispatch reviewer 審查＋親自複核關鍵證據。

## 步驟

- [x] N1. 步驟 1：`scripts/check_embedding_dim.py` 實跑——**1024 維＝config✓**；同義句 0.837 vs 無關句 0.377（bge-m3 多語實證）
- [x] N2. 步驟 2：screencapture 無螢幕權限（原始錯誤留證）→ **qlmanage 備援**渲染 HTML 假收據（另實測發現需補 charset，任務書已回修 dba2e72）
- [x] N3. 步驟 3：gemma4 真看圖（首次 2m32s）→ understood=True，六欄位全中：收據／Target／[可樂,洋芋片]／2026-08-10
- [x] N4. 步驟 4〜5：**HTTP 主路徑成功**（uvicorn＋curl，1m57s）→ 201 三鍵四欄；正式庫 id=1、dims=1024、norm≈1.0；uvicorn kill＋lsof 空＋curl exit=7 四重收尾證明
- [x] N5. 步驟 6：CLAUDE.md 指令碼區塊補兩行（0 deletions，其餘零變更）
- [x] N6. 驗收 1〜7 全過（pytest 40 passed×2 含死埠法）→ commit 7ba2e71
- [x] N7. 任務 review → Spec ✅（16/16，reviewer 親自重跑 5 項逐位吻合）／Approved，0 Critical/Important、4 Minor（收尾文件同步類）→ 主控核可

## 完成定義

驗收 1〜7 全過（原始輸出留證）；`app/` 零變更；tests/ 零新增；CLAUDE.md 僅指令節兩行差異；正式庫留有本次真實上傳的照片記錄。
