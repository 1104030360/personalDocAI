# 2026-08-19 階段W：總驗收與收尾 TODO

## 目標

Phase 11〜12 全部落地後的總驗收：主控親跑計畫裡留給主控的驗收項（P11 驗收 5〜8 真模型煙霧、P12 驗收 4 變異測試與驗收 5 死埠全量）、**主控親自逐檔 review 全部工作樹變更**、逐一核對兩份 phase 計畫每個想法都已實現（dev-prompt 約束 5），最後歸檔計畫與校正 CLAUDE.md——全程不 commit。

## 實作邏輯

- **驗收分層**：implementer 只碰假件；真模型（Ollama）與破壞性驗證（變異測試）一律主控親手做，做完立刻還原並以 `git diff` 確認無殘留。
- **變異測試（P12 驗收 4）**：暫時註解 `photo_repository.search_by_vector` 的 `COALESCE(...)` 時間條件 → 時間過濾 Example 應轉紅 → 還原 → 轉綠。證明規則是 SQL 在守，不是碰巧通過。
- **真模型煙霧（P11 驗收 5〜8）**：uvicorn 起服務 → `/docs` 看三端點 → 中文問（格式驗證：三鍵）→ 英文問（answer 應為英文句）→ 鍵排序 `['answer','retrieved_photo_ids','search_mode']`。真模型輸出非決定論，只驗格式與大方向，不當驗收依據（查法正式驗收已由假件測試完成）。
- **主控親自 review**（使用者要求）：逐檔重讀全部 diff（app 5 檔＋tests 4 檔）＋抽查兩份 review 報告的結論是否與磁碟一致＋核對 ledger 的每條 Ruling 與遞延項。
- **收尾歸檔**（循階段G/K/O/S 慣例）：phase-11/12 計畫移 `docs/plan/finish/`、CLAUDE.md「現況」段更新（Phase 01〜12、67 tests、POST /ask 全流程可用、12 條 Rule 全綠）、總覽 conftest 行補 P12 註記。

## 步驟

- [x] W1. 全量回歸與無 Ollama 證明：`pytest -q` = 67 passed；`OLLAMA_BASE_URL=http://localhost:9 pytest -q` = 67 passed（P12 驗收 5）
- [x] W2. P12 驗收 4 變異測試：註解 search_by_vector 時間過濾 → 時間 Example 紅 → 還原 → 綠；`git diff` 確認還原乾淨
- [x] W3. P11 驗收 5〜8 真模型煙霧：uvicorn＋/docs 三端點＋中英各一次 POST /ask＋鍵排序驗證
- [x] W4. 主控親自逐檔 review 全部變更（app＋tests＋docs/plan），核對 dev-prompt 約束 5（兩計畫每個想法皆實現）
- [x] W5. 歸檔 phase-11/12 至 finish/、更新 CLAUDE.md 現況段、總覽 conftest 行補 P12、勾銷 TODO、寫階段W REP、ledger 收尾
- [x] W6. 最終回報使用者：成果總覽＋所有 Rulings 清單＋遞延 Minor 清單＋未 commit 的工作樹狀態

## 完成定義

67 passed（含死埠證明）；變異測試紅→綠且零殘留；真模型煙霧中英各一次通過（格式）；phase-11/12 計畫「驗收標準」每項都有主控或 implementer 的實跑證據；CLAUDE.md／總覽／TODO／REP 全部同步；工作樹保持未 commit 交付使用者 review。
