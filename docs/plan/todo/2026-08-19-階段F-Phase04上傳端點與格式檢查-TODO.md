# 2026-08-19 階段F：Phase 04 上傳端點與格式檢查（TDD） TODO

## 目標

照更新後的 `docs/plan/unfinish/phase-04-上傳端點與格式檢查.md`，以 TDD 做出 `POST /photos`（非 JPEG/PNG 一律 415 且不做任何後續處理）並掛進 `main.py`，通過驗收（pytest `19 passed` 為正式驗證）。

## 實作邏輯

- **先紅後綠**：先寫 `tests/integration/test_photos_upload.py`（7 個，TestClient in-process），跑紅燈——端點不存在 → 404／openapi 無 `/photos`；再寫 `app/api/routers/photos.py` 與改 `app/main.py`（include_router）轉綠。Phase 3 的 12 個測試全程必須維持綠燈。
- **BDD 對應**：測試逐條對應 `上傳照片.feature` Rule U1「非圖片格式上傳失敗＋系統儲存的照片數量為 0」；415 錯誤訊息逐字比對規格句「上傳檔案必須為常見圖片格式（如 JPEG、PNG）」。
- **格式檢查放 router 層**（HTTP 輸入驗證是 router 職責）；只驗 `content_type`，不驗檔案內容、不設大小上限（已釐清決策）。合格檔案先回佔位回應（Phase 6 換正式 201 契約）。
- **正式驗證以 pytest 為準**；curl 驗收屬手動輔助（沙箱擋 localhost 連線）。
- 執行方式：opus subagent 實作，主控親自 review＋重跑驗收。

## 步驟

- [x] F1. 步驟 0：寫 `tests/integration/test_photos_upload.py`，跑出紅燈——實際 **6 紅 1 綠**（第 7 個為 vacuous pass，計畫敘述已回頭修正；Phase 3 的 12 個維持綠）
- [x] F2. 步驟 1：寫 `app/api/routers/photos.py`（415 檢查＋佔位回應，逐字轉寫）
- [x] F3. 步驟 2：改 `app/main.py` 掛上 photos.router（逐字轉寫）
- [x] F4. 綠燈：`python -m pytest tests -q` → `19 passed`
- [x] F5. 驗收：pytest 第 0 條 ✓；curl 1〜6 實跑全過（本次沙箱未擋 localhost）；資料庫 0 筆；uvicorn 已收
- [x] F6. 主控親自 review：三檔逐字核對（唯一差異＝計畫碼 `b"這不是圖片"` 語法錯誤的合理修正）＋親手重跑 19 passed／count 0／SQL grep 一行 ✓；並回頭修正 phase-04 計畫檔兩處（bytes literal、紅燈數敘述）
- [x] F7. 寫階段F report

## 完成定義

pytest 19 全綠；非圖片上傳 415 且訊息逐字正確、資料庫 0 筆；PNG/JPEG 201；缺檔 422；/photos 出現在 openapi；review 無未解 Critical/Important 發現。→ **全部完成**
