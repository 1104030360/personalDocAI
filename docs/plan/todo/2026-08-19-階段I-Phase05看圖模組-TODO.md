# 2026-08-19 階段I：Phase 05 看圖模組（TDD）TODO

## 目標

照更新後的 `docs/plan/unfinish/phase-05-看圖模組.md`，以 TDD 完成看圖服務：`app/services/vlm_service.py`（PhotoUnderstanding／VLM_PROMPT／OllamaVLM／parse_content_time）、`app/dependencies.py`（get_vlm）、router 接上「看不懂 → 422 什麼都不存」，通過驗收（`pytest -q` **29 passed** 為正式驗證）。

## 實作邏輯

- **先紅後綠（步驟 0 → 4）**：先建 `pytest.ini`＋`tests/fakes.py`（FakeVLM）＋擴充 `tests/conftest.py`（`wire_fake_ai` 安全網＋`client` fixture）＋新增 `tests/unit/test_vlm_service_unit.py`（6 個）＋`tests/integration/test_upload_smoke.py`（4 個）＋改寫 `test_photos_upload.py` 兩個佔位測試，跑出紅燈（空模組 import 失敗＝功能不存在的正確紅）；再填入 `vlm_service.py`／`dependencies.py`／改 `photos.py` 轉綠。
- **BDD 對應**：測試逐條對應 `上傳照片.feature` Rule U3（六欄位＝清單外捨棄）、U7（看不懂 → 失敗且照片數量 0）；422 訊息逐字比對 design.md §6「VLM 無法理解照片內容，未儲存任何資料」。
- **絕不打真模型**：本機 Ollama 真的在跑（gemma4）——conftest `wire_fake_ai`（autouse）把 `get_vlm` 預設為「看不懂」假件；佔位 201 測試改寫為 FakeVLM 驅動。pytest 全程零 Ollama 呼叫。
- **正式路徑即真模型**：`OllamaVLM`（ChatOllama＋with_structured_output＋base64 image block）本 phase 就寫好，`get_vlm()` 正式執行永遠回它；假件只活在 pytest。
- 執行方式：opus subagent 實作（計畫檔＝任務書，程式碼逐字轉寫），主控隨後 dispatch reviewer 審查＋親自複跑驗收。

## 步驟

- [x] I1. 步驟 0：測試先行（pytest.ini、fakes.py、conftest 擴充、unit 6、smoke 4、改寫佔位 2）→ 紅燈留證 → commit 7489eac
- [x] I2. 步驟 1〜3：vlm_service.py／dependencies.py／photos.py（逐字轉寫計畫碼）→ 綠燈 29 passed → commit d05c072
- [x] I3. 驗收 1〜7 全過（另加證：OLLAMA_BASE_URL 指向死埠仍 29 passed——pytest 確實不碰真模型）
- [ ] I4. 任務 review（reviewer subagent：規格符合＋程式品質）→ 發現須修則進 fix loop
- [ ] I5. 主控核可＋寫階段I report

## 完成定義

`pytest -q` 29 passed（unit 8＋integration 21）；驗收 1〜7 全過；review 無未解 Critical/Important 發現；Phase 3/4 既有行為未被更動。
