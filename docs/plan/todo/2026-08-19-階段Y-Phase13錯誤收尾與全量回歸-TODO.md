# 2026-08-19 階段Y：Phase 13 錯誤路徑收尾與全量回歸 TODO

## 目標

照更新後的 `docs/plan/unfinish/phase-13-錯誤收尾與全量回歸.md`：收整 P11/P12 輪遞延事項（步驟 0，67→68）、把 design.md 錯誤處理總表七列各補上測試（步驟 1，`tests/integration/test_error_paths.py`，+11）、跑「明確不做」檢查腳本（步驟 2）、核對 CLAUDE.md 指令節並補兩示例（步驟 3）、全量回歸（步驟 4）——**後端到此完成**，全量 **79 passed**。

## 實作邏輯

- **執行方式**：同前輪——主控派 opus 實作 subagent 照計畫轉錄，獨立 opus reviewer 雙審，全程不 commit。
- **順序**：步驟 0（先收遞延：category 守護測試＋三項零行為清理，改完 68 綠）→ 步驟 1（TDD：先建錯誤路徑測試檔實跑——對既有行為的驗證測試，預期直接綠；任何紅燈都代表「計畫斷言 vs 實際行為」的落差，須回報不得自行改產品碼）→ 步驟 2 檢查腳本逐項核對輸出 → 步驟 3（CLAUDE.md 核對＋兩示例）→ 步驟 4 全量 79。
- **關鍵約束**：錯誤路徑測試「指著」的行為已存在（415/422 detail 字串、fallback、查無、500 不吞錯都是既有實作）——本 phase **不改任何 `app/` 行為**（步驟 0 的施工註記刪除只動註解）；不加全域例外捕捉（Q3）；「檔案太大」刻意沒有錯誤路徑（測 201＋原始碼零上限關鍵字）。
- **驗收分工**：驗收 1〜5、7（測試與腳本、死埠法）由 implementer；驗收 6（真模型煙霧）由主控在階段AA 親自跑。

## 步驟

- [x] Y1. 派工 opus implementer：步驟 0（68 綠）→ 步驟 1（error_paths 11 個，79 綠）→ 步驟 2 腳本＋步驟 3 CLAUDE.md → 驗收 1〜5、7
- [x] Y2. 產生工作區 diff 審查包，派獨立 reviewer（opus）雙審
- [x] Y3. 修正迴圈（如有）；Minor 記 ledger 遞延
- [x] Y4. 寫階段Y report、更新 ledger

## 完成定義

`pytest tests/integration/test_error_paths.py -v` = 11 passed；`pytest -q` = **79 passed**；`OLLAMA_BASE_URL=http://localhost:9 pytest -q` = 79 passed；步驟 2 腳本輸出逐項符合驗收 5 清單；雙語 `-k "英文 or 大小寫"` = 7 passed, 72 deselected；CLAUDE.md 指令節含兩個新示例；review 無未處置 Critical/Important；未 commit。
