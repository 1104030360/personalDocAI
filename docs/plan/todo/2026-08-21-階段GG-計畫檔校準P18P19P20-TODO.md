# 2026-08-21 階段GG：計畫檔校準（phase-18／19／20）——TODO

## 本輪全局規劃（先看這裡）

依 `docs/plan/dev-prompts/phase0821.md`，本輪要把增量 Phase 18〜20 做完。拆成五個階段：

| 階段 | 內容 | 產出 |
|---|---|---|
| **GG（本檔）** | 依專案現況校準 `unfinish/phase-18〜20` 三份計畫檔（dev-prompt 任務 1：**必須先全部更新完才能動工**） | 三份計畫檔更新＋總覽 §5 預期顆數填入 |
| HH | Phase 18：VLM 資料夾推薦（`build_vlm_prompt`＋`clamp_category`），TDD | +7 tests → 110 |
| II | Phase 19：上傳存檔與讀圖端點（INSERT→寫檔→UPDATE＋失敗全清；GET 縮圖/原圖），TDD | +11 tests → 121 |
| JJ | Phase 20：上傳未分類流程與規格改版（`上傳照片.feature` 7→10 Rule），TDD+BDD | +3 tests → 124 |
| KK | 總驗收：三份計畫驗收清單逐項核對＋全量回歸＋親自 code review | 驗收報告 |

依使用者指示：**本輪一律先不 commit**（計畫檔裡的 git commit 步驟記錄下來、暫不執行）；實作可由 subagent（Opus）協助，最後由我親自 review 全部改動。

## 階段GG 實作邏輯

三份計畫檔寫於 2026-08-20（Phase 15〜17 完成後），寫法極細（含逐行行號引用）。校準方式＝**逐條核對計畫引用的行號／函式／測試名與現況程式碼**：

- 已逐檔核對：`vlm_service.py`（VLM_PROMPT 33〜51 行、協定 63 行、understand 78〜80 行、84 行）、`photos.py`（38〜39 行看圖、35 行註解、第 10 行 import）、`photo_repository.py`（PHOTO_COLUMNS 三路徑欄、reset_folders_and_photos、五個 folder 函式）、`storage_service.py`、`fakes.py`（FakeVLM／make_png_bytes／make_jpeg_bytes）、`conftest.py`（三道 autouse 安全網＋first_row／split_items）、六個計畫點名的測試檔行號——**全部吻合**。
- 開工基線實查：`pytest -q` → **103 passed**（與 CLAUDE.md 一致）；`上傳照片.feature` 現為 7 條 Rule；`data/` 已在 `.gitignore`；`understand()` 呼叫端全專案僅 `photos.py:39`。

## 步驟（要改的三類過時處）

1. [x] 核對三份計畫 vs 程式現況（結論：僅數字與兩句描述過時，其餘全準）
2. [x] `phase-18`：基線釘死 103、完成後 110（+7）
3. [x] `phase-19`：①基線 110、完成後 121（+11）；②「storage_service 五個函式」→ 四個公開函式（save_original／make_thumbnail／absolute_path／remove_if_exists）；③新函式位置描述改為「reset_folders_and_photos() 之後、Phase 16 的 list_folders() 之前」（原文寫 search_by_metadata 之前，中間其實隔著 Phase 16 的五個資料夾函式）
4. [x] `phase-20`：基線 121、完成後 124（+3）
5. [x] `phase-00-增量總覽.md` §5 進度表：P18／19／20 的預期累計欄依計畫填 110／121／124（打勾等各 phase 完成後）
6. [x] 寫階段GG REP（`report/2026-08-21-階段GG-計畫檔校準P18P19P20-REP.md`）
