# 2026-09-02 階段九：EC2 探測與 worker 映像 Phase 89〜90 — TODO

> dev-prompt：`docs/plan/dev-prompts/phase0902-2.md`。ledger：`.superpowers/sdd/phase0902-2/progress.md`。
> 計畫檔：`docs/plan/unfinish/phase-89-EC2探測running.md`、`phase-90-worker映像arm64.md`（階段七已校準）。

## 實作邏輯

- **Phase 89**：`cloud_ingest.py` 加 `Ec2Probe`——用 `mailbox.instance_state(instance_id) == "running"` 判斷遠端能不能用，答案快取 `config.EC2_PROBE_TTL_SECONDS`（預設 60）秒（時基直接用既有的 `_now()`，全檔只有一個時基）、任何例外一律回 `False` 並留 log、`instance_id` 空就連問都不問；`dependencies.get_cloud_route()` 補上 `ec2` 分支（`@lru_cache` 的 `_ec2_cloud_route()` 整個行程只建一次，TTL 快取才有意義），拿掉最後一個 `NotImplementedError`。Phase 77 那顆測試裡的「ec2 鬧鐘」段拆掉。順手改 `CLAUDE.md` AWS 段結尾那句過期話（裁決 R7）。**不建立任何 EC2**；測試全部用 `FakeMailbox.instance_state_script`。
- **Phase 90**：`Dockerfile` 改三個 stage（`base` → `cloud-worker` → **`app` 放最後**，所以 compose 的 `build: .` 不必改）；`cloud-worker` stage 帶 `ARG GIT_SHA`＋`ENV WORKER_VERSION=$GIT_SHA`、CMD `python -m app.workers.cloud_worker`、無 EXPOSE；`.dockerignore` 加一行 `deploy/`；新開 `tests/integration/test_design6_error_paths.py` 放 4 顆掃碼（三個 stage、app 在最後、ARG GIT_SHA、compose 零新增服務與零 AWS 字樣）。然後 controller 親自 `docker build --target cloud-worker` 出 arm64 映像、`docker run --env-file .env` 以容器重做 Phase 88 的端到端、`docker compose config` 前後 diff 為空、dev overlay `up -d --build` 四個服務照常（裁決 R8）。
- **★G2**：依裁決 R2，88（Mac 直跑）與 90（容器）兩次端到端都由 controller 親跑通過，才進 Phase 91。

## 步驟

- [x] Task 3：派 Phase 89 實作者（663 → 670 passed、0 skipped；三死埠 670；最後一個 `NotImplementedError` 除役）。
- [x] Task 3 review：Approved（0 Critical／0 Important／4 Minor；①失敗也進快取無測試、④實例 ID 進 log → fix wave）。
- [x] controller：`aws ec2 describe-instances --filters Name=tag:Name,Values=personaldocai-worker`（唯讀）→ 空，帳號內零 EC2。另已先存 `docker compose config` 的 before 快照（`/tmp/p90-compose-before.yaml`）。
- [x] Task 4：派 Phase 90 實作者（Dockerfile／.dockerignore／4 顆掃碼；670 → 674 passed、0 skipped；與計畫檔碼區 byte-identical）。
- [x] Task 4 review：Approved（0 Critical／0 Important／4 Minor；①②測試強度 → fix wave、③計畫檔舊數字 → 歸檔前改）。
- [x] controller：build arm64 映像 ✅ → `Architecture=arm64`、exec-form CMD、零 EXPOSE ✅ → 容器啟動 `version=bb3921a` ✅ → 容器端到端（收據 10 秒走雲端入庫 #70、證件 `route=local verdict=SENSITIVE` 零 S3、`docker stop` 21 秒優雅退）✅ → compose config diff 空 ✅ → dev overlay `up -d --build` 四服務 Up、app 映像 CMD 仍 uvicorn ✅ → `pytest -q` 674／三死埠 674 ✅ → **★G2 憑據十條齊全**（依裁決 R2 條件式通過）。
- [ ] 一次 fix wave（87 的 7 個 Minor ＋ 88／89／90／91 review 的 Minor 中值得修的）＋ scoped re-review——移到階段十一（總驗收）執行。
- [x] 寫階段九 REP（`docs/plan/report/2026-09-02-階段九-EC2探測與worker映像89到90-REP.md`）；★G2 證據表十條填好。
