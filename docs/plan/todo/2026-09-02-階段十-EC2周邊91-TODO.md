# 2026-09-02 階段十：EC2 的網路、IAM 與 ECR Phase 91（尚不啟動實例）— TODO

> dev-prompt：`docs/plan/dev-prompts/phase0902-2.md`。ledger：`.superpowers/sdd/phase0902-2/progress.md`（★G2 條件式裁決 R2）。
> 計畫檔：`docs/plan/unfinish/phase-91-EC2的網路IAM與ECR.md`（階段七已校準）。

## 實作邏輯

- **前提（★G2）**：依裁決 R2，Phase 88（Mac 直跑工人）與 Phase 90（容器跑工人）兩次端到端都由 controller 親跑通過，且 dev-prompt 已明示執行到 91，才動手。本 phase 建的東西**全部免費**（security group、S3 Gateway endpoint、IAM role／instance profile、ECR repo ≈ $0.04／月），而且**從頭到尾不 `run-instances`**。產品負責人事後若不認 G2，刪掉這四樣即可（可逆）。
- **分工**：五個進 git 的部署檔（兩份 IAM JSON、`user-data.sh`、systemd unit、`worker.env.example`）由 Opus 實作者照計畫檔逐字落地（零 Python、零測試）；所有 `aws` CLI 與 `docker push` 由 controller 用 admin profile 親自執行，每一步的輸出遮掉帳號 ID 後記進 REP。
- **順序**：查 id（預設 VPC／公有子網／主要路由表）→ SG（inbound 空、outbound 只留 tcp 443）→ S3 Gateway endpoint → IAM role ＋ inline policy ＋ `AmazonSSMManagedInstanceCore` ＋ 同名 instance profile → ECR repo（`scanOnPush=false`）→ `docker login` ＋ push `<git-sha>`／`latest` 兩個 tag → `.env` 加空的 `EC2_WORKER_INSTANCE_ID=` → §4.10 四條機密檢查。

## 步驟

- [x] ★G2 憑據齊全（88 端到端 ✅、90 容器端到端 ✅、十條證據見階段九 REP §6）。
- [x] Task 5：派實作者落地五個部署檔（五檔對計畫檔 diff 零差異、unit 與 user-data 內嵌段 IDENTICAL）；review Approved（0 Critical／0 Important／5 Minor；④⑤ 併入 fix wave）。
- [x] controller §4.1〜4.3：預設 VPC／公有子網／主要路由表 id 存 `/tmp/p91-vars.sh`；SG `personaldocai-worker-sg` inbound `[]`、egress 只 tcp 443；S3 Gateway endpoint `available`、掛主要路由表。
- [x] controller §4.5：policy 展開零殘留佔位符；role＋inline policy＋SSM managed policy＋同名 instance profile 全建好、`wait instance-profile-exists` 通過。
- [x] controller §4.6〜4.7：ECR `personaldocai-worker` 建好；`docker login` 第一次逾時（Docker Desktop 內建代理暫時卡住）重試成功；`latest` 第一次被 zsh 的 `:l` 修飾子吃掉改 `${ECR_URI}:latest` 重推；`describe-images` 兩個 tag `bb3921a`／`latest`、arm64。
- [x] controller §4.9〜4.10：`.env` 加空 `EC2_WORKER_INSTANCE_ID=`；四條 grep 全 OK；collect 674 不變；總覽 §5.5 安全檢查（零 NAT／零 EIP／零實例／Budget $5）全過。
- [x] 寫階段十 REP（`docs/plan/report/2026-09-02-階段十-EC2周邊91-REP.md`）。
