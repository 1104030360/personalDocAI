# 2026-08-19 階段T：計畫檔更新（phase-11／phase-12）TODO

## 目標

對照專案現況（master 55 passed、tests 已分層 unit/＋integration/、Phase 10 流程圖已到「兩條 retrieve 直接接 END＋TODO(Phase 11) 註記」、langchain-core 1.5.6），把 `phase-11-回答與詢問端點.md` 與 `phase-12-詢問驗收測試.md` 全面校準並同步 `phase-00-總覽.md`——完成 dev-prompt `phase0819-4.md` 任務 1，才能進入開發。

## 實作邏輯

- **先盤點再動筆**：實跑 `pytest -q`＝55 passed 當基線；逐一核對兩計畫引用的介面（`SEARCH_MODE_LABELS`、`count_photos`、`build_document`、retriever Document 的 `id`／`items` metadata、conftest 的 `client`／`wire_fake_ai`／表格小工具）全部存在；pytest-bdd 8.1.0 的 `datatable` 參數已被 `test_upload_feature.py` 實際使用。
- **API 用法不憑記憶**：計畫關鍵寫法 `ChatOllama(...).invoke([message]).text` 以官方文件（docs.langchain.com messages 章）＋本機 langchain-core 1.5.6 實測雙重確認——1.x 的 `.text` 是屬性，計畫程式碼可照抄。
- **dev-prompt 檔名誤植裁定**：任務書寫 `phase-12-用戶端.md`（不存在）；實檔 `phase-12-詢問驗收測試.md` 與 P11 內文「規格驗收在 Phase 12」互證 → 判定同一檔（「用戶端」是 P14 網頁介面，前置 P13 未做，順序上不可能先做）。
- **發現一個 TDD 順序陷阱並寫進計畫**：P11 步驟 2 把 `AskDeps` 加上**必填**欄位 `answerer` 的當下，`tests/integration/test_workflow_route.py` 既有 5 個測試立刻缺參數而壞 → 計畫加 ⚠️「步驟 2 與步驟 8 必須一起做」；紅燈階段只建步驟 9 的新測試檔（僅該檔收集失敗，其餘 55 綠）。
- **本機差異校準**：Ollama 是 App 版不歸 brew services 管 → P12 驗收 5 的 `brew services stop ollama` 改成 `OLLAMA_BASE_URL=http://localhost:9 pytest -q`（等價驗證：誰偷打真模型誰連線爆錯，且不動使用者的 Ollama）。

## 步驟

- [x] T1. `phase-11-回答與詢問端點.md`：前置基線（55）＋TDD 順序加註（紅＝步驟9 → 綠＝7→1→2+8→3〜6）＋步驟 4 改增量修改（保留既有 docstring）＋步驟 6 title 維持 personalDocAI＋步驟 8/9 測試路徑歸 `tests/integration/`＋步驟 9 fixture 改沿 `wire_fake_ai` 慣例＋驗收 1/2 與完成狀態累計數（60）＋Q1 補 1.5.6 實測註記
- [x] T2. `phase-12-詢問驗收測試.md`：前置（60）＋測試檔路徑歸 `tests/integration/`（scenarios 改 `../../`）＋fixture 改沿 `wire_fake_ai` 慣例＋驗收 2/3/5 指令與累計數（14／67／死埠法）＋Q5 fixture 實名 `clean_photo_table`＋Q6 檔名清單改實際路徑＋完成狀態（67）
- [x] T3. `phase-00-總覽.md`：tests/ 樹兩列移入 `integration/` 分支＋追加第五條 🔄 註記（P11/P12＝60/67；P13 起仍舊制）＋累計表 P11/P12 兩列改定
- [x] T4. 三檔 grep 交叉核對（60/67、integration/ 路徑無殘留舊路徑）＋基線不變確認，寫階段T report

## 完成定義

兩份 phase 計畫與總覽對現況零矛盾：測試累計數 55→60→67 一路對齊、所有測試路徑都在 `tests/unit|integration/`、計畫內程式碼引用的每個符號都真實存在；`pytest -q` 仍 55 passed（純文件工作不影響程式）。
