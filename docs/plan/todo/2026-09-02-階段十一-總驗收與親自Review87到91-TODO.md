# 2026-09-02 階段十一：總驗收與親自 Review Phase 87〜91 — TODO

> dev-prompt：`docs/plan/dev-prompts/phase0902-2.md`（「最後你再親自 review 一遍」）。ledger：`.superpowers/sdd/phase0902-2/progress.md`。

## 實作邏輯

- 五個 phase 各自過了 spec＋品質 review（87／89／90／91 Approved、88 一輪 fix round 後 re-review 乾淨）。收尾照 subagent-driven-development 的作法：**一次** fix wave 把各 review 的 Minor 中值得修的一起做（TDD）→ 一次 scoped re-review → controller 親自對整條分支做最終 review（讀 diff、跑全量、三死埠、端點、掃碼、機密、AWS 收尾狀態）→ 寫 REP、更新 CLAUDE.md 專案概述段與記憶。
- **不 commit**（產品負責人指示）；工作樹快照 BASE_TREE → 最終 tree 相減就是這一輪的全部改動。

## 步驟

- [x] Task 5（Phase 91 五檔）review 收回（Approved）；Minor ④⑤ 併入 fix wave 簡報 §E（裁決 R20）。
- [x] 派 fix wave（單一實作者、Opus）：87 五項、89 兩項、90 兩項、91 兩項 → 674 → 679、0 skipped；兩處真 bug（`read_context` 非物件 JSON、實例 ID 進 log）修掉。
- [x] scoped re-review（Opus）：11/11 ADDRESSED、零新 Critical／Important；三條 out-of-scope 觀察 parked 給 Phase 95（裁決 R21）。
- [x] controller 親自最終 review（全過；見 ledger FINAL verification）：讀 `git diff BASE_TREE FINAL_TREE` 全文（app／tests／Dockerfile／deploy／docs）、`pytest -q`、三死埠、端點 22、SQL／boto3／celery 掃碼、tokenize 零中文識別字（新碼）、機密掃描（追蹤檔零帳號 ID／bucket 後綴／佇列 URL／實例 ID）、`docs/spec/`／compose／db／前端零改動、`.env` 恢復 off／300、開關 local、佇列 0／0、S3 空、容器 dev overlay 四服務 Up。
- [x] 更新 `CLAUDE.md` 專案概述段（裁決 R16：74〜86 已 commit、87〜91 完成未 commit、★G2 條件式通過、AWS 現況、EC2 等 Phase 92）。
- [x] 寫階段十／十一 REP；更新記憶 `increment6-plan-status`。
