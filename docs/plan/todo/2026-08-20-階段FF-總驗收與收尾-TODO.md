# 2026-08-20 階段FF：總驗收與收尾（P15〜P17）TODO

## 目標

三個 phase 全部完成後的整體把關：全量測試、三份計畫驗收清單逐項重核、本人親自對整批 working-tree diff 做最終 review（使用者指示「最後你再親自 review 一遍」）、歸檔已完成計畫、更新 CLAUDE.md 現況。**全程不 git commit**——所有成果留在 working tree 交使用者檢視。

## 實作邏輯

- **驗收即重跑，不抄 implementer 報告**：全量 `pytest -q`、兩條 grep 不變量（SQL 只在 repository、無 `from app.core.config import`）、端點數不變、正式庫終態核對（2 列歸「收據」、路徑 NULL、資料夾 6 筆、收件箱唯一）全部由本人重新執行。
- **最終 review 範圍＝整批 diff**：`git diff`＋`git status` 全部改動檔逐一過目（含新檔），對照三份計畫與 design1.md §5/§6/§10，確認無計畫外改動、無殘留垃圾（`data/`、備份檔位置說明）。
- **收尾遵循歷輪慣例**：phase-15/16/17 計畫移入 `docs/plan/finish/`；CLAUDE.md 現況段更新（79→103、新能力、注意 pytest 不寫專案 data/ 的新安全網）；增量總覽若有進度標記一併更新。歸檔與 CLAUDE.md 屬檔案異動，不 commit。

## 步驟

- [x] FF1. 全量 `pytest -q`＝103 passed；三份計畫驗收清單逐項重核（commit 項標「依指示跳過」）
- [x] FF2. 本人最終 review：整批 diff 逐檔過目、對照計畫與 design1.md、確認零計畫外改動；殘留檢查（專案內無 data/、`git status` 乾淨度、`~/PersonalDocAI-backup-遷移前.sql` 去留交使用者）
- [x] FF3. 歸檔：phase-15/16/17 → `docs/plan/finish/`；更新 `unfinish/phase-00-增量總覽.md` 進度（若有標記處）；更新 CLAUDE.md 現況段
- [x] FF4. 寫階段FF REP、更新 ledger、向使用者總結（含未 commit 的改動清單與建議 commit 訊息）

## 完成定義

`pytest -q`＝103 passed 且驗收清單全勾（除 commit 項）；最終 review 無未處理的 Critical/Important 發現；計畫歸檔＋CLAUDE.md 更新完成；使用者拿到完整總結與後續 commit 建議。
