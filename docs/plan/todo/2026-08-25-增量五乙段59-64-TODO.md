# 2026-08-25 增量五乙段（Phase 59〜64）TODO

> 依 dev-prompt `docs/plan/dev-prompts/phase0825-1.md`（產品負責人 2026-08-25）執行。
> 產品負責人另以口頭指示補充：**先不 commit**；需要手機（iPhone）的測試停下來交給他手動，
> 其餘一律自己測。本檔記錄「打算怎麼做」；做完後的實錄在 `docs/plan/report/` 同日的 REP。

## 任務範圍與授權判讀

- 要做的六個 phase：**59 單圖入庫任務、60 PDF 入庫任務、61 建議落庫接線、
  62 上傳端點改 202、63 鏡頭端點改 202、64 任務清單與關閉端點**。
  （dev-prompt 檔名清單裡的「phase-63-入庫任務改201.md」為筆誤，
  實際檔案是 `phase-63-鏡頭端點改202.md`，內容即鏡頭端點改版——照實際檔案做。）
- **★G1 閘門的判讀**：總覽規定 Phase 62 起需產品負責人點頭。本次 dev-prompt 由產品負責人
  親自指名執行 59〜64（含 62 的破壞性契約變更），比照增量四「★G1 由 dev-prompt phase0824.md
  明示通過」的前例，視為**明示授權執行乙段後半**；但總覽 §5.1「階段甲四條」的瀏覽器驗收
  仍屬人的動作，實作者不勾、留給產品負責人（見 REP 的待辦提醒）。
- **commit 節奏**：遵守總覽 §7 鐵律 12——未指示前不 commit、不把 unfinish/ 搬 finish/；
  各 phase 計畫內的 commit 步驟一律跳過（訊息文字留著備用）。
- `docs/spec/` 全程唯讀（Phase 72 才動），每個 phase 結束跑 `git status --short -- docs/spec/` 驗證。

## 實作邏輯（為什麼是這個順序）

六個 phase 是一條「先建沒人呼叫的核心，再翻轉 API 契約」的安全路線：

1. **59〜61 先把 worker 的任務本體寫完**（`run_ingest_job`：單圖 → PDF → 建議落庫）。
   這段對外行為零改變（端點仍 20、`POST /photos` 仍 201），新函式只有測試在呼叫——
   核心邏輯先被 11＋9＋9 顆測試釘死，之後改 API 時紅燈才指得出兇手。
2. **62 才翻轉上傳契約**（201 → 202），全專案 113 個打上傳的測試項目逐檔改寫成
   「202 → 測試扮演 worker 跑完任務 → 原本的 Then」四步樣板（含 BDD binder；規格檔不動）。
3. **63 讓鏡頭走同一條路**（轉呼叫 `_accept_upload`），並收掉 `_ingest_image` 舊路。
4. **64 開兩支新端點**（任務清單＋dismiss，端點 20 → 22），給 Phase 67 進度面板當資料來源。

每個 phase 內部全程 TDD：先寫測試 → 親眼看紅 → 最小實作 → 轉綠 → 全量回歸
（`pytest -q` 顆數只增不減、0 skipped）→ 逐條跑計畫檔 §6 驗收清單。
Phase 62〜64 期間正式入列器是 no-op（Celery 是 Phase 65），手動上傳照片**不會**入庫＝預期行為。

## 步驟（勾選區）

- [x] 階段零：基線與計畫檔核對
  - [x] `docker compose ps`：db Up (healthy)；git 工作區乾淨（快照存 scratchpad）
  - [x] `pytest -q` 開工基線：**442 passed ＋ 0 skipped**（43s）；端點數 **20**
  - [x] 逐份核對六個計畫檔 vs 現行程式碼：photos.py／camera.py 行號、JobStore／staging
        介面、insert_photo 三建議欄（Phase 56 成果）、repository 八欄 SELECT、
        conftest 四道安全網、fakes 內容、test_端點數不變 == 20、camera 測試 34＋17 顆
        ——**全部吻合，計畫檔內容不是舊的**，僅兩處微差：
        ① phase-62 §4.3 的 conftest 匯入片段重覆 import 已存在的 `get_job_store`（實作時略過重覆行）；
        ② 總覽 §9 的「已知斷鏈」註記已過時（phase-59 §2 實際已是「加 12＝442」）→ 順手修正總覽該註記
- [x] 階段一：Phase 59〜61（worker 任務本體；對外行為零改變）
  - [x] 59：config 加 `VLM_MAX_ATTEMPTS=3` → fakes 加 `ScriptedVLM` → 新測試檔 11 顆（紅：ImportError）
        → 新建 `app/services/ingest_job.py`（綠）→ 全量 442+11=453×2 → §6 驗收清單全過
  - [x] 60：新測試檔 9 顆（紅：NotImplementedError）→ `_run_pdf_job`（綠）→ 全量 462×2 → 驗收全過
  - [x] 61：test_ingest_job 追加 7 顆＋folders_endpoint 改 1 加 2（紅：恰 4＋3 顆，與計畫逐顆吻合）
        → `_insert_photo_with_files` 帶三建議欄＋`PhotoSummary` 五鍵→八鍵（綠）
        → 全量 471×2、端點仍 20 → 驗收全過
- [x] 階段二：Phase 62〜64（契約改版；端點 20 → 22）
  - [x] 62：schemas/ingest_job.py＋TaskDispatcher／NoopDispatcher＋EagerDispatcher
        ＋conftest 六工具 → test_photos_upload 新契約（7 紅）→ 改 router（13 綠）
        → 113 個項目逐檔改寫（13 檔交 5 個並行 opus subagent、禁跑 pytest；
        BDD binder 親自改）→ 每檔逐一跑綠＋親自 review diff → 全量 477×2 → 驗收全過
  - [x] 63：camera 測試改寫（10 紅）→ 鏡頭端點轉呼叫 `_accept_upload`＋latest 永遠 204（38 綠）
        → 收掉 `_ingest_image`／`UploadResponse`／`TaskSuggestion`＋清 import → 全量 481×2 → 驗收全過
  - [x] 64：新測試檔 12 顆（11 紅）→ schemas 補兩模型＋新 router＋main 掛載（12 綠）
        → `test_端點數不變` 20→22（另發現並同步 `test_nav_header` 的計數測試，改名 仍為22）
        → 全量 493×2、端點 22、零 DELETE → 驗收全過
- [x] 階段三：總收尾
  - [x] 全量 `pytest -q`＝493＋0 skipped；`OLLAMA_BASE_URL` 指死埠同顆數；binder 三連 27 passed
  - [x] 六份計畫檔 §6 驗收清單逐條跑過；`git status --short -- docs/spec/` 乾淨
  - [x] 親自 review 全部 diff（含 13 個 subagent 改寫檔逐檔看過；真伺服器煙霧：
        202 實測 0.196 秒、正式庫零寫入、409/404 正確，煙霧殘留已清）
  - [x] 更新總覽 §6 進度勾選區與 §2 完成欄（59〜64 → [x]，註明依 phase0825-1.md、未 commit）
  - [x] 寫 REP 兩份（`2026-08-25-增量五乙段59-61-REP.md`、`…62-64-REP.md`）；
        剩餘人工驗收：★G1 甲段四條瀏覽器驗收（產品負責人）；手機／worker 相關要等 65〜66

## 預期顆數軌跡

442 →(59)+11→ 453 →(60)+9→ 462 →(61)+9→ 471 →(62)+6→ 477 →(63)+4→ 481 →(64)+12→ **493**；
skipped 全程 0；端點 20 → 22（僅 Phase 64 變動）。
