# 2026-08-21 階段LL：計畫檔校準（phase-21〜24）——TODO

## 本輪（第二輪）全局規劃

依 `docs/plan/dev-prompts/phase0821-1.md`（使用者於本 session 進行中放入，接續上一輪 P18〜20）。拆成六個階段：

| 階段 | 內容 | 產出 |
|---|---|---|
| **LL（本檔）** | 依專案現況校準 `unfinish/phase-21〜24` 四份計畫檔（任務 1：必須先全部更新完才動工） | 四份計畫更新＋總覽 §5 填數 |
| MM | Phase 21：歸類端點 `PATCH /photos/{id}/folder`（TDD） | +8 tests → 132 |
| NN | Phase 22：資料夾瀏覽端點 `GET /folders`／`/folders/{id}`（TDD） | +8 tests → 140 |
| OO | Phase 23：上傳頁彈窗（純前端；瀏覽器實操驗收，零新增測試） | upload.html 改版；仍 140 |
| PP | Phase 24：瀏覽頁＋`folder_modal.js` 共用彈窗＋三頁互連（純前端） | browse.html 等四檔；仍 140 |
| QQ | 總驗收：四份驗收清單終核＋全量回歸＋親自 review | 驗收報告 |

沿用上一輪工作法：後端 phase 由 Opus subagent 依計畫 TDD 實作、前端 phase 實作後以 **Playwright MCP** 做瀏覽器實操驗收（專案 CLAUDE.md 指定工具）；每階段我親自 review；**全程先不 commit**。

## 校準核對結果（實作邏輯）

四份計畫寫於 2026-08-20；逐條核對現況：

- **P21**：前置（P15/16/20）全就緒；`_folder_out`、`FolderOut`、`PHOTO_COLUMNS`、`find_folder_by_name` 等引用全對；測試 8 顆（5 具名＋parametrize 3）。
- **P22**：`list_folders`／`get_folder`／`list_photos_in_folder` 回傳鍵名與計畫的 Pydantic 模型完全一致；`main.py` 第 9／13〜14 行引用行號全對；端點數預期（router 7＋app 2）與 P21 完成後現況相符。
- **P23／P24**：upload.html／ask.html 現況導覽列（兩連結）與計畫預期一致；現況無 alert/confirm/prompt；`GET /` 轉址仍指上傳頁。
- **唯一過時處＝基線顆數**（計畫寫於 P18〜20 完成前）：P21 開工 124→完成 132；P22 開工 132→完成 140；P23／24 前後皆 140（零新增測試）。

## 步驟

1. [x] `phase-21`：基線釘 124、完成後 132（四處）
2. [x] `phase-22`：基線改述（124→132＝N）、完成後 140（四處）
3. [x] `phase-23`：N 釘 140（兩處）
4. [x] `phase-24`：N 釘 140（三處）
5. [x] `phase-00-增量總覽.md` §5：P21＝132、P22＝140 填入（P23/24 本就「不變」）
6. [x] 寫階段LL REP（`report/2026-08-21-階段LL-計畫檔校準P21P22P23P24-REP.md`）
