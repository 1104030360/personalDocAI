# 階段十八：總驗收與 controller 親自 Review（Phase 93〜95）

- 日期：2026-09-03
- 範圍：本輪（dev-prompt `phase0903-1.md`）從 BASE_TREE `a990543f` 到收工的整包工作樹差異——計畫檔校準、Phase 93／94／95 的程式與文件、AWS／GitHub 側的四樣免費設定
- 目標：① 最終 whole-branch review（Opus）＋ 至多一波 fix；② controller 親自逐檔 review（產品負責人要求）；③ 收工檢查（顆數、三死埠、ruff、機密掃描、零改動清單、AWS／DB 狀態）；④ 更新 CLAUDE.md 專案概述段、記憶、最終訊息（含「Rulings I made」與「待產品負責人」清單）

## 實作邏輯

```text
  review-package-tree BASE_TREE → 收工 tree ──► 最終 reviewer（Opus，讀 ledger 的 deferred minors／parked）
                                                    │ findings? ──► ONE fix dispatch ──► ONE scoped re-review ──► 殘餘 → 裁決＋ledger
  controller 親自：三個 diff 檔逐行讀（deploy.yml／兩份 JSON／test_design6_error_paths.py 全檔）
                   ＋ app/workers/cloud_worker.py 的唯一產品碼改動 ＋ 三個新測試 ＋ 驗收包 ＋ README／CLAUDE.md 改動
  收工：pytest 全量＋三死埠、ruff、tokenize、精確機密掃描（帳號 ID／實例 ID／bucket／role ARN 真值）、
        零改動清單（docs/spec、compose、Dockerfile、db、app/static、app/api、requirements、test.yml）、
        AWS／DB 再跑一次 §4.5／§4.6、四服務 Up、EC2 零 running
```

## 步驟

- [x] 收 Phase 95 回報 → review（Approved）→ complete
- [x] 最終 whole-branch review（Opus；With fixes：0 Critical、3 Important、7 Minor；deferred minors 逐條分級）
- [x] 若有 findings：一波 fix dispatch ＋ 一次 scoped re-review；殘餘裁決進 ledger（R19〜R26）
- [x] controller 親自 review（逐檔：兩份 JSON、deploy.yml、測試檔全檔、三顆停放測試、read_context、README/CLAUDE/LAUNCH、驗收包）
- [x] 收工驗證（716／716／ruff 綠／tokenize／NBSP 0／機密零命中／零改動區乾淨／AWS 與 DB 再跑一次）
- [x] CLAUDE.md 專案概述段補增量六 93〜95 成果；記憶更新；REP 十八
- [x] 最終訊息：Rulings I made（R0〜R18＋）逐條含代價、待產品負責人（review→commit→push→Demo 3；`.env` 重複行）、AWS／GitHub 上多了什麼（全免費）

## 鐵律

- 不 commit、不 push、不搬 unfinish→finish；不開 EC2；機密只寫變數名。
