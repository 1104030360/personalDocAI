# 2026-08-19 階段Q：Phase 09 檢索層 TODO

## 目標

照更新後的 `docs/plan/unfinish/phase-09-檢索層.md`，以 TDD（先紅再綠）做出檢索層：`photo_repository` 兩條查詢（條件 ILIKE／語意 pgvector `<=>`）＋ `retrieval_service` 的 `@chain` 自訂 retriever，兩路共用「最近 30 天、內容時間優先」時間過濾；全量測試 40 → **50 passed**。

## 實作邏輯

- **執行方式**：主控（Fable）派 opus 實作 subagent 照計畫逐字轉錄（計畫已含完整程式碼），完成後由獨立 review subagent 做規格符合＋程式品質雙審，發現問題進修正迴圈；全程**不 commit**（使用者指示）。
- **TDD 順序**（計畫 🧪 加註）：先建 `tests/integration/test_retrieval.py` 看紅（該檔 import 收集失敗、其餘 40 綠不受影響）→ 實作步驟 1（repository 兩函式）＋步驟 2（retrieval_service）→ 轉綠（單檔 10 passed、全量 50 passed）。
- **關鍵設計約束**（不可違反）：SQL 只准出現在 `photo_repository.py`；`ILIKE` 不帶萬用字元＝大小寫不敏感等於（雙語落地）；`EXISTS(unnest(items) ILIKE)` 讓陣列元素同享；`COALESCE(content_time, uploaded_at::date) >= 今天−30天` 兩路共用；不用 LangChain PGVector store（DD-4）；不做跨語言翻譯對映（§8.3 已知限制）。
- **驗收**：計畫驗收標準 1〜7 全跑（含 30 天邊界與 ILIKE 雙語的手動 heredoc，皆指向測試庫）。

## 步驟

- [x] Q1. 派工 opus implementer：TDD 紅（新測試檔收集失敗、其餘 40 綠）→ 綠（10 passed／50 passed）＋驗收 1〜7（全過；轉寫 byte-for-byte 零偏離）
- [x] Q2. 產生工作區 diff（394 行審查包），派獨立 reviewer（opus）雙審 → ✅ Spec compliant＋Approved
- [x] Q3. 零 Critical/Important；5 個 Minor 記 ledger 遞延（過時 docstring、零條件路徑覆蓋等），交最終 review 分流
- [x] Q4. 寫階段Q report、更新 ledger

## 完成定義

`pytest tests/integration/test_retrieval.py -v` = 10 passed；`pytest -q` = 50 passed；計畫驗收 1〜7 全過；review 無未處置的 Critical/Important；SQL 仍只在 repository 一檔。
