# 2026-08-19 階段M：Phase 07 上傳驗收測試（pytest-bdd）TODO

## 目標

照更新後的 `docs/plan/unfinish/phase-07-上傳驗收測試.md`：用 pytest-bdd 把 `docs/spec/features/上傳照片.feature` 直接當測試跑（7 條 Rule U1〜U7 全綠）、把煙霧測試檔安全退役（守衛先遷移、替代全綠才刪檔）、補英文照片雙語測試，通過驗收（`pytest -q` **40 passed** 為正式驗證）。

## 實作邏輯

- **本 phase 只寫測試、不寫產品程式碼**：規格例子紅燈＝前面 phase 的產品缺陷 → 回頭修 `app/`，不得改 `docs/spec/`（唯讀）、不得弱化步驟斷言。
- **BDD 正式落地**：`scenarios("../../docs/spec/features/上傳照片.feature")` 直接讀規格原檔（不複製）；步驟定義一句對一段程式碼；`KNOWN_UNDERSTANDINGS` 對照表把規格 When 步驟的文字映射到假 VLM 結果。
- **安全退役順序**：先建三個新檔（feature／bilingual／design_rules）→ 全量 47 passed（新舊守衛並存）→ 才刪 smoke → 40 passed。三個 `.feature` 沒涵蓋的守衛（415 不呼叫 understand、text 全空白 422 逐字、U4 向量護欄）逐字遷入 `integration/test_upload_design_rules.py`。
- **fixture 疊接**：`wire_feature_clock(wire_fake_ai, context)` 顯式依賴 conftest 安全網，確保 `get_now` 覆寫順序；全程零真 Ollama 呼叫。
- 技術前提已實證：pytest-bdd 8.1.0 × pytest 9.1.1 的 Rule／中文步驟／datatable（階段L，1 passed）。
- 執行方式：opus subagent 實作（計畫檔＝任務書，程式碼逐字轉寫），主控 dispatch reviewer 審查＋親自複跑驗收。

## 步驟

- [x] M1. 步驟 1〜2：fakes.py 加 KNOWN_UNDERSTANDINGS／understanding_for_text；conftest 加 first_row／split_items（numstat 15 0／24 0＝純追加）
- [x] M2. 步驟 3：建 `tests/integration/test_upload_feature.py` → 單檔 7 passed（7 個 Example 名對應 7 條 Rule）
- [x] M3. 步驟 4：建 `integration/test_upload_bilingual.py`＋`integration/test_upload_design_rules.py` → 全量 47 passed → commit dc99e57
- [x] M4. 步驟 5：刪 `integration/test_upload_smoke.py` → 全量 40 passed → commit ed27088
- [x] M5. 驗收 1〜7 全過（死埠法 40 passed；規格檔 grep Rule = 7、git 零變更）
- [x] M6. 任務 review → Spec ✅（15/15）／Approved；0 Critical/Important、8 Minor 遞延（其中 first_row strict／split_items strip 標記 P12 開工前必補）；reviewer 探針 A/B/C/D 獨立證實 Given 時鐘與雙層斷言真的生效
- [x] M7. 主控核可＋寫階段M report

## 完成定義

`pytest -q` 40 passed（unit 12＋integration 28）；7 條 Rule 的 Example 名稱逐一出現在 `-v` 輸出；驗收 1〜7 全過；review 無未解 Critical/Important；`docs/spec/` 零變更。
