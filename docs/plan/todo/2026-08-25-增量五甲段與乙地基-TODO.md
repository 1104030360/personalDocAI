# 2026-08-25 增量五甲段與乙地基（Phase 52〜58）——TODO

> 依 dev-prompt `docs/plan/dev-prompts/phase0825.md` 執行。
> 範圍＝增量五（`docs/design/design5.md`）的前七個 phase：
> 階段甲 52〜55（待決定獨立入口，純前端）＋階段乙地基 56〜58（對外行為零改變）。
> **Phase 62 起的 201→202 破壞性變更不在今天範圍**——那要先過 ★G1（產品負責人人工驗收）。

## 實作邏輯（為什麼是這七個、為什麼這個順序）

```text
甲（純前端，一行後端不碰）           乙地基（後端純新增，對外行為零改變）
52 pending.html 新頁                 56 photo 表加建議三欄（D16；值先全 NULL）
   ↓                                 57 JobStore 契約＋記憶體實作＋conftest 第四道安全網
53 五頁頂欄統一四格                  58 staging 暫存區服務（寫／讀／刪／24h 掃把，不接線）
   ↓                                      ↑
54 歸類彈窗窗頂加原圖                56〜58 定稿明文「可與甲並行、不被 ★G1 擋」
   ↓                                      （G1 擋的是 Phase 62 起的上傳契約變更）
55 瀏覽頁拿掉待決定 tab
   ↓
★G1 產品負責人四條驗收（人工）→ 通過後才准做 62 起的 202 改版
```

- **甲**解決「待決定（還沒歸類完的工作）跟檔案櫃（已完成的成果）擠同一頁」：
  待決定升格成頂欄一格、有自己的 `/ui/pending.html`；歸類彈窗窗頂加原圖讓人看圖決定。
- **乙地基**是之後非同步入庫（Redis＋Celery）的純地基：三個新模組／欄位都先「存在但沒人用」，
  所以對外行為、端點數（20）、回應格式全部不變——這正是它們不必等 G1 的原因。
- 順序照總覽依賴圖：52→53→54→55（甲內硬依賴），56 獨立、57→58（58 依賴 57）。

## 步驟（進度勾選）

- [x] 0-1 開工基準驗證：pytest 405 passed＋0 skipped、端點 20、db healthy、git 快照
- [x] 0-2 dev-prompt 任務 1：七份 phase 計畫檔逐檔對現況核對、過時處更新（平行驗證）
- [x] 甲-52 `app/static/pending.html` 新建（搬 browse 的 showPending；+0 顆，累計 405）
- [x] 甲-53 五頁頂欄四格＋新檔 `test_nav_header.py`（+7 顆，累計 412）
- [x] 甲-54 `folder_modal.js` 窗頂原圖＋「稍後再說」文案（+0 顆，累計 412）
- [x] 甲-55 `browse.html` 拿掉待決定 tab＋`test_nav_header.py` 追加（+3 顆，累計 415）
- [x] 乙-備份 正式庫 pg_dump ＋ `data/` tar（動正式庫結構前的雙備份）
- [x] 乙-56 `db/migrate_design5.sql` 三欄＋schema 對齊＋repository 讀寫（+5 顆，累計 420；正式庫跑兩次證冪等）
- [x] 乙-57 `ingest_job_store.py`＋`InMemoryJobStore`＋conftest `wire_memory_job_store`（+12 顆，累計 432）
- [x] 乙-58 `staging_service.py` 五函式＋24h 掃把（不接線；+10 顆，累計 442）
- [x] 收尾-1 全量 `pytest -q`（預期 442 passed＋0 skipped）＋`OLLAMA_BASE_URL` 指死埠同顆數
- [x] 收尾-2 瀏覽器實操驗收→依產品負責人裁示改由 ★G1 親自做（本 session 不操作瀏覽器；curl 可驗項全過）
- [x] 收尾-3 親自 review 全部改動（controller 逐檔看 diff）
- [x] 收尾-4 REP 報告寫入 `docs/plan/report/`
- [x] 收尾-5 停手，向產品負責人交付 ★G1 驗收清單（design5 §12 甲段四條）

## 工作紀律（本次全程有效）

1. 全程 TDD：先寫會紅的測試、親眼看紅、最小實作、跑綠（52／54 純前端零新增測試，依定稿）。
2. **不 commit**（產品負責人指示「先不 commit」＋總覽 §7 鐵律 12）。
3. `docs/spec/` 一字不改（★G3 之前唯讀）。
4. pytest 同一時間只跑一份（reset_tables 會互相 TRUNCATE）。
5. SQL 只准出現在 `photo_repository.py`；openapi 零 DELETE；QR 尺寸測試不准動。
6. Phase 56 動正式庫前先雙備份；遷移腳本必須冪等（真的跑兩次證明）。
7. 每個 phase 完成後由獨立 reviewer 檢查（spec 符合度＋程式品質），最後 controller 親自總 review。
