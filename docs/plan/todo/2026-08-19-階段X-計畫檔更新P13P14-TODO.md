# 2026-08-19 階段X：計畫檔更新（phase-13／phase-14）TODO

## 目標

對照專案現況（master 67 passed、P11/P12 已完成、conftest 安全網已涵蓋五注入點、CLAUDE.md 指令節已是整併後最終版、Ollama 為 App 版），把 `phase-13-錯誤收尾與全量回歸.md` 與 `phase-14-網頁介面.md` 全面校準並同步 `phase-00-總覽.md`——完成 dev-prompt `phase0819-5.md` 任務 1，才能進入開發。

## 實作邏輯

- **先盤點再動筆**：實跑 `pytest -q`＝67 passed；逐一實查 P13 步驟 2 檢查腳本的每一項現況（db/schema.sql 存在、requirements 無雲端關鍵字、PhotoMetadata 恰四欄、ILIKE 3 處、端點恰 3 個、空殼三者不存在、app/ 無 user 字樣）——**檢查腳本可原樣沿用**。
- **測試累計數重算**：P13 舊制 38→49 改為 **67→79**（步驟 0 遞延補洞 +1、錯誤路徑 +11）；P14 維持 0 新增（79）。
- **📌 遞延事項轉正式步驟 0**：category 守護測試改用**獨立新測試**（自備兩筆資料、只帶 category 條件），不動既有 `三張規格照片` fixture——比 review 原建議（改 fixture）更安全，不會連鎖影響既有 10 個檢索測試；三項 Minor 清理（embed_document 復用、冗餘覆寫移除、施工註記刪除）列為同步驟，全程零行為差異。
- **關鍵路徑修正**：`test_error_paths.py` 歸 `tests/integration/`，原始碼掃描的專案根目錄從 `parent.parent` 改 **`parents[2]`**（多了一層目錄）；fixture 沿 conftest `wire_fake_ai` 慣例（安全網 P12 已涵蓋五注入點，只需覆寫 get_vlm 成「看得懂」）。
- **步驟 3 已過時**：CLAUDE.md 指令節早在歷輪整併完成（無佔位段、無 Phase 8 回填段）→ 改為「核對＋補兩個小示例」（兩份規格檔單跑、schema.sql 建表）。
- **本機差異**：`brew services stop/start ollama` 全部換成死埠法或 App 版檢查；P14 main.py 程式塊的 `title` 校準為現況 `personalDocAI`。

## 步驟

- [x] X1. `phase-13`：前置基線（67）＋📌 改寫（獨立測試法）＋插入步驟 0（+1 測試與三項清理，68）＋步驟 1 校準（integration 路徑、parents[2]、wire_fake_ai 慣例 fixture）＋步驟 3 改核對式＋步驟 5 煙霧指令去 brew 化＋驗收 1〜7 重算（11／79／14／7+72 deselected／死埠法）＋Q7 與完成狀態（79）
- [x] X2. `phase-14`：前置（79）＋環境指令（Ollama App 版）＋步驟 2 title 維持 personalDocAI＋驗收 9 與完成狀態（79）
- [x] X3. `phase-00-總覽`：tests/ 樹 integration 分支收整（error_paths 移入、retrieval 註記 P13）＋第六條 🔄 註記＋累計表 P13/P14 兩列（79／79）
- [x] X4. 三檔 grep 交叉核對＋基線不變確認，寫階段X report、更新 ledger

## 完成定義

兩份 phase 計畫與總覽對現況零矛盾：累計數 67→68→79 一路對齊、測試路徑歸 `tests/unit|integration/`、計畫引用的每個符號與指令都實查過；`pytest -q` 仍 67 passed（純文件工作）。
