# 階段十四：計畫檔校準 Phase 93〜95（開工前把三份計畫改成今天可逐字照做的版本）

- 日期：2026-09-03
- dev-prompt：`docs/plan/dev-prompts/phase0903-1.md`（任務 1：「逐個更新以下計畫檔案……每個計畫檔案都更新完再進行下一步」）
- 產品負責人本次額外指示：不 commit；需要手機才停下；subagent 用 Opus、最後 controller 親自 review；**先修計畫（識別字語言）**；Phase 92 已完成並歸檔（`c40a3b3`）
- 工作區：`.superpowers/sdd/phase0903-1/`（ledger `progress.md` 裁決 R0〜R11；`brief-common.md` 共同事實；`calib-brief.md` 校準清單）
- 本階段**零程式碼變更、零測試變更**；只改 `docs/plan/unfinish/phase-93／94／95` 三份與總覽的少數幾行

---

## 1. 為什麼要先校準（一句話）

三份計畫寫於 2026-08-31、09-03 夜裡只補了「92 拆兩段」的註記，**還有五類東西跟今天的現況對不上**：
顆數（寫 662／666／672／682，實查 **692**）、同一個測試檔的顆數（寫 4，實 **6**——owner 在 `f2fc067` 補了兩顆）、
碼區裡的中文識別字（`信任文件()`、`deploy工作流程()`、`class 只記帳的S3`…——2026-09-02 產品負責人已指示識別字一律英文）、
已被前輪修掉的過期步驟（95 §4.3.1 的舊名註解早在 09-01 修完）、以及「Demo 3 要 push」跟「本輪不 commit」的衝突。
實作者只看計畫檔，計畫檔錯一個名字，實作就錯一個名字。

```text
  校準前                                   校準後
  ─────────────────────────────            ──────────────────────────────
  基線 662、檔內 4 顆                       基線 692、檔內 6 顆（6→10→16→26）
  def 信任文件() / def deploy工作流程()      def read_trust_policy() / read_deploy_workflow()
  「等 ★G3」                                「★G3 已過（c40a3b3＋dev-prompt）」
  Demo 3 = 實作者 push                      Demo 3 = 產品負責人 push（本輪不 commit）
  action 版本 2026-08-31 查的               2026-09-03 用 context7／官方頁重查、附來源
```

---

## 2. 實作邏輯

1. **先實查再改**：controller 親自查 pytest 顆數、AWS（provider／role／ECR／EC2 狀態）、GitHub（secret／variable／workflow／OIDC sub 前綴）、程式碼現名，寫成 `brief-common.md`，三位校準者只准照它改，不准憑記憶。
2. **一份計畫一個 Opus 校準者**（A:93、B:94、C:95），只准改自己名下的檔；跨檔問題回報 controller（總覽由 controller 改）。
3. **識別字英文、`test_中文` 保留**（裁決 R1）：碼區抽出來用 `tokenize` 自檢。
4. **外部事實用工具重查**（裁決 R11）：GitHub OIDC 不可變主體格式、`workflow_run` 語意、AWS OIDC thumbprint、六個 GitHub Action 的現行大版——context7 或官方頁，計畫檔附來源。
5. **不過度設計**：只對齊現況、修錯、補漏；不新增功能；保留新手友善的解釋與 ASCII 圖。

## 3. 步驟

- [x] 讀 dev-prompt、memory、上一輪 ledger（R0〜R28）、git log；確認 Phase 92-A 已完成並歸檔
- [x] 實查基線：pytest 692／0 skipped；`test_design6_error_paths.py` 6 顆；端點 22；四個容器 Up
- [x] 實查 AWS：admin 身分、零 OIDC provider、無 deploy role、ECR 三個 tag、零 running、實例 t3.xlarge stopped
- [x] 實查 GitHub：repo id／owner id 與計畫逐字相同、secret／variable 皆空、只有 `test` workflow、10 個 commit 未 push
- [x] 建工作區與四份簡報；寫 pre-flight 對照表與裁決 R0〜R11 進 ledger
- [x] 派三個 Opus 校準者（A／B／C）平行
- [x] controller 順手：建 OIDC provider（Phase 93 §4.2，免費、可重跑）、總覽 §2.7 兩個過期測試名、§6 勾選區 81〜92-A／G1／G2／G3
- [x] 收三份回報，controller 逐份抽查（顆數、識別字、來源連結、跨檔問題），必要時 SendMessage 續改
- [x] 跨檔問題回寫總覽／design6／CLAUDE.md（controller）
- [x] 寫本階段 REP，進入階段十五（Phase 93 實作）

## 4. 裁決（摘要；全文在 ledger）

| # | 裁決 | 代價 |
|---|---|---|
| R0 | 不 commit／不 push；Demo 3 留給產品負責人 | Demo 3 本輪標「待產品負責人」 |
| R1 | 識別字英文（今日「不要 function 名稱用英文」判為筆誤＝不要中文；依 09-02 指示） | 若本意真是中文，93〜95 新 helper 改回（機械） |
| R2 | ★G3 視為已過（c40a3b3、CLAUDE.md、dev-prompt、/goal） | provider／role／secret 免費可逆 |
| R4 | 顆數 692 起算：696／702／712（＋95 停放項） | 總覽 §9 加註 |
| R7 | 總覽 §2.7 兩個過期名以現況為準 | 無 |
| R9 | 上一輪停放項＋Phase 80 候選補測納入 95 | 95 顆數超出 +10 要明寫 |
| R10 | 95【掃E】與 87 ast 掃碼互補 | 無 |
