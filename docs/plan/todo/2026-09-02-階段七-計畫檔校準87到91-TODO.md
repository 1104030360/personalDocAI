# 2026-09-02 階段七：計畫檔校準 Phase 87〜91 — TODO

> dev-prompt：`docs/plan/dev-prompts/phase0902-2.md`（它列的檔名標題錯位一格，以**編號 87〜91** 為準）。
> 工作區與 ledger：`.superpowers/sdd/phase0902-2/`（`progress.md` 記所有裁決 R0〜R10）。
> 本輪整體分期：**階段七** 校準 87〜91 → **階段八** Phase 87〜88（工人核心＋主迴圈＋Mac 端到端）→ **階段九** Phase 89〜90（Ec2Probe＋多階段 Dockerfile＋容器端到端）→ **階段十** Phase 91（EC2 周邊：SG／endpoint／IAM／ECR）→ **階段十一** 總驗收與親自 Review。

## 實作邏輯

五份計畫檔是 2026-09-01 寫的，之後發生了三件會讓實作者照抄就錯的事：
1. 產品負責人 2026-09-02 指示**識別字一律英文**（計畫檔仍有 `_把log印到終端機` 這類中文函式名）。
2. Phase 81〜86 已完成並 commit（`bb3921a`）：S3／SQS 已建、`.env` 已填、`scripts/aws_check.py` 已在、`get_cloud_route()` 的 assume 分支已接；計畫檔的「前置條件」「現況碼」「顆數基線」都過期（實查 **644**，不是總覽寫的 634）。
3. 2026-09-01 改判「隱私閘門只看內容不看檔名」，但 88 的 CLAUDE.md 草稿仍寫「檔名有身分證／passport 就敏感」。

做法沿用前兩輪（phase0902／phase0902-1）實證有效的流程：controller 先實查工作樹寫成**共同事實簡報**（`brief-common.md`）＋**校準簡報**（`calib-brief.md`），再派 **五個 Opus subagent 平行**、一人一檔、只准改自己名下的檔；跨檔問題回報給 controller 裁決並回寫 ledger。

## 步驟

- [x] 讀 dev-prompt、記憶、上一輪 ledger；確認 HEAD `bb3921a`、工作樹乾淨。
- [x] 抓基線：`pytest -q` 644 passed／0 skipped；容器 dev overlay；Ollama 開著；AWS admin profile 通；`.env` 變數名清單。
- [x] 建 SDD 工作區 `.superpowers/sdd/phase0902-2/`（BASE_TREE、snapshot-tree、review-package-tree、brief-common、global-constraints、implementer-rules、calib-brief、progress.md）。
- [x] Pre-flight 掃描表（五份 vs 現況與彼此）＋裁決 R0〜R10 寫進 ledger（含 ★G2 條件式裁決 R2）。
- [x] 派 A/87、B/88、C/89、D/90、E/91 五個 Opus 校準者平行校準（A 14／B ~45／C 41／D ~40／E 38 處）。
- [x] 收回報、複核跨檔問題、補裁決 R11〜R16（含兩處校準者判斷被 controller 實查推翻：`OllamaCloudVLM` 不含計時 log；「已送去雲端」log 存在於 `cloud_ingest.py` L259）。
- [x] 總覽 §2.2／§9／§2.7 標題 87〜91 加「實查」註記；§2.7 P89 補 `CLAUDE.md`、P90 補 `.dockerignore`。
- [x] 寫階段七 REP（`docs/plan/report/2026-09-02-階段七-計畫檔校準87到91-REP.md`）。
