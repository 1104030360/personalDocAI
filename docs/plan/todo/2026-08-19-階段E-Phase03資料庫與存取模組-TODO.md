# 2026-08-19 階段E：Phase 03 資料庫與存取模組（TDD） TODO

## 目標

照更新後的 `docs/plan/unfinish/phase-03-資料庫與存取模組.md`，以 TDD 建出 `photo` 資料表（兩庫）、`app/db/session.py`、`app/repositories/photo_repository.py`，並通過該文件全部 7 條驗收（含 pytest `12 passed`）。

## 實作邏輯

- **先紅後綠**（步驟 0 → 1〜4）：先寫 `tests/conftest.py`＋`tests/unit/test_photo_repository_unit.py`（2 個）＋`tests/integration/test_photo_repository.py`（10 個），跑 `python -m pytest tests -q` 確認紅燈（空模組 → ImportError/AttributeError＝「功能還不存在」的正確失敗）；再依序寫 `db/schema.sql` → 兩庫建表 → `session.py` → `photo_repository.py`，把紅燈轉綠。
- **程式碼照計畫逐字轉寫**：計畫檔含全部完整程式碼（步驟 0〜4），實作＝轉寫＋驗證；發現計畫碼有疑慮時記錄並回報，不得默默偏離。
- **測試庫隔離**：conftest 在 import 任何 `app.*` 前把 `DATABASE_URL` 指到 `visual_memory_test`（load_dotenv 不覆蓋既有環境變數）＋直接覆寫 `config.DATABASE_URL` 雙保險；autouse fixture 每測清空且斷言 URL 含 test 字樣。
- **執行方式**：本階段由 opus subagent 實作（phase0819 授權），完成後由主控親自逐檔 review＋重跑全部驗收。
- 環境鐵則：psql 一律帶 `-p 5433`；絕不碰 5432 的 postgresql@14；非 git repo、不下 git 指令；用 `python -m pytest`（venv 內）。

## 步驟

- [x] E1. 步驟 0：建 tests 結構＋三個測試檔，跑出紅燈（11 errors：ImportError＋AttributeError，正確紅燈型態）
- [x] E2. 步驟 1〜2：寫 `db/schema.sql`，`psql -p 5433 -d visual_memory|visual_memory_test -f db/schema.sql` 兩庫建表
- [x] E3. 步驟 3〜4：寫 `app/db/session.py`、`app/repositories/photo_repository.py`
- [x] E4. 綠燈：`python -m pytest tests -q` → `12 passed`（計畫碼逐字轉寫、零修改即轉綠）
- [x] E5. 步驟 5 手動煙霧腳本（中英各一筆，輸出與計畫逐字元一致）＋驗收 1〜7 全跑（grep 僅 repository 一行、清空歸零）
- [x] E6. 主控親自 review：8 檔逐字核對 ✓、親手重跑 pytest=12 passed／grep／兩庫 hnsw 索引／count=0／未動檔案檢查 ✓
- [x] E7. 寫階段E report

## 完成定義

phase-03 文件 7 條驗收全數拿到預期輸出；12 個測試全綠；`visual_memory` 結尾照片數量為 0；review 無未解 Critical/Important 發現。→ **全部完成**
