# 2026-08-19 階段D：phase-03／phase-04 計畫檔更新 TODO

## 目標

依 `phase0819.md` 任務 1：先把 `docs/plan/unfinish/phase-03-資料庫與存取模組.md` 與 `phase-04-上傳端點與格式檢查.md` 更新到與**目前專案現況**及**本次開發方法（TDD＋BDD）**一致，兩份都更新完才進入實作。

## 實作邏輯

- **現況偵察先行**（已完成）：實測確認 PG17@5433 兩庫皆在、pgvector 0.8.6 已裝、venv 套件齊全、四個目標檔（schema.sql／session.py／photo_repository.py／photos.py）皆為空檔——計畫的「前置條件」已全數成立，內容本身不過時。
- **主要缺口是測試**：兩份計畫原本把 pytest 留到 Phase 05／07；但 phase0819 約束 1〜2 要求「先寫測試再寫功能」（TDD）且測試放 `tests/unit`（單元）與 `tests/integration`（整合）。因此更新重點＝在兩份計畫各插入「步驟 0：先寫測試（TDD red）」與測試設計（conftest 指向 visual_memory_test、每測前清空 photo 表——與 design.md §11「每測清空」一致），並在驗收標準加入 pytest 全綠。
- **BDD 對應**：整合測試以 Given/When/Then 結構撰寫、逐條對應 `docs/spec/features/上傳照片.feature` 可在本階段落地的 Rule（U1 完整可驗；U2〜U5 在 repository 層驗資料面）。完整 feature 檔驗收（pytest-bdd＋假件）仍照原計畫在 Phase 07，不提前。
- **既有教訓入檔**：0818 階段C 發現沙箱會擋 localhost 連線——phase-04 的 curl 驗收保留為手動項，正式驗證改以 TestClient（in-process、不經網路）的 pytest 為準，寫進計畫避免之後踩坑。
- **連動檢查**：phase-00 總覽的「測試數量累進表」原以 P05 為起點（P03/P04＝0 個測試）、最終結構的 tests/ 是扁平的——與本次 TDD 提前及 unit/integration 子目錄衝突，需同步修正。
- 查證原則：API 用法不憑記憶——FastAPI UploadFile／psycopg 3 行為以 Context7 官方文件確認後才寫進計畫。

## 步驟

- [x] D1. 讀 phase-00 總覽，確認是否有測試累進表等需連動之處（有：累進表＋最終結構圖＋§6 總數）
- [x] D2. Context7 查證：FastAPI `UploadFile`/`File(...)`/`content_type`/sync `def` 用 `file.file.read()` ✔；psycopg 3 連線 context manager 離開時自動 commit（例外 rollback）✔
- [x] D3. 更新 phase-03：前置條件現況註記＋「步驟 0：先寫測試（TDD red）」（conftest.py＋2 個單元＋10 個整合測試完整程式碼）＋驗收第 7 條 pytest `12 passed`＋完成狀態補述
- [x] D4. 更新 phase-04：前置條件註記＋TDD 說明（TestClient 為正式驗證、curl 降為手動輔助）＋「步驟 0」（7 個整合測試完整程式碼，含 Rule U1 BDD 對應）＋驗收第 0 條 pytest `19 passed`＋完成狀態補述
- [x] D5. 連動修正 phase-00：累進表加 P03=12/P04=19 並順移後續累計（最終 49→68）、最終結構圖 tests/ 加 unit/＋integration/、§6 總數改 68
- [x] D6. 寫階段D report

## 完成定義

兩份計畫檔讀起來與 2026-08-19 現況一致、TDD 步驟與驗收完整；phase-00 無矛盾；report 落檔。→ **全部完成**
