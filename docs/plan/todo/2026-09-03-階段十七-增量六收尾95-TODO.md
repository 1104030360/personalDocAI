# 階段十七：Phase 95 增量六錯誤收尾與驗收包

- 日期：2026-09-03
- 計畫檔：`docs/plan/unfinish/phase-95-增量六錯誤收尾與驗收包.md`（階段十四校準後版本；§4.3.3 為 2026-09-03 新增的停放項）
- 目標：把 design6 §8 錯誤表 10 列逐列清點到有測試把關（補 2 個真缺口）、把 §0 六禁與 §1.2 被否決清單變成 8 顆掃碼、收前輪 review 停放的 4 件小事（+3 顆；`read_context` 型別加固是本增量收尾唯一產品碼改動）、三死埠零依賴實證、正式庫健檢、AWS 收工檢查，最後產出增量六驗收包
- 顆數：702 → **715**（`test_design6_error_paths.py` 16 → 26；另 `test_cloud_worker_unit.py` +1、`test_gated_ingest.py` +1、`test_cloud_ingest_unit.py` +1）
- ⚠ 驗收包 D 段（Demo 3）在產品負責人 push 之前只能標「待執行」（裁決 R0）

## 實作邏輯

```text
  §4.1 盤點（誰已經測了）──► §4.2 補兩個真缺口 ──► §4.3 八顆掃碼 ──► §4.3.3 停放項 ──► §4.4 三死埠
       ▲ 先找「誰已經測了」，只補真缺口；重複的測試是負債
                                                                                    │
  §4.5 正式庫健檢 ＋ §4.6 AWS 收工檢查（controller，已 dry run 全過）◄────────────────┘
                                                                                    │
  §4.7 驗收包（A 段實作者填數字、B〜E 段留給產品負責人點）＋ §4.8 TODO ＋ §4.9 歸檔清單（只寫不做）
```

- 【補A】雲端看圖三次失敗＝整筆失敗、**不是** fallback 本機（`run_ingest_job` 零呼叫）——用 `WorkerMailbox(FakeMailbox)` 子類讓工人在本機等結果前先做完。
- 【補B】遠端不可用時 `POST /photos` 仍 202、body 恰三鍵、跑完任務後入庫一列。
- 【掃A】〜【掃H】：NAT／EIP／ALB／Lambda／ECS 字樣（詞邊界）、compose 零 AWS 設定、端點 22 零 DELETE、佇列 body 無位元組、工人不寫 DB 不算 embedding（與 87 的 ast 掃碼互補）、boto3 唯一入口、photo 16 欄凍結、閘門不碰 AI 開關。
- §4.3.3：① `read_context` 值不是 list → 當空清單＋warning（+1）；② `from_lines` 補 `re.I`；③ docstring「這 10 顆」；④a 崩潰重送遇 `CloudRouteOff` 照樣 fallback（+1）；④b 別人的訊息時 `store.get` 丟例外往外丟（+1，只釘現況）。
- §4.3.2：README×2＋CLAUDE.md L461 的 `543` → 收工實跑值（先跑完 §4.4 再改）。

## ★ 閘門紀錄（誰、什麼時候、憑什麼）

- ★G1（81 之後、82 之前）：＿＿＿＿＿＿（日期／依據）
- ★G2（90 之後、91 之前）：＿＿＿＿＿＿
- ★G3（92 之後、93 之前）：＿＿＿＿＿＿

## 74〜95 進度總表（整個增量六一次看完）

甲（74〜81）全程 TDD、**零 AWS**：先做隱私閘門與 fallback 契約，行為與增量五 100% 相同。
★G1 之後才開帳號。乙（82〜84）S3 寄物櫃、丙（85〜86）兩條 SQS、丁（87〜88）Mac 上的工人、
戊（89〜92）EC2、己（93〜94）CD、95 收尾與驗收包。三個閘門都是**人**的動作。

- [x] 甲 74〜81：privacy_gate／ingest_job 重構／cloud_ingest 契約＋第五道安全網／
      gated_ingest 接線／CloudRoute 單圖／逾時與冪等／PDF（543 → **616**）
- [x] ★G1
- [x] 乙 82〜84：開戶＋Budget＋兩個 IAM user／`aws_mailbox.py`（+16，總覽追認項 J）＋
      改 design5 的 boto3 掃碼／建 bucket（616 → **632**）
- [x] 丙 85〜86：兩條佇列／`get_cloud_route()` 補 assume ＋真 AWS 逾時煙霧（632 → **634**）
- [x] 丁 87〜88：`process_job_message`＋假信箱端到端／主迴圈＋Mac 上真跑（634 → **651**）
- [x] 戊前半 89〜90：`Ec2Probe`／Dockerfile 多階段 arm64（651 → **662**）
- [x] ★G2
- [x] 戊後半 91〜92：SG／IAM role／ECR／真機 Demo 2／2b（**92-A `t3.xlarge`**）／文件三份
      （92 是人工 phase +0，但收尾時另補了 2 顆掃碼＝`f2fc067`；**實查基線 692**）
- [x] ★G3
- [x] 己 93〜94：OIDC 部署角色（692 → **696**）／`deploy.yml`（696 → **702**）
      ＋ Demo 3（**待產品負責人 push 後執行**）
- [x] 95：錯誤表盤點＋2 顆補缺＋8 顆掃碼＋停放項 3 顆＋R18 1 顆＋三死埠實證＋驗收包（702 → **716**）
- [ ] 寫 REP（`docs/plan/report/2026-09-03-階段十七-增量六收尾95-REP.md`）

> ⚠️ 上面括號裡「543 → 616 → 632 …」那幾個是**總覽 §9 排計畫時的估計值**；
> 74〜92 每一輪 review 都有補顆數，所以從 82 起絕對值就對不上了
> （2026-09-03 實查：Phase 92 之後是 **692**，不是 662）。
> **要對的是每個 phase「新增幾顆」**，絕對值一律以當天的 `pytest -q` 為準。
> 本 phase 計畫寫 +13（累計 715），實際 **+14（累計 716）**——多的那一顆是裁決 R18 ②
> 的 `test_部署policy恰五段而且SendCommand綁實例與document`（Phase 93 review 的 deferred minor）。

## 步驟

- [x] controller：§4.5 正式庫四查詢 dry run（10 資料夾／1 收件箱／0 孤兒／16 欄／76 張）
- [x] controller：§4.6 七項 dry run（四服務、EC2 零 running、S3 空、佇列 0／0、NAT／EIP 空、SG In 空、Budget 5＋兩筆 80%）
- [x] 派 Opus 實作者（brief：計畫檔全文＋brief-common＋implementer-rules＋93／94 review 的 deferred minors）
- [x] 實作者：§4.1 盤點（`--collect-only` 對表；20 個 ✓ 測試名全部點得到）→ 【補A】【補B】**首跑就綠**（零真缺陷）→ 8 顆掃碼首跑全綠（8 次反向變異全部親眼看紅）→ §4.3.3 四項 TDD（① `read_context` 先紅 `TypeError` 再綠）→ R18 四項 → 全量 **716**／三死埠 **716**（一起指與各指一次都相同）→ 三份 binder 27 顆零 skip → ruff 綠 → README×2＋CLAUDE.md×1＋LAUNCH.md×1 的 `543` → 驗收包 A 段＋本檔補完＋§4.9 歸檔清單（只寫不做）
- [x] controller：收工再跑一次 §4.5／§4.6（見階段十八）；補驗收包裡 controller 才查得到的數字
- [x] 派 Opus 審稿者（spec ＋ quality），fix loop 若有——Approved、零 Critical／Important、11 Minor 交最終 review
- [x] 寫 REP；進入階段十八（最終 whole-branch review ＋ controller 親自 review）

## 歸檔清單（**寫下來，不執行**——等產品負責人指示 commit）

```text
docs/plan/unfinish/phase-00-增量六總覽.md
docs/plan/unfinish/phase-93-GitHub_OIDC與部署角色.md
docs/plan/unfinish/phase-94-CD工作流程.md
docs/plan/unfinish/phase-95-增量六錯誤收尾與驗收包.md
```

`unfinish/` 現在**只剩這 4 份**（73〜92 產品負責人自己搬完了，83〜92 那 10 份的搬移還沒 commit——
不要動它們、也不要「幫忙」還原）。`git mv` 會直接 stage ＝ 替人決定 commit 內容，所以本 phase 不執行。

## 鐵律

- 不 commit、不搬 unfinish→finish；`docs/spec/` 零改動；產品碼只准 `read_context` 一處；不為了讓測試綠改產品行為；不重複已有人測的東西；不開 EC2。
