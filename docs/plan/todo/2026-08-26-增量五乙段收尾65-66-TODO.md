# 2026-08-26 增量五乙段收尾（Phase 65〜66）TODO

> 對應 dev-prompt：`docs/plan/dev-prompts/phase0826.md`。
> 計畫檔：`docs/plan/unfinish/phase-65-Celery與Redis實作.md`（2026-08-26 已校準）、
> `docs/plan/unfinish/phase-66-Compose加redis與worker.md`（同）。

## 實作邏輯

乙段 57〜64 已把非同步入庫骨架做完，但佇列是空的（NoopDispatcher 佔位，
job 永遠停在 queued）。Phase 65 補上「真正的佇列」：`app/celery_app.py`
（Celery 實例＋薄薄一顆 `ingest_task`）、`RedisJobStore`（app 與 worker 是兩個行程，
記憶體版彼此看不到）、`get_task_dispatcher()` 換裝成真派工、掃把接線兩頭
（`main.py` lifespan＋`worker_ready`）、`build_vlm_for_backend()`（D14 快照消費端）。
Phase 66 把 `redis`／`worker` 兩個容器接進 compose、更新 LAUNCH.md／CLAUDE.md、
跑真容器煙霧（切雲端上傳→worker log `backend=cloud`→照片進待決定）。

分工：65 交給 Opus subagent 照計畫 TDD 執行（純程式碼；先紅後綠）、我親自 review diff；
66 由我親自執行（動 Docker、動正式庫煙霧，風險操作不假手）。

## 步驟

1. [x] Phase 65（subagent）：照計畫 §4.1〜§4.11 逐步做——
   requirements 加 celery／redis、config 加 CELERY_BROKER_URL、
   `tests/unit/test_celery_app_unit.py`（5 顆煙霧）先紅、
   `test_ingest_job_store_unit.py` 追加 RedisJobStore 9 顆（假 client 不連真 Redis）先紅、
   `app/celery_app.py`＋`RedisJobStore`＋`build_vlm_for_backend`＋dispatcher 換裝＋
   lifespan 掃把＋conftest 第四道安全網補齊 → 綠。
   驗收：**493 → 507 passed＋0 skipped**；死埠實證兩條（CELERY_BROKER_URL／OLLAMA_BASE_URL
   指死埠顆數不變）；端點仍 22、零 DELETE；`\b_memory_job_store` 零輸出。
2. [x] 我親自 review Phase 65 全部 diff（對照 §4 各步驟與接縫 ground truth）
3. [x] Phase 66（我）：compose.yaml 加 redis／worker、compose.dev.yaml 加 worker
   bind-mount、`docker compose build`、拉起四容器、LAUNCH.md／CLAUDE.md 指令區更新
4. [x] 真容器煙霧（G2 素材）：切雲端 → 上傳一張圖 → worker log `backend=cloud` →
   照片進待決定；`ls data/staging/` 收乾淨；兩把掃把的啟動 log
5. [x] 寫 REP（65 與 66 各記一節或合一份）

## 鐵律（沿用總覽 §7）

- **同一時間只有一份 pytest 在跑**：subagent 跑測試期間我不碰 pytest
- pytest 四道安全網不繞過；死埠實證是本階段最重要驗收
- 不 commit（產品負責人指示）；`docker compose down -v` 永遠禁止；docs/spec/ 唯讀
- worker 並行數固定 2（D6）；不用 Celery autoretry（§4.4）；任務 payload 只帶 job_id
