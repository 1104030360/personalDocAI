# 2026-08-19 階段V：Phase 12 詢問驗收測試 TODO

## 目標

照更新後的 `docs/plan/unfinish/phase-12-詢問驗收測試.md`，用 pytest-bdd 把 `docs/spec/features/自然語言詢問.feature`（唯讀）直接當測試跑：5 條 Rule（Q1〜Q5）共 7 個 Example 全綠——第二個驗收里程碑；全量測試 60 → **67 passed**。

## 實作邏輯

- **執行方式**：同階段U——主控派 opus 實作 subagent 照計畫轉錄 `tests/integration/test_ask_feature.py`（BDD 膠水檔，鏡射 `test_upload_feature.py` 的既有寫法：`scenarios("../../...")`、`datatable`、`context` 抽屜、顯式依賴 `wire_fake_ai`），獨立 opus reviewer 雙審，全程不 commit。
- **BDD 重點**：Given 照表格逐列 `insert_photo`（向量走 `build_document`＋`embed_query`，與上傳路徑一致）；規格 id → 實際 id 對照表；未指定現在時間的例子一律固定 `2026-08-18 10:00`（可重現性）；`get_now` 接 `context["now"]` 讓 Given 步驟即時生效。
- **關鍵約束**：規格檔一個字不能改（驗收 6 `grep -c "Rule:"` = 5 把關）；假件全就位、完全不需要 Ollama（驗收 5 用 `OLLAMA_BASE_URL=http://localhost:9` 死埠法證明）。
- **驗收**：計畫驗收 1〜3、6 由 implementer 做；驗收 4（暫時註解 `search_by_vector` 時間過濾看測試轉紅再還原——變異測試）與驗收 5（死埠全量）由主控在審查時親自做，避免 implementer 手滑留下被註解的產品程式碼。

## 步驟

- [x] V1. 派工 opus implementer：TDD 紅（新測試檔對未存在的 glue／資料就緒前失敗）→ 建 `tests/integration/test_ask_feature.py` 轉綠（單檔 7 passed／全量 67 passed）＋驗收 1〜3、6
- [x] V2. 產生工作區 diff 審查包，派獨立 reviewer（opus）雙審（規格符合度＋程式品質）
- [x] V3. 修正迴圈（如有 Critical/Important）；Minor 記 ledger 遞延；主控親跑驗收 4（變異測試）與驗收 5（死埠全量）
- [x] V4. 寫階段V report、更新 ledger

## 完成定義

`pytest tests/integration/test_ask_feature.py -v` = 7 passed（7 個 Example 名稱對應 5 條 Rule）；`pytest tests/integration/test_upload_feature.py tests/integration/test_ask_feature.py -v` = 14 passed；`pytest -q` = 67 passed；`OLLAMA_BASE_URL=http://localhost:9 pytest -q` = 67 passed；`grep -c "Rule:" docs/spec/features/自然語言詢問.feature` = 5（規格未動）；變異測試證明時間過濾靠 SQL 生效；review 無未處置 Critical/Important；未 commit。
