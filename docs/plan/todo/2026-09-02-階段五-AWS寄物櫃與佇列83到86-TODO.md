# 2026-09-02 階段五：Phase 83〜86 aws_mailbox、S3、SQS、真 AWS 接線 TODO

> 對應 dev-prompt：`docs/plan/dev-prompts/phase0902-1.md` 任務 2。上一階段：`2026-09-02-階段四-計畫檔校準83到86-TODO.md`。
> 計畫檔（校準後）：`phase-83-aws_mailbox模組.md`、`phase-84-建S3寄物櫃.md`、`phase-85-建SQS兩條佇列.md`、`phase-86-真AWS雲端路接線.md`。
> ★G1 已過；全程不 commit；AWS 資源建立／purge／.env／restart／煙霧由 controller 親自做（R3），subagent 只寫程式與測試、零 `aws` 指令；不需要手機（R6）。

## 實作邏輯（新手白話）

到 Phase 81 為止，雲端路的流程碼只認 `CloudMailbox` 這份契約、測試全用 `FakeMailbox`，專案還不知道 AWS 長什麼樣。本階段四件事：

```text
83  app/services/aws_mailbox.py    真的打 AWS 的那一層（全系統唯一 import boto3）；十四個方法；手寫 stub 16 顆測試、零出網
84  S3 寄物櫃                        東京 bucket：BPA 四項全開、SSE-S3、Lifecycle documents/ 2 天 → scripts/aws_check.py s3 印 OK
85  SQS 兩條佇列                     jobs（Visibility 900）／results（30）；保留 4 天、長輪詢 20 → aws_check.py sqs 印 OK
86  接線                             get_cloud_route() 補 assume（AwsMailbox ＋ AlwaysRunning ＋ config 逾時秒）；2 顆測試
                                     ★ 真 AWS 逾時煙霧：沒工人 → 30 秒 fallback 本機入庫、S3 清空、log fallback=local reason=result_timeout
                                     敏感檔煙霧：零 S3；做完 purge jobs、.env 改回 off／300、restart worker；LAUNCH.md 加 S3/SQS 小節
```

排程：83 的程式與 84／85 的 AWS 資源**平行**（資源建立不依賴 83 的碼；只有 `aws_check.py` 要等 `AwsMailbox`）。

## 步驟

- [x] Task 1（83）：Opus 實作者 TDD → 16 顆紅→綠 → 改 design5 掃碼那顆（變異證據）→ 全量 **640**、三死埠 640、ruff。
- [x] 84 CLI（controller）：算 bucket 名 → create-bucket（LocationConstraint）→ BPA 四項 → SSE-S3 → `deploy/aws/s3-lifecycle.json`＋套用 → 四個 get 驗證 → `.env` 加 `S3_BUCKET`。
- [x] 85 CLI（controller）：create-queue ×2 → get-queue-url → get-queue-attributes 驗四個數字 → `.env` 加兩條 URL。
- [x] 容器 restart 重讀 `.env`，worker 內 config 三值皆有（`CLOUD_ROUTE` 仍 off）。
- [x] Task 1 review（Opus）：Approved、0 Critical／0 Important／6 Minor（三項併入最後一次 fix wave，R10）。
- [x] Task 2（84 §4.8 ＋ 85 §4.6 合併一次派工；真跑兩個 ✅、佇列 0 則）：`scripts/aws_check.py`（`check_s3`／`check_sqs`／`build_mailbox`／`receive_own_message`）；controller 真跑 `python scripts/aws_check.py s3 sqs` 印 OK；85 §4.8 確認兩條佇列 0 則。
- [x] Task 3（86）：Opus 實作者：2 顆紅→綠、拆 77 鬧鐘的 assume 半邊、`get_cloud_route` assume 分支、LAUNCH.md 小節 → 全量 **642** → review。
- [x] 86 煙霧（controller）：Ollama 已開；開關撥 cloud；`.env` `CLOUD_ROUTE=assume`＋`CLOUD_RESULT_TIMEOUT_SECONDS=30`、restart worker → 上傳非敏感合成收據 PNG（202）→ 30 秒內看 S3 兩物件、jobs 1 則、results 0 → fallback log → 照片入庫、S3 清空 → 敏感檔零 S3 → purge jobs → `.env` 改回 off／300、restart → 開關撥回 local。
- [x] controller 親自 review 全部新碼；最終全量驗證（644／644、掃碼、機密、AWS 最終狀態）；寫階段五 REP。

## 驗收

| 項目 | 預期 |
|---|---|
| 全量／三死埠 | 640（83 後）→ **642**（86 後），0 skipped，兩者相同 |
| 端點 | 22 |
| boto3 | 只在 `app/services/aws_mailbox.py`；`get_cloud_route` 內部 import |
| S3 | BPA 四 true、AES256 無 KMS、Lifecycle 2 天、LocationConstraint ap-northeast-1 |
| SQS | jobs 900／results 30、345600、20；煙霧後兩條皆 0 則 |
| `.env` 收尾 | `CLOUD_ROUTE=off`、`CLOUD_RESULT_TIMEOUT_SECONDS=300`；AI 開關 local |
| 文件 | 只寫變數名，零真值（bucket 名、帳號 ID、URL 不進 docs） |
