# 2026-08-26 八份計畫檔校準（Phase 65〜72）REP

> 對應 dev-prompt：`docs/plan/dev-prompts/phase0826.md` 任務 1。
> 對應 TODO：`docs/plan/todo/2026-08-26-八份計畫檔校準65-72-TODO.md`。
> 比照 2026-08-24 階段 SSS「七份計畫檔校準」前例。

## 實作邏輯

phase-65〜72 八份計畫檔成稿於 2026-08-25 約 13:00，**早於**同日 23:55 才 commit 的
Phase 59〜64 實作（`f1a7e71`）。計畫引用 57〜64 產物（函式簽章、欄位名、測試名、行號、
顆數）寫的是「定稿預期」，不是「實際落地」。本階段把八份計畫逐項對照**真實程式碼**
校準，過期就改、對得上就留證，確保之後照計畫實作不會撞牆。

## 步驟

1. 驗開工基準：`pytest -q` ＝ **493 passed ＋ 0 skipped**（65.7s）、端點 22、
   HEAD `f1a7e71`、容器 app＋db（無 redis／worker）、requirements 無 celery／redis。
2. 派 4 個平行 subagent（Opus），各負責 2 份計畫檔：
   A＝65／66、B＝67／68、C＝69／70、D＝71／72。
   鐵律：只准改名下計畫檔；禁跑 pytest（測試名一律 `pytest --collect-only -q`）；
   `docs/spec/` 只讀；不准動設計決策，只校事實引用。
3. 我親自抽查：phase-65 diff 全文、phase-72 diff 全文逐 hunk 看過，
   其餘依各 agent 逐條「原文→改後→依據」回報核可。
4. 我另補兩處：phase-65／66 檔頭各加「契約備忘未入庫」免責句（比照 phase-57／58 定稿）。

## 測試方式

- 全程**沒有執行 pytest**（避免撞測試庫）；測試名以 `--collect-only` 收集比對。
- 端點清單以 in-process `/openapi.json` 展開 (path, method) 實比（22/22 全中）。
- 行號與原文引用以 Python 逐位元比對（例如 67 的 25 行頂欄 IIFE 在五頁完全相同、
  68 引用的五段「改前」原文全部命中、72 引用的四份 .feature 行號逐字對上）。

## 主要發現與修正（濃縮；完整清單見各檔內嵌的「2026-08-26 校準」註記）

| 檔 | 重點修正 |
|---|---|
| 65 | 基線 405→493；`grep _memory_job_store` 必加 `-E "\b…"`（否則 `wire_memory_job_store` 誤中）；include_router 七行→**八**行；假派工名「記事本入列器」→**`記帳假派工`**；§4.10 引用的 `_ingest_image` 已於 Phase 63 退役、`target=` Phase 59 已帶上（該步是複驗）；507 絕對值補上 |
| 66 | 前置 pytest 預期補 507；LAUNCH.md §6 顆數過期順手改的備註；「零程式碼變更」git 驗收改用開工前後快照相減（未 commit 節奏下原寫法會誤報） |
| 67 | 前置補 65／66 一列；「三件事」→四件事（**attempt=0 的「分析中（第 0 次）」窗**，見下方裁決）；pending.html script 錨點寫死；`計數片段的關鍵行` 四行→**六**行；錯誤文案補「AI 」前綴；總覽 §10 節名更新 |
| 68 | 補「62 已落地、upload.html 現在 202 會顯示紅字＝預期過渡態」校準框；65／66 前置；progress_panel.js 是 67 加的不要重加；行號 +2 是 67 做完後的淨值（67 前是 +26） |
| 69 | 行號 +26／+2 淨值說明；63 已落地與「桌面按快門跳 HTTP 204」過渡症狀；git 快照相減說明 |
| 70 | §4.5 閘門 grep 會撈到 `folder_modal.js:147` 註解（原文寫零輸出**一定會紅**，已改）；`git status` 預期多**兩**行（pending.html 已入版控）；§4.3 只換監聽器會 **const 重複宣告→整頁 SyntaxError**（替換範圍改成 187〜216 整塊）；§4.6 第 11 項「舊照片」在待決定牆上**不存在**（實查正式庫，改為 N/A 可） |
| 71 | `test_photo_files.py:140`→153；§4.8→§4.9；第 7 列補檔名；§0-3 補點名 Phase 64 那顆；掃碼時序註記（celery_app.py 等今天還不存在，提前跑不是缺陷） |
| 72 | 歸檔 22 份→**9 份**（52〜64 已隨兩個 commit 歸檔，68＋9＝77 不變）；`操作失敗` step 兩分支猜測改為**實況記錄**（Phase 62 已照計畫改完，現行是 202/≥400 雙分支、docstring 指名 phase-72）；檔頭插行 6→5；核准日期是佔位符的警告 |

四位校準者也都核對了 `phase-00-增量五總覽.md` §6／§9／§10：**未發現錯誤**（493→507→543
的顆數鏈、端點 22、掃把兩頭接線在 65、必帶 `target=` 等都與實況一致），依指示未改總覽。

## 遇到的問題與裁決（執行者裁決，授權依據＝phase0826.md「所有決策不必徵求同意」）

1. **「分析中（第 0 次）」顯示窗**（67 校準發現：job 建立時 attempt=0，
   `run_ingest_job` 開頭就標 analyzing，第一次 VLM 呼叫前面板會顯示「第 0 次」，
   PDF 拆頁期間肉眼可見）→ 裁決：Phase 67 前端顯示層用 `Math.max(attempt, 1)` 保護，
   零後端改動、零行為變更；實作時在計畫檔與 REP 記錄。
2. **產品碼過期註解**（校準只讀不改，列給收尾）：
   - `camera-desk.html` 檔頭仍寫已刪除的 `_ingest_image` → **Phase 69** 修（該頁本來就在大改）。
   - `entity_modal.js`／`task_modal.js` 檔頭、`folder_modal.js:147` 的呼叫端清單過期
     → **Phase 71** 做「純註解校正」（Phase 70 §3 明文禁改三支彈窗檔，不開例外）。
3. **契約備忘**：規劃階段工作文件、未入庫，65／66 引用了 8 處 → 兩檔檔頭補免責句
   （比照 57／58），驗收以檔內嵌內容為準。
4. phase-72 驗收包體例與 G1 的小差異（`##`＋表格 vs `###`＋條列）＝刻意呈現選擇，不改；
   §4.4／§4.5 的行號會隨檔頭插行漂移 → 執行 72 時以 grep 錨點定位、不靠行號。

## 測試結果

- 校準本身零產品碼、零測試改動：`git status` 中 `app/`、`tests/`、`docs/spec/` 全程乾淨。
- 改動統計：8 份計畫檔，+318／−81 行（含我補的兩處免責句）。
- 開工基準 493 passed＋0 skipped 維持不變（校準階段未再跑全量，無任何會影響測試的改動）。
