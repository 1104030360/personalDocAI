# 2026-08-21 階段NN：Phase 22 資料夾瀏覽端點——REP

## 實作邏輯

依 `unfinish/phase-22-資料夾瀏覽端點.md`（LL 校準版）。把 Phase 16 的三個 repository 函式包成兩個**唯讀、零 AI** 端點：

1. `GET /folders`：直接回陣列（照 id 排序），`FolderWithCount(**row)`——repository 回傳鍵恰為模型五欄位；空資料夾靠 LEFT JOIN 仍出現、張數 0；`response_model` 把關不外洩內部欄位。
2. `GET /folders/{id}`：`{folder, photos}`；photos 摘要恰四鍵、新的在前；**`thumbnail_url` 由端點換算**——`thumbnail_path` 有值 → `/photos/{id}/thumbnail`（Phase 19 讀圖端點的網址），NULL（遷移舊資料）→ `null` 讓前端畫占位（design1.md §10，不假裝有圖、不洩漏硬碟路徑）。找不到 404；`/folders/abc` 由框架回 422。
3. 新檔 `schemas/folder.py`（`FolderWithCount`／`PhotoSummary`／`FolderDetailResponse`）＋`api/routers/folders.py`（**零 SQL**）；`main.py` 僅 import 補一詞＋`include_router` 一行。
4. 明確不做：列出全部照片端點、分頁、改名／刪除（design1.md §7.4、不過度設計守則）。

## 步驟（TDD 紅→綠，Opus subagent 執行、我親自 review）

1. 新增 `tests/integration/test_folders_endpoint.py`（8 顆）→ 實跑**紅**（8 failed，`/folders` 不存在→404）。
2. 依序落地 `schemas/folder.py` → `routers/folders.py` → `main.py` 掛載 → **8 passed**。

## 測試方式與結果

- 全量 `pytest -q` → **140 passed**（132＋8，零既有測試變紅）；`test_folders_endpoint.py` **8 passed**（六筆種子清單／欄位恰五個／張數統計／照片摘要含縮圖網址／舊資料 null／新的在前／空資料夾空清單／404）。
- 驗收 grep：`folders.py` 零 SQL；SQL 全專案仍只在 `photo_repository.py`；`include_router` 三行；`@router` 端點 7 個＋`@app` 2 個。
- **我親自複驗**：全量 140、新檔 8 全綠；`/openapi.json` 端點清單恰 9 條（`GET /folders`、`GET /folders/{folder_id}` 已在）；三個新檔逐行過目與計畫零偏差。

## 遇到的問題與解法

- 無。計畫步驟 6（真伺服器 curl 目視）依裁定併入前端 phase（OO／PP）的瀏覽器驗收一起做。

## 備註

- 依使用者指示**先不 commit**；計畫 commit 步驟（累計 140）延後。
