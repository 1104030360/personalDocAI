# 2026-08-21 階段JJ：Phase 20 上傳未分類流程與規格改版——REP

## 實作邏輯

依 `unfinish/phase-20-上傳未分類流程與規格改版.md`（GG 校準版）。把 Phase 15〜19 的零件一次接起來，上傳語意改為「**VLM 仍然自動分類，但不再默默定案**」（design1.md §2）：

1. **★③ 建議計算**：`suggested_name = clamp_category(VLM 的 category, folders)`；收件箱用 `is_inbox` 欄位找（不用字串比對）；`suggested` 從清單挑同名那筆——兩個 `next()` 保證找得到（六筆種子＋clamp 只回清單內名稱），刻意不加 try/except（找不到＝資料庫沒建好，該炸就炸）。
2. **★④ 一律「未分類」**：`build_document` 與 `insert_photo` 的 `category` 都改用 `inbox["name"]`——上傳當下的向量就是未分類版本（誠實），使用者確認歸類後由 Phase 21 的 PATCH 整條重算；VLM 的類別**不落庫**。
3. **★⑥ 回應擴充**：`FolderOut`（id／name／description 三欄，is_inbox 與張數不外送）；`UploadResponse` 補 `folder`（一定是收件箱）／`suggested_folder`（保證在 folders 裡）／`folders`（完整清單）／`thumbnail_url`——彈窗要的資料一次帶齊，前端不必再打 API。
4. **規格檔正式改版**：`上傳照片.feature` 7→10 條 Rule 一次改到位（檔頭註明 2026-08-20 核准來源；沒被推翻的 Rule 原文一字不動；新增「保留原圖與縮圖」「回應建議資料夾與清單」「清單外→未分類」三條 Rule）；`test_upload_feature.py` 步驟定義同步改版（新增 `column()` 整欄工具、「照片所屬資料夾為」驗 `folder_id` 真的掛對、「回應的建議資料夾」同時驗「建議必在清單內」）。
5. Phase 19 落地的 ⑤ 寫檔段落**原樣保留**（計畫 ⚠️ 明文）。

## 步驟（BDD/TDD 紅→綠，Opus subagent 執行、我親自 review）

1. 步驟 0 前置確認（六樣零件都在、基線 121）。
2. `.feature` 整檔換新版 → Rule／Example 計數各 10。
3. `test_upload_feature.py` 整檔換新版 → 實跑**紅**：`5 failed, 5 passed`，錯誤恰為計畫預告的兩種（`KeyError: 'folder'／'suggested_folder'／'thumbnail_url'`、`assert '收據' == '未分類'`）。
4. `schemas/photo.py` 加 `FolderOut`＋四欄位；`photos.py` 三處★改動＋`_folder_out()` → 規格測試**綠**（10 passed，名稱與 10 個 Example 一一對應）。
5. 既有測試逐檔收拾：`test_upload_bilingual.py` 整檔換版（"Receipt" 被 clamp 成「未分類」，text／location／items 仍英文原文——同一測試同時守「不翻譯」與「清單外→未分類」）；`test_upload_design_rules.py` 向量測試期望 category 改「未分類」＋補 metadata／suggested 兩行斷言；`test_photos_upload.py`、`test_error_paths.py` **零改動**（跑過確認 7 passed／11 passed）。

## 測試方式與結果

- 全量 `pytest -q` → **124 passed**（121＋3，與計畫一致）。
- `test_upload_feature.py -v` → **10 passed**；`test_ask_feature.py` → **7 passed**（Q1〜Q5 全程保綠）。
- `git diff --stat docs/spec/features/自然語言詢問.feature` → 無輸出（紅線守住，一字未動）。
- Rule 正文已無「不含原始照片檔」約束（非註解行 grep → OK）。
- 驗收「手動確認回應長相」腳本（測試庫＋/tmp DATA_DIR，我親自跑）：`metadata.category`＝`folder.name`＝「未分類」、`suggested_folder`＝「收據」（id 2）、`thumbnail_url`＝`/photos/1/thumbnail`、folders 六個名稱依序正確；跑完已清掉 /tmp 殘檔。
- 專案 `data/` 於 pytest 前後皆不存在（isolated_data_dir 隔離有效）。

## 遇到的問題與解法

1. **計畫自我矛盾（subagent 抓到、我裁定）**：驗收要求 `grep "不含原始照片檔"` 零命中，但計畫步驟 1 逐字指定的新規格**檔頭註解**就引用了該詞（描述被推翻的舊定案；查 `git show HEAD` 舊 Rule 原文確實就是這個詞，引用是準確的）。**裁定**：規格檔保持計畫指定原文（歷史引用有價值），改計畫的驗收指令為「排除 `#` 註解行再掃」並註記——實質意圖（Rule 正文無此約束）已達成且可驗證。
2. subagent 另回報：P19 的「④ 一條 INSERT 寫入（先拿到 id）」單行註解被計畫的新 ★④ 段落標頭吸收——屬計畫預期的整段改寫，非遺漏。

## 備註

- 依使用者指示**先不 commit**；計畫的 commit 步驟（含訊息模板，累計顆數＝124）延後。
- 至此 design1.md 的上傳端行為（§2 流程、§7.1 回應、§8 prompt、§12 錯誤表上傳相關列）全數落地；Phase 21（PATCH 歸類）為下一輪。
