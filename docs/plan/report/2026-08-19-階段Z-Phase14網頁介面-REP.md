# 2026-08-19 階段Z：Phase 14 極簡網頁介面 REPORT

## 做了什麼（一句話）

照校準後的 `phase-14-網頁介面.md`：`app/static/upload.html`＋`ask.html` 兩個純 HTML 頁（原生 JS `fetch`）＋ `main.py` 一行 `app.mount("/ui", StaticFiles(...))`——**零框架、零打包工具、零新增端點、零新增自動化測試**，`pytest -q` 維持 79 passed 一個不變；獨立 review 雙軸通過。

## 實作邏輯

- **執行方式**：主控派 opus 實作 subagent 照計畫逐字轉錄（HTML 兩檔與 main.py delta），獨立 opus reviewer 雙審，全程不 commit。
- **同源設計**：頁面與 API 同 `localhost:8000` → `fetch("/photos")`／`fetch("/ask")` 相對路徑、零 CORS 問題、不裝 CORSMiddleware（計畫 Q1 明講不要）。
- **掛載**：`STATIC_DIR = Path(__file__).resolve().parent / "static"` 絕對路徑，任何目錄啟動 uvicorn 都找得到；`app.mount` 不是 API 端點（reviewer 以路由表型別佐證 `/ui` 是 `Mount` 非 `APIRoute`）。
- **title 維持 `personalDocAI`**（階段X 校準；頁面 `<h1>` 的 Visual Memory RAG 是系統名稱、屬頁面內容）。

## 步驟

1. 派工 opus implementer → **DONE**：`mkdir app/static` → main.py 掛載 → 兩 HTML 逐字建檔；驗收 1 用 TestClient 等效法（`GET /ui/*.html` → `200 200`，不起 uvicorn 不碰 Ollama）；驗收 9＝79 passed 不變；驗收 10＝端點仍恰 3；驗收 11＝零 npm／零 CDN／零外部函式庫；HTML 與計畫逐字元比對 `identical=True`。
2. 主控製審查包（main.py＋兩 HTML，標頭註明 P11／本階段責任切分），派獨立 opus reviewer。
3. Review：**Spec ✅＋Approved**；Critical 0／Important 1／Minor 5；修正迴圈 0 輪。reviewer 亮點：獨立重跑逐字比對與 79 passed、JS 取值路徑對 `photo.py`／`ask.py`／`SEARCH_MODE_LABELS` 全欄位交叉核對一致、全用 `textContent` 無 XSS、路徑穿越實測被擋。
4. 主控裁定（記 ledger）：唯一 Important（真 500 回 `text/plain` 時 `response.json()` 會拋錯落入 catch，訊息把「最常見原因」指向 uvicorn 沒跑）——計畫 binding 的逐字內容、且 catch 第二行本就導向「看伺服器終端機的錯誤訊息」（符合「不吞錯、log 留原始錯誤」的設計），裁定遞延記錄為已知限制，不修；5 Minor 遞延（含「驗收 8 的 console 紅字」判讀提醒：4xx/5xx 網路記錄屬預期）。

## 測試方式與結果

- implementer：TestClient `200 200`；`pytest -q` = **79 passed**（零增減）；端點 3；零前端相依。
- 主控（階段AA 瀏覽器實操，Playwright 驅動真 Chrome，走真模型路徑）：**驗收 2〜8 全數通過**——上傳真照片 201（七行齊：id 2／收據／Target／可樂、洋芋片／2026-08-10）；.txt 上傳 415＋正確錯誤訊息；中文問答（回答中文＋metadata search＋ids 1, 2）；英文問答（"You bought Coke."＋vector semantic search）；查無（「查無相關照片」＋（沒有找到相關照片）——用「問 Costco」天然查無，不清正式庫）；兩頁雙向互連；console 全程僅 favicon 404 與刻意測試的 415 兩筆網路記錄，**零 JS 錯誤、零 CORS**。
- 等待期間頁面回饋正確：按鈕鎖定＋「上傳中…／思考中…」提示，完成後按鈕解鎖（finally 生效）。

## 遇到的問題與解法

1. **Playwright 檔案上傳限制在 repo 根目錄內** → 測試檔暫放 `.playwright-mcp/`，驗收完整個資料夾刪除（工作樹零殘留）。
2. **Playwright 文字等待內建逾時僅 5 秒**，等不了本機模型 2〜7 分鐘的推論 → 改用「純時間等待＋重取全頁快照」；並發現 by-ref 局部快照會吃到舊畫面快取，一律以全頁快照為準。
3. **驗收 6 的 TRUNCATE 正式庫**改用「問 Costco」天然查無替代（庫裡只有 Target 照片）——同樣驗到查無路徑，不動使用者的 demo 資料（裁定記 ledger）。

## 測試結果（總）

`pytest -q` = **79 passed**；`/ui` 兩頁瀏覽器實操 7 項驗收全過；未 commit；零前端相依；`docs/spec/**` 零異動。**專案完成。**
