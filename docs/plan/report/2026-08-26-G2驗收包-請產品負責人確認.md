# ★ 閘門 G2 驗收包——請產品負責人確認（design5.md §12「階段乙」五條）

> **給產品負責人：** 階段乙（Phase 56〜66，非同步入庫佇列）已全部實作完成。
> 下面五條是 design5 §12 的原文驗收，每一條我都已先跑過一次並附上證據；
> **勾選與「契約穩了，前端可以開工」那句話由你做**（G2 是人的閘門，實作者依規不自行勾選）。
>
> 📌 執行依據：dev-prompt `docs/plan/dev-prompts/phase0826.md` 明示指名執行 Phase 65〜72
> 並要求獨立完成（比照增量四「★G1 由 phase0824.md 明示通過」與「59〜64 由 phase0825-1.md
> 指名執行」的前例），故階段丙已依同一授權接續實作；本包供你事後逐條複核。

**準備（複核時的環境）：**

```bash
cd /Users/linjunting/personalDocAI && source .venv/bin/activate
docker compose ps --no-trunc     # 四列：db／redis 是 Up (healthy)、app／worker 是 Up
```

## 五條驗收（實作者已預跑，證據如下）

- [ ] **① `pytest -q` 全綠、0 skipped**
  - 2026-08-26 實測：**507 passed＋0 skipped**（493＋Phase 65 的 14 顆）。
  - 加碼兩條死埠實證（顆數完全相同）：
    `CELERY_BROKER_URL=redis://127.0.0.1:9/0 pytest -q` → 507（**真 Redis 在 6379 活著時跑的**，
    測後 `redis-cli smembers ingest:open` 零污染）；`OLLAMA_BASE_URL=http://localhost:9 pytest -q` → 507。

- [ ] **② 單檔上傳 HTTP 202；當下待決定不會多一張；worker 跑完任務後才出現**
  - 實測：上傳 `data/photos/10.jpg` → **202、0.27 秒**、body 恰三鍵
    `{job_id, filename, content_type}`；202 當下 `select count(*) from photo` 仍 52；
    worker 做完後才 53（photo #53 進收件箱、`suggested_category=飲食` 一併落庫）。

- [ ] **③ Fake 三次失敗：待決定不出現、磁碟 staging 不留**
  - 自動化：`tests/integration/test_ingest_job.py` 三次失敗那組全綠（photo 列數 0、
    staging 不在、job=failed）。
  - 真容器加碼：上傳壞 PNG → worker log 恰 3 次 `kind=vlm … ok=false` →
    `GET /ingest-jobs` 那筆 `status=failed, attempt=3, error=「AI 看不懂這張照片（已試 3 次）」`；
    `data/staging/` 沒有它；照片數不變。
    （這筆失敗列刻意留著給你在瀏覽器看進度面板用；按 × 關掉就消失。）

- [ ] **④ `docker compose ps` 看得到 `redis` 與 `worker`；worker 為 2 個 concurrency**
  - 實測 `docker compose ps --no-trunc`：四列全 Up；redis `Up (healthy)`、
    只綁 `127.0.0.1:6379`、command 帶 `--appendonly yes`；
    worker command 逐字 `celery -A app.celery_app.celery_app worker --loglevel=info --concurrency=2`。
  - worker 啟動橫幅：`transport: redis://redis:6379/0`、`concurrency: 2 (prefork)`、
    `[tasks] . personaldocai.ingest`、`celery@… ready.`，
    且兩把掃把都在真容器 log 留證：`staging 掃把（worker 啟動）：清掉 0 個過期暫存檔`／
    `staging 掃把（app 啟動）：清掉 0 個過期暫存檔`。

- [ ] **⑤ 頁首切雲端後上傳，worker log 的 `backend=cloud`（手動）**
  - 實測（PUT /settings/ai-backend 切雲端後上傳）：
    ```
    AI 開始 kind=vlm backend=cloud model=gemma4
    AI 結束 kind=vlm backend=cloud model=gemma4 elapsed_s=1.1 ok=true understood=true …
    AI 開始 kind=embed backend=local model=bge-m3      ← 向量永遠本機，這是對的
    AI 結束 kind=embed backend=local model=bge-m3 elapsed_s=4.4 ok=true
    job 1002744682e442aca76fe49c045ff320 入庫完成：photo_id=53（先進「未分類」…）
    ```
  - D14 成立的意思：worker 是另一個行程、讀不到頁首開關，它吃的是**入列當下的快照**。
  - 測完已把開關切回本機。

## 加碼證據（不在五條裡，但值得你知道）

- **AOF 有效**：`docker compose restart redis` 後，失敗列那筆 job **還在**
  （`appendonly yes` 真的生效，重開 Docker 進度列不會消失）。
- **成功＝job 刪掉**：成功上傳後 `redis-cli smembers ingest:open` 是空的
  （進度面板天生不含成功列，design5 §4.3）。
- **staging 收乾淨**：成功與失敗兩條路都刪；目前 `data/staging/` 只剩兩個
  2026-08-25 23:20 的孤兒檔（59〜64 期間 NoopDispatcher 時代收下但沒人做的），
  未滿 24 小時所以掃把**正確地**沒動它們，到期會自動清掉。
- **Phase 65 唯一計畫外改動**：既有守門測試 `test_SQL只出現在repository與db層`
  把 Redis pipeline 的 `pipe.execute()` 誤判成 SQL（redis-py 的方法名，改不掉）。
  修法＝掃描前剔除該慣用法字串，`cur.execute(` 的防護強度一分不減、
  **不動豁免名單**（Phase 71 明文禁止 ingest_job_store.py 進名單，此修法相容）。

## 簽名

- 五條全部確認無誤，同意進入階段丙：日期：__________
