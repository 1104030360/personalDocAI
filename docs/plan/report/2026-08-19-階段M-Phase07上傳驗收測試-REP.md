# 2026-08-19 階段M：Phase 07 上傳驗收測試（pytest-bdd）REPORT

## 做了什麼（一句話）

用 pytest-bdd 把 `docs/spec/features/上傳照片.feature` 直接當測試跑——**7 條 Rule（U1〜U7）一次全綠**；把暫時性煙霧測試檔安全退役（3 個設計守衛先遷移至 `integration/test_upload_design_rules.py`、替代測試全綠後才刪檔）；補英文雙語整合測試——`pytest -q` **40 passed**，第一個驗收里程碑達成，本 phase 零產品程式碼變更。

## 實作邏輯

- **規格即測試**：`scenarios("../../docs/spec/features/上傳照片.feature")` 直接讀規格原檔（不複製、不改寫）；14 條步驟定義一句對一段程式碼；`KNOWN_UNDERSTANDINGS` 對照表把 When 步驟的文字映射到假 VLM 六欄位結果，規格新增例子只需補表。
- **安全退役**：先建三個新檔 → 全量 47 passed（新舊守衛並存）→ 才刪 smoke → 40 passed。任務書的「退役規則」對照表逐測試標了刪／遷去向，review 逐一稽核。
- **fixture 疊接**：`wire_feature_clock(wire_fake_ai, context)` 顯式依賴 conftest 安全網，`get_now` 覆寫以 feature 檔的 context 為準——Given「現在時間為 …」步驟即時生效。
- 執行方式：opus implementer → opus reviewer（做了四組探針實驗）→ 主控核可；零 fix round。

## 步驟與 commit

| 步驟 | 內容 | commit |
|---|---|---|
| 1〜2 | fakes.py＋conftest 檔尾追加（KNOWN_UNDERSTANDINGS／understanding_for_text／first_row／split_items），既有內容零刪改（numstat 15 0／24 0） | （併入下一列） |
| 3〜4 | feature 檔（7 步驟定義×14 條）＋bilingual＋design_rules → 47 passed | `dc99e57`（test） |
| 5 | 刪 `integration/test_upload_smoke.py` → 40 passed | `ed27088`（test） |

## 測試方式

- 全量：`python -m pytest tests -q` → **40 passed**（unit 12＋integration 28；implementer 與 reviewer 各自獨立重跑一致）。
- 驗收 1〜7 全過：feature 單檔 7 passed（7 個 Example 名對應 7 條 Rule）；無 StepDefinitionNotFound；smoke 已刪；正式庫前後筆數相同；`OLLAMA_BASE_URL=http://localhost:9 pytest -q` → 40 passed（零真模型依賴）；`grep -c "Rule:" 上傳照片.feature` = 7 且 `docs/spec/` git 零變更。
- **Reviewer 的獨立探針實驗**（不動 repo 任何檔案，經 scratchpad 外掛）：
  - 探針 A/B（U5 時鐘）：把 conftest FixedClock 與 DEFAULT_NOW 全改成 2020-01-01 → U5 **仍過**（值只剩 Given 一個來源）；再蓋掉 context 通道 → U5 **紅**（負向對照證明實驗有鑑別力）。結論：Given 的時間真的一路寫進 DB。
  - 探針 C（U7）：把預設理解結果翻成「看得懂」→ U7 仍過，證明 Given「VLM 無法理解」真的在扛。
  - 探針 D（斷言誠實）：注入錯誤期望值 → DB 層與回應層斷言**雙雙紅燈**。
  - 步驟字串稽核：`.feature` 全部 20 行步驟 vs 14 個步驟定義——0 unmatched、0 unused。
  - 守衛等價性：三個遷移測試與被刪版本 difflib 比對——斷言完全相同、零弱化。

## 遇到的問題與解法

1. **U5 的「三值巧合」**（implementer 主動點出）：DEFAULT_NOW、conftest FixedClock、Given 的時間三者同值（2026-08-18 10:00），單看綠燈無法分辨覆寫是否生效。implementer 用 `--setup-show` 驗 fixture 順序，reviewer 再用探針 A/B 雙向實證——確認非缺陷。
2. **驗收 5 鑑別力弱**（implementer 註記）：正式庫前後皆 0 筆，比對成立但保障實際來自 conftest 的測試庫導向＋防呆 assert——如實記錄。
3. **零 fix round**：review 無 Critical/Important；8 項 Minor 全數記帳遞延（見下）。

## 遞延事項（記錄於 ledger，供後續 phase 開工前計畫更新取用）

- **P12 開工前必補**：`first_row` 的 `zip(..., strict=True)`（欄位數不一致會靜默丟欄）；`split_items` 逐段 `strip()`——兩者都是給 P12 重用的 helper。
- P12 擴充 KNOWN_UNDERSTANDINGS 時：改回傳 `model_copy(deep=True)`（現為共用單一實例，目前只讀無害）。
- 順手項：U4 護欄補 `len(stored) == config.EMBEDDING_DIM`（承接自 smoke 的既有寫法）；`understanding_for_text` 的 KeyError 排版改 `pytest.fail`。
- 資訊性（無需處理）：`fake.calls == 1` 的去向由 U2 等價覆蓋；「embedding 不為空」字面斷言由 U4 護欄補強；`from tests.conftest import` 實測單例無重複執行問題。

## 測試結果

- `python -m pytest tests -q` → **40 passed, 1 warning**（warning 為既有 starlette 噪音）。
- Task review verdict：**Spec ✅（15/15 硬性規格）／Quality Approved**，零 fix round。
- 資料庫收尾：visual_memory_test 0 筆；正式庫未受影響；5432 全程未觸碰；`docs/spec/` 零變更。
