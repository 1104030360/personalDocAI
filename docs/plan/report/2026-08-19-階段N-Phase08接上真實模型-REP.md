# 2026-08-19 階段N：Phase 08 接上真實模型 REPORT

## 做了什麼（一句話）

正式路徑第一次通電：`bge-m3` 實測 **1024 維**（＝`vector(1024)` 假設成立，未觸發改表分支）並量得中英同義句相似度 **0.837** vs 無關句 **0.377**（多語能力實證）；`gemma4` 真的看懂一張假收據（六欄位全中）；走真 HTTP 完成一次完整上傳（正式庫 id=1、dims=1024）；`scripts/check_embedding_dim.py` 入庫、CLAUDE.md 指令節補煙霧測試兩行——自動化測試維持 **40 passed** 且完全不依賴 Ollama。

## 實作邏輯

- **實測取代假設**（design.md §14）：維度與多語能力都用腳本量，不用信的。
- **本 phase 不改 app/**：真模型接線在 Phase 5/6 的 `dependencies.py` 早已完成，本次只是第一次走「真」的那條路；程式碼變更恰兩檔（新腳本＋CLAUDE.md 兩行，0 deletions）。
- **證據即交付物**：每一步的原始輸出全記錄在任務報告；reviewer 親自重跑便宜項（維度腳本、pytest、psql、死埠法）做逐位比對。
- 執行方式：opus implementer → opus reviewer → 主控核可；零 fix round。

## 步驟與證據摘要

| 步驟 | 結果 |
|---|---|
| 1 維度實測 | `實測維度：1024`＝`config.EMBEDDING_DIM`，「✅ 維度一致」；同義句 cosine **0.837**、無關句 **0.377**（reviewer 重跑連前 5 個向量數字都逐位相同——bge-m3 決定論） |
| 2 測試照片 | `screencapture` 無螢幕錄製權限（`could not create image from display`）→ **qlmanage 備援**：HTML 假收據（Target／2026-08-10／可樂 $25／洋芋片 $40）渲染成 PNG。實測發現任務書備援片段缺 `<meta charset="utf-8">` 會渲染中文亂碼——implementer 在 /tmp 副本補上，主控事後回修任務書（commit `dba2e72`） |
| 3 真看圖 | gemma4 首次呼叫 2m32s → `understood=True`、text 有意義、category=收據、location=Target、items=[可樂, 洋芋片]、content_time=2026-08-10——**假收據被完整讀出** |
| 4 真上傳 | HTTP 主路徑成功（背景 uvicorn＋curl，1m57s）→ `201` 三鍵（id/text/metadata 恰四欄）；uvicorn 收尾四重證明（kill＋ps＋lsof 空＋curl exit=7） |
| 5 落地查證 | `visual_memory`（正式庫）id=1：收據／Target／{可樂,洋芋片}／2026-08-10／dims=**1024**／norm≈1.0——**保留**（真實使用的產出） |
| 6 文件界線 | CLAUDE.md 指令碼區塊補兩行（手動煙霧指令＋「不進驗收與 CI」註記） |

## 測試方式

- 驗收 1〜7 全過；其中 1／2／5／6／7 由 reviewer **親自重跑**：維度腳本輸出逐位吻合、psql 該筆逐欄吻合、`pytest -q` 與裸 `pytest` 皆 40 passed、`OLLAMA_BASE_URL=http://localhost:9` 死埠法 40 passed（未動常駐 Ollama）。
- 驗收 3／4（分鐘級真模型呼叫）依審查指示不重跑，以正式庫該筆資料＋uvicorn log（有且僅有一筆 201）交叉佐證；兩次呼叫的 text 用字不同，符合真模型非決定論、非複製貼上。

## 遇到的問題與解法

1. **screencapture 被權限擋下** → qlmanage 渲染 HTML 假收據（更貼近規格例子、內容可控）；過程與原始錯誤全留證。
2. **任務書備援 HTML 缺 charset**（implementer 實測會亂碼、可能讓測試變成「模型看不看得懂亂碼」）→ implementer 未越權改 docs、只改自己 /tmp 副本並回報；主控裁定後回修任務書（`dba2e72`）。**註記（回應 review 發現 1）**：該 commit 為主控依裁定所為、已記錄於 ledger，非 implementer 違反禁改區。
3. **真模型延遲實測**：首次看圖 2m32s、完整上傳 1m57s——「上傳為同步處理」既定決策的自然後果，非缺陷；**遞延給 Phase 14**：網頁介面需有等待中提示的體感設計預期。

## 測試結果

- `python -m pytest tests -q` → **40 passed**（implementer 與 reviewer 各自重跑一致；死埠法亦 40）。
- Task review verdict：**Spec ✅（16/16 硬性規格）／Quality Approved**；0 Critical/Important、4 Minor（皆收尾文件同步類：CLAUDE.md 現況段與 TODO 入庫由階段O 處理）。
- 資料庫：正式庫 1 筆真實照片（id=1，保留）；測試庫 0 筆；5432 全程未觸碰。
