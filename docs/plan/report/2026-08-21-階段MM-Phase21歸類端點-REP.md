# 2026-08-21 階段MM：Phase 21 歸類端點——REP

## 實作邏輯

依 `unfinish/phase-21-歸類端點.md`（LL 校準版）。彈窗確認後的後端動作：

1. **`AssignFolderRequest`「恰一」驗證**：`@model_validator(mode="after")` 跨欄位檢查 `folder_id` 與 `name` 有且只有一個（都給／都不給／name 空白 → 422，FastAPI 在進端點前就擋掉）；`name` 順手去頭尾空白，與稍後 `create_folder` 存進去的名稱一字不差。
2. **順序鐵律**：① `fetch_photo` 404（先查照片，錯誤訊息才符合直覺）→ ②③ `get_folder` 404／`find_folder_by_name` 409（只查不寫）→ ④ `build_document`（category 用新名稱、text 與其餘三欄原封不動）＋ `embed_document`（唯一可能失敗的 AI 步驟）→ ⑤ 自建這時才 `create_folder` → ⑥ `update_photo_folder()` **一條 UPDATE** 同寫 `folder_id`＋`category`＋`embedding`（RETURNING 拿回整列）。**embedding 失敗＝500 且資料庫完全沒動、不留空資料夾**——靠排序達成，不靠交易。
3. **為什麼重算 embedding**：上傳當下向量是「未分類」版本；不重算則語意查詢少掉正確類別訊號（design1.md §7.3）。彈窗選項①②在後端是同一條路（`{"folder_id": N}`），少一條路少一種出錯。

## 步驟（TDD 紅→綠，Opus subagent 執行、我親自 review）

1. 新增 `tests/integration/test_assign_folder.py`（8 顆）→ 實跑**紅**（8 failed——`/photos/{id}/folder` 路徑不存在一律 404）。
2. `schemas/photo.py`：`AssignFolderRequest`＋`AssignFolderResponse`；驗證器單獨檢查腳本輸出與計畫逐字相同（三種不合法 body 全擋、去空白、folder_id 單獨合法）。
3. `photo_repository.py`：`update_photo_folder()`（沿用 `PHOTO_COLUMNS`）。
4. `photos.py`：PATCH 端點（重用 `_folder_out`）→ **8 passed**。

## 測試方式與結果

- 全量 `pytest -q` → **132 passed**（124＋8，與計畫一致）；`test_assign_folder.py` **8 passed**；ask 規格 **7 passed**（歸類後 category 仍供 ILIKE，5 條 Rule 不受影響）。
- 驗收 grep：`router.patch` 一行（175 行）；SQL 仍只在 `photo_repository.py`。
- 「embedding 失敗資料庫完全沒動」手動確認腳本（測試庫＋/tmp）：狀態碼 500、自建路徑 500、category／folder_id／embedding 沒變＋沒留空資料夾四項全 True；/tmp 殘檔已清。
- 測試把「真的有重算」守死：歸類後向量 `<1e-6` 等於「用新類別重算」的期望值、且 `>1e-3` 異於上傳當下（未分類版本）——假裝重算會被抓。
- **我親自複驗**：全量／新檔／ask 規格重跑全綠；diff 逐段對照計畫零偏差。

## 遇到的問題與解法

- subagent 回報一項極輕微疑慮：`photos.py` 模組 docstring 未提新的 PATCH 端點（計畫沒要求、鐵則禁止順便改）。**我裁定**：docstring 是檔案的門面，補列 PATCH 保持誠實——由我在 review 時親自加上（一行，零行為）。其餘零偏差。

## 備註

- 依使用者指示**先不 commit**；計畫 commit 步驟（訊息模板、累計 132）延後。
