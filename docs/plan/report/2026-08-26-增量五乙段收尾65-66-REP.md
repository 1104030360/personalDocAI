# 2026-08-26 增量五乙段收尾（Phase 65〜66）REP

> 對應 TODO：`docs/plan/todo/2026-08-26-增量五乙段收尾65-66-TODO.md`。
> 計畫檔：`phase-65-Celery與Redis實作.md`、`phase-66-Compose加redis與worker.md`（皆為 2026-08-26 校準版）。
> G2 素材另見 `2026-08-26-G2驗收包-請產品負責人確認.md`。

## 實作邏輯

Phase 57〜64 的非同步骨架缺「真正的佇列」：NoopDispatcher 佔位、job 永遠 queued。
65 補程式（Celery 實例＋薄任務、RedisJobStore、派工換裝、掃把接線兩頭、D14 快照消費），
66 補容器（redis／worker 進 compose）與操作文件，最後真容器雲端煙霧閉環。

分工：65 由 Opus subagent 照計畫 TDD 執行、我逐檔親自 review diff；
66（動 Docker、動正式庫的煙霧）由我親自執行。

## 步驟與結果

### Phase 65（subagent 實作＋我親自 review）

1. TDD 紅→綠留證：`test_celery_app_unit.py` 首跑 `ModuleNotFoundError`（紅）、
   RedisJobStore 9 顆首跑 `AttributeError`（紅）→ 實作後全綠。
2. 落地物：`requirements.txt` 加 `celery>=5.4`／`redis>=5.0`（實裝 5.6.3／8.1.0）；
   `config.CELERY_BROKER_URL`；`app/celery_app.py`（`Celery("personaldocai", broker=…, backend=None)`、
   `broker_connection_retry_on_startup=True`、`ingest_task` 只做「撈 job→組零件→呼叫 run_ingest_job」、
   `CeleryDispatcher.dispatch=delay`、`worker_ready` 掃把）；`RedisJobStore`
   （json＋`ingest:` 前綴＋`ingest:open` 集合、pipeline 綁雙寫、mget 批讀、孤兒順手清、
   decode_responses 前提寫明）；`dependencies.build_vlm_for_backend()`（get_vlm 改呼叫它）、
   `get_job_store()` 換 Redis 版、刪 `_memory_job_store` 單例、`get_task_dispatcher()`
   函式內 import 換 `CeleryDispatcher`（避循環匯入）；`main.py` lifespan 掃把（包 try）；
   conftest 第四道安全網加長（`記帳假派工` 雙管接線、移除舊單例清空）。
3. 我親自 review 全部 9 檔 diff：與計畫逐字相符；薄任務層零 SQL 零 autoretry；
   redis import 全在函式內（舊映像的 dev 容器不會 import 炸）；
   RedisJobStore.list_open 以 job_id 排序（計畫內嵌的裁決：每次輪詢順序穩定即可）。

### 遇到的問題與解法

- **唯一的紅（506＋1）**：既有守門 `test_SQL只出現在repository與db層` 的關鍵字 `.execute(`
  誤中 Redis pipeline 的 `pipe.execute()`。subagent 依鐵律停手、列四個選項給我裁決。
  我裁決採「強度不減」方案：新增 `非SQL的execute慣用法 = ("pipe.execute()",)`，
  掃描前剔除該字串——31 處真 SQL（全是 `cur.execute(`）照抓、
  **不動豁免名單**（Phase 71 的掃碼測試明文禁止 ingest_job_store.py 進名單，此法相容）、
  測試檔內留註解說明。改完守門檔 17 顆全綠。
- 計畫 §4.10 的 grep（`log_ai("vlm"` 單行寫法）在現碼抓不到（呼叫是換行的）；
  實質內容（`target=vlm_timing_target(vlm)`）Phase 59 早已帶上，複驗通過、零補碼。
- `grep AI_BACKEND app/celery_app.py` 命中 1 筆——在 docstring 散文裡（計畫內嵌的
  docstring 自己解釋「為何不讀它」）；AST 驗證程式碼零讀取，D14 實質通過。

### Phase 65 驗收（實跑）

| 條 | 結果 |
|---|---|
| `pytest -q` | **507 passed＋0 skipped**（493＋14） |
| `CELERY_BROKER_URL` 死埠 | 507（零 Redis 依賴實證） |
| `OLLAMA_BASE_URL` 死埠 | 507（零 Ollama 依賴實證） |
| 端點數／零 DELETE | 仍 22／零 DELETE（2 passed） |
| `\b_memory_job_store` | 零輸出（過渡單例刪乾淨） |
| 逃逸字元掃描 | 五檔零 `\uXXXX` 殘留 |

### Phase 66（我親自執行）

1. `compose.yaml`：`redis`（7-alpine、`--appendonly yes`、`127.0.0.1:6379`、
   volume `redisdata`、`redis-cli ping` healthcheck）＋`worker`（`build .`＋
   `image: personaldocai-app` 同映像、契約逐字 command、`--concurrency=2`、
   data／.env 兩個 mount、**不掛 certs**、無 ports）＋`app` 三處
   （image 明名、`CELERY_BROKER_URL`、depends_on redis healthy）＋volumes 加 `redisdata`。
2. `compose.dev.yaml` 加 worker（bind-mount ./app、`restart: "no"`、**不覆寫 command**）。
3. 兩份 `docker compose config` 逐項驗過 → `up -d --build`（映像重建含新套件）
   → 四服務全 Up、redis healthy、worker 橫幅四樣俱全、兩頭掃把 log 都出現。
4. 真容器雲端煙霧 ①〜⑩ 全過（細節與 log 摘錄見 G2 驗收包）：
   202＝0.27 秒／`kind=vlm backend=cloud` 1.1 秒／`kind=embed backend=local`／
   photo 52→53 進收件箱帶建議欄／成功＝job 刪掉／壞檔恰 3 次→failed 列／
   AOF 重啟不掉／staging 收乾淨／開關切回本機。
5. `LAUNCH.md` 五處＋顆數、`CLAUDE.md` 四處（指令區三段＋現況段補乙段成果、
   405→507、三道安全網改四道）。
6. 66 期間 `app/`／`tests/` 零新增改動（git status 快照比對＝仍是 65 的那 10 個檔）。

### Phase 66 驗收（實跑）

全數通過：四服務 grep／redis 未發佈 0.0.0.0／worker command 逐字（無 --reload）／
worker 容器內 `ls /app/certs` = No such file／`CELERY_BROKER_URL` 恰 2 處／
dev overlay worker 有 ./app 無 command／**真 Redis 活著時** `pytest -q` 507＋
死埠 507＋測後 `ingest:open` 零污染／LAUNCH、CLAUDE greps 全中。

## 測試結果總結

- **493 → 507 passed＋0 skipped**（+14＝5 celery 煙霧＋9 RedisJobStore 序列化），與總覽 §9 一致。
- 端點仍 22、openapi 零 DELETE。
- 正式庫 52→53 張（真煙霧照片 #53，收件箱、`suggested_category=飲食`）；
  Redis 留一筆刻意的壞檔 failed 列（給後續進度面板瀏覽器驗收用，按 × 即消失）。
- ★G2 五條已全部由我預跑並留證；**勾選由產品負責人做**（驗收包首段已註明
  依 phase0826.md 授權接續丙段的依據）。
