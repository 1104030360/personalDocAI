# 2026-08-20 階段FF：總驗收與收尾（P15〜P17）REPORT

## 做了什麼（一句話）

對 Phase 15〜17 整批成果做最終把關：全量 `pytest -q`＝**103 passed**、兩份規格檔 BDD 驗收 `14 passed`（12 條 Rule 全程保綠）、三份計畫驗收清單逐項重核（僅 git commit 項依使用者指示跳過）、控制者親自完成整批 diff 的最終 review（零未處理 Critical/Important）；收尾：phase-15/16/17 計畫歸檔至 `docs/plan/finish/`、`phase-00-增量總覽.md` 進度表打勾（P17 累計填 **103**）、CLAUDE.md 六處更新（功能範圍含 design1.md 增量、現況、DB 指令防呆、規格進度、定案推翻註記、`data/` 版控禁令）。**全程未做任何 git commit**。

## 實作邏輯

- **驗收即重跑**：所有數字與 grep 由控制者親自重新執行，不轉抄 implementer 報告——全量 103、BDD 14、SQL 分層 grep（恰 `app/repositories/photo_repository.py` 一行）、`from app.core.config import` 禁令（排除註解後 OK）、端點數 4 不變、正式庫終態（2 列 folder_id=2、路徑 NULL、資料夾 6 筆、收件箱唯一）、專案內無 `data/` 殘留、備份檔在。
- **最終 review＝逐檔對照計畫**：9 個程式／設定檔（4 修改於 P15、1 修改於 P16、5 修改＋2 新增於 P17，其中 repository 與 conftest 跨 phase 疊加）全部與計畫**逐字比對**通過；兩個跨 task 交互點專查——(1) `PHOTO_COLUMNS` 加欄不外漏（router／retrieval 逐鍵取值）、(2) Phase 13 的原始碼掃描測試只查「檔案大小上限」，與 Phase 17 的寫檔程式無衝突。
- **文件收尾遵循歷輪慣例**：歸檔、總覽進度表、CLAUDE.md 現況——並因 design1.md 正式改規格，同步修正 CLAUDE.md 的「功能僅兩項」「不可違反定案」兩節（明確標注僅四項被推翻、其餘禁令仍有效），避免未來 session 誤拒 Phase 18〜26 的合法工作。

## 步驟

1. 全量 `pytest -q` → **103 passed**；規格檔 `pytest tests/integration/test_upload_feature.py tests/integration/test_ask_feature.py -q` → **14 passed**（`自然語言詢問.feature` 全程未動、全綠）。
2. 三份計畫驗收清單重核：P15 十四項（13✅＋commit 跳過）、P16 十二項（11✅＋commit 跳過）、P17 十三項（12✅＋commit 跳過；驗收項 8 依修正後指令驗證 OK）。
3. 最終整批 review（見實作邏輯）；`git status` 逐項核對＝恰為三個 phase 的產出＋本輪 TODO/REP/歸檔/CLAUDE.md，無計畫外檔案。
4. 歸檔 `mv` 三份計畫至 `finish/`；總覽 §0.1 狀態改「執行中（P15〜17 完成）」、§5 前四列打勾＋P17 累計填 103；CLAUDE.md 六處更新。
5. TODO 勾稽：BB／CC／DD／EE／FF 五份 TODO 步驟全數 `[x]`。

## 測試方式

- `pytest -q`（最終）＝ **103 passed, 1 warning**（warning 為既有 Starlette 棄用警告）。
- BDD：兩份 `.feature` 掛 pytest-bdd 共 14 例全綠；`自然語言詢問.feature` 5 條 Rule 全程保綠（增量總覽 §6.1 的持續性要求）。
- 不變量 grep 三條全過；正式庫四查全過；`ls data` 不存在。

## 遇到的問題與解法

1. **CLAUDE.md 與 design1.md 的規格衝突**：CLAUDE.md 原文「不得新增任何其他能力／不儲存原始照片檔」與 design1.md 的正式改規格牴觸，若不更新，未來 session 會拒做 Phase 18〜26。解法：更新為「僅四項被推翻、其餘禁令仍有效」的精確敘述，並在定案清單加 ⚠️ 前言與行內標注。
2. **CLAUDE.md 指令節的重建炸彈**：原指令示範 `psql -d PersonalDocAI -f db/schema.sql`（正式庫重建）——增量後正式庫有真實資料且 schema.sql 開頭就是 DROP TABLE，照抄會滅資料。解法：指令節改為「測試庫才用 schema.sql；正式庫一律走 migrate_folders.sql」並加 ⚠️。
3. **Phase 17 計畫自我衝突**（詳見階段EE REP）：驗收 grep 會命中計畫自己指定的警告註解——已修正計畫檢查指令（排除註解行）後歸檔。

## 測試結果（總驗收數字）

| 項目 | 結果 |
|---|---|
| 全量 pytest | **103 passed**（79 → 83 → 93 → 103；三個 phase 各 +4／+10／+10） |
| 規格 BDD（12 條 Rule） | **14 passed**，`自然語言詢問.feature` 未動 |
| 對外行為 | 零改變（端點 4 個不變、回應模型不變、79 個既有測試零紅） |
| 正式庫 | 2 列真照片歸「收據」、三路徑欄 NULL、六筆資料夾、收件箱唯一、遷移可重跑 |
| git | **零 commit／零 add**（依使用者指示），全部成果留在 working tree |
| 備份 | `~/PersonalDocAI-backup-遷移前.sql`（28,253 bytes）保留，去留交使用者 |

## 建議的後續（交使用者決定）

- 檢視 working tree 後自行 commit；建議訊息：
  `feat: Phase 15〜17 資料夾增量地基——folder 表＋六筆種子（partial unique index 單一收件箱）、photo 掛 folder_id 與路徑三欄、insert_photo 自動歸夾（category 值不動）、正式庫 migrate_folders.sql 可重跑遷移（2 列歸「收據」）、資料夾資料層五函式、storage_service 原圖落地＋Pillow 縮圖（長邊 512 等比不放大）＋DATA_DIR 測試隔離，+24 tests（79→103）`
- 遷移驗收已過，`~/PersonalDocAI-backup-遷移前.sql` 可依 phase-15 計畫說明自行刪除（或多留幾天保險）。
- 下一步為 Phase 18（VLM 資料夾推薦），依總覽路線圖開工前先實查基線 103。
