# 2026-09-02 階段六：Phase 83〜86 總驗收與親自 Review REP

> 對應 TODO：`docs/plan/todo/2026-09-02-階段六-總驗收與親自Review83到86-TODO.md`。ledger：`.superpowers/sdd/phase0902-1/progress.md`（裁決總表 R0〜R11 在檔尾）。
> 全程不 commit（HEAD 仍 `a159131`、零 staged）。快照鏈：BASE `ce1069d…` → T83_BASE `293726f…` → T83_HEAD `cebeb34…` → T85_HEAD `005da45…` → T86_BASE `1f97329…` → T86_HEAD `7c43fea…` → T_FIX_BASE `c13badd…` → T_FIX_HEAD `ce4a23c…`。

## 實作邏輯

兩個程式 task（83、86）各自過 Opus review 後都 Approved、各留幾條 Minor；controller 裁決（R10／R11）把七條可行的併成**一次** fix wave（產品碼只動 `aws_mailbox.py` 一行 log，其餘是守門測試與文件措辭），派一次 scoped re-review；最終整體 review 由 controller 親自做：逐行讀 `aws_mailbox.py`（含 fix）、`get_cloud_route()`、兩個新測試檔、`aws_check.py`、LAUNCH.md 小節 → 自己跑全部證據 → 逐條核對四份計畫檔 §6 與總覽 → 收尾文件與 memory。

## 步驟與結果

| # | 做了什麼 | 結果 |
|---|---|---|
| ① fix wave（resume Task 1 實作者） | 83：壞紙條 warning 只印佇列名＋body 前 200 字（真 RED→GREEN）；+1 顆 `test_put_object與send失敗時例外原樣往外丟`（變異：包 try → 紅）；掃碼改比相對路徑＋守 `dependencies.py` 檔頭。86：+1 顆 `test_assume模式把config的四個值對應到AwsMailbox`（變異：對調兩 URL → 紅）；LAUNCH.md 開場句與 `$AWS_REGION` 註解 | **644 passed、0 skipped**；三死埠 644；ruff 綠；`dependencies.py` 零改動 |
| ② scoped re-review（Opus，明令不跑 pytest） | 七條逐一裁決 | **ADDRESSED ×7、零新破壞**；1 Minor（計畫檔 86 §8.1 舊數字 643）由 controller 改成 644（R12） |
| ③ controller 親讀 | `aws_mailbox.py` 全文（十四支、四種不往外丟、`_receive` 共用）、fix diff、`get_cloud_route()` assume 分支、`test_dependencies_cloud_unit.py` 3 顆、`aws_check.py` 全文、LAUNCH.md 小節 | 與契約一致；早綁定取原版、側錄建構子、四值互異，都對 |
| ④ 最終驗證（controller 自跑） | 全量 **644／0 skipped**；三死埠 644；端點三顆 3 passed（22）；SQL 掃碼＋boto3 掃碼 2 passed；`ruff format --check` 109 檔＋`check` 綠；boto3 import 只在 `app/services/aws_mailbox.py`、`dependencies.py` 檔頭無 aws_mailbox；`aws_mailbox.py` 零 SQL token；六個改動 py 檔 tokenize 零中文識別字；追蹤／未追蹤檔（`.env` 除外）零帳號 ID／bucket 後綴／佇列 URL；`docs/spec/`／compose／Dockerfile／`db/`／`app/static`／`app/api`／`data/` 零改動、staging 空；`.env` `CLOUD_ROUTE=off`／`300`、開關 local；兩佇列 0／0、S3 documents/ 空；改動恰 10 個程式檔（+1377／−22） | 全綠 |
| ⑤ 文件 | 四份計畫檔全部勾完（83／86 §8 含 fix wave）；總覽 §2.2／§2.7／§9 row 83〜86 → 實 641／644；階段四／五／六 TODO＋REP；memory `increment6-plan-status` 更新 | 完成 |

## re-review 結果

Opus scoped re-review（明令不跑 pytest，純靜態比對）：**七條全部 ADDRESSED、零新 Critical／Important 破壞**。逐條要點：① warning 只印 `queue=jobs` 且完整 URL 不在 caplog；② `is` 比對例外物件＋`put_calls`／`send_calls` 計數證明真的打到 stub，包 try 會 `DID NOT RAISE`；③ 相對路徑；④ 負面／正面兩條 regex 對 `dependencies.py` 實檔一綠一綠；⑤ keyword 傳參＋四值互異，對調即紅，`CloudMailbox` 非 runtime_checkable 所以側錄假件不會被型別檢查炸掉；⑥ LAUNCH.md 措辭與既有四個 reason 說明一致、零實際值；⑦ 兩份計畫檔 §2.3／§6／§8 一致。唯一 Minor：計畫檔 86 §8.1 殘留 controller 早先寫的預測值「643」→ 已由 controller 改成 644（R12）；另兩處 §6 三死埠舊數字補「fix wave 後實查 644」註記。Info：`(message.get("Body") or "")[:200]` 假設 Body 是 str（真 SQS 保證），不動。

## 改動總表（對 BASE_TREE）

| 類別 | 檔案 |
|---|---|
| 產品碼 | `app/services/aws_mailbox.py`（新，328 行）、`app/dependencies.py`（`assume` 分支 +36／−）、`requirements.txt`（`boto3>=1.35`） |
| 測試 | `tests/unit/test_aws_mailbox_unit.py`（新，17 顆）、`tests/unit/test_dependencies_cloud_unit.py`（新，3 顆）、`tests/unit/test_cloud_ingest_unit.py`（鬧鐘拆 assume 半邊）、`tests/integration/test_design5_error_paths.py`（那一顆改成 boto3 必須在） |
| 腳本／部署 | `scripts/aws_check.py`（新，192 行）、`deploy/aws/s3-lifecycle.json`（新） |
| 文件 | `LAUNCH.md` §9 "S3 / SQS layer"；四份計畫檔；總覽；三份 TODO；三份 REP |
| 本機／AWS 狀態 | `.env` 新增 `S3_BUCKET`／`SQS_JOBS_QUEUE_URL`／`SQS_RESULTS_QUEUE_URL`／`CLOUD_ROUTE=off`／`CLOUD_RESULT_TIMEOUT_SECONDS=300`；映像重建（boto3 進容器）；S3 bucket＋兩條佇列建於東京；正式庫 66 張（煙霧 +2） |
| 沒動 | `docs/spec/`、compose、Dockerfile、`db/`、前端、`gated_ingest.py`、`cloud_ingest.py`、`celery_app.py`、正式庫結構 |

## 遇到的問題與解決

| 問題 | 解決 |
|---|---|
| 兩個 review 的 Minor 分散在兩個 task | R11：併成一次 fix wave（+2 顆），一次 scoped re-review |
| 三個背景工作可能同時跑 pytest | reviewer／re-reviewer 明令不跑全量；一次只有一位實作者；controller 的最終驗證在 fix wave 收工後跑 |
| 更新 memory 的 Python 片段因字串內雙引號語法錯誤 | 改用 scratchpad 的 .py 檔（三引號）重跑 |

## 已知限制（記錄，不在本輪處理）

- `aws_mailbox.INPUT_EXTENSIONS`／`MAX_WAIT_SECONDS` 與 `staging_service`／`cloud_ingest` 各留一份（R9，計畫檔明寫理由；測試釘住三對值）。
- `aws_check.py` 走真 AWS，無自動化測試（R4）。
- `assume` 模式不做探測，日常不可留在 assume（`.env` 已改回 off）；`ec2` 分支等 Phase 89。
- `docs/plan/aws/` 四份新手步驟檔帶 `--profile personaldocai-admin`（本機 `~/.aws` 只有 `[default]`），屬產品負責人 Phase 82 的檔，未動。
- LAUNCH.md 小節含字面資源名 `personaldocai-admin`／`personaldocai-jobs`（非機密；review M5 接受）。

## 測試結果

**644 passed、0 skipped**（624 ＋ 83 的 17 ＋ 86 的 3）；三死埠 644；端點 22；ruff 綠；零 commit；AWS：S3 空、兩佇列 0／0、`CLOUD_ROUTE=off`。

**下一步**：Phase 87〜90（cloud_worker 在 Mac 上跑、arm64 映像），做完才是 ★G2（人的動作）→ 91〜92 才開 EC2。
