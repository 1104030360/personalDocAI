# 2026-08-19 階段S：總驗收與收尾 REPORT

## 做了什麼（一句話）

Phase 09＋10 落地後的最終驗收：主控**親自**盤點工作區、重跑全量測試與結構性驗收、逐行 review 全部六個程式檔、實跑真模型路由煙霧（中英各一）、分流兩輪 review 的全部遞延發現（唯一必修項已修正並覆核）、歸檔完成的計畫並校正 CLAUDE.md——**dev-prompt `phase1819-3.md` 全部要求成立，最終狀態 `pytest -q` = 55 passed，全程未 commit**。

## 實作邏輯

- 使用者指示「subagent 協助、最後主控親自 review」：本階段所有驗證由主控親手執行，不再假手 subagent（唯一例外：docstring 一行修正依 SDD 紀律派單一 fix subagent，再由主控範圍內覆核）。
- 同時解掉兩輪獨立 review 留下的三個 ⚠️：未過濾工作區盤點、驗收輸出證據、`OllamaRouter` 從未實跑。

## 步驟與結果

1. **S1 工作區盤點**（解 ⚠️）：未過濾 `git status` 逐檔對照——程式 6 檔（repo／retrieval／ask_workflow／fakes／兩個新測試檔）＋文件（P09/P10 計畫更新、CLAUDE.md、TODO×4、REP×4、歸檔搬移、使用者既有的總覽搬移與任務書）——**無未預期變更**。
2. **S2 親自重跑驗收**：`pytest -q` = **55 passed, 1 warning**（warning 為既有 Starlette 基線噪音）；P09 驗收 3（COALESCE 兩處 141/173）、驗收 4（ILIKE 三行＋無 `=` 比對）、驗收 5（SQL 僅 repository 一檔）、P10 驗收 3（英文 few-shot＋「不要翻譯」）全部 grep 實證。
3. **S3 親自逐行 review**：六個檔全部讀過——分層正確（service 零 SQL）、參數全走 psycopg 具名綁定（f-string 只內插常數）、`ROUTE_PROMPT` 除 `{question}` 外無雜散大括號（`.format` 安全）、fallback 集中 route 節點一處、`OllamaRouter` 僅在 `__init__` 建模（import 不通電）、`_retrieve` 傳入顯式 mode 保證節點與查法一致、測試資料照 `自然語言詢問.feature` 原表、無任何真 Ollama 測試路徑。**無新增 Critical／Important**。
4. **S4 真模型路由煙霧**（P10 驗收 5，解 ⚠️；Ollama 0.32.14 在線）：
   - 中文「有哪些在 Target 拍的收據？」→ `mode='metadata' category='收據' location='Target' recent=False` ✅
   - 英文 "What drinks did I buy recently?" → `mode='vector' recent=True` ✅
   - 證實 gemma4＋`with_structured_output(RouteDecision)` 實機可用，中英 few-shot 生效。
5. **DB 安全親自驗證**：正式庫 `visual_memory` photo 仍為 **1 列**（Phase 08 煙霧照片）未被波及；測試庫乾淨。
6. **S5 遞延分流**：兩輪 review 共 0 Critical、1 Important（plan-mandated）、10 Minor。唯一**必修**＝`photo_repository.py` 檔頭「Phase 9 會在這個檔案加上兩條檢索查詢」已成假敘述 → 單一 fix subagent 改為「本檔含上傳寫入與兩條檢索查詢（search_by_metadata／search_by_vector，Phase 9 加入）」，主控覆核 diff 僅該行、`pytest -q` 重跑 55 passed。其餘全部 park（見「遺留事項」）。
7. **S6 收尾**：phase-09／phase-10 計畫檔歸檔至 `docs/plan/finish/`（循階段O 慣例）；CLAUDE.md 三處校正（現況改 Phase 01〜10＋檢索/路由段＋55 測試、路線圖改 phase-11〜14、陷阱段總覽位置與 dev-prompt 清單）。

## 測試方式

全部主控親手：`pytest -q`（55 passed）、四組結構性 grep、真模型煙霧兩題、psql 兩庫計數、六檔逐行閱讀、fix 後範圍內 diff 覆核＋重跑。

## 遇到的問題與解法

無新問題。本階段主要工作是把兩輪 review 的 ⚠️ 與遞延項逐一收斂：三個 ⚠️ 全部以實證解除；必修一項修畢；其餘記錄於下。

## 遺留事項（park，附裁定；後續 phase 的計畫更新輪應納入）

1. **route 節點 fallback 無日誌**（唯一 Important，reviewer 標 plan-mandated）：`except Exception` 靜默改走語意查詢，維運端無法區分「router 壞掉」與「正常判 vector」。裁定本輪不改（計畫強制逐字轉錄；design §10 未要求）→ **建議 Phase 11／13 計畫加一行 `logging.warning("路由判斷失敗，改走語意查詢", exc_info=True)`**（repo 先例：vlm_service.py）。
2. metadata 零條件（＝查全部）路徑無測試、且 metadata 查詢無 LIMIT——Phase 11 把結果餵 LLM 時要知道。
3. 英文向量測試在「單張照片＋無相似度門檻」下驗不到真正的跨語言**排序**——Phase 12/13 補一張語意無關照片拉開高下。
4. 格式不符 fallback 測試只斷言 mode，未斷言條件全空——Phase 12 補一行。
5. `search_by_vector` 的 `limit or config.TOP_K` 對 `limit=0` 會回退 5——現無呼叫端觸發；若開放自訂改 `if limit is None`。
6. `run_ask` 每次呼叫重新 compile 圖——Phase 12 放進 `POST /ask` 請求路徑時考慮快取 `build_workflow`。
7. `grandalf`（＋相依 pyparsing）只裝在 venv、未進 requirements.txt（計畫 Q1 定位視覺化工具）——重建 venv 後跑 P10 驗收 4 需重裝。
8. `photo_retriever` docstring「五鍵」敘述：裁定**非缺陷**（描述呼叫契約，兩個呼叫端皆給五鍵）。
9. ILIKE 不逸出 `%`／`_`：計畫 Q5 明訂不解，僅記錄。

## 測試結果

- 最終：`python -m pytest -q` → **55 passed, 1 warning**（基線噪音）。
- P09 驗收 1〜7、P10 驗收 1〜5 **全數通過**（1〜4 由實作輪實證、3/4/5 類結構檢查由主控重證、P10 驗收 5 由主控真模型實跑）。
- 兩輪獨立 review：Spec ✅／Approved ×2；主控親自 review：無新增發現；必修一項已修畢覆核。
- 外部查證來源：<https://docs.langchain.com/oss/python/langchain/knowledge-base#use-retrievers>、<https://docs.langchain.com/oss/python/langgraph/use-graph-api>。
