# 2026-09-02 階段四：Phase 83〜86 計畫檔校準 REP

> 對應 TODO：`docs/plan/todo/2026-09-02-階段四-計畫檔校準83到86-TODO.md`。工作區：`.superpowers/sdd/phase0902-1/`（ledger `progress.md`、`brief-common.md`、`calib-brief.md`、`calib-{A,B,C,D}-report.md`）。全程不 commit。

## 實作邏輯

★G1 判定通過：產品負責人親自完成 Phase 82（開戶、$5 Budget、`personaldocai-admin`／`personaldocai-mac` 兩個 IAM 身分、CLI 打通）、commit（`2b8f0e9`）並歸檔到 82（`a159131`），再以 dev-prompt `phase0902-1.md` 明示執行 83〜86。controller 開工前實查：default profile 就是 admin、`.env` 的 key 屬 `personaldocai-mac`、掛在它身上的 `personaldocai-mac-policy` v1 帶真帳號 ID 且含 `s3:ListBucket`、帳號內零 bucket 零佇列。

四份計畫檔成稿於 2026-08-31，與現況的落差同 Phase 81 那一輪：識別字中文、顆數以 616 起算、`--profile personaldocai-admin`（本機沒有這個 profile）、`get_cloud_route` 現況碼是舊的 `模式`、86 煙霧靠檔名過閘門（改判後閘門看圖）、Ollama 沒開。做法沿用 SDD：controller 寫共同簡報（含命名契約與裁決 R0〜R7）、pre-flight 表，四份各派一個 Opus 校準者**平行**、只准改自己名下的檔；controller 複核並補裁決。

## 步驟

| # | 做了什麼 | 結果 |
|---|---|---|
| 1 | 建工作區、BASE_TREE `ce1069d…`、ledger（R0〜R7＋pre-flight 14 列）、brief-common、rules、calib-brief、階段四 TODO | `.superpowers/sdd/phase0902-1/` |
| 2 | 實查 AWS 前提（read-only）：`.env` key ＝ `user/personaldocai-mac`；policy 內容正確；零 bucket／佇列；Budget 在 | 通過 |
| 3 | 四個 Opus 校準者平行 | A/83 **24** 處（1748→1893 行）、B/84 **34** 處（1213→1387）、C/85 **33** 處（1225→1251）、D/86 **54** 處（1450→1690） |
| 4 | controller 複核與補裁決：R8 `check_sqs() -> None`（B／C 一致）、R9 `INPUT_EXTENSIONS`／`MAX_WAIT_SECONDS` 維持兩份（EC2 工人不拉 `config`／`ingest_job_store`）；改 phase-84 §7 陷阱 12 的 `create-policy-version` 寫法（sed 換佔位再 file:///tmp）；總覽 §2.7 P83〜P86 標題、§2.2／§9 row 83〜86 加「實 640／642」；P86 煙霧字樣改成「內容判定」 | 完成 |
| 5 | 提早開 Ollama（0.33.2；embeddings 一律本機）、準備兩張煙霧圖（合成收據、假身分證，字型驗過） | 就緒 |

## 校準摘要

- **共通**：識別字英文（`mode`／`mailbox`／`check_s3`／`check_sqs`／`build_mailbox`／`CHECK_JOB_ID`／`PROJECT_ROOT`／`make_client_error`／`make_mailbox`／`real_get_cloud_route`／`configure_assume_mode`／`RecordingCloudRoute`…；`test_中文` 名維持）；顆數 624 → 640（83）→ 642（86）；`--profile` 移除；commit → 快照；AWS 操作標「controller 親自執行」；檔頭校準紀錄。
- **83**：`import re` 實查已在（不必加）；陷阱 10 Sid → `ListMailboxBucket`；十四支簽章與總覽 §2.4.1 diff 全等。
- **84**：`.env` 實查沒有 `S3_BUCKET` 那行 → 「新增」；`aws_check.py` 17 個名字英文化＋跨檔契約表；陷阱 13／14（ProfileNotFound、獨立 shell）。
- **85**：§4.6 改成「以 84 為基準只做四件小事」；policy 佔位 bug 改 sed 暫時檔；`.env` 兩條 URL 是新增。
- **86**：`LAUNCH.md` §9 已存在（L296，插在 Redis layer 與 app layer 之間）；煙霧改「開關撥 cloud ＋ 內容非敏感的合成圖」；改正「開關與閘門是兩件事」與「gate 看檔名」兩個實質錯誤。

## 測試方式

校準階段不跑 pytest（只 `--collect-only`：624）。各校準者以 `ruff format --line-length 100 --diff` 驗計畫檔完整 python 區塊零 diff、`ruff check E,F,I` 通過、tokenize 零中文識別字、NBSP 零、JSON 區塊 `json.loads` 通過（84 九個）；D 實跑 Pillow 產圖碼確認可用。

## 遇到的問題與解決

| 問題 | 解決 |
|---|---|
| `check_sqs()` 佔位簽章 brief 舉例 `-> bool`、B 採 `-> None` | R8：一律 `-> None`（失敗 `raise SystemExit`，`main()` 不看回傳值）；C 實查一致 |
| A 問 `INPUT_EXTENSIONS`／`MAX_WAIT_SECONDS` 要不要 import 既有常數 | R9：維持兩份（計畫檔 §3 明寫：工人不拉 config；兩層各夾一次；測試釘三對值） |
| phase-84 陷阱 12 直接 `file://deploy/aws/mac-policy.json` 會 `MalformedPolicyDocument`（檔帶 `<ACCOUNT_ID>`） | controller 改成 sed 換佔位；註明實查 v1 已正確 |
| `docs/plan/aws/` 四份新手步驟檔全帶 `--profile personaldocai-admin`（本機沒有） | 產品負責人的 Phase 82 檔，未動；計畫檔 84／85 加陷阱擋；最終訊息提醒 |
| 總覽 P86／§5.2 仍寫檔名判定 | P86 已改；§5.2 由 controller 接著查 |

## 測試結果

| 項目 | 結果 |
|---|---|
| 計畫檔改動 | A 24／B 34／C 33／D 54 處，各只動自己名下檔；`app/`／`tests/`／`docs/spec/` 零改動 |
| 自驗 | NBSP 0、中文識別字 0、python 區塊 ruff 零 diff |
| 開工基線 | 624（collect）；HEAD `a159131`、零 staged |
| AWS 前提 | admin default profile ✅、mac key ✅、policy ✅、Budget ✅、零資源 ✅ |
