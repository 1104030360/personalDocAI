# 2026-08-19 階段P：計畫檔更新（phase-09／phase-10）REPORT

## 做了什麼（一句話）

對照專案現況（master 40 passed、tests 已分層 unit/＋integration/、`retrieval_service.py`／`ask_workflow.py` 為 Phase02 骨架空檔、langgraph 1.2.11 已裝），把 `phase-09-檢索層.md` 與 `phase-10-路由與流程圖.md` 全面校準並同步 `phase-00-總覽.md`——**dev-prompt `phase1819-3.md` 任務 1 完成，可進入開發**。

## 實作邏輯

- **先盤點再動筆**：實查基線 `pytest -q` = 40 passed；`uv pip list` 確認 langgraph 1.2.11／langchain-core 1.5.6／langchain-ollama 1.1.0 已裝、`grandalf` 未裝（P10 驗收 4 用，屆時照 Q1 安裝）；`config.py` 已有 `RECENT_DAYS`／`TOP_K`；`photo_repository.py` 的既有 import 行與 `insert_photo` 簽名和計畫假設**完全吻合**，計畫的程式碼區塊不必改。
- **API 用法不憑記憶**：以官方文件 MCP（docs.langchain.com）查證計畫程式碼的兩個關鍵寫法——`@chain` 自訂 retriever（Use retrievers 一節的官方示範同款）與 LangGraph `add_conditional_edges` 條件分岔（use-graph-api Conditional branching 一節），皆與現行 API 一致，計畫程式碼可照抄。
- **發現一個 TDD 順序陷阱並寫進計畫**：phase-10 步驟 2 要把 `from app.services.ask_workflow import RouteDecision` 加進 `tests/fakes.py`，但 `conftest.py` 會 import fakes——若在 `ask_workflow.py` 還是空檔時先加，**整個測試套件會從收集階段爆掉**。已在 phase-10 加 ⚠️ 明定「步驟 2 必須等步驟 1 完成後才做」，紅燈階段只建測試檔。

## 步驟（做了哪些更新）

### phase-09-檢索層.md（4 類修正）

1. **前置條件**補基線：40 passed、`retrieval_service.py` 為空佔位檔、config 常數已在。
2. **TDD 執行順序加註**（逐步驟操作開頭）：先步驟 3 建測試檔看紅（該檔 import 收集失敗、其餘 40 個不受影響），再步驟 1〜2 轉綠；步驟編號維持閱讀順序不重排。
3. **測試檔路徑**：`tests/test_retrieval.py` → `tests/integration/test_retrieval.py`（連真測試庫＝整合測試；步驟 3 標題、驗收 1 指令同步改）。
4. **累計數**：驗收 2 `21 passed`（=11+10 舊制）→ **`50 passed`**（=基線 40＋本 phase 10）；「完成後的專案狀態」21 → 50。

### phase-10-路由與流程圖.md（5 類修正）

1. **前置條件**：測試累計 21 → 50；補基線（空佔位檔、langgraph 1.2.11 已裝、grandalf 未裝）。
2. **TDD 加註＋順序陷阱 ⚠️**：先步驟 3（紅）→ 步驟 1（ask_workflow）→ 步驟 2（FakeRouter）；明文警告 fakes.py 的 import 不可先加。
3. **步驟 2 import 位置**：`RouteDecision` import 改放 fakes.py 檔頂（循檔內既有 vlm_service import 慣例）。
4. **測試檔路徑**：→ `tests/integration/test_workflow_route.py`（步驟 3 標題、驗收 1 同步改）。
5. **累計數**：驗收 2 `26 passed` → **`55 passed`**（=50＋5）；「完成後的專案狀態」26 → 55；驗收 4 補「需先 `uv pip install grandalf`」提示。

### phase-00-總覽.md（三處，循階段L 慣例）

- §4 tests/ 樹：`test_retrieval.py`／`test_workflow_route.py` 兩列移入 `integration/` 分支（P11 起維持「歸屬子目錄屆時再定」）。
- 追加第四條 🔄 註記（P09/P10 開工前重算，50／55；P11 起仍舊制）。
- 累計表 P09/P10 兩列：`integration/` 路徑＋ **50**／**55**。

## 測試方式

文件工作，驗證＝(1) 開工前基線實跑 `pytest -q`（40 passed, 1 warning）；(2) `uv pip list` 套件版本實查；(3) 官方文件 MCP 查證兩個 API 寫法；(4) 三檔 grep 交叉核對（50/55、integration/ 路徑全一致，無殘留 `tests/test_retrieval.py`／`tests/test_workflow_route.py` 舊路徑）。

## 遇到的問題與解法

1. **dev-prompt 檔名誤植**：任務書路徑寫 `phase0819-3.md` 但檔案不存在；目錄內實檔為 `phase1819-3.md`（今日 14:19 新增、內容即 P09/P10 任務書）→ 判定同一檔（誤植），裁定記錄於 ledger。
2. **兩計畫的累計數與路徑是 P07 重算前的舊制**：21/26 與 tests/ 根路徑——依總覽 🔄 註記「P09 起開工前再重算」慣例全部改定（50/55、integration/）。
3. **fakes.py import 順序陷阱**（上述）：寫進 phase-10 的 ⚠️ 加註，避免紅燈階段把整個套件弄爆。

## 測試結果

- 基線：`python -m pytest -q` → **40 passed, 1 warning**（計畫更新不影響程式）。
- 三檔一致性 grep 核對：通過（P4）。
- 外部查證來源：<https://docs.langchain.com/oss/python/langchain/knowledge-base#use-retrievers>（@chain 自訂 retriever）、<https://docs.langchain.com/oss/python/langgraph/use-graph-api>（Conditional branching／add_conditional_edges）。
