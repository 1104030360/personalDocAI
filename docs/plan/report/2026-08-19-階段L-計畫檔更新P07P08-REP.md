# 2026-08-19 階段L：計畫檔更新（phase-07／phase-08）REPORT

## 做了什麼（一句話）

對照專案現況（master 36 passed、smoke 檔含兩個 review 守衛、unit indexing 檔已存在、conftest 已有 wire_fake_ai 安全網），把 `phase-07-上傳驗收測試.md` 與 `phase-08-接上真實模型.md` 全面修正並同步 `phase-00-總覽.md`——**dev-prompt `phase0819-2.md` 任務 1 完成，可進入開發**。

## 實作邏輯

- **先實證再寫計畫**：pytest-bdd 相容性是本輪最大技術風險（pytest 9.1.1 很新），不猜——在 scratchpad 建最小專案實跑：pytest-bdd 8.1.0＋gherkin-official 29 之下 **Rule／Example／中文步驟／`parsers.parse` 佔位／`datatable` 參數全部通過（1 passed）**。官方文件：<https://pytest-bdd.readthedocs.io/>。另更正一個誤判：`pip show` 顯示未安裝是因 PATH 上的 pip 非 venv 的——`python -m pip show` 確認 **pytest-bdd 8.1.0 已安裝**。
- **dev-prompt 誤植的處理**：任務書第 27〜28 行「你要直到…」列的是 phase-05/06 路徑（上一輪複製殘留）；依上下文與約束 4、5 判定實際指 **phase-07/08**（裁定記錄於 ledger）。

## 步驟（做了哪些更新）

### phase-07-上傳驗收測試.md（重寫）

1. **守衛遷移表**（原計畫沒有的核心增補）：smoke 檔現有 7 個測試中，3 個是 `.feature` 沒涵蓋、design.md／review 裁定有規定的守衛（415 不呼叫 understand、text 全空白 422 逐字、U4 向量護欄）——若照原計畫直接刪檔會**丟掉階段I/J review 加上的防線**。更新版明定：先遷移到 `tests/integration/test_upload_design_rules.py`、替代測試全綠後才刪 smoke（兩個 commit：47 passed 過渡 → 40 passed 定稿）。
2. **不再新建 `tests/test_indexing.py`**：其 3 個測試已被 P06 的 `unit/test_indexing_service_unit.py` 涵蓋（外加決定論測試），重建即重複。
3. **檔案路徑**：feature／bilingual／design_rules 皆放 `tests/integration/`（dev-prompt 分目錄規則）；`scenarios()` 相對路徑改 `../../docs/spec/features/…`。
4. **fixture 疊接**：feature 檔的動態時鐘 fixture `wire_feature_clock(wire_fake_ai, context)` **顯式依賴** conftest 的安全網——同 scope 兩個 autouse 的執行順序沒有可靠保證，顯式依賴才確保 `get_now` 覆寫以 feature 檔為準（新增常見問題 Q7 說明）；bilingual 檔沿 repo 慣例直接在測試本體覆寫 `get_vlm`，不另設 fixture。
5. **數字**：驗收改為 feature 7、全量 **40**（36−7＋7＋1＋3；unit 12＋integration 28）；「停 Ollama」驗收改 `OLLAMA_BASE_URL=http://localhost:9` 死埠法（不動使用者常駐服務，brew 法降為註記）；psql 補 `-p 5433`。

### phase-08-接上真實模型.md（修訂）

1. **步驟 6 改寫**：原計畫假設 CLAUDE.md 的「## 指令」是佔位節（cat >> 附加重複章節）——實際已是完整版（階段G 更新過）→ 改為在既有指令碼區塊補「煙霧測試」兩行，其餘一字不動。
2. **兩個備援**：步驟 2 截圖權限受限 → qlmanage 渲染 HTML 假收據成 PNG（內容可控、更像規格例子）；步驟 4 環境擋 localhost → in-process TestClient 走**同一條**真依賴路徑。
3. **數字與指令**：前置／步驟 1／驗收 6／完成後狀態 11→**40**；驗收 7 改死埠法；psql 全部補 `-p 5433`；步驟 5 加註「真上傳資料保留在正式庫」（產品的正常產出）。

### phase-00-總覽.md（三處）

- P07／P08 兩列改新明細與 **40／40**；新增第三條 🔄 註記（P09 起仍舊制、開工前再算）。
- §4 tests/ 樹狀圖對齊現況：unit/（＋P05/P06 兩檔）、integration/（＋P07 三檔）、P09 之後檔案標「歸屬子目錄屆時再定」。

## 測試方式

文件工作，驗證＝(1) pytest-bdd 最小專案實證（1 passed，Rule/中文/datatable/parse 全過）；(2) 三檔交叉一致核對（路徑、fixture 名、40 這個數字在 phase-07 兩處、phase-08 四處、phase-00 兩列全一致）；(3) 基線重確認 `pytest -q` 36 passed、gemma4＋bge-m3 在位、PG 5433 up。

## 遇到的問題與解法

1. **`pip show pytest-bdd` 誤報未安裝**：PATH 上的 pip 不是 venv 的。改 `python -m pip show`＋`importlib.metadata` 確認 8.1.0 已裝——差點在計畫裡多寫一個不必要的安裝步驟。
2. **原 P07 計畫會誤刪 review 守衛**：計畫寫於階段I/J 之前，不知道 smoke 檔多了兩個裁定加上的守衛測試。解法：加「退役規則」對照表（7 個測試逐一標刪／遷），遷移先行、刪檔殿後。
3. **原 P08 步驟 6 會在 CLAUDE.md 造出重複章節**：以現況改寫為單行增補。

## 測試結果

- pytest-bdd 實證：`1 passed`（scratchpad 最小專案）。
- 基線：`python -m pytest tests -q` → **36 passed**（計畫更新不影響程式）。
- 三檔一致性核對：通過（L4）。
