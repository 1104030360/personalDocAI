# 2026-08-19 階段H：計畫檔更新（phase-05／phase-06）REPORT

## 做了什麼（一句話）

對照專案現況（19 測試全綠、conftest 已存在、測試已分 unit/integration、本機 Ollama 真的在跑），把 `phase-05-看圖模組.md` 與 `phase-06-向量與寫入.md` 全面改為 TDD 順序並修正全部過時內容，同步 `phase-00-總覽.md` 受影響段落——**dev-prompt `phase0819-1.md` 任務 1 完成，可進入開發**。

## 實作邏輯

- **先盤點再動筆**：實查 git 狀態、`app/` 與 `tests/` 全部相關檔案內容、`pytest -q`（19 passed）、已裝套件版本（langchain-core 1.5.6／langchain-ollama 1.1.0）、`ollama list`（gemma4 9.6GB＋bge-m3 都在本機跑）。
- **計畫檔是後續 subagent 的任務書**：更新時以「每個步驟都能在當前專案狀態下照做不誤」為標準；程式碼區塊除必要處外逐字保留原計畫（vlm_service／dependencies／photos／fakes／indexing_service 五塊未動）。
- **API 先查證再沿用**（依 MCP 使用規則，來源如下）：
  - image content block `{"type": "image", "base64": …, "mime_type": …}`＝langchain 1.x 標準寫法：<https://docs.langchain.com/oss/python/langchain/messages>
  - `with_structured_output(PydanticModel)`＝1.x 現行 API：<https://docs.langchain.com/oss/python/langchain/models>
  - `OllamaEmbeddings(model=…, base_url=…)`：<https://docs.langchain.com/oss/python/integrations/providers/ollama>
  - 結論：計畫碼與現行 API 一致，不需改。

## 步驟（做了哪些更新）

### phase-05-看圖模組.md（重寫）

1. **TDD 重排**：新增「步驟 0 測試先行（紅燈）」（0-1 pytest.ini／0-2 fakes.py／0-3 conftest 擴充／0-4 unit 6 個／0-5 smoke 4 個／0-6 改寫佔位測試／0-7 紅燈留證），原實作內容改為步驟 1〜3，步驟 4 轉綠。
2. **conftest「建立」→「擴充」**：Phase 3 版 conftest 已存在（測試庫導向＋每測清空），只在檔尾追加 `wire_fake_ai`（autouse 安全網）與 `client` fixture。
3. **新增原計畫沒發現的陷阱處置**：本機 Ollama 真的在跑——(a) Phase 4 兩個「佔位 201」測試在接上 VLM 後若不覆寫會**真打 gemma4** → 步驟 0-6 改寫為 FakeVLM 驅動；(b) conftest `wire_fake_ai` 把 `get_vlm` 預設為「看不懂」假件，忘記覆寫的下場是可預期的 422 而不是真推論（新增常見問題 Q9/Q10 說明）。
4. **測試檔分目錄**（dev-prompt 指示）：smoke → `tests/integration/`；新增 `tests/unit/test_vlm_service_unit.py`（6 個：六欄位清單、VLM_PROMPT 雙語規則、parse_content_time 四情境）——把原驗收 3〜5 條的 `python -c`／grep 自動化成測試（指令保留供人工複核）。
5. **數字全面校正**：驗收改為 smoke 4＋unit 6＋全量 **29 passed**（原 19 其中 2 個改寫）；完成後狀態同步。

### phase-06-向量與寫入.md（重寫）

1. **TDD 重排**：步驟 0（0-1 fakes 加 FakeEmbeddings/FixedClock／0-2 conftest `wire_fake_ai` 擴充／0-3 unit 4 個／0-4 smoke +1 與英文斷言改巢狀／0-5 紅燈留證）→ 步驟 1〜3 實作 → 步驟 4 轉綠。
2. **wire_fakes 上移 conftest**：原計畫把假 embedding／固定時鐘 fixture 放煙霧測試檔——但 Phase 5 改寫的兩個 201 測試也會走完寫入流程，放 conftest 一處搞定並保證全套 pytest 永不打真 Ollama（新增常見問題 Q7 說明）。
3. **「合併順序固定」移到 unit**：純函式測試 → `tests/unit/test_indexing_service_unit.py`（4 個：固定順序＋metadata、空欄位省略、英文值中文標籤、embed_document 決定論＋1024 維）。
4. **影響面檢查段**：明列哪些既有測試在換成 `UploadResponse` 後不用再改（看得懂 201、兩個改寫的 201）、哪個要改（英文斷言 → metadata 巢狀）。
5. **數字全面校正**：驗收改為 smoke 5＋unit 4＋全量 **34 passed**；完成後狀態同步。

### phase-00-總覽.md（三處小改）

- 測試累計表 P05／P06 兩列改為新明細與 **29／34**，並加 🔄 註記（P07 起仍為舊制、開工前再更新）。
- 暫時檔路徑改為 `tests/integration/test_upload_smoke.py`。
- conftest 註記改為「P05/P06 擴充假件安全網與 client fixture」。

## 測試方式

本階段是文件工作，驗證＝一致性核對：三份檔案的檔案路徑、fixture 名稱（`wire_fake_ai`／`client`）、測試數（29/34）、步驟編號交叉一致；與不可違反決策逐項對照（不存原始檔、固定四欄位、同步上傳、無大小上限、422 不存——全部未違反）；`pytest -q` 於更新前實跑確認基線 19 passed。

## 遇到的問題與解法

1. **原計畫假設「零測試基礎」**（要建立 conftest／pytest.ini、累計數 4/6）——與現況 19 個測試矛盾。解法：全部改寫為「擴充」語意並重算數字。
2. **原計畫沒發現「真 Ollama 在跑」的測試污染風險**：佔位 201 測試在 P05 後會真打 gemma4、在 P06 後會真打 bge-m3。解法：改寫兩個佔位測試＋conftest autouse `wire_fake_ai` 安全網（get_vlm 預設看不懂；P06 起加 FakeEmbeddings／FixedClock）。
3. **docs-langchain MCP 首查回傳過大且偏 JS**。解法：改用該 MCP 的文件檔案系統查詢（`rg` 指定 `/oss/python/` 路徑）取得 Python 版原文。

## 測試結果

- 更新前基線：`python -m pytest tests -q` → **19 passed**（未動任何程式碼，計畫更新不影響測試）。
- 計畫檔一致性核對：**通過**（H4）。
- 產出 commit：見 git log（docs：phase-05／06 重寫、phase-00 三處、TODO/REP、收錄 dev-prompts）。
