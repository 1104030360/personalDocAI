# 2026-08-19 階段X：計畫檔更新（phase-13／phase-14）REPORT

## 做了什麼（一句話）

對照專案現況（master 67 passed、P11/P12 完成、conftest 安全網涵蓋五注入點、CLAUDE.md 指令節已是最終版、Ollama 為 App 版），把 `phase-13-錯誤收尾與全量回歸.md` 與 `phase-14-網頁介面.md` 全面校準並同步 `phase-00-總覽.md`——**dev-prompt `phase0819-5.md` 任務 1 完成，可進入開發**。

## 實作邏輯

- **先盤點再動筆**：實跑 `pytest -q`＝67 passed；P13 步驟 2 檢查腳本引用的每一項事實逐一實查（`db/schema.sql` 存在且開頭 DROP TABLE、requirements 無雲端關鍵字、`PhotoMetadata` 恰四欄、ILIKE 恰 3 處、端點恰 3 個、空殼三者不存在、`app/` 無 user 字樣）——**檢查腳本可原樣沿用**，開工前已知全數會過。
- **遞延事項轉正式步驟**：P11/P12 輪 review 的 📌 事項改寫為 P13 步驟 0——category 守護測試改用**獨立新測試**（自備兩筆資料、只帶 category 條件、沿用該檔既有 `_insert`／`metadata_search`／`TODAY` 名稱），比原建議（改 `三張規格照片` fixture）安全，不會連鎖影響既有 10 個檢索測試；三項 Minor 清理列同步驟並明定「零行為差異、改完全量必須仍綠」。
- **關鍵路徑陷阱**：`test_error_paths.py` 歸 `tests/integration/` 後，原始碼掃描測試的專案根目錄推算必須從 `parent.parent` 改成 **`parents[2]`**（多一層目錄），否則會讀不到 `app/` 原始碼——這是本次校準抓到的最實質錯誤。
- **fixture 沿慣例**：P12 之後安全網已涵蓋五注入點 → P13 測試檔的 `wire_fakes`（五件全接＋自行 clear）縮成 `wire_error_fakes(wire_fake_ai)`（只把 VLM 換成「看得懂」），個別測試要更壞行為就地覆寫。
- **步驟 3 已過時**：計畫描述的「CLAUDE.md 兩處指令段合併」歷輪早已完成 → 改為「核對＋補兩個小示例」（兩份規格檔單跑、schema.sql 建表）。
- **本機差異**：`brew services start/stop ollama` 全部改為 App 版檢查（`pgrep || open -a Ollama`）或死埠法（`OLLAMA_BASE_URL=http://localhost:9`）；P14 main.py 程式塊 `title` 校準為現況 `personalDocAI`；P14 前置的 PostgreSQL 指令保留 brew（@17 確為 brew 管理）。

## 步驟（做了哪些更新）

### phase-13-錯誤收尾與全量回歸.md（12 處）

1. 前置條件：38 → **67**＋開工前基線清單；📌 改寫為「已轉步驟 0」。
2. **新增步驟 0**：category 守護測試（獨立測試完整程式碼）＋三項 Minor 清理（含 import 收拾明細）＋「67→68」檢查點。
3. 步驟 1：檔名歸 `tests/integration/`；fixture 改 `wire_error_fakes(wire_fake_ai)`；刪未用的 NOW 與多餘 import；原始碼掃描改 `parents[2]`。
4. 步驟 3：整段改為核對式（補「兩份規格檔單跑」與「建表」兩示例，路徑用 integration）。
5. 步驟 5：`brew services start ollama` → `pgrep || open -a Ollama`；照片來源補「現成照片／PIL 測試圖」選項。
6. 驗收 1〜4、7：路徑歸 integration；49 → **79**（=67＋1＋11）；組成表改 13 個實際檔案；雙語 `7 passed, 72 deselected`＋名單校正；驗收 7 改死埠法。
7. Q7 與完成後狀態：49 → 79。

### phase-14-網頁介面.md（6 處）

1. 前置：49 → **79**；環境指令 Ollama 改 App 版檢查。
2. 步驟 2 main.py 程式塊：`title="Visual Memory RAG"` → 維持現況 **`personalDocAI`**。
3. 「零新增自動化測試」段、驗收 9、完成後狀態：49 → 79。

### phase-00-總覽.md（4 處）

- tests/ 樹：`integration/` 改為最後分支（`└──`）並把 `test_error_paths.py` 移入；`test_retrieval.py` 註記「P13 補 category 守護」。
- 追加第六條 🔄 註記（P13/P14＝79／79）。
- 累計表 P13/P14 兩列改定；§驗收 sentence「68 個測試全綠」→ 79。

## 測試方式

文件工作，驗證＝(1) 基線實跑 `pytest -q`（67 passed）；(2) P13 檢查腳本各項事實逐一實查（見實作邏輯）；(3) `test_retrieval.py` 既有名稱（`_insert`／`metadata_search`／`TODAY`／`NOW`）grep 確認步驟 0 程式碼零新 import；(4) 三檔 grep 交叉核對：舊值（49 passed／42 deselected／`tests/test_*` 舊路徑／brew ollama 指令）零殘留，新值（79）全就位。

## 遇到的問題與解法

1. **步驟 3 描述的 CLAUDE.md 狀態早已不存在**（佔位段與 Phase 8 回填段歷輪已整併）→ 整段改為核對式，避免 implementer 找不到目標而亂改。
2. **測試檔搬進 integration/ 的隱藏地雷**：原始碼掃描測試的 `parent.parent` 會指到 `tests/` 而非專案根 → 校準為 `parents[2]`。
3. **改 fixture 補 category 洞的連鎖風險**：reviewer 原建議動 `三張規格照片` fixture，但該 fixture 被 5 個測試共用 → 裁定改獨立測試（Ruling 記 ledger）。
4. **總覽殘留一處「68 個測試全綠」**（§驗收）——grep 交叉核對抓到，一併改 79。

## 測試結果

- 基線：`pytest -q` → **67 passed, 1 warning**（計畫更新不影響程式）。
- 三檔一致性 grep：通過（舊值 0 殘留、79 全就位）。
