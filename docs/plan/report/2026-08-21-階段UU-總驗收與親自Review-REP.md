# 2026-08-21 階段UU：總驗收與親自 Review（P25〜P26＝增量完結）——REP

## 實作邏輯與步驟

第三輪收尾：SS（P25）與 TT（P26）各自逐項驗收後，此處做整輪總確認與裁定彙整。P25 測試檔由 Opus subagent 依計畫逐字落地（我終審全文）；其餘（檢查腳本、正式庫核對、真模型煙霧、P26 全部設計與前端、瀏覽器實操）由我親自執行。

## 最終證據總表

| 項目 | 結果 |
|---|---|
| `pytest -q` | **149 passed**（P25 後＝P26 後，零變動；140＋9） |
| 零外部依賴 | `OLLAMA_BASE_URL=http://localhost:9` 全量同 **149** |
| 規格 | ask 7 例全綠且該檔自 init（64c412f）從未被改；upload 10 例全綠 |
| §12 錯誤表 | 12 列全數有歸屬：9 顆新測試（首跑 0 紅）＋既有測試對照＋前端占位實操 |
| 端點 | 恰 **9**、無刪除端點、寫檔僅 storage_service、SQL 僅 repository（精確 pattern） |
| 正式庫 | 2 張舊照片在「收據」、路徑 NULL；收件箱恰 1；雙寫零不一致 |
| 真模型煙霧 | 上傳三選項／關窗／409／415／占位／維持／中英問答／查無不虛構——全部真 gemma4＋bge-m3 實走 |
| UI | 三頁＋彈窗共用 `style.css` tokens；前後 6＋6 截圖對比八項全「後優於前」；console 僅預期日誌；`pytest` 不變 |

## 本輪裁定彙整（含代價）

1. **P25 步驟 8／9 改寫與延後**（RR）：CLAUDE.md 已逐輪更新故改增量式收尾；歸檔＋commit 因 `git mv` 會 stage 而延後——若錯，代價是文件節奏與計畫原文不同，內容無損。
2. **SQL grep 用精確 pattern**（RR，沿 QQ）：泛用 pattern 誤中 P21 計畫指定的中文註解——若錯，代價是漏抓「UPDATE 開頭的非 photo 表 SQL」，但全專案 SQL 僅 repository 一檔、有整表 grep 佐證。
3. **P25 煙霧 1〜8 採認 OO／PP 證據**（SS）：同伺服器同模型同流程的 32 項實操；「煙霧測試」資料夾名稱後於 P26 實操中真實建立補齊——若錯，代價是某項只被前一天……同日稍早的證據覆蓋，風險趨近零。
4. **P26 底線腳本的註解誤中判定**（TT）：禁止清單註解是計畫自己要求抄入的，排除註解行後零實際使用；計畫已加校準註記——與 P20 規格檔頭同類自我矛盾的同款裁定。
5. **`fmAfterOpen()` 取代而非並存 `fm-primary.focus()`**（TT）：並存會讓焦點還原記錯位置——若錯，代價是初始焦點落在 × 而非主鈕（可 Tab 一步到主鈕），換來關窗焦點正確還原。
6. **`.caption` 殘影修正**（TT）：計畫骨架的 clamp 在新版 Chrome 會於 padding-bottom 露出第三行——下 padding 歸零＋定高修正，一條 CSS、截圖覆核。
7. **幽靈事件記錄**（TT）：一次上傳點擊 handler 未執行但後端建檔（照片 12 留未分類，合法資料保留）；同文件未 reload、零 JS 錯、重測即常、未再現——不歸因、不掩蓋，如實記錄。
8. **快取教訓**：StaticFiles 無 Cache-Control → 瀏覽器 heuristic cache 會吃舊頁（含帶 query 的獨立 cache key）——驗收以 `fetch(cache:'reload')` 處理；正是 P23 計畫要求強制重新整理的原因；不改伺服器（P26 零 Python 紅線）。

## 未完成／延後（依使用者指示）

- **commit 未執行**。建議拆法：①`feat: Phase 25`（新測試檔 9 顆＋149）②`feat: Phase 26`（`style.css`＋四檔改版）③`docs:` 校準＋TODO/REP（RR〜UU 共 8 份）＋CLAUDE.md＋**歸檔剩餘 3 份**（phase-00-增量總覽、25、26 → `finish/`，屆時 `unfinish/` 清空）。P25 計畫的 commit 訊息模板在其步驟 10（NNN=149）、P26 的在其步驟 11（參考連結與借用做法見 TT REP 表格）。
- 增量至此**無後續 phase**——design1.md 全部落地。
