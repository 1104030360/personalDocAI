# 2026-09-02 階段一：Phase 81 計畫檔校準 REP

> 對應 TODO：`docs/plan/todo/2026-09-02-階段一-計畫檔校準81-TODO.md`。計畫檔：`docs/plan/unfinish/phase-81-雲端路PDF.md`。
> 工作區：`.superpowers/sdd/phase0902/`（ledger `progress.md`、`brief-common.md`、`calib-81-brief.md`、`calib-81-report.md`）。全程不 commit。

## 實作邏輯

計畫檔成稿於 2026-08-31，之後程式碼改了三次：Phase 80 落地時多了三處裁決（R14 `wait_result` try、`_resume_cloud_route` 落庫前重讀、`_parse_understanding` 註解），
2026-09-01 閘門改判（VlmGate 看圖不看檔名），2026-09-02 上午全部識別字改英文。照舊計畫檔做會**看起來成功、實際退回 80 的裁決**
（校準者實查：整檔照貼只有 R14 那一項會被既有測試抓到，重讀行與註解的遺失全綠通過）。所以 dev-prompt 任務 1「先校準再實作」不能省。

做法沿用上一場（phase0901）的 SDD 流程：controller 先實查工作樹、寫共同事實簡報與命名契約、做 pre-flight 一致性掃描並裁決（R0〜R7），
再派一個 Opus subagent 依 brief 改計畫檔（只准改那一個檔、每處附「原文 → 改後 → 依據」），最後 controller 親自複核並補裁決（R8、R9）。

## 步驟

| # | 做了什麼 | 結果 |
|---|---|---|
| 1 | 建工作區、抄 `snapshot-tree`／`review-package-tree`、記 BASE_TREE `5509872…`、寫 ledger（R0〜R7）與 pre-flight 表（12 列，4 列衝突） | `.superpowers/sdd/phase0902/` |
| 2 | 寫 `brief-common.md`（git／基線／design6 現況／實檔事實／**命名契約 §5**／裁決）、`implementer-rules.md`、`global-constraints.md`（沿用上一場＋英文識別字、R2、R4 追加）、`calib-81-brief.md`（A〜J 十項） | 4 份 |
| 3 | controller 先改總覽 5 處（§2.2 row 81／★G1、§2.7 P81 標題與動到的檔、§5.1 註解、§9 row 81）：R4 兩檔＋2 顆、累計 622（實查） | 總覽 |
| 4 | 派 Opus 校準 subagent | **59 處**（A11／B1 段／C5／D7／E13／F6／G5／H2／I1／J8）；1490→1477 行；只動那一個檔 |
| 5 | controller 複核：讀回報全文；親讀步驟 1b 兩顆測試與 4-4〜4-6 三支函式；tokenize 掃計畫檔 14 個 python 區塊零中文識別字；NBSP 0；R6 禁詞 0；實查 `fail_job` 只寫 status／error（pre-flight 那條成立） | 通過 |
| 6 | 裁決 R8（區域變數 `already_done`，與本機路同名）、R9（`page`／步驟 1b 位置接受）；改 brief §5 與計畫檔 4-5 四處；brief 三個行數改成實查值；總覽 P81 註記英文化 | 完成 |
| 7 | 記 T81_BASE `3ff4a7b…`，派 Task 1 實作者 | 進入階段二 |

## 校準內容摘要（詳見 `calib-81-report.md`）

- **A 英文識別字**：`_用雲端結果落庫`→`_store_cloud_result`（沿用既有名、改分流器）、原本體→`_store_image_result`、`_PDF用結果落庫`→`_store_pdf_result`、`_落一頁`→`_store_pdf_page`、`_假工人看PDF`→`_fake_worker_read_pdf`、測試工具 `WorkerMailbox`／`create_pdf_job`／`cloud_route`／`run`／`RememberDeletedStore`…；`test_中文` 名維持。
- **B 步驟 4 改成編輯清單**（606 行整檔區塊 → 313 行 4-1〜4-6 ＋「⛔ 不准動」表 ＋ 函式順序）；實檔 `_store_cloud_result` 本體與計畫檔 80 版逐字比對後確認只需改名。
- **C 改判前字眼清零**（規則版／只看檔名／模型備援版／`SENSITIVE_KEYWORDS`→`PRIVACY_PROMPT`）。
- **D 顆數** 609→613、616→**622**。
- **E R4 `max_pages`**：新步驟 1b（兩顆紅測試、`render_pages` 新簽章與迴圈要點、閘門一行）、§1／§3／§6／§7 對應補齊。
- **F 不 commit**：步驟 6 改快照相減；`ingest_job.py` 零改動的驗法改成兩顆 tree 相減（`git diff HEAD` 在未 commit 的工作樹上一定有輸出）；收工檔數 3→7。
- **J 其他**：陷阱 9 引用了從未存在的 `fake_worker_process_pdf`（成稿時就錯）已修；新增陷阱 11（照舊版整檔貼會假成功）與 §6 三句守門 grep；圖二補重讀行與分流。

## 測試方式

校準階段不跑 pytest（會動測試庫），只用 `pytest --collect-only -q`：`test_ingest_job_pdf.py` 9、`test_pdf_service_unit.py`＋`test_privacy_gate_unit.py` 25（3＋22）——計畫檔的「9 passed」「27 passed」有實查根據。計畫檔內 11 個完整 python 區塊全部 `ruff format --line-length 100 --diff` 零 diff、`ruff check E,F` 與 isort 通過（校準者跑），controller 再以 tokenize 掃全部 14 個區塊零中文識別字。

## 遇到的問題與解決

| 問題 | 解決 |
|---|---|
| 計畫檔的 `gated_ingest.py` 整檔區塊缺 80 的三處裁決，而且只有一處會被測試抓到 | R2：改成以實檔為基準的編輯清單＋「不准動」表＋§6 三句 grep 守門＋陷阱 11 |
| 全部識別字是中文，與今天上午的英文化衝突 | R1：命名契約寫進 brief §5，計畫檔逐字換；`test_中文` 名維持 |
| 命名契約把區域變數 `已處理` 訂成 `pages_done`，與 JobStore 欄位同名、與本機路 `already_done` 不同名 | 校準者提出；R8 改採 `already_done` |
| `git diff --stat app/services/ingest_job.py` 在未 commit 的工作樹上必有輸出（74〜80 都在裡面） | 校準者發現；驗法改成 `git diff --stat <開工SHA> <收工SHA> -- …` |
| 上一場 review 兩度把「閘門對 PDF 全頁渲染只取第一頁」留給 81 | R4：本 phase 順手做 `max_pages`（+2 顆） |
| brief 裡三個行數是英文化前的舊值；總覽 P81 註記仍用中文名 | controller 改成實查值／英文名 |

## 測試結果

| 項目 | 結果 |
|---|---|
| 計畫檔改動 | 59 處、只動 `phase-81-雲端路PDF.md`；`app/`／`tests/`／`docs/spec/` 零改動 |
| 自驗 | R6 禁詞 0、NBSP 0、舊中文識別字 0、`NotImplementedError` 0、python 區塊 ruff 零 diff |
| 總覽 | 5 處＋P81 註記英文化 |
| 開工基線 | pytest 613 passed、0 skipped；三死埠 613；端點 22 |
| 快照 | BASE_TREE `5509872…` → T81_BASE `3ff4a7b…` |

**裁決一覽**：R0 不 commit／快照相減；R1 英文識別字；R2 實檔為基準只加 PDF 分支；R3 613→622；R4 `max_pages`；R5 步驟 6 不 commit；R6 改判前字眼清零；R7 本 phase 不需手機／真機／真模型測試；R8 `already_done`；R9 `page`／步驟 1b 位置接受。
