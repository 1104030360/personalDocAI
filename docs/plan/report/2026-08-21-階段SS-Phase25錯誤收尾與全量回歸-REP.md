# 2026-08-21 階段SS：Phase 25 錯誤收尾與全量回歸——REP

## 實作邏輯

依 `unfinish/phase-25-錯誤收尾與全量回歸.md`（RR 校準版）。「確認做出來的東西壞掉時，壞得跟設計說好的一樣」——design1.md §12 每一列都有測試指著：已被 P15〜24 覆蓋的列以計畫的 ASCII 對照地圖註明歸屬、不重寫；九個 ★ 缺口由新檔 `tests/integration/test_folder_error_paths.py` 補上。本 phase 的「先紅」語意特殊：測的是前面 phase **應該已經做對**的行為——紅燈＝回去修對應 phase 的產品碼，不改測試。

## 步驟與結果

1. **新測試檔（Opus subagent 落地、我終審）**：計畫逐字照抄，258 行、8 函式 9 顆。**首跑 0 紅 9 綠**——一次全過，實證：P19/20 失敗清理（415/422 不寫庫不留檔**不建資料夾**）、P18/20 clamp（清單外→未分類；未分類被當建議允許且指同一筆）、P16/21 重名大小寫不敏感 409 不覆蓋、P19 原圖被刪讀原圖 404（補齊 Phase 19 只驗縮圖的那半）、P21 順序鐵律（PATCH embedding 失敗 500：採用路徑三欄零變動＋自建路徑不留空資料夾）、全站無刪除端點（掃碼）。**零產品碼修改**。
2. **全量回歸（步驟 4，我親自）**：`pytest -q` → **149 passed**（140＋9）；`OLLAMA_BASE_URL=http://localhost:9 pytest -q` → **149 passed**（與正常完全相同＝零 Ollama 依賴實證）；每檔顆數表：integration 15 檔 121 顆（ask_endpoint 5、ask_feature 7、assign_folder 8、error_paths 11、folder_error_paths 9、folder_repository 10、folders_endpoint 8、photo_files 11、photo_repository 14、photos_upload 7、retrieval 11、upload_bilingual 1、upload_design_rules 4、upload_feature 10、workflow_route 5）＋ unit 4 檔 28 顆（indexing 4、photo_repository 2、storage 10、vlm 12）＝**合計 149**。
3. **兩份規格（步驟 3）**：ask **7 passed**；`自然語言詢問.feature` 與增量基線 `a4e44ef` 零 diff，且**自 init commit（64c412f）後沒有任何 commit 動過它**（單筆 log）；upload **10 passed**；合計 17 例。
4. **明確不做最終版（步驟 5）逐項全符**：端點恰 **9**（photos 4＋ask 1＋folders 2＋main 2）、無刪除端點、寫檔只在 `storage_service.py`、無 user 欄位、無佇列、metadata 恰四欄、全本地、無 ORM、SQL 只在 repository（精確 pattern）、無全域例外捕捉、`.gitignore` 有 `data/` 且 git 零追蹤、ChatOllama＝vlm 2／ask_workflow 3 其餘 0（無第二模型）、前端零相依。
5. **正式庫核對（步驟 6）四查詢全過**：資料夾 8 個（六預設 id 1〜6＋煙霧自建的專案X／旅遊）、收件箱恰 **1**；孤兒照片 **0**；路徑 NULL 的列**恰 2**（id 1、2，category＝資料夾＝「收據」）；category 與 folder.name 不一致 **0**。
6. **真模型煙霧（步驟 7）**：項目 1〜8（上傳→建議合理→三選項各一→關窗留未分類→瀏覽卡片→縮圖牆→「維持」彈窗→舊照片占位）**採認階段 OO／PP 的 Playwright 真模型實操證據**（同伺服器 :8000、同 gemma4、同流程，計 32 項；選項③已實建「專案X」「旅遊」兩資料夾——裁定不再為名稱「煙霧測試」多建第三個）。**本階段新走 9〜12**：中文條件問→`metadata search`、ids [1,2,4,10]、中文回答正確引用四張收據；語意問→`vector semantic search`、中文回答 Coca-Cola；「南極」→ids []、「查無相關照片」**不編造**；英文問→英文回答。項目 13：四問皆 200、全程零 500（伺服器 terminal 屬使用者的視窗、無法直接讀取，以零 500 為實證）。項目 14：`data/photos/`＝8 檔（id 3〜10）＋`data/thumbs/` 對應，縮圖較小（先前 SHA1／尺寸已驗）。
7. **CLAUDE.md（步驟 8 校準版）**：指令段補「只跑錯誤路徑把關」、陷阱段補 data/ 占位為預期行為一條；現況段最終收尾（全數完成＋149）依 RR 裁定待 P26 完成後一併寫入。
8. **步驟 9／10（歸檔＋commit）**：依校準延後至使用者要求 commit 時（剩 3 份待移）。

## 遇到的問題與解法

- 步驟 5 腳本首跑在 python 檢查步斷鏈（該 Bash 呼叫未帶 venv）→ 補 `source .venv/bin/activate` 重跑後半，全過。
- 其餘零問題：九顆首跑全綠、零產品碼修改、零計畫偏差。

## 測試結果

**149 passed**（全量＝零 Ollama＝逐檔合計）；design1.md §12 至此每一列都有測試或實操證據指著。
