# 2026-08-19 階段T：計畫檔更新（phase-11／phase-12）REPORT

## 做了什麼（一句話）

對照專案現況（master 55 passed、tests 已分層 unit/＋integration/、Phase 10 流程圖停在「兩條 retrieve 接 END＋TODO(Phase 11)」、langchain-core 1.5.6、Ollama 為 App 版），把 `phase-11-回答與詢問端點.md` 與 `phase-12-詢問驗收測試.md` 全面校準並同步 `phase-00-總覽.md`——**dev-prompt `phase0819-4.md` 任務 1 完成，可進入開發**。

## 實作邏輯

- **先盤點再動筆**：實跑 `pytest -q`＝55 passed 當基線；逐一核對兩計畫引用的每個符號都真實存在——`config.SEARCH_MODE_LABELS`、`photo_repository.count_photos`、`indexing_service.build_document`、retriever Document 的 `id`／`items` metadata、conftest 的 `client`／`wire_fake_ai`／`first_row`／`split_items`；pytest-bdd 8.1.0 的 `datatable` 參數已被 `test_upload_feature.py` 實際使用，P12 可照抄同款寫法。
- **API 用法不憑記憶**：計畫關鍵寫法 `ChatOllama(...).invoke([message]).text` 以官方文件（<https://docs.langchain.com/oss/python/langchain/messages#message-content>，1.x 訊息文字取法）＋本機實測（`AIMessage(content="哈囉").text` → `"哈囉"`）雙重確認——1.5.6 的 `.text` 是屬性，計畫程式碼可照抄，並把版本註記寫進 P11 常見問題 Q1。
- **dev-prompt 檔名誤植裁定**：任務書寫 `phase-12-用戶端.md`（不存在）；實檔 `phase-12-詢問驗收測試.md` 與 P11 內文「規格驗收在 Phase 12」互證 → 判定同一檔（「用戶端」是 P14 網頁介面，前置 P13 未做，順序上不可能先做）。裁定記錄於 ledger。
- **發現一個 TDD 順序陷阱並寫進計畫**：P11 步驟 2 把 `AskDeps` 加上**必填**欄位 `answerer` 的當下，`tests/integration/test_workflow_route.py` 既有 5 個測試立刻缺參數而壞 → P11 加 🧪 註記「紅＝先建步驟 9 新檔（僅該檔收集失敗、其餘 55 綠）；綠＝步驟 7→1→**2+8 一起做**→3〜6」。
- **本機差異校準**：Ollama 是 `/Applications/Ollama.app`（App 版）不歸 brew services 管 → P12 驗收 5 的 `brew services stop ollama` 改成 `OLLAMA_BASE_URL=http://localhost:9 pytest -q`——等價驗證（誰偷打真模型誰連線爆錯）且不動使用者的 Ollama。

## 步驟（做了哪些更新）

### phase-11-回答與詢問端點.md（12 處）

1. **前置條件**：測試累計 26 → **55**；補開工前基線（版本、既有注入點與 fixture 盤點）。
2. **TDD 執行順序 🧪 加註**（逐步驟操作開頭）：含「步驟 2 與 8 必須一起做」的 ⚠️ 順序陷阱。
3. **步驟 4 改增量修改**：原「整檔重寫」會滅掉既有 docstring → 改為「import 補齊＋docstring 一句改字＋檔尾加三組注入點」，其餘一字不動。
4. **步驟 6**：計畫塊的 `FastAPI(title="Visual Memory RAG")` → 維持現況 `personalDocAI`（title 非本 phase 目標，不做無關變更）。
5. **測試路徑**：步驟 8 `tests/test_workflow_route.py`、步驟 9／驗收 1 `tests/test_ask_endpoint.py` → 一律 `tests/integration/`。
6. **步驟 9 fixture 校準**：原自建 `wire_fakes`（重複接 embeddings／now、teardown 自行 clear）→ 顯式依賴 conftest `wire_fake_ai`、只補 `get_router`／`get_answerer`（沿 `test_upload_feature.py` 慣例）。
7. **累計數**：驗收 2 `31 passed` → **`60 passed`**（=55＋5）；「完成後的專案狀態」31 → 60。
8. **Q1 補版本註記**：langchain-core 1.5.6 實測 `.text` 屬性可用。

### phase-12-詢問驗收測試.md（11 處）

1. **前置條件**：測試累計 31 → **60**。
2. **測試檔路徑**：`tests/test_ask_feature.py` → `tests/integration/test_ask_feature.py`（ASCII 圖、步驟 1 標題、步驟 2、驗收 1/2/4 全面替換）；`scenarios(...)` 相對路徑 `../` → `../../`；驗收 2 的上傳檔也改 `tests/integration/test_upload_feature.py`。
3. **fixture 校準**：`wire_fakes(context)` → `wire_ask_fakes(wire_fake_ai, context)`（同 P11 的理由；`get_now` 仍接 `context["now"]` 讓 Given 步驟即時生效）。
4. **累計數**：驗收 3 `38 passed` → **`67 passed`**（=60＋7）；驗收 5 同步；「完成後的專案狀態」38 → 67。
5. **驗收 5 指令**：brew 版 → `OLLAMA_BASE_URL=http://localhost:9 pytest -q`（理由見上）。
6. **Q5**：fixture 實名 `clean_photo_table`＋實際 SQL `TRUNCATE TABLE photo RESTART IDENTITY`。
7. **Q6**：英文測試清單改實際路徑（unit 1＋upload_bilingual 1＋retrieval 2＋workflow_route 1＋P11 將新增的 ask_endpoint 1 ＝ 6 個 ✓ 數字不變）。

### phase-00-總覽.md（三處，循階段P 慣例）

- §4 tests/ 樹：`test_ask_endpoint.py`／`test_ask_feature.py` 兩列移入 `integration/` 分支；`test_error_paths.py`（P13）維持「屆時再定」。
- 追加第五條 🔄 註記（P11/P12 開工前重算，60／67；P13 起仍舊制）。
- 累計表 P11/P12 兩列：`integration/` 路徑＋ **60**／**67**。

## 測試方式

文件工作，驗證＝(1) 開工前基線實跑 `pytest -q`（55 passed, 1 warning）；(2) `uv pip show` 版本實查（langchain-core 1.5.6／langchain-ollama 1.1.0／langgraph 1.2.11／pytest-bdd 8.1.0）；(3) 官方文件 MCP＋本機 python 雙重查證 `.text` 寫法；(4) 三檔 grep 交叉核對：舊路徑（`tests/test_ask*`／`tests/test_workflow*`）與舊累計數（31/38）在 P11/P12 零殘留，新數字（60/67）與 `integration/` 路徑全就位。

## 遇到的問題與解法

1. **dev-prompt 檔名誤植**：任務書寫 `phase-12-用戶端.md` 但檔案不存在 → 依 P11 內文互證判定為 `phase-12-詢問驗收測試.md`（Ruling 1，見 ledger）。
2. **兩計畫的累計數與路徑是 P07 重算前的舊制**：26/31/38 與 tests/ 根路徑 → 依總覽 🔄 註記「開工前再重算」慣例全部改定（55/60/67、integration/）。
3. **AskDeps 必填欄位的連鎖破壞**（上述 TDD 順序陷阱）→ 寫進 P11 的 🧪 加註，紅燈階段不會弄爆既有 55 個測試。
4. **P12 驗收 5 的 brew 指令在本機不可行**（Ollama 是 App 版）→ 換成環境變數指向死埠的等價驗證，不碰使用者的 Ollama 程序。

## 測試結果

- 基線：`pytest -q` → **55 passed, 1 warning**（計畫更新不影響程式）。
- 三檔一致性 grep 核對：通過（舊值 0 筆殘留；`integration/test_ask*` 於 P11×3、P12×7、總覽×2 全就位）。
- 外部查證來源：<https://docs.langchain.com/oss/python/langchain/messages#message-content>（LangChain 1.x 訊息的 `.text` 用法）。
