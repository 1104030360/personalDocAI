# 2026-08-21 階段OO：Phase 23 上傳頁彈窗——REP

## 實作邏輯

依 `unfinish/phase-23-上傳頁彈窗.md`（LL 校準版）。只改 `app/static/upload.html` 一檔（整檔照計畫逐字替換，由我親自落地——內容計畫已完整給定，避免 subagent 轉譯漂移）：

- 201 後開 modal 三選項：①`採用「{suggested_folder.name}」`（附說明灰字）②`<select>` 綁完整 `folders`（預設選中建議）③名稱＋說明＋「建立並歸類」；×／Esc 關閉＝不呼叫 API、照片留「未分類」。
- **禁用 `alert`／`confirm`／`prompt`**：所有錯誤寫進彈窗內 `<p id="fm-error">`（409／422／連線失敗），PATCH 失敗彈窗不關、可繼續操作。
- 彈窗程式碼刻意寫成共用形狀（`fm` 前綴隔離全域、只靠 `onAssigned`／`onClosed` callback 對外）——Phase 24 整段搬進 `folder_modal.js` 時一行不改。
- `innerHTML` 僅用於寫死的固定樣板字串（零外來資料）；動態內容一律 `textContent`（security hook 提示已審視，安全）。

## 步驟

1. 整檔替換 `upload.html`（計畫逐字）。
2. 靜態檢查：無原生對話框／無 CDN・框架／無 npm；`git status` static 範圍僅 `upload.html`。
3. `pytest -q` → **140 passed**（零測試增減，符合「本 phase 做完必須還是 N＝140」）。
4. 瀏覽器實操驗收（Playwright MCP；伺服器＝使用者常駐的 `uvicorn --reload :8000`，已熱載入最新程式碼；照片＝自產真收據 JPEG，走真 gemma4）。

## 測試方式與結果（驗收清單 13 項逐項）

| # | 項目 | 結果 |
|---|---|---|
| 1 | 上傳成功開彈窗 | ✓ 結果區八行（`資料夾（category）：未分類`）＋畫面壓暗、彈窗標題正確（照片 4） |
| 2 | 三選項齊全 | ✓ ①`採用「收據」`＋灰字說明；②六選項且預設選中「收據」；③兩輸入框＋按鈕；底部空訊息列 |
| 3 | 選項① | ✓ 彈窗關、`✅ 已歸到「收據」。`；`GET /folders`：收據 2→3、未分類不變 |
| 4 | 選項② 改選飲食 | ✓ `✅ 已歸到「飲食」。`（照片 5→飲食=1） |
| 5 | 選項③ 自建專案X | ✓ 新資料夾出現（is_inbox=false、1 張）；照片 6 歸它；**下一次上傳的②清單已含專案X**（清單是活的） |
| 6 | 409 重名 | ✓ 彈窗不關、紅字`（HTTP 409）資料夾名稱已存在`、無任何原生對話框；隨後同彈窗按①成功（照片 7→收據） |
| 7 | 422 空白名稱 | ✓ 紅字`（HTTP 422）…` |
| 8 | × 關閉 | ✓ `已放進「未分類」，之後可到瀏覽頁再歸類。`（照片 8 留未分類；**裁定**：沿用項目 7 的既開彈窗驗×——行為覆蓋相同，省一次真模型上傳） |
| 9 | Esc 關閉 | ✓ 行為同×（照片 9 留未分類） |
| 10 | grep 無原生對話框 | ✓ `OK：沒有原生對話框` |
| 11 | Console | 僅 3 條：favicon.ico 404（**Phase 14 以來既有**、瀏覽器自動請求、與頁面程式無關）＋409／422 兩條（**刻意錯誤路徑測試**的瀏覽器網路日誌，頁面均已頁內妥善顯示）；無 CORS、無 JS 例外 |
| 12 | 零相依零端點 | ✓ 無 npm／CDN／框架；static 範圍 git status 僅 upload.html |
| 13 | 測試數不變 | ✓ `pytest -q` → **140 passed** |

正式庫終態核對（`SELECT id, category, folder_id FROM photo`）：九列與操作軌跡一一對應——1,2 舊列（收據）、3 煙霧（未分類）、4→收據(①)、5→飲食(②)、6→專案X(③)、7→收據(409 後①)、8 未分類(×)、9 未分類(Esc)。

## 遇到的問題與解法

1. Playwright MCP 檔案上傳限定專案根目錄內 → 測試照片複製到其工作目錄 `.playwright-mcp/`（驗收後整目錄清除，不入版控）。
2. `browser_wait_for` 的 text 模式固定 5 秒逾時，等不動真模型 10〜60 秒 → 改「`time` 睡眠＋text 確認」兩段式等待。
3. favicon 404 為既有噪音（本輪 diff 未涉及）；**不**為此加 favicon（計畫外、YAGNI）。

## 備註

- **先不 commit**；計畫 commit 步驟延後。總覽 §5 的 P23 列本就「累計不變」，已打勾。
