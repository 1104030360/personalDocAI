# 2026-08-19 階段Y：Phase 13 錯誤路徑收尾與全量回歸 REPORT

## 做了什麼（一句話）

照校準後的 `phase-13-錯誤收尾與全量回歸.md`：收整 P11/P12 輪遞延事項（步驟 0：category 守護測試＋三項零行為清理，67→68）、為 design.md 錯誤處理總表七列各補上測試（`tests/integration/test_error_paths.py`，+11 首跑全綠）、跑「明確不做」檢查腳本 11 項全符、CLAUDE.md 指令節補兩示例、全量回歸 **79 passed**——**後端自動化部分到此完成**，獨立 review 雙軸通過。

## 實作邏輯

- **執行方式**：主控派 opus 實作 subagent 照計畫轉錄，獨立 opus reviewer 雙審，全程不 commit。
- **錯誤路徑測試＝對既有行為的驗證**：415／422 detail 字串、fallback、查無、500 不吞錯都是既有實作 → 預期首跑即綠（實際也是），零紅燈＝零產品碼改動的證明；任何紅燈都會被視為「計畫斷言 vs 實際行為」的落差而回報，不准改產品碼遷就。
- **零行為紅線**：`app/` 只刪三處施工註記（reviewer 逐行比對 P11 需求書證實只少 4 行行尾註記）；`exception_handler`／`add_middleware`／大小上限關鍵字全 repo 零命中。
- **500 不吞錯的驗法**：`TestClient(app, raise_server_exceptions=False)`＋壞掉的 embeddings／monkeypatch `config.DATABASE_URL`（session 每連線重讀，Phase 3 寫法）。

## 步驟

1. 派工 opus implementer → **DONE**：步驟 0（category 守護測試獨立新增＋ask_feature 清理＋ask_workflow 註記刪除，68 passed）→ 步驟 1（error_paths 11，首跑全綠）→ 步驟 2 腳本 11 項全符 → 步驟 3（CLAUDE.md 只多兩示例，主控已核對）→ 步驟 4 全量 79。
2. 主控製審查包（5 檔、標頭註明前輪／本輪責任切分），派獨立 opus reviewer。
3. Review：**Spec ✅＋Approved**；Critical 0／Important 3／Minor 6；修正迴圈 0 輪。亮點：reviewer 以 diff 證實三個 code block 逐字一致、以 `--collect-only` 復核 79／13 列逐檔數／雙語 7 選全對、驗證 `parents[2]` 推錯會 FileNotFoundError 而非假綠、category 守護對變異確實會紅。
4. 主控裁定（記 ledger）：I1（掃描範圍限兩檔）＝計畫既定取捨遞延；I2（英文查無屬假件 plumbing，真雙語保證靠真模型煙霧）＝design.md §11 既定取捨，明示於此避免高估「雙語 7 個」的覆蓋度；I3（真模型煙霧）＋M6（CLAUDE.md 現況校正）＝階段AA 主控親辦；M1〜M5 遞延記 ledger。

## 測試方式與結果

- 步驟 0 後：`pytest -q` = **68 passed**；全量：**79 passed, 1 warning**（=67＋1＋11，與計畫組成表 13 列逐檔相符）。
- `pytest tests/integration/test_error_paths.py -v` = **11 passed**；雙規格 14 PASSED；雙語 `-k "英文 or 大小寫"` = **7 passed, 72 deselected**。
- 死埠法 `OLLAMA_BASE_URL=http://localhost:9 pytest -q` = **79 passed**（零 Ollama 依賴）。
- 步驟 2 腳本：端點恰 3、無刪改端點、無寫檔、無 user 欄位、無佇列、metadata 恰四欄、全本地、空殼三不存在、SQL 只在 repository、ILIKE=3、無全域捕捉——11 項全符（實作當下輸出；P14 並行加上的 `app.mount` 不是端點，不影響結論）。
- 真模型煙霧（步驟 5／驗收 6）由主控在階段AA 親跑後，Phase 13 才完全結案。

## 遇到的問題與解法

1. **並行時序**：reviewer 審查期間 Phase 14 的改動同時落盤（main.py mount、static/）——reviewer 以行號證據反證階段Y 報告輸出取自 P14 改動前、未造假；主控在審查包標頭預先聲明了責任切分。
2. **「雙語 7 個」的語意釐清**（I2）：其中查無兩測驗的是「端點原樣回傳 answerer 輸出＋ids 空」的 plumbing；產品端語言跟隨鐵律由真模型煙霧驗——記錄以免綠燈被過度解讀。

## 測試結果（總）

`pytest -q` = **79 passed**；review Critical 0；未 commit；`docs/spec/**` 零異動；pytest 全程未打真 Ollama。
