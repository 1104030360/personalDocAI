# 2026-08-19 階段G：總驗收與收尾 REPORT

## 做了什麼（一句話）

把 phase-03／phase-04 的驗收**全部重跑一次取證**、逐項核對兩份文件的每個想法皆已實現、主控完成最終親自 review，並歸檔已完成計畫（phase-01〜04 → `docs/plan/finish/`）、校正 `CLAUDE.md`——**phase0819 的任務 1、2 與約束 1〜7 全數達成**。

## 實作邏輯

不信任「剛剛跑過」：總驗收一律重新執行取證（pytest 全量、schema、SQL 唯一性、端點行為、資料庫收尾狀態）；再對照計畫文件逐條打勾；最後用 `find -newermt` 盤點今日全部檔案變動、逐一歸因，無法歸因的記錄回報。

## 總驗收結果（G1，全部重跑）

| 檢查 | 結果 |
|---|---|
| `python -m pytest tests -q` | **19 passed**（unit 2＋integration 17；重跑多次一致） |
| bare `pytest -q`（venv） | 19 passed（`which pytest`=.venv/bin/pytest → CLAUDE.md 的 `pytest -q` 指令維持有效） |
| `\d photo`（visual_memory） | 8 欄位型別全對（items=text[]、content_time=date、uploaded_at=timestamptz、embedding=vector(1024)）＋`photo_embedding_idx` hnsw |
| `\d photo`（visual_memory_test） | `Table "public.photo"` 存在，索引同上 |
| SQL 唯一性 grep（app/ 內） | 恰一行：`app/repositories/photo_repository.py` |
| 端點行為（TestClient 取證） | openapi paths=`['/health','/photos']`；非圖片→415＋`{"detail":"上傳檔案必須為常見圖片格式（如 JPEG、PNG）"}`；PNG→201 `{"accepted":true,...,"size":70}`；/health ok |
| 資料庫收尾 | visual_memory＝0 筆、visual_memory_test＝0 筆 |
| 5432（postgresql@14） | 資料庫清單完好（wanderlove、fse_chat_room），全程未觸碰 |

註：`app.routes` 直接列舉看不到 `/photos` 是 FastAPI 0.141 `include_router` 的延遲載入內部型別（`_IncludedRouter`）所致——以 openapi 與實際請求為準（皆正確），非缺陷。

## 逐項核對（G2，phase0819 約束 5）

**phase-03**：步驟 0（tests 結構＋conftest＋12 個測試、紅燈留證）✅／步驟 1（schema.sql 與 design.md §7 一致＋DROP 冪等）✅／步驟 2（兩庫建表）✅／步驟 3（session.py，唯一 psycopg.connect）✅／步驟 4（repository 五操作）✅／步驟 5（煙霧腳本輸出逐字元一致）✅；驗收 1〜7 全過（見階段E REP 表格）。

**phase-04**：步驟 0（7 個測試、紅燈 6 紅 1 綠留證）✅／步驟 1（photos.py：415 前置檢查、無大小上限、佔位回應）✅／步驟 2（main.py 掛載）✅／步驟 3〜4＋curl 驗收 1〜6 實跑全過、驗收 0 pytest 19 passed ✅（見階段F REP 表格）。

**規則落地**：Rule U1 完整實現（非圖片 415＋不進後續處理＋照片數量 0）；U2/U3/U4/U5 的資料層基礎（repository/schema）就緒，服務層留待 P05/P06。

## 最終親自 review（G3，使用者指定由主控執行）

- **通讀全部 10 個新增／修改檔**：`db/schema.sql`、`app/db/session.py`、`app/repositories/photo_repository.py`、`app/api/routers/photos.py`、`app/main.py`、`tests/conftest.py`、`tests/unit/test_photo_repository_unit.py`、`tests/integration/test_photo_repository.py`、`tests/integration/test_photos_upload.py`、兩個空 `__init__.py`。
- 分層依賴方向正確且無反向：main→routers→config；repository→session→config；tests→app。dependencies.py／services 未動（正確，屬 P05+）。
- 測試誠實性：全部斷言真實行為（HTTP 狀態碼、逐字 JSON、DB 計數、DB 回傳值），零 mock；conftest 有「絕不清正式庫」防呆。
- 實作與計畫唯一差異＝`"這不是圖片".encode()`（計畫碼 `b"這不是圖片"` 為非法 Python）——已認可並**回修計畫檔**；另回修「紅燈 7 個」→「6 紅 1 綠（vacuous pass 說明）」。

## 歸檔與文件校正（G4／G5）

- `phase-01〜04` 四份計畫移至 `docs/plan/finish/`（已先確認全 repo 無任何檔案以路徑引用它們）；`phase-00` §1 增補歸檔規則說明。
- `CLAUDE.md` 四處更新：現況（Phase 01〜04 完成、19 測試全綠、tests/unit＋integration 結構與 conftest 行為）、指令節（測試自 Phase 03 起）、規格進度段與重要陷阱段的 `docs/plan/` 目錄敘述（unfinish／finish 分佈）。

## 遇到的問題與解法／異常記錄（依 phase0819「非本次改動的問題要記錄回報」）

1. **（異常，非本次工作所為）**`docs/plan/unfinish/phase-05-看圖模組.md` 與 `phase-08-接上真實模型.md` 的 mtime 在今日 09:10:50／09:10:55 有變動——主控與兩個 implementer 皆未寫入該二檔（implementer 寫入清單已逐一驗證），且內容**不含任何本次 session 的痕跡**（無 2026-08-19／TDD／tests 子目錄等字樣）。研判為 session 外部觸碰（例如使用者出門前在編輯器存檔）。內容與路線圖一致，未做任何處置；後續執行 P05／P08 前本就會照工作流程重新校對計畫檔。
2. **（既有噪音）**pytest 每次輸出 1 個 starlette 的 `StarletteDeprecationWarning`（testclient/httpx），與本次改動無關、不影響結果；處置屬套件升級議題，未動 requirements。
3. **（正常產物）**專案根目錄出現 `.pytest_cache/`——pytest 執行的標準快取目錄（自帶 .gitignore），保留。
4. 無任何「與本次改動相關」的測試失敗；19/19 綠。

## 目前專案狀態與下一步

Phase 01〜04 完成：系統可存照片資料（兩庫 schema＋repository），`POST /photos` 已落實 U1 格式檢查（415），合格檔回佔位回應；TDD 基礎（19 測試）就緒。下一步＝Phase 05（看圖模組 vlm_service＋dependencies 注入點＋422 路徑），依工作流程屆時先校對 phase-05 計畫檔再動工。
