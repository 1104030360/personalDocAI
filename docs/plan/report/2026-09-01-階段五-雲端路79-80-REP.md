# 2026-09-01 階段五：Phase 79 雲端路本機端單圖 ＋ Phase 80 逾時與冪等 REP

> 對應 TODO：`docs/plan/todo/2026-09-01-階段五-雲端路79-80-TODO.md`。計畫檔：`docs/plan/unfinish/phase-79-雲端路本機端單圖.md`、`phase-80-雲端路逾時與冪等.md`。
> 全程不 commit；驗收用工作樹 tree 快照相減。★G1 仍未過：**零 boto3、零 AWS 指令**，整圈靠 `FakeMailbox` 走通；正式路徑 `CLOUD_ROUTE` 預設 off。

## 實作邏輯

**Phase 79** 把 Phase 78 留下的 `NotImplementedError("Phase 79")` 換成真正的雲端路：`CloudRoute(mailbox, probe, *, timeout_seconds)`
（`available` 吞探測例外→False；`submit` 順序鐵律 **context.json → input.* → jobs 訊息**，訊息只有 `job_id`／`s3_key`；
`wait_result` 基本版；`fetch_result`；`cleanup` 刪三鍵）；`gated_ingest` 的雲端成功路：submit 失敗→`fallback=local reason=submit_failed`、
等不到→`reason=result_timeout`、工人說看不懂→`fail_job`（**不是** fallback，總覽 §10.1 g）、成功→本機 `embed_understanding`（只重算向量、最多 3 次）
→ `insert_photo_with_files` → **立刻 `store.update(photo_ids=[…])`** → cleanup → `finish_image_job`（§10.2 R；D17）。
`tests/fakes.py` 加假工人 `fake_worker_process_one`（把 jobs 第一則變成 `result.json` 九欄＋一則 results 訊息）。

**Phase 80** 把 `wait_result` 補成總覽 §2.5 的**五條規則**（每次長輪詢 1〜20 秒；自己的訊息→GetObject／刪訊息；**別人的訊息**→
store 查無或 `route=local` 就當殘訊息刪掉並連它的三個 S3 物件一起清、否則 `release_result_message` 還回去＋sleep；deadline→None），
加上 `route=cloud` 崩潰重送分支 `_繼續雲端路`（`fetch_result` 有→**落庫前重新 `store.get`**（D17 最後一道保險）→落庫；無→
`reason=redelivered_without_result` 走本機；**絕不重新 submit、不再問閘門**）、`_盡力清雲端`（cleanup 例外只 log——`cloud` 可能已是 `CloudRouteOff`）、
以及裁決 **R14**：`cloud.wait_result(...)` 包 try/except，信箱例外視為逾時（否則 job 會永遠卡 analyzing 而面板不顯示失敗）。

## 步驟

| # | Phase | 做了什麼 | 結果 |
|---|---|---|---|
| 1 | 79 | 實作 subagent（Opus）：4＋6 顆看紅（`ImportError: CloudRoute`／`fake_worker_process_one`）→ 假工人 → `CloudRoute` → `gated_ingest.py` 79 版整檔重貼 → 78 的兩顆升級成真 `CloudRoute`（含 78 review Minor：`probe.calls == 1`、caplog `route=cloud verdict=NON_SENSITIVE` 恰一次） | 全量 **601**（+10）、三死埠 601、ruff 綠 |
| 2 | 79 | 審稿：兩個產品檔與計畫逐位元相同（difflib 零 hunk）；五種結局逐條對到行號；**2 Important 都是測試守備**（`results == []` 不會咬；`_遠端可用嗎` 的 except 失去覆蓋） | Approved（行為）→ 裁決 **R13** |
| 3 | 79 | fix round 1：加 `delete_result_message`／`delete_job_message` 流水帳斷言＋新顆 `test_雲端路available本身丟例外_閘門層也當作不可用`（`FakeCloudRoute(RuntimeError)`）；兩個變異證據（註掉刪訊息→該顆紅；拆 try→新顆獨紅）；產品碼零改動 | **602**（79 實 +11）；re-review 全數 ADDRESSED、零新破壞 |
| 4 | 80 | 實作 subagent：5＋5＋1（R14）顆——6 顆真紅（殘訊息刪 S3 ×2、route=cloud 重送 ×2、送兩次、信箱丟例外）、5 顆如計畫預告因 79 的保守實作本來就綠 → `cloud_ingest.py` 補五條規則＋`_處理別人的訊息` → `gated_ingest.py` 80 版整檔重貼（`_繼續雲端路` 重讀、`_盡力清雲端` 七處、R14、`_理解` 註解修正）；R14 變異證據（拆 try→只那顆紅） | 全量 **613**（+11）、三死埠 613、ruff 綠 |
| 5 | 80 | 審稿：五條規則、重送分支、R14 try 範圍（只包 `wait_result`，落庫不在內→不會雙 INSERT）、§10.2 R 順序逐條對到行號；手算 `wait_seconds_log` ＝ `[20,20,20,18,15,12,9,6,3]`；實跑單元檔 20 passed／0.5s（沒有任何測試真的睡） | **Approved**，0 Critical／0 Important／4 Minor |
| 6 | — | 我親自讀 `cloud_ingest.py` 的 `CloudRoute`＋`_處理別人的訊息`、`gated_ingest.py` 80 版全文 | 與 §2.5 一致 |

## 測試方式

- TDD：兩個 phase 回報都有 RED→GREEN 證據；80 誠實列出哪 5 顆一開始就綠與原因（79 的 `wait_result` 是五條規則的保守子集）。
- 全量：591 → **601** → **602**（R13）→ **613**（含 R14），`skipped` 恆 0，只有基線 warning；三死埠一起指顆數相同。
- 順序鐵律靠 `FakeMailbox.calls` 流水帳斷言（context→input→jobs；photo_ids 先於 cleanup；fallback 時 cleanup 先於 photo_ids——兩個方向相反的斷言共用同一本流水帳）。
- D17：`test_同一個job_id的結果送兩次_照片仍然只有一列` 第二趟真的重建 job、走進 `_繼續雲端路`→`fetch_result`→`photo_ids` 分支收尾；拿掉那個分支會 INSERT 第二張而紅。
- 變異證據三組（79：刪訊息／拆 try；80：拆 R14 try）都是「只有目標那顆紅、其餘綠」。
- 掃碼：兩個 service 檔零 boto3、零 SQL token；端點 22；`ingest_job.py` 零改動；`docs/spec/`／compose／前端零改動。

## 遇到的問題與解決

| 問題 | 解決 |
|---|---|
| 79 的 `assert 信箱.results == []` 證明不了「刪了 results 訊息」（假件在 receive 當下就 pop 出佇列） | R13：改斷言流水帳 `"delete_result_message" in 信箱.calls`；變異實證舊斷言確實不會咬 |
| 78 的「探測丟例外」那顆升級成真 `CloudRoute` 後，`gated_ingest._遠端可用嗎` 的 except 失去覆蓋 | R13：加 1 顆用 `FakeCloudRoute(RuntimeError)` 釘住雙保險（79 實 +11） |
| 真 `AwsMailbox` 等結果時丟例外會讓 job 永遠卡 analyzing | R14：`cloud.wait_result` 包 try → 視為逾時 → cleanup＋fallback；加 1 顆測試（80 實 +11） |
| `_繼續雲端路` 的落庫前重讀沒有測試抓得到其刪除（`--concurrency=2` 並行窗口） | R15：保留，reviewer 不得以「刪了照樣綠」要求移除；計畫 §7 陷阱 12 已承認 |
| `_理解` 註解誤稱 `model_validate` 會驗多餘鍵（`PhotoUnderstanding` 未設 `extra="forbid"`） | 80 重貼時改措辭 |
| 計畫檔 79／80 的絕對顆數（599／600／609／610）與實查（602／613）不同 | 要對的是「本 phase 新增幾顆」；計畫 §2／§6／§8 已由 controller 補註實查值與裁決來源 |
| Phase 81 若照它自己的計畫檔整檔重貼 `gated_ingest.py` 會弄丟 80 的 R14／重讀／註解修正 | 總覽 §2.7 P81 註記：**以 80 落地版為基準只加 PDF 分支**，派 81 前先 diff |

## 測試結果

| 項目 | 79 | 80 |
|---|---|---|
| 新增顆數 | 11（`test_cloud_ingest_unit.py` +4、`test_gated_ingest.py` +7 含 R13） | 11（同兩檔 +5／+6 含 R14） |
| 全量 | 602 passed、0 skipped | **613 passed、0 skipped** |
| 三死埠 | 602 | 613 |
| 端點 | 22 | 22 |
| 改到的檔 | `cloud_ingest.py`、`gated_ingest.py`、`fakes.py`、兩個測試檔 | `cloud_ingest.py`、`gated_ingest.py`、兩個測試檔 |

**留給後續（已寫進總覽 §2.7 P81／P95）**：81 以 80 落地版為基準；PDF 結果 `kind` 分支；`CloudRouteOff` 在重送路的兩個 try 零測試（95 候選）；`_處理別人的訊息` 的 `store.get` 例外會被 R14 接成逾時（行為安全）。
