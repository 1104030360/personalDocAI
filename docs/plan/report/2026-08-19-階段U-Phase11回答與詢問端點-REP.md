# 2026-08-19 階段U：Phase 11 回答與詢問端點 REPORT

## 做了什麼（一句話）

照校準後的 `phase-11-回答與詢問端點.md`，以 TDD（紅→綠）完成詢問流程最後一塊：`generate` 節點（ANSWER_PROMPT 三條鐵律：只依檢索內容、查無不虛構、**回答語言跟隨提問語言**）＋ `POST /ask` 端點（`search_mode`／`retrieved_photo_ids` 直取 state、不經 AI）——全量測試 **55 → 60 passed**，獨立 review 雙軸通過（Spec ✅＋Approved）。

## 實作邏輯

- **執行方式**：主控派 opus 實作 subagent 照計畫逐字轉錄（9 步驟、11 個程式碼區塊），獨立 opus reviewer 雙審，全程不 commit。
- **TDD 順序**（照計畫 🧪 加註）：先建 `tests/integration/test_ask_endpoint.py` 看紅（`ImportError: cannot import name 'get_answerer'`，僅該檔收集失敗）→ 步驟 7（FakeAnswerLLM）→ 步驟 1（ANSWER_PROMPT／AnswerClient／OllamaAnswerer）→ 步驟 2＋8 一起做（AskDeps 加必填 `answerer` 的同時補既有 5 個 route 測試）→ 步驟 3（`Field(min_length=1)`）→ 4（dependencies 增量加 get_router／get_answerer／get_today）→ 5（ask.py router）→ 6（main.py 掛第二個 router）→ 綠。
- **關鍵設計落地**：查無仍走 generate 交 LLM（流程圖無旁路、產品碼零寫死查無句）；可驗證欄位不經 AI（`config.SEARCH_MODE_LABELS` 轉全名）；產品碼零語言偵測（`_looks_english` 只在測試假件）；照片內容原文照抄不翻譯（英文提問測試同時斷言英文框架句＋中文「可樂」）。

## 步驟

1. 派工 opus implementer 依計畫執行（中途遇一次**暫時性 API 網路錯誤**中斷，以 SendMessage 從斷點續跑完成）→ 回報 DONE_WITH_CONCERNS。
2. 主控以工作樹 diff 製作審查包（新檔 `git add -N` 後 `git diff HEAD -U10`，537 行、8 檔 +275/−15），派獨立 opus reviewer 雙審。
3. Review 結果：**Spec compliant ✅＋Task quality Approved**；Critical 0／Important 1／Minor 6；修正迴圈 0 輪。逐字一致性由 reviewer 以腳本逐塊比對確認（11 區塊全數一致，僅佔位符與指示註解未抄＝正確）。
4. 主控裁定（記 ledger）：Important-1（conftest 安全網未涵蓋新注入點，屬 plan-mandated 的前瞻風險）→ 列為 **Phase 12 步驟 0**；Minor-1（fakes.py 中段 import）→ 併入步驟 0 修正＋計畫同步校準；Minor-5（計畫 TDD 用語）→ 主控直接校正計畫；Minor-2/3/4 遞延（記 ledger 給最終 review 分揀）；Minor-6（intent-to-add）係主控製包所為、符合預期。

## 測試方式

- 紅燈：`pytest -q` → Interrupted（1 error：新檔 import 失敗）；`pytest -q --continue-on-collection-errors` → `55 passed, 1 error`（其餘測試不受影響）。
- 綠燈：`pytest tests/integration/test_ask_endpoint.py -v` → **5 passed**；`pytest -q` → **60 passed, 1 warning**。
- 計畫驗收 3：`grep -n "回答語言必須跟隨使用者提問的語言" app/services/ask_workflow.py` 命中（reviewer 獨立複驗＝第 93 行）。
- 計畫驗收 4：流程圖節點 `['__start__','route','retrieve_metadata','retrieve_vector','generate','__end__']`（四節點含 generate）。
- 計畫驗收 5〜8（/docs 目視＋真 Ollama 中英煙霧）依分工留待階段W 主控親跑——那是 `OllamaAnswerer` 產品路徑唯一的檢驗（review Minor-4 亦如此提醒）。

## 遇到的問題與解法

1. **implementer 中途斷線**（API ENOTFOUND，非任務錯誤）：主控核對磁碟斷點（步驟 1＋AskDeps 欄位已落、接線與步驟 8 未做）後 SendMessage 續跑，零重工。
2. **pytest 收集錯誤的輸出行為**：計畫原文暗示紅燈可見「55 passed＋1 error」，實際 pytest 9.1.1 預設 Interrupted——implementer 兩種輸出都留證；主控校正計畫用語（加 `--continue-on-collection-errors` 說明）。
3. **conftest 安全網缺口**（review 唯一 Important）：不是本 phase 的偏離（計畫明令不多做），但 Phase 12 起有「默默打真 Ollama」風險 → 裁定為 Phase 12 步驟 0 補齊，並已寫進 phase-12 計畫。
4. **fakes.py 中段 import**：逐字轉錄的結果、責任在需求書 → 計畫已校準（import 移檔頂），程式修正併入 Phase 12 步驟 0 走正常 review 流程。

## 測試結果

- `pytest -q` = **60 passed, 1 warning**（55＋5，與計畫驗收 2 一致；warning 為 starlette TestClient 棄用警告，非本 phase 引入）。
- 獨立 review：Spec ✅／Approved，Critical 0；報告見 `.superpowers/sdd/phase0819-4/stage-U-review.md`。
- 未 commit（依使用者指示）；`docs/spec/**` 零異動；pytest 全程未打真 Ollama。
