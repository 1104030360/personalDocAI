# 階段十四 REP：計畫檔校準 Phase 93〜95

- 日期：2026-09-03
- 對應 TODO：`docs/plan/todo/2026-09-03-階段十四-計畫檔校準93到95-TODO.md`
- 工作區：`.superpowers/sdd/phase0903-1/`（ledger `progress.md`、`brief-common.md`、`calib-brief.md`、三份 `calib-{A,B,C}-report.md`）
- 結果：**三份計畫檔全部校準完成**、總覽同步 16 處、**零程式碼／零測試變更**

---

## 1. 實作邏輯

三份計畫寫於 2026-08-31，跟 09-03 的現況有五類落差（顆數、同一測試檔的顆數、碼區的中文識別字、已被前輪修掉的步驟、Demo 3 需要 push）。
做法：**controller 先實查現況寫成共同事實簡報 → 三個 Opus 校準者各改一份、只准照簡報改 → controller 抽查（舊數字殘留、tokenize、NBSP、碼區逐行讀）→ 跨檔問題由 controller 回寫總覽**。

```text
controller 實查 ──► brief-common.md ──┬──► 校準者 A（phase-93）──► calib-A-report.md ─┐
（pytest／AWS／GitHub／實檔）          ├──► 校準者 B（phase-94）──► calib-B-report.md ─┼──► controller 抽查＋回寫總覽
                                       └──► 校準者 C（phase-95）──► calib-C-report.md ─┘
```

## 2. 步驟（實際發生的順序）

1. 讀 dev-prompt、memory、上一輪 ledger（R0〜R28）、`git log`：Phase 92-A 已完成並歸檔（`c40a3b3`），**10 個 commit 未 push**（CI 最後一次跑在 08-28）。
2. 實查基線：`pytest -q` **692 passed／0 skipped**（memory 記 690；owner 在 `f2fc067` 補了 2 顆）；`test_design6_error_paths.py` **6 顆**；端點 22。
3. 實查 AWS：admin 身分、**零 OIDC provider、無 `personaldocai-github-deploy`**、ECR `bb3921a`／`bb3921a-dirty`／`latest`、零 running、實例 t3.xlarge stopped。
4. 實查 GitHub：`sub_claim_prefix` 與計畫**逐字相同**、secret／variable 皆空、只有 `test` workflow。
5. 寫 ledger：裁決 R0〜R11 ＋ pre-flight 對照表（16 列）。
6. 派三個 Opus 校準者平行；controller 同時建 OIDC provider（Phase 93 §4.2）、改總覽 §2.7／§6。
7. A 回報 → 抽查通過；兩條跨檔（`role-duration-seconds`、docstring 的 arm64）SendMessage 給 B；兩條（掃A 輸入、CLAUDE.md 543）給 C。
8. B 回報 → 抽查通過；B 抓到 95【掃A】會在 `Terminate`／`ExecStartPre` 假紅 → SendMessage 給 C 要求詞邊界。
9. C 回報 → 抽查通過；三處偏離全部認可（R12〜R14）；總覽 §2.7／§9／§2.2／§3.3 回寫。

## 3. 測試方式

- 本階段不改程式碼，**不跑全量 pytest**（只有開工基線那一次 692）。
- 每份計畫檔的 python 碼區抽出來跑 `tokenize`：93 三碼區、94 一碼區零非 ASCII 識別字；95 十碼區只剩 conftest 既有的 `跑完任務`／`目前的任務清單`（R12 豁免）。
- 三檔 NBSP（`\xc2\xa0`）HEAD 與工作樹皆 0。
- 舊數字（662／666／672／682）只剩在「校準紀錄」的說明句裡。
- 校準者 C 從改完的計畫檔 `exec` 出【掃A】regex，對 16 份設定檔輸入＋`app/` 49 個 .py 實跑：**零命中**（含 `Terminate`、`ExecStartPre` 的檔）。
- 校準者 B 用六顆測試的 regex 對 §4.3 的 deploy.yml 實跑：全部命中；`${{` 恰 8 行。

## 4. 遇到的問題與解法

| 問題 | 解法 |
|---|---|
| 使用者今日說「我不要 function 名稱用英文」，與 09-02「全部改成英文」的指示相反 | 判為筆誤（本意＝不要中文）：計畫檔碼區仍有中文 helper 才需要「先修計畫」；記為 **R1**，最終訊息明列供反轉 |
| 三份計畫的顆數基線 662 vs 實查 692 | R4：以 692 起算（696／702／715），總覽 §9 加「實」括號；「要對的是 +N」 |
| 95 計畫引用的 helper 名（`專案根目錄`／`dockerfile原始碼()`…）從未存在 | C 改成沿用實檔的 `PROJECT_ROOT`／`read_dockerfile()`…，import 段改「合併」 |
| 95【掃A】`nat`／`ecs` 子字串會誤中 `Terminate`／`ExecStartPre` | 詞邊界 regex ＋ 實跑 16 份輸入零命中 |
| 95【掃E】若照 R10 掃全文，工人 docstring 自己就含 `photo_repository`／`embedding` | R13：改「識別字＋字串常數＋動態載入＋兩個 import 定錨」 |
| 官方文件其實沒有逐字建議 `event == 'push'`（那是 Security Lab 範式） | B 改寫五處措辭，條件式不動 |
| Demo 3 需要 commit＋push，本輪不 commit | R0：計畫改成「由產品負責人執行」手冊；驗收包 D 段標待辦 |
| 總覽 §2.7 兩個測試名過期、§6 勾選區停在 Phase 80 | R7；controller 直接改 |

## 5. 測試結果

- 開工基線 `pytest -q`：**692 passed, 0 skipped**（44.1 秒，1 warning＝環境層 Starlette）。
- 本階段結束時程式碼零改動，顆數不變。
- 三份計畫檔：93 1506→1817 行（54 處）、94 1532→1855 行（約 55 處）、95 1731→2430 行（37 處）；勾選項全部保留（23→24、30→35、＋§4.3.3 新項）。
- AWS 副作用：**一個** OIDC provider（`token.actions.githubusercontent.com`，audience `sts.amazonaws.com`，免費）。

## 6. 下一步

階段十五：Phase 93 實作（subagent 寫兩份 JSON＋4 顆測試＋CLAUDE.md 三行；controller 建 role／policy／secret）。
