# 2026-08-19 階段R：Phase 10 路由與流程圖 REPORT

## 做了什麼（一句話）

照更新後的 `phase-10-路由與流程圖.md` 以 TDD 完成詢問流程前半段：`ask_workflow.py` 從空檔填入 LangGraph 流程圖（START → route → 條件邊 → retrieve_metadata／retrieve_vector → END）與 route 節點（`RouteDecision` 結構化輸出、中英 few-shot prompt、失敗一律 fallback 語意查詢），`tests/fakes.py` 加 `FakeRouter`，新增 5 個整合測試——**全量 50 → 55 passed，驗收 1〜4 全過，獨立 review 雙軸通過（Spec ✅／Approved）**。

## 實作邏輯

- **執行方式**：同階段Q——opus 實作 subagent 逐字轉錄計畫程式碼，主控產生工作區 diff 審查包（347 行），獨立 opus reviewer 雙審；全程不 commit。
- **TDD 先紅再綠＋順序陷阱**（計畫 🧪＋⚠️ 加註）：先建 `tests/integration/test_workflow_route.py` → 該檔收集失敗（`ask_workflow`／`FakeRouter` 不存在）、其餘 50 綠 → 步驟 1 寫 `ask_workflow.py` → **之後**才動 `tests/fakes.py`（conftest 會 import fakes，若在 ask_workflow 還是空檔時先加 import 會讓整個套件收集爆掉）→ 單檔 5 passed → 全量 55 passed。
- **設計決策落地**：路由由 LLM 判斷（一次結構化輸出同時拿查法＋條件；關鍵字規則是被否決方案）；ROUTE_PROMPT few-shot 中英各有（含 "What drinks did I buy recently?"）、條件值照原文抽不翻譯（§8.3）；**規則 Q3 的 fallback 集中在 route 節點一處**（例外與格式不符收斂到同一行 `RouteDecision(mode="vector")`，`OllamaRouter` 內刻意不做 fallback——計畫 Q4）；相依全經 `AskDeps` 注入、模組層級零建模（import 不通電）；檢索復用 Phase 09 的 `photo_retriever` 五鍵介面、本 phase 零 SQL。
- **驗收 5（真模型路由煙霧）**：計畫標「可選」，裁定由主控在階段S 親自跑（implementer 不碰真 Ollama）。

## 步驟

1. 派工（brief＝更新後計畫檔；報告落 `task-R-report.md`；硬約束：不 commit、只動三檔、fakes ⚠️ 順序、驗收 5 跳過、`uv pip install grandalf` 為唯一允許安裝且不進 requirements.txt）。
2. 實作 subagent：RED（收集失敗＋`50 passed, 1 error`）→ 步驟 1→2 → GREEN（5 passed／55 passed）→ 驗收 1〜4（含 grandalf 安裝後 `draw_ascii()` 圖：`__start__ → route` 虛線分岔到兩個 retrieve 節點、皆連 `__end__`）→ 自我複查（逐字比對 IDENTICAL、grep 證實 tests 無任何真 Ollama 呼叫路徑）。
3. 主控產審查包，派獨立 reviewer：逐 hunk 抽查 10 個關鍵段落全符；實查 import 鏈健康（fakes → ask_workflow → retrieval_service → photo_repository，位於 conftest 的 DATABASE_URL 導向之後）、`FakeEmbeddings` 對英文問題的決定性成立、被禁止的過度設計（語言偵測節點、關鍵字路由）皆未出現。
4. 主控處置審查結果（詳見下方「遇到的問題」）。

## 測試方式

- 單檔：`pytest tests/integration/test_workflow_route.py -v` → **5 passed**（條件型走 metadata＋條件正確、語意型走 vector＋recent、英文問題走 vector 且召回中文照片、無法判斷 fallback、格式不符 fallback）。
- 全量：`pytest -q` → **55 passed, 1 warning**（warning 為既有基線噪音）。
- 驗收 3 grep：英文 few-shot 例句與「不要翻譯」各命中一行。
- 驗收 4：ASCII 流程圖結構正確（route 之後條件邊分岔、兩節點會合 END）。

## 遇到的問題與解法

1. **審查唯一 Important（reviewer 自標 plan-mandated）**：`route_node` 的 `except Exception` fallback **沒有任何日誌**——運作時「router 壞掉」與「正常判 vector」外部不可區分。主控裁定：**本輪不改**（計畫強制逐字轉錄；design.md §10 對路由失敗只要求「fallback、流程繼續」未要求 log；任務書約束「只做計畫寫到的事」），歸 **Phase 11／13（錯誤收尾）** 處理並回報使用者——建議屆時在 except 加一行 `logging.warning("路由判斷失敗，改走語意查詢", exc_info=True)`（repo 已有 `vlm_service.py` 用 logging 的先例）。風險緩解：階段S 主控親自跑真模型路由煙霧，先驗證 `OllamaRouter` 實際可用。
2. **reviewer ⚠️：`OllamaRouter`／`with_structured_output(RouteDecision)` 從未被執行過**（測試全用假件、驗收 5 被跳過）→ 由階段S 主控煙霧補上。
3. 其餘 5 個 Minor 記 ledger 遞延：英文向量測試在「單張照片＋無相似度門檻」下驗不到真正的跨語言排序（P12/13 補無關照片）；格式不符 fallback 測試未斷言條件全空（P12 補）；`photo_retriever` docstring「五鍵」經裁定非缺陷（描述呼叫契約、兩呼叫端皆給五鍵）；`run_ask` 每問重 compile 圖（P12 放請求路徑時再快取）；grandalf 未進 requirements.txt（依計畫 Q1 屬預期，重建 venv 需重裝）。

## 測試結果

- `pytest tests/integration/test_workflow_route.py -v` → **5 passed**。
- `pytest -q` → **55 passed, 1 warning**（符合計畫「累計 55」）。
- 計畫驗收 **1〜4 全數通過**（5 由階段S 主控煙霧）；獨立 review：**Spec ✅／Task quality Approved**、0 Critical、1 Important（plan-mandated，裁定遞延）、5 Minor（遞延）。
