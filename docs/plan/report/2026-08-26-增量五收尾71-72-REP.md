# 2026-08-26 增量五收尾（Phase 71〜72）REP

> 對應 TODO：`docs/plan/todo/2026-08-26-增量五收尾71-72-TODO.md`。
> 71 由 Opus subagent 照計畫 TDD 執行、我親自 review；72（規格區）由我親自執行。
> 授權依據＝phase0826.md 明示指名執行 65〜72（★G3 的核准依據詳見 TODO 與驗收包附註）。

## Phase 71 錯誤收尾與全量回歸（523 → 543）

### 實作邏輯與步驟
- §4.1 逐列點名：計畫點名的 26 顆既有測試以 `--collect-only` 逐字複驗全部存在、零裁掉。
- 5 顆補缺（【補7】寫原圖失敗清半成品、【補8】JobStore 掛掉 500 且 staging／job 全清、
  【補9】`analyzing`／`retrying`／`queued` 三種進行中狀態 dismiss 全 409）——
  首跑就綠（計畫預告；它們釘的是 52〜70 已做出來的行為），逐顆解釋了為什麼綠是對的。
- 15 顆掃碼（掃A 待決定無批次勾選＋面板無重試、掃B photo 表無狀態欄／job_id 欄＋
  D16 四欄在、掃C compose 零 replica／flower／ollama＋concurrency=2＋redis 只綁本機、
  掃D 任務只吃 job_id 零 bytes＋PDF 非每頁一任務＋零 BackgroundTasks、掃E 端點 22 逐支
  列名＋入列當下收件箱零動靜）。
- **反向驗證 9 次**（計畫只要求 4）：每一顆「不存在」類斷言都改壞一次親眼看紅再復原，
  `diff` 證明復原乾淨——「首跑就綠」因此不是假綠。
- 執行者裁決落地：三支彈窗檔（entity_modal／task_modal／folder_modal:147）過期檔頭
  註解純註解校正；`grep -rn "classify_chain" app/` 自此**零輸出**；
  phase-70 計畫檔補「事後追記」說明判準演進。

### 測試結果
`pytest -q` **543 passed＋0 skipped**；`CELERY_BROKER_URL` 死埠 543、`OLLAMA_BASE_URL`
死埠 543、兩個同時死埠 543；三份 binder 27 綠零 skip；`docs/spec/` 全程乾淨。

### 遇到的問題
無阻擋。已知觀察：staging 兩個 2026-08-25 23:20 的孤兒檔未滿 24h、掃把正確不動它們
（預期行為；到期自動清）。

## Phase 72 規格檔改版與增量五驗收包（543 → 543，+0 顆如計畫）

### 實作邏輯與步驟
1. 前置存證：spec 區乾淨、app/ 快照、binder step 清單（34＋6 行）；
   `grep -c "^  Rule:"`＝18、binder step 裡零 `Rule`（Rule 行自由、步驟行綁死——實證）。
2. `上傳照片.feature`：檔頭核准紀錄第 6〜8 條（**日期填實際核准日 2026-08-26**，
   計畫校準明言 08-25 是佔位符）；Feature 敘述 +2 行；12 條 Rule 行「上傳後儲存」→
   「受理後分析成功才儲存」「回應」→「提供」；Example 改名＋刪 `Then 操作失敗`＋
   `And`→`Then`（本 phase 僅有的步驟行改動，binder 的 `操作失敗` step 保留給 415）；
   檔尾新增「可一次多檔」`#TODO` Rule。改完先跑上傳 binder＝16 綠零 skip、
   新測試名 `test_vlm_三次都無法理解照片內容` 如預期。
3. `無線鏡頭拍攝.feature`：檔頭紀錄 3 條＋原第 5 行註解改現況＋Rule「走既有上傳」→
   「進入入庫佇列」＋新增「桌面不在快門當下開彈窗」`#TODO` Rule。
4. `歸類照片.feature`：檔頭紀錄＋第 90 行那條整個反過來（D16 推翻 design2 D5，
   待決定**有**採用建議）。`瀏覽檔案櫃.feature`：檔頭紀錄＋預設分頁 Rule／Example／
   `Then` 參數改「資料夾」＋Feature 敘述改寫（此檔無 binder，改動零測試影響、零保護，
   靠逐字對照計畫）。
5. `釘選實體`／`建立待辦`／`自然語言詢問` **一字未動**（計畫逐行判定＋
   `git diff --stat docs/spec/` 恰四檔實證）。
6. 驗收包 `2026-08-26-增量五驗收包-請產品負責人確認.md`：A 段自動化數字已填實勾選、
   **B〜E 段全部留白**給產品負責人；附註記 ★G3 依據與四檔改動摘要、已知限制、
   規格措辭債（步驟行仍用「回應」）。
7. CLAUDE.md 三段：現況段換上完整增量五成果段（543 填實；把我 Phase 66 時寫的
   過渡版整段替換掉避免重複）、指令段加「四服務／202 不是 201／data/staging」、
   陷阱段加 202 一條；`grep "201"` 全檔僅剩 2 處歷史敘述。
8. §4.9 歸檔清單核對：unfinish 恰 **9** 份（總覽＋65〜72）、finish 的 52〜64 恰 13 份；
   **未搬、未 commit**（隨 commit 執行）。
9. 收尾環境：切回**常駐模式並重建映像**（`up -d --build`，static 新碼進映像——
   驗收包的準備段用常駐跑，重開機也自動回來）；rebuild 後輕量煙霧全過
   （health、pending 頁含 task_modal、upload 含 multiple、progress_panel.js 被服務、
   `GET /ingest-jobs` 對真 Redis 正常、worker 掃把 log 在）。Python 自 Phase 66 build
   後零改動（67〜71 只動 static／tests／JS 註解），故未重跑重量級 VLM 煙霧——
   66 當天的真容器雲端煙霧即本映像 Python 路徑的實證。

### 測試方式與結果
- 三份 binder 27 passed 零 SKIPPED；`pytest -q` **543 passed＋0 skipped**（與 71 完成時
  同顆數＝規格改版零測試影響，如計畫宣稱）；雙死埠合併實證 543。
- `git status --short -- app/` 與 71 完成時逐行相同（產品碼零改動）。
- `git diff --stat docs/spec/` 恰四檔、+52/−23 行。

## 尚待產品負責人（本輪停在這裡的原因）

1. **瀏覽器實操驗收**（驗收包 B〜D 段＋各前端 phase 的人工項）——Chrome 擴充連著
   兩個瀏覽器、自動化工具強制要使用者當面選定其一；素材已備妥（見驗收包 💡 註）。
2. **iPhone 連拍真機驗收**（丙4：連拍兩張不必等、桌面不跳彈窗）。
3. ★G2／★G3 勾選格與驗收包 B〜E 段簽名；**commit 時機**（歸檔隨 commit 執行，
   §4.9 已備好指令）。
