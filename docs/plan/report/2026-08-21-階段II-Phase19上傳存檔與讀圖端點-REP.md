# 2026-08-21 階段II：Phase 19 上傳存檔與讀圖端點——REP

## 實作邏輯

依 `unfinish/phase-19-上傳存檔與讀圖端點.md`（GG 校準版）。

1. **INSERT → 寫檔 → UPDATE**：檔名用 `photo.id`（`data/photos/7.png`），id 是 INSERT 當下才配發，順序無可替代。INSERT 後：`save_original()` 原圖原樣落地 → `make_thumbnail()` Pillow 縮圖（長邊≤512 等比不放大）→ `update_photo_paths()` 把兩個相對路徑＋`content_type` 補回那一列。
2. **失敗全清＋re-raise**：⑤⑥⑦ 任何一步炸掉 → `remove_if_exists()`×2（吃得下 None 與檔案不在）＋ `delete_photo()` → 裸 `raise`。結果「跟沒上傳過一樣」，使用者收到 500、log 留原始 traceback——不吞錯原則不變。交易包不住磁碟檔案，手動清理反而一致（計畫 Q&A 有論證，不改連線管理）。
3. **讀圖端點**：`GET /photos/{id}/thumbnail`／`/image` 共用 `_send_photo_file()`——沒這列／路徑 NULL（遷移舊照片，前端顯示占位）／磁碟檔案不見，三種都 404；都在則 `FileResponse(絕對路徑, media_type=該列 content_type)`。
4. **既有測試假位元組維護**（本 phase 起上傳真的用 Pillow 開圖）：五檔五處換成真圖（`test_upload_feature`／`test_upload_bilingual`／`test_upload_design_rules` 的 `PNG_BYTES = make_png_bytes()`、`test_photos_upload` 的 JPEG、`test_error_paths` 大檔案改 `make_large_png_bytes()`＝1200² 隨機雜訊 >3MB 壓不掉）；**刻意保留**失敗路徑（422／embedding 失敗）的假位元組——通過即證明那些路徑沒去解碼圖片。
5. 回應 JSON 與 `category` 行為零改動（`schemas/photo.py` 零 diff；Phase 20 才動）。

## 步驟（TDD 紅→綠，Opus subagent 執行、我親自 review）

1. 新增 `tests/integration/test_photo_files.py`（11 測試，計畫逐字）→ 實跑**紅**：`7 failed, 4 passed`（`update_photo_paths` 不存在、路徑欄位 None、讀圖路由 404）。
2. `photo_repository.py` 加 `update_photo_paths()`／`delete_photo()`（依 GG 校準：`reset_folders_and_photos()` 之後、`list_folders()` 之前）。
3. `photos.py`：import 補 `FileResponse`／`storage_service`；④ 後插入 ⑤ 寫檔＋失敗清理；檔尾 `_send_photo_file`＋兩個 GET；35 行 v4 舊註解改版 → 新檔**綠**（11 passed）。
4. `tests/fakes.py` 加 `make_large_png_bytes`＋`import os`；五處假位元組逐表改完。
5. 全量回歸。

## 測試方式與結果

- `pytest tests/integration/test_photo_files.py -v` → **11 passed**（原圖位元組一致、縮圖 512×256、JPEG 副檔名 .jpg、讀圖 200／三種 404、寫檔失敗與 UPDATE 失敗都不留檔不留列、415/422 完全不寫檔）。
- 全量 `pytest -q` → **121 passed**（110＋11，與計畫一致）；規格兩檔 **14 passed**（12 條 Rule）。
- 驗收清單（我親自複跑）：SQL 只在 `photo_repository.py` ✓；`photos.py` 順序 grep＝insert(63)→save_original(80)→make_thumbnail(83)→update_photo_paths(86)→remove_if_exists(94,95)→delete_photo(96)→裸 raise(98) ✓；`except Exception` 恰 1 處 ✓；`git diff --stat app/schemas/photo.py` 無輸出 ✓；假位元組 grep 印 OK ✓；專案 `data/` 不存在（pytest 全程寫暫存目錄）✓；端點恰 6 個、淨增 2 ✓。

## 遇到的問題與解法

1. **計畫的端點列舉指令在本專案 FastAPI 0.141 失效**：`include_router()` 被包成 `_IncludedRouter`（無 `methods`、`path=None`）不攤平進 `app.routes`，該 python 片段連既有端點都印不出。**解法**：改查 `/openapi.json`（與既有測試 `test_openapi_has_photos_endpoint` 同慣例），實得 6 端點與預期逐字相同；計畫檔驗收清單已同步改成 openapi 版並註記原因（我裁定並修改）。
2. **驗收 grep 誤中說明註解**：`tests/fakes.py` 第 21 行 Phase 17 的註解含「fake image bytes」字樣，使「假位元組已清完」的 grep 出現一筆誤報。**解法**：把註解措辭改為 `b"\x89PNG…" 這種手打的假位元組`（語意不變、僅註解），grep 轉為乾淨 OK（我裁定並修改，屬文件級微調）。
3. subagent 另回報：計畫步驟 5 引用的「第 144 行」在改動後位移一行——計畫寫的是改前行號，屬正常，不動計畫。

## 備註

- 依使用者指示**先不 commit**；計畫的 commit 步驟延後。
- 計畫步驟 7（真模型瀏覽器實測：uvicorn＋curl＋正式庫舊照片 404）延到階段KK 統一做。
