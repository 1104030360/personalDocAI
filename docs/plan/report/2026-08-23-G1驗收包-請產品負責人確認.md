# G1 驗收包（2026-08-23）——請產品負責人確認

> **給產品負責人：** 下面是「照片詳情」與「AI 計時 log」兩件事的驗收清單。
> 全部看過、沒問題的話，請明確說一句「**甲乙沒問題，可以做 Docker**」——
> 有這句話，實作者才會開始動資料庫搬家那件事（那件事有風險，所以要你點頭）。
>
> 依 design4.md §0／§7：G1 沒過之前，Phase 45〜50（Docker）一行都不會做，
> `compose.yaml`／`Dockerfile` 連「先寫好放著」都不准。目前專案根目錄已掃碼確認
> **沒有任何丙階段檔案**。

**準備（重要）：** 如果現在已有舊的 uvicorn instance，先在它所在的視窗按 Ctrl+C 停掉，
再用下面指令重新啟動；不要沿用改版前、沒有 reload 的程序：

```bash
cd /Users/linjunting/personalDocAI && source .venv/bin/activate
uvicorn app.main:app --reload --port 8000
```

另開一個視窗看著跑 uvicorn 的終端機（C 段要看的 log 就印在那裡）。
B 段用瀏覽頁 `http://localhost:8000/ui/browse.html`；
C 段用上傳頁 `http://localhost:8000/ui/upload.html`（「AI 模型：本機｜雲端」開關在頁首）。

本機環境（實測自 `.env`）：看圖 `gemma4:e2b`、路由／回答／實體建議 `gemma4:e2b-mlx`、
向量 `bge-m3`；雲端看圖與文字都是 `gemma4`。

### A. 自動化（實作者已跑，請看數字）

- [x] Phase 38〜44 targeted ＝ **112 passed ＋ 2 skipped ＋ 1 warning**（9.42s）
- [x] 三份規格 binder ＝ **25 passed ＋ 2 skipped ＋ 1 warning**（2.19s；
      兩條 `@未實作` 仍照規格 skip）
- [x] `pytest -q` ＝ **402 passed ＋ 2 skipped ＋ 1 warning**（27.73s）
- [x] `OLLAMA_BASE_URL=http://localhost:9 pytest -q` ＝ **402 passed ＋ 2 skipped ＋
      1 warning**（26.47s；同顆數，證明測試沒偷打真模型）
- [x] `/openapi.json` 端點數 ＝ **20**（原 19，加了「讀一張照片」`GET /photos/{photo_id}`）
- [x] DELETE 動詞 ＝ **0**（系統仍然沒有任何刪除功能）；也沒有「列出全部照片」的端點

唯一 warning：
`StarletteDeprecationWarning: Using httpx with starlette.testclient is deprecated; install httpx2 instead.`

- [x] 最新 hardening regression 已 RED→GREEN：focus trap／背景 `inert`／Tab 與 Shift+Tab／
      focus restore、stale modal generation、structured-output failure truth、單行截斷的隱私安全 log、
      request-selected immutable target，以及遺失圖片／raw error／長 CJK。

### B. 瀏覽器（請親自點，五條）

- [ ] **資料夾**分頁 → 進一個資料夾 → 點一張照片：
      跳出彈窗，上面大圖、下面說明、四個欄位（類別／地點／物品／內容日期）都在。
      按 **Esc** 關掉之後，背後**還是那個資料夾的縮圖牆**。
- [ ] 上一條那顆窗裡，**沒有任何「改資料夾」的按鈕**（照片定案了就是定案了）。
      空的欄位顯示「**無**」，不是空白。
- [ ] **待辦**分頁 → 點一列：**沒有跳出新分頁**，原地開同一顆窗，
      窗最上面多一行待辦標題與到期日（沒有到期日的寫「無到期日」）。
- [ ] **待決定**分頁 → 點一張照片：跳出來的是**歸類彈窗**（採用／改選／自建／稍後再說），
      不是詳情窗。（這條是確認舊功能沒被改壞。）
- [ ] 挑一張**很舊的照片**（正式庫最早那兩張沒有原圖）→ 窗仍然開得起來，
      圖的位置是灰底「無原圖」。這是**預期行為**，不是壞掉。

### C. 終端機 log（請看四段）

- [ ] 在**上傳頁**（`/ui/upload.html`）**上傳一張照片**：看到 `kind=vlm` 一組（開始／結束）
      ＋ `kind=embed` 一組。結束行有 `elapsed_s=` 秒數與 `ok=true`。
      （實作者最近一次本機樣本：VLM 33.1s、embed 2.4s；你的時間可不同。）
- [ ] **把上傳頁頁首的開關撥到「雲端」再上傳一張**：`kind=vlm` 的 `backend` 變成 `cloud`
      （實作者最近一次樣本為 7.1s），而 `kind=embed` **仍然是 `local`**
      （向量必須本機算，才跟資料庫裡既有的比得起來）。
- [ ] 到**問問題頁**（`/ui/ask.html`）**問一句語意題**（例如「我最近買過什麼飲料？」）：
      看到 `route`／`embed`／`answer` **三組**。
      再問一句條件題（例如「有哪些在 Target 拍的收據？」）：只有 `route`／`answer` **兩組**
      （那種查法不需要轉向量）。
- [ ] 回上傳頁**上傳一份兩頁 PDF**：看到 **兩組 `vlm` ＋ 兩組 `embed`**（每頁各一組，看得出哪一頁慢）。

log 長相（實測樣本，欄位間就是一個空格）：

```text
AI 開始 kind=vlm backend=local model=gemma4:e2b
AI 結束 kind=vlm backend=local model=gemma4:e2b elapsed_s=33.1 ok=true understood=true text_chars=42 item_count=2 category_present=true entity_present=false task_present=true
```

### D. 兩個「壞掉時應該怎樣」（可選，想看再看；**兩條都在瀏覽頁 `/ui/browse.html` 做**）

- [ ] 把某張照片的原圖檔案從 `data/photos/` **改個名字**（例如 `7.jpg` → `7.jpg.bak`），
      回瀏覽頁重新整理再點它 → 窗照樣開，圖變成灰底占位，**不是**紅字錯誤。
      ⚠️ **看完請把檔名改回來**（那是正式的照片檔，不是測試資料；改名比刪掉安全，就是為了改得回來）。
- [ ] 在**瀏覽頁**開瀏覽器開發者工具的 Console（詳情窗這支程式只掛在這一頁，
      在上傳頁貼會說 `openPhotoDetailModal is not defined`），貼上
      `openPhotoDetailModal({photoId: 99999, task: {title: "測試", due_date: null}})` →
      窗開著、上面看得到「測試／無到期日」、下面是紅字「找不到這張照片」，
      **不是**空白分頁、**不是**瀏覽器的原生警告框。

### E. 最後

- [ ] 我（產品負責人）確認：**甲乙沒問題，可以做 Docker。** 日期：__________

---

## 附註（實作者）

- A 段已由自動化測試與實跑釘住（勾在上面）；B／C／D 段主 agent 已先用
  瀏覽器／真模型走過一輪 localhost 自查（結果見
  `2026-08-23-階段RRR-總驗收與親自Review-REP.md`），
  但 design4 §7 明文要求**你親眼看過**才算數，所以 B〜E 的勾留給你。
- 25 張全新瀏覽器 JPEG 證據位於
  `/Users/linjunting/.codex/visualizations/2026/08/24/01a03246-133e-7a31-974d-3eb734ae0a9e/phase38-44-final-pass-8/`。
  精確分布為 11 張 `1280x900`、7 張 `768x900`、7 張 `375x812`；最新兩位獨立視覺
  reviewer（`final_visual_qa_k`、`final_visual_qa_l`）皆為
  **PASS／HIGH confidence／25 of 25／zero blockers**；這完成技術視覺 gate，仍不代替 B／D／E
  的產品負責人親自勾選。
- 你目前在圖書館公共網路：B／C 段全部走 `localhost`，**不需要**手機、掃 QR 或熱點，
  現在就能做。只有無線鏡頭真機驗收（phase-36 遺留）才需要手機＋自家網路，那件事
  與 G1 無關，手冊在 `docs/plan/report/2026-08-22-Phase36真機驗收手冊.md`。
- 目前狀態固定為 **TECHNICAL PASS / G1 HUMAN PENDING / Phase 45 Docker NO-GO**。工作樹仍
  dirty；沒有 commit、release、Docker／Compose 或 Phase 45 工作。A 是實作者技術證據，
  B／C／D／E 全部保持未勾，只有產品負責人可完成。
