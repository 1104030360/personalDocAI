# 2026-08-19 階段V：Phase 12 詢問驗收測試 REPORT

## 做了什麼（一句話）

照校準後的 `phase-12-詢問驗收測試.md`，先補齊 conftest 假件安全網（步驟 0＝階段U review 的裁定），再用 pytest-bdd 把 `docs/spec/features/自然語言詢問.feature`（唯讀）直接當測試跑——5 條 Rule（Q1〜Q5）共 7 個 Example 一次全綠，**第二個驗收里程碑達成、12 條 Rule 全數綠燈**，全量 **60 → 67 passed**。

## 實作邏輯

- **執行方式**：主控派 opus 實作 subagent 照計畫轉錄，獨立 opus reviewer 雙審，全程不 commit。
- **步驟 0（安全網先行）**：`tests/conftest.py` 的 autouse `wire_fake_ai` 併入 `get_router`／`get_answerer` 覆寫（自此五個注入點全覆蓋，任何測試忘記自行覆寫也不會默默打真 Ollama）＋`tests/fakes.py` 的 `Document` import 移檔頂（階段U review Minor-1 的風格校準）；改完全量仍 60 passed 才進 BDD。
- **BDD glue（鏡射 `test_upload_feature.py` 慣例）**：`scenarios("../../...")` 直掛規格原檔；`context` 抽屜＋「規格 id → 實際 id」對照表（`_expected_ids` 只查表、不重算邏輯，無恆真式）；未指定現在時間的例子固定 `2026-08-18 10:00`；`get_now` 接 `lambda: context["now"]`（late binding，Given 改值即時生效）；Given 照表格逐列 `insert_photo`，向量走 `build_document`＋`embed_query` 與上傳路徑一致。
- **測試真的貫穿整條線**：`client.post("/ask")` → router → LangGraph（route→條件邊→retrieve→generate）→ retrieval_service → photo_repository 真 SQL；假件只有 router／answerer／embeddings／時鐘。

## 步驟

1. 派工 opus implementer → 回報 **DONE**：步驟 0 後 60 passed → BDD 單檔 **7 passed**（7 名稱一一對應 5 條 Rule 的 7 個 Example，首跑即綠）→ 雙規格 **14 passed** → 全量 **67 passed** → `grep -c "Rule:"`＝5（規格零異動）。
2. 主控製作審查包（三檔、標頭註明階段U/V 責任切分），派獨立 opus reviewer 雙審。
3. Review 結果：**Spec compliant ✅＋Task quality Approved**；Critical 0／Important 1／Minor 5；修正迴圈 0 輪。亮點：reviewer 以 pytest-bdd parser 實測規格 19 條步驟對 9 個 step 定義**全數唯一命中、無多餘定義**（含全形問號 U+FF1F 字元層核對）；另以 monkeypatch **變異測試** 7 組驗證斷言鑑別力——6 組確實打死對應測試（時間過濾靠 SQL、location 過濾、SEARCH_MODE_LABELS、回答依據檢索內容、回答出自 LLM 節點），證明**無恆真式斷言**。
4. 主控裁定（記 ledger）：唯一 Important（`search_by_metadata` 的 `category ILIKE` 全套件無斷言守護）為 **Phase 09 遺留缺口、非本階段引入** → 遞延 Phase 13，已把補法寫進 `phase-13` 計畫的 📌 備忘；5 條 Minor（glue 等價複製 embed 轉法、Given 現在時間鑑別力低、與安全網重複的兩行覆寫、排序未驗、行長）全數遞延記 ledger。

## 測試方式

- implementer：驗收 1（7 passed）、2（14 passed）、3（67 passed）、6（Rule=5）實跑留證。
- reviewer：逐字比對（程式化，136 行全同）＋步驟匹配實測＋7 組變異測試＋死埠全量（67 passed）。
- 主控親跑：驗收 3（67 passed）、驗收 5（`OLLAMA_BASE_URL=http://localhost:9 pytest -q` → 67 passed）、驗收 4（照計畫原文手法：暫時把 `search_by_vector` 的時間過濾改為不生效 → `最近的照片…` Example **FAILED** → 還原 → passed → `git diff` 零殘留）、驗收 1/2 單檔複跑（12 passed／14 passed）。

## 遇到的問題與解法

1. **conftest 安全網缺口**（階段U review Important-1）：依裁定作為本階段步驟 0 先行補齊；補完以死埠全量 67 passed 實證「/ask 的全部注入點都在安全網內」。
2. **category 過濾無測試守護**（本階段 review 唯一 Important）：變異測試揭露的 Phase 09 遺留缺口——BDD 例子被 `location=Target` 唯一鎖定、`test_retrieval.py` fixture 三張照片 category 全同。補洞超出本需求書範圍 → 遞延 Phase 13（備忘已寫進計畫檔）。
3. **無恆真式的證明方式**：不能只看綠燈——reviewer 以 monkeypatch 變異 7 組產品行為逐一驗證「改壞會不會被抓到」，6/7 會；主控再以計畫原文的註解法親手複證時間過濾那組。

## 測試結果

- `pytest tests/integration/test_ask_feature.py -q` = **7 passed**；雙規格 = **14 passed**；`pytest -q` = **67 passed, 1 warning**；死埠全量 = **67 passed**（零 Ollama 依賴）；變異測試紅→綠、零殘留；`grep -c "Rule:"` = **5**（`docs/spec/` 唯讀未動）。
- 獨立 review：Spec ✅／Approved，Critical 0；報告見 `.superpowers/sdd/phase0819-4/stage-V-review.md`。
- 未 commit（依使用者指示）。
