# 2026-08-26 增量五丙段前端（Phase 67〜70）REP

> 對應 TODO：`docs/plan/todo/2026-08-26-增量五丙段67-69-TODO.md`（70 併入本階段）。
> 計畫檔：phase-67／68／69／70（皆 2026-08-26 校準版）。四個 phase 各由一個 Opus
> subagent 照計畫 TDD 執行（先紅後綠、紅綠證據都留在各自回報），我逐 phase 親自
> review 全部 diff。授權依據＝phase0826.md 明示指名執行 65〜72。

## 實作邏輯

乙段契約（202＋`GET /ingest-jobs` 八鍵 jobs＋pending_count）已穩，前端逐頁追上：
67 面板（全站唯一輪詢）→ 68 上傳頁（多檔、拆開鏈）→ 69 鏡頭（連拍、桌面拆開鏈）
→ 70 待決定（三關補完、`classify_chain.js` 零呼叫者後刪除）。

## 各 phase 成果與測試結果

### Phase 67 全站進度面板（507 → 514）
- 新檔 `app/static/progress_panel.js`（285 行）：`ppStart`／`ppStop`／`ppRender`／`ppDismiss`；
  2 秒輪詢、**對帳式渲染**（不整份重畫——保焦點、防 hover 閃爍）、失敗退避（2^n 至 30s、
  console 只印一次 info）、`document.hidden` 不打、visibilitychange 回前景立刻補一次、
  樂觀 dismiss（409 由下一輪長回來）、aria-live、全程 textContent。
- 五頁掛面板＋刪 Phase 53 的 25 行過渡 IIFE（先以 Python assert 五份 byte-identical 才動手）；
  camera-phone 刻意不掛。style.css 加 `pp-` 區塊、`.fm-backdrop` 補 z-index。
- 新檔 `test_progress_panel_contract.py` 7 顆＋`test_nav_header.py` 一顆原地換（不計顆）。
- **執行者裁決落地**：「分析中（第 0 次）」窗以 `Math.max(job.attempt, 1)` 顯示保護
  （零後端改動；計畫檔 §4.1 第 4 點已補裁決註記；契約測試未釘該字串、零同步成本）。

### Phase 68 上傳頁多檔選檔（514 → 517）
- `upload.html`：`multiple`、**每檔一個 POST 順序送**（第一個 202 就 `ppStart()`）、
  批次結果卡逐檔一行（415 這種同步擋下的檔只出現在這裡、不進面板）、
  拿掉 201 開鏈與三顆彈窗＋鏈的 script（檔案本身不刪）、文案改「先收下、背景分析、
  待決定歸類」＋D14 開關語意說明。收尾清空選檔框、解鎖按鈕。
- `test_progress_panel_contract.py` 追加 3 顆。

### Phase 69 鏡頭連拍與桌面拿掉開鏈（517 → 520）
- `camera-phone.html`：快門判 **202** 即解鎖可連拍；新增純本地窄條
  （`cp已送出` 計數，**不打任何 API**——總覽 §10 第 3 列裁決）；`uploaded` WS 通知保留。
- `camera-desk.html`：刪「`GET latest` → 三關彈窗鏈」42 行；`收到照片()` 改為
  解鎖＋計數＋`ppStart()`、**零彈窗**（D13）；WebRTC／QR／快門／閃光「不准動」片語
  grep 逐字保全（含 `iceServers: []`、`toBlob(...0.92)`、`settings.torch !== true`）。
- `test_progress_panel_contract.py` 追加 3 顆（含「鏡頭核心一個字都沒動」回歸護欄）。
- **執行者裁決落地**：camera-desk 檔頭過期註解（寫著已刪的 `_ingest_image`）改為現況。

### Phase 70 待決定走完整三關（520 → 523）
- `pending.html`：`開始三關()`→`接著釘實體()`→`接著確認待辦()`→`收工()`；
  三關建議全讀八鍵摘要（零再看圖）；兩個資料形狀轉換有註解釘住
  （suggested_entity 名字→整筆實體物件；`due` 鍵名＋ISO 格式）；
  「有待辦建議才開」＝ `(suggested_task_title || "").trim()` 空就收工；
  鏈收工才 `location.reload()`。
- `classify_chain.js` 過 grep 閘門後刪除（已入版控、可隨時找回）。
- +3 顆：跨端點名字契約 1（首跑就綠的回歸護欄——兩端都沒做正規化，
  agent 以反向驗證證明它真的會抓）＋新檔 `test_pending_chain.py` 2。

## 遇到的問題與解法

1. **Phase 69 計畫文案撞既有護欄**：計畫指定的手機頁文案含「待決定」三字，
   會紅 `test_nav_header.py::test_手機取景頁沒有頂欄`（以字串斷言「手機頁沒有導覽面」）。
   處理＝**改自己的新文案**（「稍後在電腦上歸類」）、不動別人的測試、護欄零弱化；
   桌面頁的「待決定」字樣照計畫保留（那頁有頂欄）。若產品負責人堅持手機頁逐字照計畫，
   需放寬 Phase 53 那顆斷言——留待裁決。
2. **行號漂移**：67 前基準 +26、67 後 +2（校準已預告）；70 的替換塊實際在 163〜192
   （校準警告的 const 重複宣告陷阱照本意整塊換，未踩雷）。
3. **Phase 69 agent 的基準探測**在正式庫留下照片 **#54**（真收據圖經真雲端入庫、
   `suggested_category=收據`）——已知、無害，正好作為瀏覽器驗收「無待辦建議」的三關素材。

## 測試方式與結果

- 每 phase：契約測試先紅（親眼）→ 實作 → 綠；`node --check`（70 另做四支 script
  合併檢查 1212 行，證明全域零撞名）；`\uXXXX` 逃逸掃描。
- 全量軌跡：507 → 514 → 517 → 520 → **523 passed＋0 skipped**；
  70 另跑 `OLLAMA_BASE_URL` 死埠＝523（零真模型實證）；端點全程 22。

## 尚未完成（延後項，非缺陷）

- **瀏覽器實操驗收（§6.2 各 phase 的人工項）延後到最後一輪**：Chrome 擴充同時連著
  兩個瀏覽器，自動化工具強制要使用者當面選定其一才准動作——與「需要 iPhone 的
  鏡頭連拍真機驗收」一起等產品負責人回座。環境已備妥：開發 overlay 已切
  （bind-mount 新 static 實測有被服務）、Redis 留著一筆失敗列當 × 素材、
  收件箱有 #53／#54 當三關素材、scratchpad 備好「測試帳單／測試風景」兩張圖
  （帳單圖含 2026-09-15 期限，供觸發待辦建議）。
