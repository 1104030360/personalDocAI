# 階段十七 REP：Phase 95 增量六錯誤收尾與驗收包

- 日期：2026-09-03
- 對應 TODO：`docs/plan/todo/2026-09-03-階段十七-增量六收尾95-TODO.md`
- 計畫檔：`docs/plan/unfinish/phase-95-增量六錯誤收尾與驗收包.md`（實作紀錄在其 §10）
- 工作區：`.superpowers/sdd/phase0903-1/`（`task-95-report.md`、`review-95.diff`、ledger R18）
- 結果：**完成**。顆數 702 → **716**（0 skipped）；review **Approved**（零 Critical／Important、11 Minor 交最終 review）；**首跑零真缺陷**

---

## 1. 實作邏輯

收尾 phase 的重心不是「再測一次功能」，而是把「**不該發生的事**變成看得見的斷言」：
敏感照片沒有被上傳、佇列裡沒有影像、工人沒碰資料庫、沒有人偷加端點或欄位、沒有人誤開 NAT／EIP——
這些成功時**完全沒有訊號**，只有掃碼測試守得住。做法：先盤點「誰已經測了」（§8 錯誤表 10 列 → 20 顆既有測試逐一 `--collect-only` 點名），
只補兩個真缺口；八顆掃碼；再把前輪 review 停放的四件小事與 93 review 的 minors 一起收掉。

```text
  §4.1 盤點（20 顆既有測試逐一點名）──► 【補A】【補B】首跑綠 ──► 【掃A】〜【掃H】首跑綠 ──► §4.3.3 停放項（① 先紅後綠）
                                                                                                │
  controller：§4.5 正式庫四查詢 ＋ §4.6 AWS 七項（dry run 全過）◄───────────────────────────────┘
                                                                                                │
  §4.7 驗收包（A 段實跑值；B〜E 留給產品負責人；D 段 Demo 3 ⏳ 待 push）＋ §4.8 TODO 進度總表 ＋ §4.9 歸檔清單（只寫不做）
```

## 2. 步驟（實際發生的順序）

1. controller 先 dry run §4.5／§4.6（read-only）：76 張／10 資料夾／1 收件箱／0 孤兒／16 欄；四服務、EC2 零 running、S3 空、佇列 0／0、NAT／EIP 空、SG In 空、Budget 5＋兩筆 80%。
2. controller 裁決 R18：93 review 的 deferred minors 併入本 phase（三處改斷言＋1 顆新測試）。
3. 實作者：§4.1 盤點（20 個 ✓ 全點得到）→【補A】【補B】首跑綠＋反向變異各紅 → 八顆掃碼首跑綠＋八次反向變異各紅 → §4.3.3 ①（`read_context`）**先紅（TypeError）後綠**、② `re.I` 正反證明、③ docstring、④a／④b 各 1 顆 → R18 四項 → 全量 716、三死埠合指／各單指 716、binder 27／0 skip、ruff 綠、tokenize → README×2／CLAUDE.md／LAUNCH.md 543→716 → 驗收包＋TODO 併入＋歸檔清單。
4. controller 同步總覽 §2.7／§9／§2.2（+14／716）與 §6 勾 93／94。
5. review（Opus）：Approved；reviewer 親算 716、重跑 regex 陷阱輸入、追 `gated_ingest.py` 三處呼叫證明 monkeypatch seam 正確、replay【掃E】【掃F】非空。
6. controller 親跑全量 716、ruff 綠；親讀所有新測試與唯一產品碼改動。

## 3. 測試方式

- §4.1：`pytest --collect-only -q` 對表；design6 §9「必釘」9 條關鍵字 collect（三個舊名隨 09-01 閘門改判作廢，由 `test_檔名完全不影響判斷` 守）。
- 補缺與掃碼：**首跑全綠**（＝74〜94 沒留真缺陷）；12 次反向變異全部親眼看紅（暫改設定檔／塞禁字／改斷言，還原後 `git diff`／`shasum -c` 乾淨）。
- §4.3.3 ①：RED `TypeError: 'int' object is not iterable` → GREEN；`test_cloud_roundtrip.py` 仍 2 passed（正常路徑沒被波及）。
- 全量 716／0 skipped；三死埠合指 716、各單指 716；三份規格 binder 27 顆零 SKIPPED；`git log -1 -- docs/spec/` 仍 `39e1c7e`、`-- test.yml` 仍 `4269985`；ruff 綠；tokenize 只剩 conftest 既有兩名。
- 機密掃描：驗收包／README／CLAUDE／LAUNCH／TODO／REP 零 12 位數字、零實例 ID、零 bucket 名。

## 4. 遇到的問題與解法

| 問題 | 解法 |
|---|---|
| 計畫 §4.2 的 import 段與中文 helper 名從未存在（實檔早已英文） | 校準時改成「合併」＋沿用 `PROJECT_ROOT`／`read_compose()`…；實作以 `ruff check --fix` 排序 |
| `ruff check --fix` 會先刪掉「還沒有人用」的 import | 貼完 §4.2＋§4.3 再補 import、再 `--fix` |
| §9「必釘」三個關鍵字點不到 | 不是缺口：RuleGate 時代舊名，改判後由 `test_檔名完全不影響判斷` 守（計畫的 `-k` 指令過期） |
| §6「多五行」與實際四行對不上 | `test_design6_error_paths.py` 開工時已是 `M`（93／94 未 commit）；§10 註明 |
| 顆數 715 vs 716 | R18 ② 多一顆；計畫 §10、驗收包 A 段、總覽 §9 三處註明 |

## 5. 測試結果

| 項目 | 值 |
|---|---|
| `pytest -q` | **716 passed, 0 skipped**（實作者與 controller 各跑一次） |
| 三死埠（合指／AWS／Redis／Ollama） | 716／716／716／716 |
| 規格 binder | 27 passed、0 SKIPPED |
| `test_design6_error_paths.py` | 27 passed（4＋2＋4＋6＋11） |
| 其他檔 | `test_cloud_worker_unit.py` 32、`test_gated_ingest.py` 22、`test_cloud_ingest_unit.py` 29 |
| ruff | 綠 |
| review | Approved；0 Critical、0 Important、11 Minor（交最終 review 分級） |
| 產品碼 | 只改 `app/workers/cloud_worker.py::read_context`（`_only_list`＋warning） |

## 6. 下一步

階段十八：最終 whole-branch review（Opus）→ 至多一波 fix → controller 親自 review → 收工驗證 → CLAUDE.md 概述段 → 記憶 → 最終訊息（含 Rulings 與待產品負責人清單）。
