# 2026-09-01 階段三：Phase 76 純重構五積木 ＋ Phase 77 雲端路契約與第五道安全網 REP

> 對應 TODO：`docs/plan/todo/2026-09-01-階段三-積木與雲端路契約76-77-TODO.md`。
> 計畫檔：`docs/plan/unfinish/phase-76-入庫任務拆成看圖與落庫.md`、`phase-77-雲端路契約與第五道安全網.md`。
> 全程不 commit；驗收用工作樹 tree 快照相減。

## 實作邏輯

**Phase 76（純重構）**：把 `run_ingest_job()`「看圖成功之後」埋在私有函式裡的三段抽成五個公開積木
（`PromptContext`＋`load_prompt_context`／`embed_understanding`／`insert_photo_with_files`／`finish_image_job`／`fail_job`），
Phase 79 的雲端路拿回 `result.json` 後直接沿用，避免兩份會漂移的同款程式碼（產品負責人「不留過渡產物」）。
`run_ingest_job` 簽章一字不改、log 字樣不改、既有測試零刪除行。

**Phase 77（契約與假件）**：雲端路四件事（探測遠端、放 S3、發 jobs、等 results）每件都會打 AWS；先訂契約再寫假件，
78〜81 的流程測試才不必連網。新檔 `cloud_ingest.py`（`MailboxMessage`、`CloudMailbox` 14 支 Protocol、`RemoteProbe`、
`AlwaysRunning`、`CloudRouteOff`、`build_context`；**零 boto3**）、config 九個設定（只有名字與預設值）、
`IngestJob` +`privacy`／+`route`、`dependencies.get_cloud_route()`（只認 `off`）、四顆假件（`FakeMailbox` 一顆同時扮 S3＋兩條佇列、
帶呼叫流水帳）、conftest **第五道 autouse 安全網 `wire_fake_cloud`**（`CLOUD_ROUTE` 蓋 off、`get_cloud_route` 雙管換替身、
`AWS_ENDPOINT_URL` 指死埠）。做完系統行為與增量五逐字相同。

## 步驟

| # | Phase | 做了什麼 | 結果 |
|---|---|---|---|
| 1 | 76 | 實作 subagent（Opus）：4 顆積木測試看紅（4×`AttributeError`）→ 整檔換掉 `ingest_job.py`（計畫 §4 步驟 3，校準時已三重驗證等價）→ 22 綠 → §6 驗收全過（`run_ingest_job` 簽章 diff 零輸出、AST 證兩支改名函式逐字相同、離線 harness 14 情境全「相同」、`tests/` 相減恰 `+126／−0 test_ingest_job.py`） | 全量 **570**（+4）、雙死埠 570、ruff 綠 |
| 2 | 76 | 審稿 subagent：自己抽計畫替換碼與實檔 diff → 零差異；52 條刪除行逐條可歸位；logger 字面零改動 | **Approved**，0 Critical／0 Important／5 Minor |
| 3 | 77 | 實作 subagent：12 顆看紅（`ImportError: get_cloud_route`）→ `IngestJob` 兩欄 → config 九設定 → `cloud_ingest.py` → `get_cloud_route` → 四顆假件 → 第五道安全網（把 `autouse=True` 拿掉實測 `KeyError: 'AWS_ENDPOINT_URL'`，證明安全網真的在擋） | 全量 **582**（+12）、三死埠 582、ruff 綠 |
| 4 | 77 | 審稿：八段實作碼與計畫逐位元相同；`CloudMailbox` 恰 14 支；鍵名對照 `STAGING_EXTENSIONS`；五道 autouse 順序正確；實跑兩檔 33 passed | **Approved**，0 Critical／0 Important／7 Minor（全 plan-mandated） |
| 5 | — | 我親自讀 `ingest_job.py` 全文、`cloud_ingest.py` 全文、`FakeMailbox` 族與 `wire_fake_cloud` | 與契約一致 |

## 測試方式

- TDD：兩個 phase 的回報都留 RED（積木／模組不存在）→ GREEN 證據。
- 全量：566 → **570** → **582**，`skipped` 恆 0，只有基線那顆環境層 warning。
- 零依賴實證：76 雙死埠（`OLLAMA_BASE_URL`／`CELERY_BROKER_URL`）570；77 起**三死埠一起指**（加 `AWS_ENDPOINT_URL=http://127.0.0.1:9`）582。
- 等價性（76）：計畫替換碼 vs 實檔 `diff` 零差異；AST 比對；離線 harness 14 情境；`tests/` 快照相減零刪除行。
- 掃碼：`cloud_ingest.py`／`dependencies.py`／`config.py` 零 boto3 import；`app/` 全樹 SQL token 掃碼綠（既有 `test_SQL只出現在repository與db層`，豁免字面 `pipe.execute()`）；端點 22；`docs/spec/`、`compose*.yaml`、`requirements.txt` 零改動。

## 遇到的問題與解決

| 問題 | 解決 |
|---|---|
| 76 的計畫檔顆數基線寫 564／568，實際 566／570（Phase 75 依 R10 多 2 顆） | 實作者在計畫 §8 補註；**要對的是 +4**（總覽 §9 已註記絕對值自 75 起 +2） |
| 76 重構後 `parse_content_time`＋`build_document` 進了 embed 的 try（原碼在外） | 裁決 R11 接受：兩者對合法 `PhotoUnderstanding` 不會 raise，分支不可達 |
| 76 後 `test_ingest_job_pdf.py:216／239` 兩處 docstring 仍寫舊私有名 | 不在 76 修（會破「`tests/` 零刪除行」最硬的驗收）；已排進總覽 §2.7 Phase 95 純註解校正 |
| 我給 reviewer 的掃碼措辭「`app/` 全樹無 `.execute(`」字面不成立（`ingest_job_store.py` 有 redis `pipe.execute()`） | 對照既有掃碼測試：token 是 `psycopg`／`get_connection`／`cursor(`／`.execute(`，豁免字面 `pipe.execute()`；workspace 三份規則檔已同步 |
| 77 的 `get_cloud_route()` 回傳註記引用 79 才存在的 `CloudRoute` | `from __future__ import annotations` 讓它只是字串；`app/` 無任何 `Depends(get_cloud_route)`；78 的 `celery_app` 直接呼叫（雙管的 monkeypatch 那一管攔得到） |
| 77 的 RED 是 `ImportError`（計畫寫 `ModuleNotFoundError`） | 測試檔 import 順序讓 `dependencies` 那行先炸；同一類「功能不存在」的紅，接受 |
| 77 的 `FakeProbe`／`ScriptedProbe`／`FakeCloudRoute` 在本 phase 零覆蓋（§2.7 只給 12 顆） | plan-mandated；78 起的測試會第一次執行它們，reviewer 逐行讀過三者邏輯正確 |

## 測試結果

| 項目 | 76 | 77 |
|---|---|---|
| 新增顆數 | 4（`test_ingest_job.py` 18→22） | 12（`test_cloud_ingest_unit.py` 11 新、`test_ingest_job_store_unit.py` 21→22） |
| 全量 | 570 passed、0 skipped | **582 passed、0 skipped** |
| 死埠 | 雙 570 | 三 582 |
| 端點 | 22 | 22 |
| 改到的檔 | `ingest_job.py`、`test_ingest_job.py` | `config.py`、`dependencies.py`、`cloud_ingest.py`（新）、`ingest_job_store.py`、`conftest.py`、`fakes.py`、`test_cloud_ingest_unit.py`（新）、`test_ingest_job_store_unit.py` |

**留給後續**：77 的七項 Minor（`logger` 未用到 79 才用、`JobStore` 執行時 import 對 87 工人端 import 重量的提醒、流水帳 `delete_objects` 只記數量）在 79／87 實作時順手考量，不改 77 契約。
