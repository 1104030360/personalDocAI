# 2026-09-02 階段十一：總驗收與親自 Review Phase 87〜91 — REP

> dev-prompt：`docs/plan/dev-prompts/phase0902-2.md`。ledger：`.superpowers/sdd/phase0902-2/progress.md`（裁決 R0〜R22 全列）。
> 對應 TODO：`docs/plan/todo/2026-09-02-階段十一-總驗收與親自Review87到91-TODO.md`。

## 1. 做了什麼（一句話）

五個 phase 各自的 review 收齊後，做**一次** fix wave（87 五項、89 兩項、90 兩項、91 兩項）＋一次 scoped re-review，再由 controller 親自對整條工作樹做最終 review 與驗證；全部通過，**零 commit**，AWS 與本機狀態都收乾淨。

## 2. 實作邏輯

- 各 review 的 Minor 不各自開 fix round（會多好幾席 review），而是集中成一波、由一位 Opus 實作者 TDD 做完，再一次 scoped re-review 逐條裁 ADDRESSED／NOT ADDRESSED。
- controller 的最終 review 不靠 reviewer 的話：自己讀 `app/` 三檔的 diff、五個 deploy 檔、LAUNCH §12、CLAUDE 小段、38 個新測試名，自己跑全量／三死埠／掃碼子集／ruff／tokenize／精確機密掃描／零改動區檢查／本機與 AWS 狀態。

## 3. 步驟與結果

### 3.1 fix wave（674 → 679）

| 項 | 內容 | 性質 |
|---|---|---|
| A1 | `read_context` 收到合法但非物件的 JSON（`[]`／`null`）會 `AttributeError` → 訊息不刪 → 毒訊息每 900 秒回來 → 改成 `isinstance(payload, dict)` 否則三份空清單 | **真 bug** |
| A2／A3 | 壞 context 分支測試；caplog 釘 `target=vlm_timing_target(vlm)`（拿掉會退成 `backend=local`） | 回歸鎖 |
| A4／A5 | PDF e2e 斷言本機 VLM 零呼叫；ast 掃碼加「零相對 import」 | 回歸鎖 |
| C1 | `Ec2Probe` 失敗也進快取：第二次 `is_running()` 仍只打一次 | 回歸鎖 |
| C2 | log 只印實例 ID 尾 4 碼（repo PUBLIC、log 常貼進報告） | **真 bug**（洩露線索） |
| D1／D2 | ARG／ENV／CMD 掃碼只看 cloud-worker stage 區段；`FROM` 數＝命名 stage 數 | 回歸鎖（變異證據附回報） |
| E1／E2 | env 範本「值不要加引號」註解；unit 加 `After=`／`Wants=network-online.target`＋「aws CLI 是 AL2023 內建」註解，user-data 內嵌段同步 | 文件／健壯性 |

re-review：11/11 ADDRESSED、零新 Critical／Important。三條 out-of-scope 觀察 parked 給 Phase 95（裁決 R21）：dict 但值不是 list 的 context.json 仍會 TypeError（只有本機 `build_context` 會產這個檔，機率極低）；`from_lines` 沒 `re.I`；測試模組 docstring 的顆數過期。

### 3.2 controller 最終驗證（全過）

| 檢查 | 結果 |
|---|---|
| `pytest -q` | **679 passed／0 skipped**（`-rs` 零 skipped） |
| 三死埠（AWS／Redis／Ollama 全指 `127.0.0.1:9`） | 679 |
| 掃碼子集（SQL 只在 repository、boto3 只在 aws_mailbox、celery_app 字樣、工人 import、端點、Dockerfile、compose） | 28 passed |
| ruff | format 114 檔已排好、check All checks passed |
| tokenize（九個改動 py 檔） | 零中文識別字（唯一例外＝conftest 既有 fixture 名，R12） |
| design3 子字串掃 `app/` | 零命中（repository／db 之外） |
| boto3 import 位置 | 只有 `app/services/aws_mailbox.py` |
| 工人檔頭 import | stdlib ＋ `config`／`ai_timing`／`pdf_service`／`vlm_service` |
| 精確機密掃描（追蹤＋未追蹤、.env 除外） | 帳號 ID／bucket 後綴／佇列 URL／真 SG・VPCE・VPC・subnet・RTB id 全部零命中 |
| 零改動區 | `docs/spec/`、compose 兩檔、`db/`、`app/static`、`app/api`、`app/repositories`、`celery_app.py`、`gated_ingest.py`、`ingest_job.py`、`aws_mailbox.py`、`tests/fakes.py`、`tests/conftest.py`、`requirements.txt` 全零改動 |
| 端點 | 22、DELETE 0 |
| 本機狀態 | `.env` `CLOUD_ROUTE=off`／`300`、`EC2_WORKER_INSTANCE_ID=` 空；開關 local；四服務 Up（dev overlay）；staging 空 |
| AWS 狀態 | 兩佇列 0／0、S3 documents/ 空；SG inbound `[]`；零 NAT、零 EIP、零 EC2 實例；Budget $5 |

工作樹自開工快照共 34 檔 +5662／−1040（含產品負責人自己放進來的 `docs/design/design7.md` 411 行）。

## 4. 遇到的問題與怎麼解決

- 本階段沒有新的技術問題；兩處真 bug 都是前面 review 抓到、集中在這一波修掉。
- 工作樹多了一份非本 session 產出的 `docs/design/design7.md`（增量七：雲端工人改 Lambda）——沒讀、沒動，只在最終訊息提醒；它可能讓 Phase 92（EC2 真機）被取代。

## 5. 留給產品負責人

1. **review 後 commit**（本輪零 commit）：34 檔；commit 後把 `docs/plan/unfinish/phase-87〜91` 與 83〜86 一起 `git mv` 到 `finish/`。
2. **★G2 是條件式通過**（裁決 R2）：憑據十條在階段九 REP §6。不認的話，AWS 上只要刪四樣免費資源（SG、S3 Gateway endpoint、IAM role＋instance profile、ECR repo）。
3. AWS Console 的「仍是 Free plan、未升 Paid」人工確認（CLI 看不到）。
4. `docs/plan/aws/` 五份新手步驟帶 `--profile personaldocai-admin`，這台機器沒有那個具名 profile（default 就是 admin）。
5. Phase 92 動手前先決定 design7（Lambda）是否取代 EC2 路線。
