# 2026-08-20 階段BB：計畫檔校準（phase-15／16／17）REPORT

## 做了什麼（一句話）

對照專案現況逐段實查三份計畫（phase-15／16／17），結論：**三份計畫與現況零矛盾、零過時內容，不需任何修改**——它們是 2026-08-20 以當前 master（79 passed）為基準寫成的，所有「改前」引文逐字相符、插入點全部存在、預期測試數一路對齊；dev-prompt `phase0820.md` 任務 1 完成，可進入開發（階段CC）。

## 實作邏輯

「校準」不是重寫，而是把計畫引用的每一個事實拿去和 repo 對質；對得上就留證據，對不上才動筆。本次全部對得上，因此本 REP 的價值在**證據清單**——之後 implementer 可以放心照計畫逐字執行。

## 步驟（核對了什麼、結果如何）

### BB1. 基線實查

| 項目 | 計畫聲稱 | 實查結果 |
|---|---|---|
| `pytest -q` | 79 passed | ✅ `79 passed, 1 warning in 2.89s` |
| 正式庫 photo | 2 列、category 皆「收據」 | ✅ id 1、2，category 皆「收據」、location 皆 Target |
| 測試庫 | 可隨意重建 | ✅ conftest 強制 `PersonalDocAI_test` |

### BB2〜BB3. Phase 15 核對（12 項全過）

1. ✅ `db/schema.sql` 現況＝v4 單表版（photo＋HNSW），與計畫「改版前」圖一致。
2. ✅ `photo_repository.py` 開頭 docstring、`PHOTO_COLUMNS`、`insert_photo` SQL 三段「改前」引文**逐字相符**。
3. ✅ `tests/conftest.py` 的 `clean_photo_table` 引文與檔首 docstring 逐字相符。
4. ✅ `test_photo_repository.py` 現有恰 **10** 個測試、最後一個是 `test_count_and_clear_photos`（新測試插入點正確）；`_insert_sample(**overrides)` 預設 category="收據"，計畫新測試的呼叫方式全部相容。
5. ✅ 預期紅燈數成立：新 4 測分別因 `repo.DEFAULT_FOLDERS` AttributeError（1 個）與 `row["folder_id"]` KeyError（3 個）而紅 → `4 failed, 10 passed`。
6. ✅ **「加欄不漏鍵」行為面證明**（本次校準最重要的一查）：`PHOTO_COLUMNS` 加四欄後，多出的鍵不會外漏——
   - `api/routers/photos.py` 組 `UploadResponse` 是逐鍵取值（`row["id"]`…），Pydantic 模型欄位固定；
   - `retrieval_service.row_to_document` 逐鍵取值組 Document；
   - `ask_workflow` 只讀 Document 的 `page_content`／`metadata["id"]`。
   → 對外回應（201 與 /ask）位元組級不變，79 個既有測試不會紅。
7. ✅ `tests/unit/test_photo_repository_unit.py` 只測 `to_vector_literal` 純函式，不 mock SQL、不斷言欄位清單——加欄不影響。
8. ✅ `clear_photos()` 仍被 `test_count_and_clear_photos` 使用，計畫「保留不動」正確；`TRUNCATE TABLE photo` 單表在 FK（photo→folder）方向下合法。
9. ✅ `migrate_folders.sql` 可重跑性逐句成立（`IF NOT EXISTS`×5、`ON CONFLICT DO NOTHING`、`WHERE folder_id IS NULL`、`SET NOT NULL` 重跑無害）；預期輸出行數（4 個 ADD＋1 個 SET NOT NULL＝5 行 ALTER TABLE、UPDATE 2、UPDATE 0）與正式庫 2 列現況一致。
10. ✅ 计畫圖二把遷移 ③④ 壓成一個泡泡描述（歸夾＋改 category），與逐步驟 ③④ 分列語意一致，非矛盾。
11. ✅ 名詞表、FAQ 與 design1.md §5／§6／§10 交叉一致（六筆種子原文逐字同 design1.md §5）。
12. ✅ 驗收 grep 預演：`grep -rlnE "SELECT |INSERT INTO|TRUNCATE|ALTER TABLE" app/ --include="*.py"` 現況只回 `app/repositories/photo_repository.py`。

### BB4. Phase 16 核對（6 項全過）

1. ✅ 測試檔引用的 `repo.insert_photo` 具名參數簽名與現檔一致；`config.EMBEDDING_DIM`＝1024 存在。
2. ✅ 種子 id 1〜6 依賴成立：P15 的 `reset_tables` 每測 TRUNCATE…RESTART IDENTITY＋重播，`create_folder` 後 `id == 7` 斷言可靠。
3. ✅ 五函式鍵名契約自洽（`photo_count` 只在 list／get、`FOLDER_COLUMNS` 四鍵、照片摘要四鍵不含 embedding）。
4. ✅ `count(p.id)`／`LEFT JOIN`／`GROUP BY f.id`（主鍵函數依賴）皆為 PostgreSQL 17 合法寫法。
5. ✅ 插入點「`DEFAULT_FOLDERS` 後接 `FOLDER_COLUMNS`、`reset_folders_and_photos()` 後接五函式、`search_by_metadata` 之前」與 P15 完成後的檔案布局相容。
6. ✅ 基線鏈 83→93 與 P15 的 79→83 對齊。

### BB5. Phase 17 核對（8 項全過）

1. ✅ conftest 插入點精確：`import pytest` 在第 10 行、`from app.core import config` 在第 12 行、`wire_fake_ai` 之後緊接 `client` fixture——計畫括號註記與現檔完全一致。
2. ✅ `tests/fakes.py` import 區引文（hashlib／math／datetime／Document）逐字相符；`FakeVLM` 為第一個 class，插入點存在。
3. ✅ `requirements.txt` 有「--- 資料庫 ---」與「--- AI 積木 ---」兩段，插入點存在；目前無 Pillow。
4. ✅ `app/core/config.py` import 區（`import os`＋`from dotenv import load_dotenv`）逐字相符；「--- 業務常數 ---」段與 `ALLOWED_CONTENT_TYPES` 存在。
5. ✅ `.gitignore` 現況恰為計畫「改成」清單去掉 `data/` 的前五行——追加一行即達目標狀態。
6. ✅ `grep -rn "from app.core.config import" app/ tests/`＝0 筆，全專案已是 `config.XXX` 慣例（DATA_DIR monkeypatch 可行的前提成立）。
7. ✅ 新單元測試 import 的 `make_png_bytes`／`make_jpeg_bytes` 由同計畫步驟 5 提供，順序自洽；`tmp_path` 在 autouse fixture 與測試函式間共享同一實例（pytest 語意），`test_測試期間DATA_DIR指向暫存目錄` 斷言成立。
8. ✅ 基線鏈：79＋4＋10＝93 開工、＋10＝103 收工，與 P15/P16 對齊。

### BB6. 結論

**三份計畫零修改**。與歷輪（階段D/H/L/P/T/X 均有實質改動）不同，本輪計畫本身就是今天以現況為底寫成，校準工作的產出是上述證據清單而非 diff。

## 測試方式

文件驗證工作：(1) 實跑 `pytest -q`；(2) psql 實查正式庫；(3) 三份計畫全部「改前」引文與現檔逐字 diff（Read 對照）；(4) 兩條驗收 grep 預演；(5) 行為面以實讀 router／retrieval／workflow 原始碼證明。

## 遇到的問題與解法

1. **Phase 15「不改對外行為」的隱形風險**：加寬 `PHOTO_COLUMNS` 會改變 repository 回傳字典的鍵集合——若任何一層是「整列透傳」就會漏鍵到 HTTP 回應。實讀三處呼叫端證明全是逐鍵取值，風險排除（見 BB2-6）。
2. **計畫圖二與逐步驟的編號差**（③ 泡泡寫了 ③＋④ 兩件事）：判定為圖面壓縮非錯誤，不動計畫。

## 測試結果

- `pytest -q` → **79 passed, 1 warning**（校準為純驗證，未動任何程式）。
- 校準核對 26 項全數通過，計畫檔零修改。
