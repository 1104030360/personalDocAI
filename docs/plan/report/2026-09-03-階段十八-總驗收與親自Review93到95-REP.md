# 階段十八 REP：總驗收與 controller 親自 Review（Phase 93〜95）

- 日期：2026-09-03
- 對應 TODO：`docs/plan/todo/2026-09-03-階段十八-總驗收與親自Review93到95-TODO.md`
- 工作區：`.superpowers/sdd/phase0903-1/`（ledger `progress.md` R0〜R25；`review-final.diff`、`review-final-fix.diff`、`task-final-fix-report.md`）
- 結果：**增量六 Phase 93〜95 全部完成、最終 review「With fixes」的 fix wave 已做完並 re-review**；未 commit（產品負責人指示）；Demo 3 待產品負責人 push

---

## 1. 實作邏輯

最終 whole-branch review 只做一次（Opus），拿整包工作樹差異（BASE_TREE `a990543f` → 收工）＋ ledger 裡三個 task 的 deferred minors 一起分級；
有 findings 就**一波** fix dispatch ＋ **一次** scoped re-review；殘餘由 controller 裁決進 ledger。
controller 另外親自逐檔讀：兩份 JSON、deploy.yml、`test_design6_error_paths.py` 全檔、三個新測試、`read_context` 改動、README／CLAUDE／LAUNCH 改動、驗收包。

```text
  review-final.diff（code／deploy／workflow／README／CLAUDE／LAUNCH／驗收包；排除計畫檔）
        │
        ▼ 最終 reviewer（Opus）：0 Critical、3 Important、7 Minor；deferred minors 逐條 must-fix／leave
        │
        ├─ controller 裁決 R19（維持 @vN＋檔頭註解）、R20（ignore-error=true）、R21（not-found 印 stderr）、R22（_only_list 不過濾元素，parked）、R23（CLAUDE.md／驗收包由 controller 親改）
        │
        ├─ ONE fix wave（Opus）：測試檔檔頭、LAUNCH 日期、README Size、deploy.yml 三處、計畫同步 ──► ONE scoped re-review
        │
        └─ controller 收工驗證：pytest 全量＋三死埠、ruff、tokenize、NBSP、機密掃描、零改動區、§4.5／§4.6 再跑一次
```

## 2. 步驟（實際發生的順序）

1. 最終 review（Opus）：Strengths 八項（OIDC 最小權限與結構斷言、workflow 零注入面、補缺測試走真行為、`read_context` 最小且與同函式降級路徑同規矩、regex 錨定可證、驗收包誠實）；Important 三項：#1 CLAUDE.md 概述段過期、#2 `@vN` 大版釘的供應鏈風險要明示決定、#3 workflow 從未跑過（首跑風險含 `cache-to: type=gha`）；Minor 七項；deferred minors 分級（95 ①②⑨⑩ must fix、其餘 leave、93 四項已於 R18 修）。
2. 裁決 R19〜R23（見 ledger 與驗收包追認表）。
3. fix wave（Opus，一波）：F1 檔頭（11 顆、四類相依、`AWS_SETTING_NAMES` 註解寫實話——實查 Phase 90 掃的是 `AWS_`／`S3_BUCKET`／`SQS_`／`CLOUD_ROUTE` 四個字串）；F2 LAUNCH 日期；F3 README Size（8,225／17,845，算法沿 77ae7bf）；F4 deploy.yml 檔頭 R19 段；F5 `ignore-error=true`（來源：Docker GHA cache backend 文件，**不是**通用選項）；F6 not-found 印 stderr（三個 aws stub 實跑：NotFound／UnauthorizedOperation／stopped 皆 exit 0 且 log 分得出）；F7 計畫 §4.3／§4.5 零差異、§7 兩條陷阱、§10.7。
4. controller 同時做 R23：CLAUDE.md 概述段（93〜95 收官整段＋「93〜95 已完成」＋「收工 716」）、「部署角色」段換機兩處同步警語；驗收包準備段改成不切模式的兩個 `-f`、C2 同步、追認表補 R12〜R22；R25 檔頭「分三次」→「分五次」。
5. scoped re-review（Opus）：9 項中 8 項 ADDRESSED；殘餘 #1（CLAUDE.md 另一處「尚未開始」）與三個文字數字（README 17,848、驗收包 23 條、CLAUDE.md R0〜R25）由 controller 依 R26 直接改畢（docs-only）。
6. controller 收工驗證（§3）。

## 3. 測試方式（收工驗證，全部實跑）

- `pytest -q`：**716 passed／0 skipped**（controller 親跑；fix wave 後三死埠合指亦 716）。
- ruff 兩句綠；tokenize 五個改動 .py 零非 ASCII 識別字（conftest 既有 `跑完任務`／`目前的任務清單` 例外）；六個改動文字檔 NBSP 0。
- 零改動區：`docs/spec/`、`compose*.yaml`、`Dockerfile`、`db/`、`requirements.txt`、`app/static/`、`app/api/`、`app/repositories/`、`app/services/`、`.github/workflows/test.yml`、`data/` 全乾淨；`app/` 只有 `workers/cloud_worker.py`。
- 精確機密掃描（帳號 ID／實例 ID／bucket 名，追蹤＋未追蹤、.env 除外）零命中；deploy／.github／README／CLAUDE／LAUNCH 零 12 位數字。
- §4.6 收工再跑：admin 身分；四服務 Up；EC2 零 running（.env 那台 t3.xlarge stopped）；S3 `documents/` 空；兩佇列 0／0；NAT／EIP `[]`；SG In `[]`／Out tcp 443；Budget 5 USD＋ACTUAL／FORECASTED 80；OIDC provider 1、role max session 3600、零 managed、inline policy 在；gh secret／variable 在。
- §4.5 收工再跑：76 張／10 資料夾／1 收件箱／0 無資料夾／0 不一致／0 無文字／16 欄；staging 孤兒 0。

## 4. 遇到的問題與解法

| 問題 | 解法 |
|---|---|
| CLAUDE.md 概述段仍寫 93〜95 未開始（最終 review Important #1） | R23：controller 補「增量六收官」整段、改兩句 |
| 六個 action 用 `@vN` 不用 SHA（Important #2） | R19：維持大版、檔頭明寫「已評估刻意接受」＋四道緩解＋改 SHA 的做法 |
| workflow 從未跑過、`cache-to: type=gha` 匯出失敗可能致命（Important #3） | R20：`ignore-error=true`（GHA backend 文件明列）；Demo 3 首跑「紅了先修 CI」已寫進計畫與驗收包 |
| not-found 分支吞掉權限錯（Minor #7） | R21：stderr 存檔並隨 state 印出 |
| dispatch 假設的 `CLOUD_` 前綴掃碼不存在 | 實作者寫實話；R24：四個非機密設定名不補掃碼（parked） |
| `ignore-error` 不是「BuildKit 通用選項」 | 實作者改寫註解、附 GHA backend 文件 URL |

## 5. 測試結果

| 項目 | 值 |
|---|---|
| `pytest -q` | **716 passed, 0 skipped** |
| 三死埠 | 716 |
| ruff | 綠 |
| 最終 review | With fixes → 一波 fix → re-review 8/9 ADDRESSED → 殘餘 R26 改畢 |
| 產品碼 | 只改 `app/workers/cloud_worker.py::read_context` |
| AWS／GitHub 副作用（全免費） | OIDC provider、role＋inline policy、secret、variable |
| 未做 | commit／push、Demo 3（需 push）、92-B（GPU 配額） |

## 6. 給產品負責人

1. review 工作樹 → commit → `git push origin main`（10 個既有 commit ＋ 本輪一起上；CI 自 08-28 沒跑，可能紅在別處——先修 CI、不動 `test.yml`）。
2. Demo 3 照 `phase-94` §4.8：改 `cloud_worker.py` 一行註解 → push → `test` 綠 → `deploy` 綠 → ECR `<sha>` → Start 92-A → `version=<sha>` → Stop。
3. 驗收包 `docs/plan/report/2026-09-03-增量六驗收包-請產品負責人確認.md`：A 段已填，B〜F 請親自點；追認表 R0〜R22。
4. `.env` 有兩行重複的 `CLOUD_ROUTE`／`CLOUD_RESULT_TIMEOUT_SECONDS`（後者生效）——建議刪掉前一組。
5. 歸檔：commit 時把 `docs/plan/unfinish/phase-93／94／95` 與總覽搬進 `finish/`（清單在 phase-95 §4.9）。
