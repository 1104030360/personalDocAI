# 2026-09-02 階段七：計畫檔校準 Phase 87〜91 — REP

> dev-prompt：`docs/plan/dev-prompts/phase0902-2.md`。ledger：`.superpowers/sdd/phase0902-2/progress.md`（裁決 R0〜R16）。
> 對應 TODO：`docs/plan/todo/2026-09-02-階段七-計畫檔校準87到91-TODO.md`。

## 1. 做了什麼（一句話）

把 2026-09-01 寫的五份計畫檔（87〜91）校準到 2026-09-02 晚間的工作樹現況（HEAD `bb3921a`，83〜86 已 commit），**識別字全部改英文**、顆數以實查 644 起算、AWS／docker／`.env` 操作標明由 controller 親做、閘門改成「看內容不看檔名」；零產品碼、零測試碼改動。

## 2. 實作邏輯

- **為什麼要先校準**：計畫檔寫在 Phase 81〜86 落地之前。之後三件事讓「照抄就錯」：產品負責人指示識別字一律英文（計畫檔有 `_把log印到終端機`、`假裝過了` 這類名字）；83〜86 已完成（S3／SQS 已建、`.env` 已填、`get_cloud_route()` 已接 assume、`AwsMailbox.__init__` 全 keyword-only）；2026-09-01 改判閘門只看內容。
- **怎麼校準**：controller 先實查工作樹寫成兩份簡報（`brief-common.md` 事實＋命名契約；`calib-brief.md` 逐人任務 A〜H），再派五個 Opus subagent 平行、一人一檔、只准改自己名下的檔；跨檔問題回報，由 controller 實查後裁決。
- **dev-prompt 的檔名錯位**：它把五個標題各錯位一格（例如「phase-88-EC2探測running」）；以編號 87〜91 為準（ledger Setup 段有對照）。

## 3. 步驟

1. 基線實查：`pytest -q` **644 passed／0 skipped**；容器 dev overlay、Ollama 開著、AWS admin profile 通、`.env` 變數名清單。
2. 建 SDD 工作區：BASE_TREE、snapshot-tree／review-package-tree、四份簡報、ledger（含 pre-flight 掃描表 27 列＋裁決 R0〜R10）。
3. 派 A/87、B/88、C/89、D/90、E/91 平行校準（各約 15〜20 分鐘）。
4. 收回報、實查跨檔問題、補裁決 R11〜R16；controller 親改總覽（§2.2／§9／§2.7 五處實查註記、P89 補 `CLAUDE.md`、P90 補 `.dockerignore`）與 88 一處（見 §5）。

## 4. 結果

| 檔 | 改動 | 重點 |
|---|---|---|
| phase-87 | 14 處（1466→1569 行） | 12 顆測試碼＋工人整檔碼英文化；端到端改用既有 `WorkerMailbox` 慣例；掃碼零違規子字串 |
| phase-88 | ~45 處（1293→1576 行） | 21 個識別字英文化（`_configure_logging`／`_install_stop_signal`／`stop_after_rounds`…）；閘門改內容判定九處＋Pillow 合成圖；加 ②b 開關撥 cloud 加速 |
| phase-89 | 41 處（1032→1287 行） | 三處「照舊做會紅」修正（`advance_clock_frozen`、`test_dependencies_cloud_unit.py` 實 3 顆、§4.5 貼實檔原文）；新增步驟 7 改 CLAUDE.md 過期句 |
| phase-90 | ~40 處（1468→1792 行） | helper 英文；R8 dev overlay＋image inspect；R10 第 4 顆多守 compose 零 AWS 字樣；G2 改條件式 |
| phase-91 | 38 處（1445→1572 行） | ★G2 框改 R2；`worker.env.example` 刪 `VLM_MAX_ATTEMPTS`；unit 與 user-data 內嵌段程式化比對 IDENTICAL |

顆數軌跡（實查）：644 → 87 **656** → 88 **661** → 89 **668** → 90 **672** → 91 **672**。

## 5. 遇到的問題與怎麼解決

- **簡報自己寫錯一條事實**：我寫「`OllamaCloudVLM.understand()` 內部已包計時 log、工人不必再包」。校準者 A 實查 `vlm_service.py` 沒有 `log_ai`（全站 `kind=vlm` 只有 `ingest_job.py` L403 呼叫端在包）。controller 複查屬實 → 三份簡報更正、裁決 R11（工人每次嘗試自己包 `log_ai("vlm", target=vlm_service.vlm_timing_target(vlm))`）。A 沒有照簡報把 log 刪掉，是對的。
- **校準者 B 一條判斷被推翻**：B 說「已送去雲端」那行 log 不存在並從 88 刪掉。controller 實查 `cloud_ingest.CloudRoute.submit()` L259 就是那行 INFO（只是不在 `gated_ingest`）→ 已改回 88 檔頭與 §4.6 ⑧。
- **★G2 與 dev-prompt 的矛盾**：總覽說 G2 由人確認、實作者不可自勾；dev-prompt 卻明示做到 91。裁決 R2：88 的 Mac 端到端與 90 的容器端到端由 controller 親跑，**兩者都通過才進 91**；91 全是免費資源且不啟動實例。產品負責人事後不認的話，刪 SG／endpoint／role／ECR 即可（免費、可逆）。
- **`AwsMailbox.__init__` 是全 keyword-only**（B 指出）→ 簡報更正，避免 89 的 ec2 分支寫位置參數。

## 6. 測試方式與結果

本階段零程式碼改動；`pytest -q` 基線 644 passed／0 skipped 未變。校準者自驗：五檔 NBSP 零、python 區塊 `ruff format --line-length 100 --diff` 零 diff、tokenize 掃非 ASCII 識別字只剩既有的 conftest fixture 名（`目前的任務清單`，R12 不在範圍）；D 的四顆掃碼對現況乾跑 3 紅 1 綠（與預期逐字相同）。

## 7. 留給後面階段的事

- CLAUDE.md 專案概述段仍寫「74〜80 未 commit／81〜95 尚未開始／★G1 前零 aws」→ 收尾階段一次改（R16）。
- `docs/plan/aws/` 五份新手步驟帶 `--profile personaldocai-admin`（本機沒這個 profile）——產品負責人的檔，未動。
- 不 commit 之下，Phase 90 烙進映像的 `WORKER_VERSION` 會是 `bb3921a`（R15）。
