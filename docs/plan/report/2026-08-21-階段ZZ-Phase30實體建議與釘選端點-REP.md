# 2026-08-21 階段ZZ：Phase 30 實體建議與釘選端點——REP

## 實作邏輯

design3.md D8／D12 的後端主體。三件事：①**VLM 契約一次擴齊**——`PhotoUnderstanding` 6→9 欄
（entity／task_title／task_due），prompt 同輪注入「現有實體清單」與待辦判斷規則；仍只有一次看圖呼叫，
多的是建議欄位不是第二個模型。②上傳回應把三個彈窗要的資料一次帶齊（`suggested_entity`／`entities`／
`suggested_task`）。③三個新端點（9→12）：`GET /entities`、`POST /photos/{id}/entities`（釘選，
檢查全過才寫、**不重算 embedding**）、`POST /photos/{id}/entity-suggestion`（「再建議一個」＝獨立的
**文字** LLM 呼叫，不重看圖；第六個注入點 `get_entity_suggester`）。

## 步驟

1. TDD 先紅（unit 10 顆）：`clamp_entity` 四情境（命中原文／大小寫空白命中／清單外 None／空清單 None——
   實體沒有「未分類」保底）＋prompt 注入斷言（實體段、待辦段、空清單「一律填 null」句、資料夾段不動）。
2. 綠：vlm_service 擴欄＋`build_vlm_prompt(folders, entities)`＋Protocol/OllamaVLM/FakeVLM/分頁VLM 簽名同步。
3. TDD 先紅（integration 25 顆）：釘選（201 兩路、一張多釘、自創清單+1、重名 409、重複釘 409、404×2、
   恰一 422×3、**釘選前後 fetch_embedding 相同**）；再建議（登記答案、exclude 生效、候選空回 null 且
   **零 LLM 呼叫**、模型挑清單外一律夾 None、404）；上傳回應（建議命中／清單外 None／空表兩欄位空、
   待辦建議三情境——含**到期日壞字串只丟 due 不炸上傳**，沿用 parse_content_time 的寬容）。
4. 綠：`schemas/entity.py`（PinEntityRequest 恰一驗證鏡射 AssignFolderRequest；EntityOut 由 photos.py
   import 共用不複製）→ `entity_suggestion_service.py`（單次嘗試＋log fallback——建議可有可無，失敗回
   null 不 500，註解記明取捨）→ `routers/entities.py`（零 SQL；實體用 list_entities() 挑免加 SQL）→
   photos.py／dependencies／conftest（六注入點）／fakes（FakeEntitySuggester）。

## 測試方式與結果

- 實作者（opus subagent）：RED（兩檔 ImportError＋2 failed）→ GREEN；全量 **207 passed**（172＋35）。
- Controller 複驗：`pytest -q`＝207、零 Ollama＝207；`/openapi.json` 清點 **12**；逐檔 review 全部 diff。

## 遇到的問題與解法

1. `test_pdf_upload.py::test_單圖上傳回應形狀不變` 釘死回應鍵集合——依裁定擴 3 鍵（守門測試，非規格檔）。
2. 既有 `test_photo_understanding_只有六個欄位` 更名九欄版——計畫明訂的唯一既有測試更新。
3. 釘選「先建實體再建連結」兩條 SQL 非交易——沿 `assign_folder`「靠排序不靠交易」先例：
   檢查全過才開始寫，失敗只可能發生在最後的 INSERT，不會留半套狀態。

## 備註

- 新增 6 檔、修改 9 檔；不 commit。規格 .feature 本 phase 零修改。
- Playwright 瀏覽器驗收裁定：統一由 controller 在前端 phase（31／33）落地後親自跑（正式庫為真資料）。
