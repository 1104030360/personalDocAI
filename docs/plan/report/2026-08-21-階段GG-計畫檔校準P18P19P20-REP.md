# 2026-08-21 階段GG：計畫檔校準（phase-18／19／20）——REP

## 實作邏輯

dev-prompt（`phase0821.md`）任務 1 要求：動工前先依專案現況逐個更新三份計畫檔。三份計畫寫於 2026-08-20（Phase 15〜17 完成當天），內含大量「逐行行號引用」與「既有測試逐一點名」，所以校準方式不是重寫，而是**逐條核對計畫引用 vs 程式現況**，只修真正過時的地方——過度改寫反而會製造新的不一致。

## 步驟

1. **讀齊依據**：`design1.md`（增量 canonical design）、`phase-00-增量總覽.md`、三份計畫檔全文、`phase-21`（確認範圍邊界：歸類端點不在本輪）。
2. **逐檔核對程式現況**（結果全部吻合，無需改計畫）：
   - `app/services/vlm_service.py`：`VLM_PROMPT` 常數在 33〜51 行、`VLMClient.understand` 簽名在 63 行、`OllamaVLM.understand` 在 78 行起、prompt 組訊息在 84 行——與 phase-18 引用一致。
   - `app/api/routers/photos.py`：「② 看圖」在 38〜39 行、`photo_repository` import 在第 10 行、35 行有 v4 舊註解——與 phase-18／19 引用一致；全專案 `understand()` 呼叫端僅此一處（grep 實查）。
   - `photo_repository.py`：`PHOTO_COLUMNS` 已含三路徑欄、`reset_folders_and_photos()`＋五個資料夾函式＋兩條檢索查詢俱在；`fetch_photo`／`count_photos`／`fetch_embedding` 可供新測試直接用。
   - `storage_service.py`：`save_original`／`make_thumbnail`／`absolute_path`／`remove_if_exists` 四個公開函式、路徑約定 `data/` 開頭相對路徑。
   - `tests/fakes.py`：`FakeVLM`（尚無 folders 參數，待 P18 改）、`make_png_bytes`／`make_jpeg_bytes` 已備、尚無 `make_large_png_bytes`（P19 會加）。
   - `tests/conftest.py`：三道 autouse 安全網（`reset_tables`／`wire_fake_ai`／`isolated_data_dir`）＋`first_row`／`split_items` 俱在。
   - 六個計畫點名的測試檔（`test_vlm_service_unit`、`test_upload_feature` 21 行、`test_upload_bilingual` 14 行、`test_upload_design_rules` 19 行、`test_photos_upload` 75 行、`test_error_paths` 70 行）假位元組位置**行號全對**。
3. **開工基線實查**：`pytest -q` → **103 passed**（與 CLAUDE.md、總覽 P17 累計一致）；`上傳照片.feature` 現為 7 條 Rule／7 個 Example；`data/` 已在 `.gitignore`；`scripts/check_embedding_dim.py` 不呼叫 `understand()`（phase-18 Q8 的宣稱屬實）。
4. **修正三類過時處**（見下）。

## 改了什麼（三份計畫檔＋總覽）

| 檔案 | 修正 |
|---|---|
| `phase-18` | 基線由「79＋P15〜17 各自新增」釘死為 **103**（2026-08-21 實查）；步驟 7／驗收清單／完成狀態補「＋7 ＝ **110**」 |
| `phase-19` | ①前置條件「storage_service 五個函式」→ **四個公開函式**（save_original／make_thumbnail／absolute_path／remove_if_exists）；②步驟 2 新函式位置由「reset_folders_and_photos 之後、search_by_metadata 之前」改為「**reset_folders_and_photos 之後、Phase 16 的 list_folders() 之前**」（原描述沒算到 Phase 16 已插入的五個資料夾函式）；③基線釘死 **110**、完成後 **121** |
| `phase-20` | 基線釘死 **121**、完成後 **124**（規格 Example 7→10） |
| `phase-00-增量總覽` §5 | P18／19／20 累計欄依計畫填入 **110／121／124**（打勾留待各 phase 完成） |

**沒改的**：三份計畫的所有程式碼片段、行號引用、測試點名、驗收清單邏輯——核對後全數與現況一致，維持原文。計畫內的 git commit 步驟保留原文，但本輪依使用者指示**先不 commit**（於各階段 REP 註記）。

## 測試方式與結果

- 校準本身不動程式碼；驗證＝基線實查 `pytest -q` → `103 passed, 1 warning`（warning 為 Starlette TestClient 的既有棄用警告，與本輪無關）。
- 三份計畫更新後互相對得上：P18 完成 110 → P19 開工基線 110 → 完成 121 → P20 開工基線 121 → 完成 124。

## 遇到的問題與解法

- **問題**：phase-19 對兩個新 repository 函式的擺放描述寫於 Phase 16 完成前的視角（「search_by_metadata 之前」），現況中間隔著五個資料夾函式，照字面放會拆散「照片寫入類」聚落。**解法**：把位置描述改精確（`reset_folders_and_photos()` 之後、`list_folders()` 之前），維持原意「寫入類聚在一起」。
- 其餘引用逐條核對皆準，無需其他修正。
