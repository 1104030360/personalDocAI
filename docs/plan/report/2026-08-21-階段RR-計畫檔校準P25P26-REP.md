# 2026-08-21 階段RR：計畫檔校準（phase-25／26）——REP

## 實作邏輯

dev-prompt `phase0821-2.md` 任務 1：動工前校準最後兩份計畫。兩份寫於 2026-08-20（P15〜17 完成當天視角），與現況的落差比前兩輪大——不只基線數字，還有「CLAUDE.md 已逐輪更新」「15〜24 計畫已歸檔」「本輪先不 commit」三個狀態性前提改變，需要改寫兩個步驟的指引而非只填數字。

## 步驟與修正內容

**查證**（改計畫前先實查）：基線 `pytest -q`＝**140**；`a4e44ef`（feat: Phase 11〜14）＝增量前基線 commit（原計畫的 grep「Phase 14」在歷史訊息裡撈不到——訊息寫的是「Phase 11〜14」）；`自然語言詢問.feature` 自 init commit（`64c412f`）後**沒有任何 commit 動過**（單筆 log，比 diff 更強的證明）；P26 步驟 5a 預期的 class／id（`#ask-form`／`#question-input`／`#result` 等）與現況逐一相符；寫檔 grep 預跑僅命中 `storage_service.py`；`unfinish/` 剩 3 份。

**phase-25 六處修正**：
1. 基線釘 **140**、完成後 **149**（含步驟 4c 零 Ollama 預期＝149）。
2. 步驟 0(b)：基線 commit 改釘 `a4e44ef`（原 grep 撈不到）。
3. 步驟 3(b)：diff 對象釘 `a4e44ef`＋補「該檔僅一筆 log」的更強證明指令。
4. 步驟 5 SQL grep：泛用「UPDATE 」pattern 會誤中 `photos.py:225` 的 P21 計畫指定中文註解（階段QQ 已查證非 SQL）→ 改精確 pattern「UPDATE photo」。
5. 步驟 8（CLAUDE.md）改寫為**增量式收尾**：原文假設 CLAUDE.md 停在 79 tests、要求一次寫入整段長敘述；實際 P15〜24 成果段已逐輪寫入（現況 140 顆）→ 改為 header 改「全數完成」＋追加 P25〜26 短段＋140→149＋指令段只補一條（`migrate_folders.sql` 已存在不重複）＋陷阱段一條。
6. 步驟 9／10（歸檔＋commit）：`git mv` 會 stage、與「先不 commit」衝突 → 延後至使用者要求 commit 時執行；範圍修正為剩餘 **3 份**（15〜24 十份已於前兩輪歸檔）。驗收清單同步註記。

**phase-26 兩處修正**：
1. 基線釘 **149**（做完必須仍 149）。
2. 步驟 8 ⑤「零 Python／測試變動」的 git status 檢查：本輪不 commit，P25 合法產出會出現在輸出 → 判準改為「相對 P25 完成時快照，P26 只動 `app/static/` 五檔」。

## 測試方式與結果

校準不動程式碼；所有修正值皆有實查證據（pytest、git log、grep）。數字銜接：140 →(P25+9)→ 149 →(P26+0)→ 149。

## 遇到的問題與解法

- 本輪校準是三輪中幅度最大的（兩個步驟改寫而非填空），原因是 P25 身兼「總收尾」角色、其指引綁定撰寫當時的文件狀態。修正原則維持：改「與現況矛盾的指引」，不動「行為與驗收標準」。
