# 2026-08-19 階段W：總驗收與收尾 REPORT

## 做了什麼（一句話）

Phase 11〜12 落地後的總驗收：主控親跑全部留給主控的驗收項（死埠全量、SQL 變異測試、真模型中英煙霧）、**親自逐行 review 全部工作樹變更**、逐項核對兩份 phase 計畫的每個想法皆已實現，最後歸檔計畫並校正 CLAUDE.md——**dev-prompt `phase0819-4.md` 全部任務完成，工作樹保持未 commit 交付使用者**。

## 實作邏輯

- **驗收分層**：implementer 只碰假件；真模型與破壞性驗證（變異）由主控親手做、做完立即還原並以 `git diff` 確認零殘留。
- **綠燈不夠，要證明測試有牙齒**：變異測試（把產品行為改壞看測試會不會抓到）由 reviewer 以 monkeypatch 做 7 組、主控再照計畫原文的註解法親手複證時間過濾那組。
- **真模型只驗格式與大方向**（非決定論，不當驗收依據）；查法與行為的正式驗收全靠假件測試（12 條 Rule）。

## 步驟與測試結果

1. **W1 全量回歸＋零 Ollama 證明**：`pytest -q` = **67 passed, 1 warning**（2.33s）；`OLLAMA_BASE_URL=http://localhost:9 pytest -q` = **67 passed**（P12 驗收 5——任何測試偷打真模型都會連線爆錯，實證安全網覆蓋全部注入點）。
2. **W2 變異測試（P12 驗收 4）**：暫時停用 `search_by_vector` 的 `COALESCE(content_time, uploaded_at::date)` 時間條件 → `test_最近的照片以內容時間優先過濾缺漏時用上傳時間` **FAILED**（1 failed, 6 deselected）→ 還原 → **1 passed** → `git diff -- app/repositories/photo_repository.py` 空（零殘留）。時間過濾確實由 SQL 在守。
3. **W3 真模型煙霧（P11 驗收 5〜8，需 Ollama＋正式庫既有照片）**：
   - 驗收 5：uvicorn 起於 8000，`openapi.json` paths = `['/ask', '/health', '/photos']`（三端點齊）。
   - 驗收 6（中文）：「有哪些在 Target 拍的收據？」→ `search_mode="metadata search"`、`retrieved_photo_ids=[1]`、answer 以繁中正確引用照片內容（Target／可樂／洋芋片／2026-08-10），三鍵齊。耗時 2m47s（本機 gemma4）。
   - 驗收 7（英文）："What drinks did I buy recently?" → `search_mode="vector semantic search"`、answer = "You bought Cola (可樂)."——**英文句、照片內容原文保留**，正是 design.md §8.3 鐵律 3 的行為。
   - 驗收 8：回應鍵排序 = `['answer', 'retrieved_photo_ids', 'search_mode']`。
   - 服務已乾淨關閉（port 8000 釋放、行程消失）。
4. **W4 主控親自 review**：逐行重讀 `git diff HEAD -- app tests` 全部 9 檔（app 5＋tests 4，+283/−15 等級）——分層邊界（零 SQL 進 service/router）、可驗證欄位不經 AI、查無路徑無短路、prompt 三鐵律逐字、產品碼零語言偵測、conftest 安全網五注入點、BDD glue 無恆真式，全部成立；並親跑單檔驗收複核（endpoint＋feature 12 passed；upload＋ask 14 passed；grep 鐵律＝1；Rule 數＝5）。dev-prompt 約束 5 核對：**兩份計畫的每一個步驟與驗收項都有 implementer、reviewer 或主控的實跑證據**。
5. **W5 歸檔與校正**：`phase-11`／`phase-12` 計畫移入 `docs/plan/finish/`（unfinish/ 剩 13〜14）；CLAUDE.md 現況段改「Phase 01〜12 已完成」＋詢問端點與規格驗收摘要＋67 tests＋路線圖 13〜14；總覽 conftest 行補「P12 安全網併入詢問假件」；`phase-13` 計畫加 📌 遞延事項備忘（category 過濾補測＋三項 Minor 清理）；四份 REP（T/U/V/W）與五份 TODO 全數就位。

## 遇到的問題與解法

1. **P12 驗收 5 的 brew 指令在本機不可行**（Ollama 為 App 版）→ 階段T 已校準為死埠法，本階段照新指令實跑通過。
2. **審查期間不能做變異測試**（reviewer 讀檔中、變異會誤導）→ 調整順序：W1/W3（不改檔）與審查並行，W2 等審查完成後才動手。
3. **真模型單次問答 2〜3 分鐘**：屬本機 gemma4 正常延遲，煙霧只跑三次必要請求並即時關閉服務。

## 最終狀態

- `pytest -q` = **67 passed**（12 條 Rule 全綠：上傳 U1〜U7＋詢問 Q1〜Q5）。
- 工作樹**未 commit**（依使用者指示）；`docs/spec/**` 零異動；正式庫僅煙霧查詢、無寫入。
- 遞延事項全數記錄於 ledger（`.superpowers/sdd/phase0819-4/progress.md`）與 `phase-13` 計畫 📌 備忘。
