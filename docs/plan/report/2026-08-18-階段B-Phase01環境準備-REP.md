# 2026-08-18 階段B：Phase 01 環境準備 REPORT

## 做了什麼（一句話）

照更新後的 `phase-01-環境準備.md` 完成三塊地基——Python 3.12 虛擬環境＋全部套件、PostgreSQL@17（5433 埠）＋pgvector＋兩個資料庫、Ollama 的 gemma4 與 bge-m3——**9 條驗收全數通過**。

## 實作邏輯

- 最耗時的模型下載（gemma4 9.6GB、bge-m3 1.2GB）最先以背景啟動，與其餘安裝並行，總工時最短。
- 「測試先行」的形式：每一步完成立即執行 phase-01 對應驗收指令並比對預期輸出（pytest 測試依計畫從 Phase 05 開始，不提前建立）。
- PostgreSQL 依階段A 決策裝 @17 於 **5433**，全程不碰既有 @14。

## 步驟與結果

| # | 動作 | 結果 |
|---|---|---|
| B1 | `uv venv --python 3.12` | ✓ Python 3.12.7（沿用 brew 既有直譯器） |
| B2 | 建 `requirements.txt`＋`uv pip install -r` | ✓ 全部套件安裝成功（含 fastapi／psycopg3／langchain-ollama／langgraph／pytest-bdd） |
| B3 | `brew install postgresql@17` → conf 檔尾追加 `port = 5433` → `brew services start` | ✓ @17＝17.11 於 5433 上線 |
| B4 | `~/.zshrc` 追加 PATH＋`PGPORT=5433`（冪等寫法）；`brew install pgvector` | ✓ pgvector 0.8.6，extension 檔已在 @17 目錄 |
| B5 | `createdb visual_memory`／`visual_memory_test`＋兩庫 `CREATE EXTENSION vector` | ✓ 兩庫皆 CREATE EXTENSION 成功 |
| B6 | 背景 `ollama pull gemma4`／`bge-m3` | ✓ 兩個模型下載完成（gemma4:latest 9.6GB、bge-m3:latest 1.2GB） |

## 測試方式與結果（phase-01 驗收 9 條）

| 驗收 | 預期 | 實際 | 判定 |
|---|---|---|---|
| 1 Python 版本 | Python 3.12.x | `Python 3.12.7` | ✅ |
| 2 套件 import | `python ok` | `python ok` | ✅ |
| 3 正式庫 pgvector | `vector`＋`(1 row)` | `vector`＋`(1 筆資料)`（中文 locale 的同義輸出） | ✅ |
| 4 測試庫 pgvector | 同上 | 同上 | ✅ |
| 5 Ollama 服務 | `{"models":[` 開頭 JSON | ✓ | ✅ |
| 6 兩個模型在列 | gemma4＋bge-m3 | `gemma4:latest`、`bge-m3:latest` | ✅ |
| 7 中文向量 | `embeddings:[[` ＋小數 | ✓ | ✅ |
| 8 英文向量 | 同上 | ✓ | ✅ |
| 9 gemma4 看圖 | 回答出文字即可 | 「這張圖片是**白色**的。」（且顏色答對） | ✅ |

**安全確認**：驗收後再次 `psql -p 5432 -l`——@14 的三個資料庫（postgres、wanderlove、fse_chat_room）原封不動。

## 遇到的問題與解法

- **pgvector 對應版本疑慮**：brew 的 pgvector 同時鋪了 @17 與 @18 的 extension 檔，@17 的 `CREATE EXTENSION` 直接成功，無需 FAQ Q2 的重裝流程。
- **@17 預設埠衝突**：照階段A 決策以 conf 檔尾追加 `port = 5433` 解決；`brew services start` 一次成功。
- 其他無阻礙。

## 下一步

階段C：Phase 02 分層專案骨架（21 個 `.py`、config、schemas、/health）。
