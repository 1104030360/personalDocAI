# 階段EEE REP：Phase 34〜37 四份計畫檔對現況校準（phase0822 任務 1）

> 日期：2026-08-22　狀態：✅ 完成
> 對應 TODO：`docs/plan/todo/2026-08-22-階段EEE-四計畫檔校準-TODO.md`

## 實作邏輯

四份計畫寫成於 Phase 28〜33 之前／之中；35、36 已由產品負責人於 2026-08-22 補「已釐清」
（P35 選 B＋D、P36 定案路線 B），方向都不必改。缺的是**對照現有程式碼的落點**：
介面簽名要動哪幾處、新欄位怎麼遷移、端點數變多少、測試寫哪個檔。本階段把這些先寫死，
後續 TDD 實作直接照抄，不邊做邊猜。

## 步驟

1. 讀四份計畫＋`design3.md`＋增量三總覽。
2. 通讀 `app/` 現況（ask_workflow／retrieval_service／photo_repository／vlm_service／
   photos router／ask router／config／dependencies／main）＋`tests/`（conftest／fakes）＋
   `db/`（schema／migrate_design3）＋前端結構（browse.html 分頁、folder_modal 介面）。
3. 跑 baseline：`pytest -q`＝**218 passed**；git HEAD＝`0cabb45`（工作樹程式碼區乾淨）。
4. 逐檔加「## 2026-08-22 對現況校準」段（方向不動、只補落點），總覽 §3 補端點數註記。

## 每檔補了什麼（摘要）

| 檔 | 核對結果 | 補充重點 |
|---|---|---|
| phase-34 | 名稱／位置全對、端點不變正確 | route 介面加實體名單（OllamaRouter／FakeRouter／ask.py 三處同步）；task Document 的 `metadata["id"]`＝來源照片 id（`retrieved_photo_ids` 契約不變）；`list_photos_with_entity` 欄位對齊 `row_to_document`；`search_tasks` 語意（None＝全部、有值排除無期限）；「這週」＝7 天；測試檔＝`test_ask_three_paths.py` |
| phase-35 | 表已建正確；B／D 決策已入檔 | **`suggested_category` 要遷移**（schema＋migrate_design3 冪等 ALTER＋正式庫執行）；`PHOTO_COLUMNS`／`insert_photo` 擴欄；`understand()` 第五參數 corrections（protocol／OllamaVLM／FakeVLM／router 四處同步、PDF 各頁共用）；摘要四鍵→五鍵（待決定畫①的資料路徑）；casefold 比對；record 失敗不影響歸類 |
| phase-36 | 路線 B 已釐清段完整 | **端點 14→17（WS 不進 openapi）**；QR 定案 segno；session 細節（token_urlsafe／monotonic 時基可注入／新建汰舊／桌面斷線失效）；WS 純 relay＋拒連規則；LAN IP 推導；camera photos 轉呼叫 `_ingest_image()`；mkcert 含 iPhone 信任 CA 步驟；自動化測試邊界（真機驗收＝產品負責人手動） |
| phase-37 | 模式正確 | 錯誤表 +5 列（P34 待辦查無、P35 record 失敗、P36 三列）；端點數改 17；健檢含 suggested_category；「明確不做」補無雲端信令等；歸檔隨 commit（本輪不 commit → 留待） |
| 總覽 §3 | — | 補「下輪 P36 +3 → 17」與 P35 加欄註記 |

## 遇到的問題與解法

- **CLAUDE.md 說「31〜33 未 commit」與 git 不符**：`git log` 實證 31〜33 已含在
  `0cabb45`。不動 CLAUDE.md（那是 Phase 37 的 CLAUDE.md 現況更新一併修正），先記錄。
- **P35 待決定畫①缺資料路徑**：計畫只說「資料來自照片上存的建議」，但 GET /folders/{id}
  摘要恰四鍵拿不到建議。裁決：摘要加第五鍵 `suggested_category`（design1 四鍵規則由
  P35 明文修訂），已寫入計畫。
- **P37「端點數與總覽 §3 一致」會失真**：總覽 §3 只寫到 14，P36 落地後是 17。
  兩邊同步補註記，P37 直接寫 17。

## 測試方式與結果

本階段純文件（`docs/plan/` 五檔），不動程式碼——以 baseline `pytest -q`＝218 passed
證明起點乾淨；四檔更新後開工順序與依賴（34→35→36→37）在各檔內自洽。
