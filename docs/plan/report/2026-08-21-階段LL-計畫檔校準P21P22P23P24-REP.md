# 2026-08-21 階段LL：計畫檔校準（phase-21〜24）——REP

## 實作邏輯

dev-prompt `phase0821-1.md`（使用者於本 session 進行中放入，接續上一輪 P18〜20）任務 1：動工前逐個更新四份計畫檔。校準方式同階段GG——逐條核對計畫引用 vs 程式現況，只修真正過時處。

## 步驟與核對結果

1. 讀四份計畫全文（21〜24），逐項核對前置條件與程式引用：
   - **P21**：前置 P15／16／20 全就緒（`get_folder`／`find_folder_by_name`／`create_folder`、`FolderOut`、`_folder_out()`、`PHOTO_COLUMNS` 含四個新欄位）；測試檔 8 顆（5 具名＋parametrize 3）與計畫預告一致；步驟 3 擺位描述（`delete_photo` 之後）與現況相容。
   - **P22**：`list_folders`／`get_folder` 回傳鍵（id/name/description/is_inbox/photo_count）與 `FolderWithCount(**row)` 完全對得上；`list_photos_in_folder` 回 id/text/uploaded_at/thumbnail_path 與 `PhotoSummary` 需求一致；`main.py` 第 9、13〜14 行引用行號實查全對；端點數預期（`@router` 7＋`@app` 2）與 P21 完成後的現況推演相符。
   - **P23**：Phase 20 的 201 回應四欄位已落地（上一輪實測）；`PATCH` 是 P21 的產出（依序做即就緒）；現況 `upload.html` 將被整檔替換，無相容問題。
   - **P24**：`upload.html`／`ask.html` 現況導覽列恰為計畫步驟 2(b)／3 引用的兩連結版本；`GET /` 轉址仍指上傳頁；全 static 現況無 `alert`／`confirm`／`prompt`。
2. **唯一過時處＝基線顆數**（四份計畫寫於 P18〜20 完成前）。已釘死：P21 開工 **124** → 完成 **132**；P22 基線 **N＝132** → 完成 **140**；P23／24 前後皆 **140**（零新增測試）。共修 13 處。
3. `phase-00-增量總覽.md` §5：P21＝132、P22＝140 填入（P23／24 本就標「不變」）。

## 測試方式與結果

校準不動程式碼；基線 124 由上一輪階段KK 的最終驗證背書（`pytest -q` → 124 passed）。四份計畫更新後數字互相銜接：124 →(P21+8)→ 132 →(P22+8)→ 140 →(P23/24+0)→ 140。

## 遇到的問題與解法

- 無。四份計畫除基線數字外全部與現況吻合，維持原文。
- 執行方式沿用上一輪：後端（MM／NN）由 Opus subagent TDD 實作；前端（OO／PP）實作後以 Playwright MCP 做瀏覽器實操驗收（專案 CLAUDE.md 指定工具）；每階段我親自 review；全程先不 commit。
