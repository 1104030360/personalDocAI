# 2026-08-21 階段DDD：增量三本輪（Phase 28〜33）總驗收與親自 Review——REP

## 實作邏輯

Phase 28〜33 全數落地後的收官驗收，全程由 controller 親自執行。中途產品負責人指示
「**做完 Phase 33 後就停下來**」——驗收據此收斂為「Phase 33 完成定義」的緊湊版
（彈窗鏈與三分頁實測、curl 錯誤路徑、回歸快照），PDF 真模型瀏覽器實測裁掉（理由見下）。

## 測試方式與結果

**回歸快照**：`pytest -q`＝**218 passed**＝`OLLAMA_BASE_URL=http://localhost:9` 同顆數（零外部依賴）；
`/openapi.json` 端點＝**14**。

**Playwright 實操（真伺服器 :8000＋真 gemma4＋正式庫）**：

1. **待決定補完鏈（#21 繳費單）**：點卡→抽屜窗（無×、Esc 無效、無①、下拉排除收件箱——design2 回歸）→
   歸「文件」→**實體窗自動開**（實體清單空→①②隱藏、④＝「不釘，繼續」）→③自創「我的 MacBook」→
   **窗不關**＋已釘列表＋④變「完成，繼續」＋②出現＋輸入清空→「再建議一個」候選空→
   「沒有其他適合的實體了。」→②重複釘→**409 紅字在窗內、窗不關**→④完成→頁面刷新、#21 離開待決定（7→6）。
2. **上傳鏈（#22 繳費單，真 gemma 看圖）**：201→抽屜窗①「採用「收據」」（真模型從注入清單挑中）→
   ①定案→卡片「✅ 已歸到「收據」」→**實體窗自動開**（建議 null 合理——電費單與 MacBook 無關）→④→
   **待辦窗未出現＝「空關不跳」實測**（該次模型未給 task_title，屬 D13 自由心證）。
3. **待辦窗（真頁面驅動）**：`openTaskModal` 於真頁面呼叫→預填標題／到期→「建立待辦」→201→窗關→
   onDone 帶 TaskOut。正式庫留兩筆真待辦（#21 到期 09-18、#22 到期 09-01）。
4. **三分頁**：「待決定（6）｜資料夾｜待辦（2）」計數正確；`?tab=tasks` 直達＋aria-current；
   待辦列表**到期排序**（09-01 在 09-18 前）＋縮圖＋點列 `/photos/{id}/image` `_blank`；
   資料夾牆照片＝`DIV.photo-static`、點了三個彈窗都不動。
5. **curl 錯誤路徑六項**：重複釘 409／已定案 PATCH 409／待辦標題空白 422（中文訊息）／
   合法建立 201（含 thumbnail_url）／同照片第二筆待辦 409／GET /tasks 正確。
6. **422 顯示**：合成風景圖被真模型判「看不懂」→卡片正確顯示「失敗（HTTP 422）VLM 無法理解…」。
7. **console**：全程僅 favicon 404（既有噪音）＋兩筆刻意錯誤測試的資源列（422／409），**零 JS 錯誤**。

**親自 review**：各 phase 完成當下已逐檔 review（階段XX〜CCC 的 REP 各有記錄）；
本階段補靜態掃碼（alert/confirm/prompt＝0、無 DELETE 端點、SQL 只在 repository）。

## 裁掉與縮減（依產品負責人「做完 33 就停」指示，全部記錄）

- **PDF 真模型瀏覽器實測裁掉**：PDF 後端由 7 顆整合測試＋BDD Rule 把關（P28 已 commit）；
  前端摘要與單圖共用 `開始歸類()` 路徑（P28 曾以 node 存根驗過三結局文案）。
- 上傳頁「稍後再說→續鏈」未在瀏覽器單獨走一次：它與 onAssigned 共用 `接著釘實體()`
  （onAssigned 路徑已實測；onClosed 由存根覆蓋）。
- Phase 34〜37 完全未動（計畫檔在 unfinish/ 等下輪）。

## 遇到的問題與發現（給產品負責人）

1. **真模型單次看圖延遲明顯變長**：9 欄 prompt（含實體清單＋待辦規則）後實測 2〜5 分鐘／張
   （先前 6 欄約 10〜60 秒；期間曾有兩請求併行搶 GPU 加劇）。功能正確但體感變慢——
   可能的後續選項：精簡 prompt、跑 P35 時一併調整、或換更快的量化版模型。**建議下輪議定**。
2. 合成的純文字測試圖（風景）被真模型判 422——422 路徑本來就該存在，非缺陷；
   繳費單同款合成圖則兩次都成功入庫。
3. 驗收期間瀏覽器曾被外部操作切頁，導致一次上傳的前端回呼中斷（照片仍正確入庫、進待決定）——
   之後以「同頁等待」完成其餘步驟。

## 備註

- 正式庫新增驗收軌跡：照片 #21（文件、釘「我的 MacBook」、待辦 09-18）、#22（收據、待辦 09-01）、
  另有先前中斷回呼所留的待決定照片——皆可由待決定分頁正常補完，屬真實資料非垃圾。
- 28〜30 已 commit（`e29f5a1`）；**31〜33 依指示未 commit**（工作樹乾淨可審：4 新檔＋5 修改檔＋文件）。
