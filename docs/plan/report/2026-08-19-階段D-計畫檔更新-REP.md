# 2026-08-19 階段D：phase-03／phase-04 計畫檔更新 REPORT

## 做了什麼（一句話）

實測環境與程式碼現況後，把 `phase-03`／`phase-04` 兩份計畫更新為 **TDD＋BDD 版本**（各插入「步驟 0：先寫測試」與完整測試程式碼、驗收加入 pytest 全綠），並連動修正 `phase-00` 總覽的測試累進表與最終結構圖，消除文件矛盾。

## 實作邏輯

1. **先偵察再動筆**：實測確認 PG17@5433 兩庫皆在、pgvector 0.8.6 兩庫已啟用、venv 套件齊全（psycopg 3.3.4／pytest 9.1.1／httpx 0.28.1／python-multipart／pytest-bdd）、`db/schema.sql`＋`session.py`＋`photo_repository.py`＋`photos.py` 皆為空檔——兩份計畫的「前置條件」與「步驟」描述**與現況相符，不過時**；真正的缺口是測試方法。
2. **缺口＝TDD**：原路線把 pytest 留到 P05／07，但 `phase0819.md` 約束 1〜3 要求「先寫測試、再寫功能、全部通過」且指定 `tests/unit`＋`tests/integration`。裁決：**dev-prompt（使用者最新直接指示）優先**，P03/P04 起即建測試；完整 `.feature` 驗收（pytest-bdd＋假件）仍留 P07 不提前，避免過度設計。
3. **測試設計原則**：單元測試只放純函式（`to_vector_literal`）；repository 與 router 行為都要碰真測試庫／真 app，歸整合測試。conftest 在 import 任何 `app.*` 之前寫入 `DATABASE_URL=visual_memory_test`（利用 `load_dotenv()` 不覆蓋既有環境變數的特性）＋直接改 `config.DATABASE_URL` 雙保險＋autouse fixture 每測清空（並斷言 URL 含 `visual_memory_test` 才動手，絕不清正式庫）。
4. **BDD 對應**：phase-04 的 7 個整合測試逐條對應 `上傳照片.feature` Rule U1（415＋照片數量 0）；phase-03 的 10 個整合測試在資料層對應 U2/U3/U4/U5（含「現在時間為…」的固定時間注入點）。
5. **文件一致性**：phase-00 累進表原本 P03/P04＝0 個測試、tests/ 最終結構是扁平的——一併修正（P03=12、P04=19、後續累計順移 +19、最終 49→68；結構圖加 `unit/`＋`integration/`），並註明 P05+ 測試檔歸屬子目錄等各該 phase 開工前更新計畫時再定。

## 步驟

D1 現況偵察（DB／套件／檔案）→ D2 Context7 查證 → D3 改 phase-03（4 處編輯）→ D4 改 phase-04（5 處編輯）→ D5 連動改 phase-00（3 處編輯）→ D6 本 report。

## 測試方式與結果

本階段是文件更新，驗證方式＝逐項核對：

| 檢查 | 結果 |
|---|---|
| phase-03/04 前置條件與 2026-08-19 實測現況一致 | ✅（PG17@5433、兩庫＋pgvector 0.8.6、套件版本入檔） |
| 兩份計畫皆有「步驟 0 先寫測試→紅燈→實作→綠燈」完整迴圈 | ✅（含測試完整程式碼與紅燈預期樣態） |
| 驗收標準含 pytest 數字（P03：12 passed；P04：19 passed） | ✅ |
| phase-00 累進表／結構圖／§6 總數與上述一致 | ✅（68 個測試收尾） |
| API 用法經官方文件查證 | ✅（見下方來源） |

## 遇到的問題與解法

- **問題 1**：phase-00 累進表與 dev-prompt 的 TDD 要求直接衝突（P03/P04 原為 0 測試、tests/ 原為扁平結構）。**解法**：以使用者最新指示（phase0819.md）為準修正 phase-00，但只動受影響的三處（累進表＋結構圖＋§6 總數），P05 之後各檔的子目錄歸屬留給各該 phase 更新時決定——避免今天替未來 phase 做過多決定。
- **問題 2**：0818 階段C 證實自動化沙箱會擋 localhost 連線（curl→uvicorn 回 000），phase-04 原驗收全靠 curl。**解法**：驗收新增第 0 條 pytest（TestClient in-process，不經網路）為正式驗證，curl 降為手動輔助並註明原因。

## 外部查證來源（Context7 MCP，2026-08-19）

- FastAPI `UploadFile`（`content_type` 屬性、sync `def` 用 `file.file.read()`、`File(...)` 參數）：<https://fastapi.tiangolo.com/tutorial/request-files>、<https://fastapi.tiangolo.com/reference/uploadfile>
- psycopg 3 連線 context manager（離開 with 區塊：成功 commit／例外 rollback，並關閉連線）：<https://www.psycopg.org/psycopg3/docs/basic/usage.html>、<https://www.psycopg.org/psycopg3/docs/basic/transactions.html>

## 目前專案狀態與下一步

計畫檔已全部更新完（phase0819 任務 1 完成），程式碼未動。下一步：階段E——照更新後的 phase-03 以 TDD 實作（先紅後綠），接著階段F 做 phase-04。
