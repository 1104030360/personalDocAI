# 2026-09-01 階段一：八份計畫檔校準（Phase 73〜80）REP

> 對應 dev-prompt：`docs/plan/dev-prompts/phase0901.md` 任務 1。
> 對應 TODO：`docs/plan/todo/2026-09-01-階段一-八份計畫檔校準73-80-TODO.md`。
> 比照 2026-08-26「八份計畫檔校準 65〜72」前例。

## 實作邏輯

八份計畫檔與 design6 的 2026-09-01 改判（閘門只用 VLM 短問、不看檔名、跟頁首開關）成稿時間交錯：
74／75 是改判後重寫的、78 只補了檔頭警語、76／77／79／80 成稿於改判前；Phase 73 則已在 08-28 由產品負責人
自己完成最後一步（建 GitHub repo、push、CI 綠）。實作前必須逐份把「引用的事實」對回真實程式碼與改判後的契約，
否則實作者會照舊契約寫出 `RuleGate`／`PRIVACY_GATE_LOCAL_MODEL`，或把顆數、前置條件、指令預期全部對不上。

校準只校**事實引用與改判殘留**，不動設計決策（design6 ＞ 總覽 §2.4〜2.7 ＞ phase 檔）。

## 步驟

1. 驗開工基準：Docker Desktop 原本沒開（`docker compose ps` 連不到 daemon）→ `open -a Docker`，四容器
   （db／redis healthy、app／worker）自己回來；`pytest -q` ＝ **543 passed ＋ 0 skipped**（33.5s）；端點 22；
   HEAD `a53ab57`、分支 `main`、remote `1104030360/personalDocAI`（PUBLIC）、CI `test` 綠 ×2。
2. 我親自讀完 design6.md 全文、總覽 §2.1／§2.4〜2.7／§7／§9〜§11、phase-73／74／75 全文、76〜80 的章節結構
   與可疑段落，以及 `ingest_job.py`／`celery_app.py`／`dependencies.py`／`ai_timing.py`／`ingest_job_store.py`／
   `config.py`／`conftest.py` 現況，寫成共同事實簡報（`.superpowers/sdd/phase0901/brief-common.md`），
   所有校準者先讀它再讀自己的檔。
3. 派 6 個平行 subagent（Opus）：A＝73＋74＋75、B＝76、C＝77、D＝78、E＝79、F＝80。
   鐵律：只准改自己名下的計畫檔；`app/`／`tests/`／`docs/spec/`／總覽不碰；禁跑 pytest（只准 `--collect-only`）；
   每一處改動記「原文 → 改後 → 依據」。總覽層問題回報給我，由我（唯一寫總覽的人）統一改。
4. 我親自：全庫 grep 殘留關鍵字、NBSP 檢查、`git status` 確認程式碼零改動、抽出 78／79／80 三份
   `gated_ingest.py` 與 79／80 兩份 `cloud_ingest.py` 的整檔重貼碼做連續 diff（確認 78→79→80 的差異只有各 phase
   該加的功能，不會互相覆蓋）。
5. 改總覽 16 處（見下）。

## 測試方式

- 全程**沒有執行 pytest**（避免撞測試庫）；只用 `pytest --collect-only -q` 核對顆數與測試名。
- 計畫內嵌程式碼的驗法：校準 B 用 `diff`／AST 比對／離線等價 harness（14 情境）三重驗證 phase-76 的 595 行重構碼
  與現行 `ingest_job.py` 逐字等價；A／D／F 對貼進檔案的 python 區塊跑專案的 `ruff format --check`＋`ast.parse`。
- 殘留掃描：`RuleGate|規則版|PRIVACY_GATE_LOCAL_MODEL|PRIVACY_MODEL|SENSITIVE_KEYWORDS|filename_stem|只看檔名|模型備援|master`
  在八份檔只剩「已否決／不要復活」的擋牆句；`grep -P '\xc2\xa0'` 全零。

## 主要發現與修正（濃縮；完整清單見 `.superpowers/sdd/phase0901/calib-{A..F}-report.md`）

| 檔 | 改動 | 重點 |
|---|---|---|
| 73 | 15 處 | §0 狀態表：§6.5 已 commit `4269985`、§6.6 已由產品負責人 08-28 完成（repo **PUBLIC**、分支 **main**、CI 綠 ×2）、`a53ab57` 把 pytest.ini 併進 pyproject；全檔 `master`→`main`；§7 負面 CI 驗證標 ⏸ 可選；歸檔隨 commit |
| 74 | 8 處 | **真缺陷**：測試的 `_png()` 是假位元組，Phase 75 一加縮圖 11 顆會翻 9 顆 → 改 `make_png_bytes()`；兩行沒用到的 import 會被 F401 擋 → 刪；4 處 assert 改成 formatter-clean；補「config.py 本 phase 不動」 |
| 75 | 11 處 | **裁決 R1 落地**：新增 `dependencies.build_privacy_gate_for_backend(ai_backend)`、`get_privacy_gate()` 改吃 `config.AI_BACKEND`；**真缺陷**：`vlm_service.CLOUD_JSON_INSTRUCTION` 逐字列著 understand 九個鍵，接到隱私短問會讓雲端恆 `UNCERTAIN`（安靜壞掉）→ 釘死「自己家一份 `_CLOUD_JSON_INSTRUCTION`」；import 區塊補齊並說明 E402／isort |
| 76 | 17 處 | 重構碼三重驗證可照貼；§6「舊名字完全消失」的 grep 會被新 docstring 誤報（實測命中）→ 改成只抓定義與呼叫；前置改 564（本次排在 75 之後）；§1 誤引修正 |
| 77 | 10 處 | 拿掉「Phase 74 加了 `PRIVACY_MODEL` 到 config.py」的假設（改判後 74／75 都不加 config）、config 插入點改 `SEARCH_MODE_LABELS` 之後、`CloudRoute` 在 77「整個不存在」（非 stub）、conftest docstring 交回 74 定義 |
| 78 | 22 處 | 本文十幾處規則版殘留全清（含整段 `PRIVACY_GATE_LOCAL_MODEL=on` 煙霧步驟改寫成「兩趟頁首開關煙霧、零 env」）；**R1 三處落地**（celery_app 用 `build_privacy_gate_for_backend(job["ai_backend"])`＋docstring 理由、conftest 雙名 monkeypatch、`test_ingest_task把gate與cloud都傳進去` 加快照斷言，顆數仍 +9） |
| 79 | 13 處 | R4 兩處註解；cloud_ingest 重貼碼漏列三行 import 補上；前置 589 加 R1 但書；§3 補「不要加第三個 NotImplementedError」；R3 |
| 80 | 16 處 | 唯一一處改測試斷言（`test_逾時fallback之前會先清掉S3物件` 原斷言撐不起「之前」，R6 接受）；`wait_result` 五條規則 docstring 逐字對齊 §2.5；**吸收 E 的發現**：`route=cloud` 崩潰重送落庫前重新 `store.get`（D17 最後一道保險）；R3／R4 |

**總覽（我親自改，16 處）**：§2.2 P76／P78 列（依賴補 75、顆數措辭）；§2.3 依賴圖兩處；§2.4.1 加
`build_privacy_gate_for_backend`；§2.4.5 `FakePrivacyModel` 列（74 建、回 `PrivacyJudgement`）；§2.5 開頭補
「gate／cloud 從哪來」；§2.7 P74 拿掉 `config.py`、P75 拿掉 `fakes.py`、P78 補 R1、P95 補純註解校正一列；
§6 勾選區與 §9／§10.2 H 的「543 顆逐顆綠」改成「既有顆數（564）」；鐵律 2 `wire_fake_ai` 列補雙名 monkeypatch、
三死埠起點改 77；鐵律 10 補「repo 是 PUBLIC」；§8.6 改成 VLM 短問會漏＋耗時待填；§10.2 標題 8 條→19 條、新增
**追認項 S**（＝裁決 R1）。

## 遇到的問題與裁決（執行者裁決，授權依據＝phase0901.md「所有決策不必徵求同意」）

| # | 裁決 | 理由 | 代價 |
|---|---|---|---|
| R0 | 不開 worktree、不 commit；每個 phase 的 review 用工作樹 tree 快照相減（`snapshot-tree`） | 產品負責人「先不 commit」＋要在本機 Docker 手測 | 沒有 commit 可回退，改靠快照 diff |
| **R1** | 閘門在 worker 必須用 `job["ai_backend"]` 快照建（新增 `build_privacy_gate_for_backend`） | worker 行程的 `config.AI_BACKEND` 永遠 `local`（`celery_app.ingest_task` docstring 明寫）；否則 D6「跟著開關走」在正式路徑安靜失守 | 多一支公開函式 |
| R2 | Phase 73 只更新狀態、不搬 finish/ | 鐵律 12：歸檔隨 commit | unfinish/ 多留一份 |
| R3 | 各 phase「步驟 N：commit」改「不 commit——記快照」 | 同 R0 | — |
| R4 | 78／79／80 重貼碼內規則版註解改 VlmGate 語意 | 改判 | — |
| R5 | 73 §7 負面 CI 驗證不列為結案條件 | 73 自己寫可後補；需 push（外部副作用） | CI 紅燈路徑未親眼驗過 |
| R6 | 接受 F 改一顆測試的斷言內容 | 原斷言撐不起測試名；顆數不變 | — |
| R7 | 三份重貼碼的兩段註解措辭不統一 | 統一要連動差異清單，風險＞收益 | reviewer 會看到註解 diff（預期） |
| R8 | dev-prompt 的 phase-75 檔名筆誤以實檔為準 | 不改產品負責人的檔 | — |

其他觀察（未動、記錄給後續 phase）：`test_ingest_job_pdf.py:216／239` 兩處 docstring 在 76 改名後會過期
→ 已寫進總覽 §2.7 Phase 95「純註解校正」；repo 是 PUBLIC → 82／84／85 做到時檔頭要再提醒「只寫變數名」；
基線 pytest 有 1 個環境層 warning（`fastapi/testclient.py` 的 StarletteDeprecationWarning「httpx→httpx2」），與專案碼無關、不處理。

## 測試結果

- 校準本身零產品碼、零測試改動：`git status --short -- app tests db docs/spec compose.yaml Dockerfile requirements.txt pyproject.toml` 全程空。
- 八份計畫檔行數：73 582→637、74 381→444、75 273→432、76 1649→1736、77 1579→1629、78 1341→1476、79 1742→1784、80 1755→1827；總覽 1999→2009。
- 開工基準 543 passed＋0 skipped 維持不變（校準階段未再跑全量，無任何會影響測試的改動）。
