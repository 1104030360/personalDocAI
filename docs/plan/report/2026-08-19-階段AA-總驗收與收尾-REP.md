# 2026-08-19 階段AA：總驗收與收尾 REPORT

## 做了什麼（一句話）

Phase 13〜14 的主控總驗收：真模型中英雙語煙霧 8 步全過（P13 驗收 6 結案）、`/ui` 兩頁瀏覽器實操 7 項全過（P14 驗收 2〜8 結案）、主控親自逐行 review 本輪全部變更、最終回歸 **79 passed**（含死埠再證）——**Phase 01〜14 全數完成、專案完成**，工作樹保持未 commit 交付使用者。

## 實作邏輯

- **同一個 uvicorn 服務串兩組驗收**：重建正式庫（schema.sql，計畫明定清空重建）→ P13 的 curl 煙霧（真照片＋五種詢問＋415）→ P14 的 Playwright 真 Chrome 實操（等同手動操作，符合 design.md §6「頁面驗收以手動瀏覽器操作為準」，且未寫任何自動化測試碼）。
- **真模型只驗格式與大方向**：非決定論輸出不當驗收依據；行為的正式驗收由 79 個假件測試把關。

## 步驟與測試結果

1. **P13 步驟 5／驗收 6 真模型煙霧（8/8 符合預期）**：
   | 步驟 | 結果 |
   |---|---|
   | 建表 | `DROP/CREATE/CREATE INDEX`，0 筆起 |
   | 上傳真照片 | 201，gemma4 讀出收據內容（text＋收據/Target/可樂、洋芋片/2026-08-10 四欄位齊），耗時 2m09s |
   | 上傳 .txt | `HTTP/1.1 415 Unsupported Media Type` |
   | 中文條件型 | `metadata search`、ids [1]、中文回答正確引用 |
   | 中文語意型 | `vector semantic search`、中文回答（可樂） |
   | 英文語意型 | `vector semantic search`、**英文**回答 "Cola"、ids [1] |
   | 英文條件型 | `metadata search`、ids []＋**英文**查無句——計畫 Q6 已知限制（router 抽英文條件值 `receipt`，ILIKE 對不上中文「收據」，不做跨語言翻譯對映），**預期行為** |
   | 模糊問題 | `vector semantic search`、中文回答正確引用（fallback 生效） |
2. **P14 驗收 2〜8 瀏覽器實操（7/7 通過）**：上傳 201 七行齊（id 2）；415 訊息逐字正確；中文問答「有兩張在 Target 拍的收據。」＋metadata search＋ids 1, 2；英文問答 "You bought Coke."＋vector semantic search；查無（問 Costco 天然查無，不清 demo 資料）＋（沒有找到相關照片）；兩頁雙向互連；console 全程零 JS 錯誤零 CORS（僅 favicon 404 與刻意 415 兩筆網路資源記錄，屬預期雜訊）。等待中的按鈕鎖定／提示／完成解鎖全部正確。
3. **主控親自逐行 review**：本輪全部 diff（`test_error_paths.py` 11 測、`test_retrieval.py` category 守護、`test_ask_feature.py` 清理四點、`ask_workflow.py` 純註記刪除、`CLAUDE.md` 指令兩示例、`main.py`＋兩 HTML）——分層邊界、零行為紅線、JS 安全（textContent）與取值路徑全部成立；dev-prompt 約束 5 核對：**兩份計畫每個步驟與驗收項都有 implementer／reviewer／主控的實跑證據**。
4. **最終驗證與收尾**：`pytest -q` = **79 passed**；`OLLAMA_BASE_URL=http://localhost:9 pytest -q` = **79 passed**；端點恰 3（P14 mount 後不變）；P13/P14 計畫歸檔 `finish/`（`unfinish/` 清空、finish/ 共 15 檔）；CLAUDE.md 終版（現況＝Phase 01〜14 全數完成、79 tests、P13/P14 摘要、路線圖改「已全數完成」）；環境清理（uvicorn 停、port 8000 釋放、`.playwright-mcp/` 刪除）。

## 遇到的問題與解法

1. **本機模型連續推論變慢**（瀏覽器實操的詢問單次 4〜8 分鐘 vs 稍早 curl 的 2〜3 分鐘）：屬 Ollama 模型換載與長 context 的正常現象，頁面「思考中…」回饋設計正好派上用場；以伺服器 log（200 已回）與全頁快照交叉確認，非當機。
2. **Playwright by-ref 局部快照吃到舊畫面**：伺服器 log 顯示 200 已回但局部快照仍是「思考中」→ 改用全頁快照為準。
3. **驗收 6 的 TRUNCATE 正式庫**：改「問 Costco」天然查無替代，不動 demo 資料（Ruling 22）。

## 測試結果（總）

**79 passed**（含死埠證明）；P13 煙霧 8/8；P14 實操 7/7；12 條 Rule＋錯誤總表七列＋雙語七測全綠；未 commit；`docs/spec/**` 從頭到尾零異動。**專案完成。**
