# 釐清問題

能否提供一個具體的上傳例子（例如：一張 Target 收據照片，VLM 應轉出什麼文字、產生哪些 metadata），作為「上傳照片」三條規則的可驗證 Example？（目前三條規則皆無 Example，標記 #TODO）

# 定位

Feature：「上傳照片」的三條儲存規則（圖片資訊/文字、metadata、embeddings 向量）的 Example 覆蓋（B3）

# 多選題

| 選項 | 描述 |
|--------|-------------|
| Short | 提供例子情境：When 上傳什麼照片 → Then 儲存的文字/metadata 應為何 |

`Format: Short answer`（此題為例子蒐集題，可提供較長的具體情境描述）

# 影響範圍

- 功能「上傳照片」：三條規則的 Example（目前狀態 Missing）
- 測試：上傳流程的驗收案例基準資料

# 優先級

Medium
- 影響測試完整性（B3 不可妥協項），但需先釐清 metadata 欄位與上傳回應內容才能構造完整 Example

# 依賴/組合

- 依賴「Photo_metadata的具體欄位為何」、「上傳照片_上傳成功後系統回應內容為何」

---
# 解決記錄

- **回答**：A - 由 Claude 依已釐清規格代擬「Target 收據照片」例子（VLM 理解結果寫在 When 步驟、測試時以 stub 模擬），經使用者確認後寫入
- **更新的規格檔**：spec/features/上傳照片.feature
- **變更內容**：為 5 條規則（照片資訊、metadata、embedding 向量、上傳時間、上傳回應）各補 1 個 Example，移除全部 #TODO
