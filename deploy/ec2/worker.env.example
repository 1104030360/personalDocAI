# /opt/personaldocai/worker.env 的範本（增量六 Phase 91）。
#
# ⛔ 這個檔**只寫變數名，永遠不寫值**（增量六總覽 §7 鐵律 10）。真正的檔案由人用
#    SSM Session Manager 進機器手動建、chmod 600（步驟在 Phase 92 §4.5）；
#    它**不進版控、不進映像、不寫進 user-data**。
# ⛔ **沒有** AWS_ACCESS_KEY_ID／AWS_SECRET_ACCESS_KEY——EC2 用 instance profile
#    （personaldocai-worker-role），boto3 自己去機器的 metadata 服務拿臨時憑證。
# ⛔ **沒有** AWS_ENDPOINT_URL——那只用在 pytest 的第五道安全網（把 boto3 指到死埠）；
#    放進來會讓工人安靜地打不到任何 AWS 服務。
#
# ⚠ **值一律不要加引號。** 這個檔會被**兩個東西**讀，而它們對引號的看法不一樣：
#      systemd 的 EnvironmentFile=   會把成對的引號**剝掉**（"tokyo" → tokyo）
#      docker run --env-file         **不會**剝，引號是值的一部分（"tokyo" → "tokyo"）
#    unit 檔兩種都用到了（ExecStartPre 用 systemd 代入 ${AWS_REGION} 去登入 ECR，
#    ExecStart 則把整個檔案原樣交給 --env-file 給容器裡的工人讀），
#    所以加了引號的話會變成「登入 ECR 成功、但工人拿到的 region 是帶引號的字串」——
#    boto3 會說查無此區域，而 unit 那一半看起來完全正常。**最難查的那一種。**
#      ✓ AWS_REGION=ap-northeast-1
#      ✗ AWS_REGION="ap-northeast-1"
#    同理：**不要**寫 `export`、不要在等號前後留空白、值裡也不要寫 ${…}
#    （--env-file 不做任何展開，會原樣把 `${FOO}` 這五個字元當成值）。

# --- systemd unit 代入用的三個（unit 檔裡的 ${…} 代的就是這三個）---
# ⚠ AWS_REGION 是**兩邊都要**的那一個：unit 的 ExecStartPre 用它去換 ECR 的臨時密碼，
#   而工人程式自己也讀它（config.AWS_REGION → AwsMailbox 的 region 參數）。
#   ECR_REGISTRY 與 ECR_IMAGE 則**只有 unit 用**，工人程式一個字都不讀。
AWS_REGION=
ECR_REGISTRY=
ECR_IMAGE=

# --- 工人程式讀的（app/core/config.py）---
S3_BUCKET=
SQS_JOBS_QUEUE_URL=
SQS_RESULTS_QUEUE_URL=

# --- 看圖用哪一顆（2026-09-03 產品負責人改判，design6 D12 作廢）---
# WORKER_VLM_BACKEND 只認兩個值，打錯字工人會當場拒絕啟動（不會安靜地退回某一種）。
# **留空（就是下面那一行的樣子）或整行不寫 ＝ cloud**：範本一律只寫變數名，
# 所以「值是空字串」與「沒有這個 key」被當成同一件事（config.py 用 `or` 收掉了）。
#
#   local  ＝ 跟工人**同一台機器**上的 Ollama。**92-B 的 GPU EC2 填這個**，要一起填：
#              WORKER_VLM_BACKEND=local
#              OLLAMA_BASE_URL=http://127.0.0.1:11434
#              VLM_MODEL=gemma4:e2b
#            ⚠ Linux 上**沒有** Mac 那個 `-mlx` 標籤（那是 Apple Silicon 專用的建置），
#              寫成 gemma4:e2b-mlx 會變成「每張圖都 404、看三次、標成看不懂」。
#            ⚠ 127.0.0.1 指的是**那台 EC2 自己**：unit 檔的 docker run 帶 --network host，
#              容器與 host 共用同一個網路命名空間，所以打得到只聽 loopback 的 ollama.service。
#            此時 OLLAMA_API_KEY 與 OLLAMA_CLOUD_VLM_MODEL **可以留空**（工人不打 ollama.com）。
#
#   cloud  ＝ ollama.com。這是**預設值**（留空或不填就是它），也是這台 Mac 手動煙霧走的路。
#            **92-A 的 CPU EC2（t3.xlarge）也填這個**——機型不決定後端，那台只是把圖轉送出去。
#            要一起填 OLLAMA_API_KEY 與 OLLAMA_CLOUD_VLM_MODEL；OLLAMA_BASE_URL／VLM_MODEL 用不到。
#            ⚠ OLLAMA_CLOUD_VLM_MODEL **cloud 模式必填**（例如 gemma4）：留空不會退回 VLM_MODEL，
#              而是變成空的模型名＝每張圖三次看不懂。雲端沒有本機那些 -mlx／:e2b 的 tag，
#              名字要自己去 ollama.com 確認，別照抄 .env 的 VLM_MODEL。
WORKER_VLM_BACKEND=
OLLAMA_BASE_URL=
VLM_MODEL=
OLLAMA_API_KEY=
OLLAMA_CLOUD_VLM_MODEL=

# ⛔ **不要**加 VLM_MAX_ATTEMPTS：config.py 裡它是**寫死的常數**（＝3），根本不讀環境變數，
#    列在這裡只會讓下一個人以為改得動（2026-09-02 裁決 R9）。
# ⛔ **不要**加 WORKER_VERSION：它由 Dockerfile 的 ARG GIT_SHA 烙進映像（Phase 90）。
#    docker run --env-file 的值會**蓋掉**映像裡的 ENV，填了等於把「EC2 上跑的是哪一版」
#    這個證據弄假（Demo 3 就是靠工人啟動 log 的 version=<sha>）。
# ⛔ **不要**加 CLOUD_ROUTE／CLOUD_RESULT_TIMEOUT_SECONDS／DATABASE_URL／CELERY_BROKER_URL：
#    那些是**本機那一側**的設定，工人一個都不讀（工人不碰資料庫、不碰 Redis——design6 D11）。
